# 2026-05-06 01:10:00 UTC - noTrace cfg32 NIC enters route_design

## Goal

Record that the active noTrace cfg32 NIC F2 build has entered Vivado routing.
This is the next decisive phase after the mainline cfg32 NIC build failed in
placement and the noTrace fallback completed placement plus pre-route phys-opt.

## Build

- FireSim tmux session:
  `pairdummy-cfg32-nic-notrace-20260505T171453Z`
- Build host:
  `i-0ecf73dad2a8ae8d0`, private IP `192.168.0.213`
- Target config:
  `FireSimGemminiReRoCCPairDummy16x16C4P12Sbus128NICNoTraceConfig`
- Platform config:
  `FRFCFS16GBQuadRank_BaseF2Config`
- Strategy/frequency:
  `TIMING`, `20MHz`
- Remote Vivado log:
  `/home/ubuntu/firesim-build/platforms/f2/aws-fpga-firesim-f2/hdk/cl/developer_designs/cl_f2-firesim-FireSim-FireSimGemminiReRoCCPairDummy16x16C4P12Sbus128NICNoTraceConfig-FRFCFS16GBQuadRank_BaseF2Config/build/scripts/2026_05_05-191909.vivado.log`

## Poll evidence

The `2026-05-06T01:09:39Z` 1200s poll showed:

```text
AWS FPGA: (00:49:13): Writing post-phy_opt design checkpoint and report
AWS FPGA: (00:53:25): Start routing customer design ...
AWS FPGA: route command: route_design -tns_cleanup -directive Explore -timing_summary
Phase 2 Router Initialization
Phase 2.2 Pre Route Cleanup
Phase 2.2 Pre Route Cleanup | Checksum: 28a0d644d
Phase 2.3 Global Clock Net Routing
Phase 2.3 Global Clock Net Routing | Checksum: 1c6abe9bf
```

The poll also confirmed post-phys-opt artifacts now exist:

```text
post_phy_opt_timing.rpt
post_phys_opt.dcp
```

The Vivado process was still alive, and AWS still showed only the one
`z1d.3xlarge` build host. No F2 runfarm was running.

## Interpretation

The noTrace fallback has passed the build boundary that killed the mainline
cfg32 NIC image and is now in route. This is still not a usable gdbserver AGFI.
Do not update the cfg32 NIC HWDB and do not launch a runfarm until AGFI/AFI
creation completes.

If routing completes with timing violations but FireSim still creates an AGFI,
the next required evidence is live cfg32 NIC gdbserver behavior. If routing
fails, collect the route/post-route reports and first failing error before
starting any next build.
