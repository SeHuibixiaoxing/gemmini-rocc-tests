# 2026-04-15 12:27:54 UTC

## Round Goal

基于上一轮
`20260415T120322Z.md`
的 live snapshot，
重新回到“无 `subbatch` 过滤”的 breadcrumb-only rerun，
判断当前真实 frontier 是：

- 再次稳定卡在
  `stage-fixed-load-sparse begin slot=1 tensor=1000001`

还是会像之前的无过滤 rerun 那样继续推进到：

- `subbatch=3`
- `tensor=2`
- `export target_seq=1`
- `rr_acquire_after_call`

## Why The Previous Run Was Stopped

上一轮配置：

- `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=0`
- `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH=3`
- `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH_RADIUS=0`

其作用是成功证明：

- `sb=3 tensor=2 page63` 已再次越过

但 live evidence 也显示该配置的观测面已经饱和：

1. `heartbeat.csv` 继续增长到 `817`
2. `bertmini-batch8.log` 长时间固定在 `361880` bytes
3. 主日志末尾稳定停在：
   - `stage-fixed-load-sparse phase=begin ... slot=1 tensor=1000001`
4. `runner-proc.stage` 也只到 `bin-poll-1`

因此保留 `subbatch=3` 过滤不会再给出更多有效信息，
反而把 `tensor=1000001` 之后的真实窗口遮住。

## New Rerun Configuration

下一轮固定保持：

- `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=0`
- `PIPELINE_RUNTIME_BREADCRUMB_ENABLE=1`
- `PIPELINE_RUNTIME_BREADCRUMB_SEGMENT=0`
- `PIPELINE_RUNTIME_BREADCRUMB_GLOBAL_STAGE=0`
- `PIPELINE_RUNTIME_BREADCRUMB_LOCAL_STAGE=0`
- `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH` 留空
- `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH_RADIUS=0`

其它固定约束保持不变：

- private-IP-only SSH / freshness
- `PIPELINE_RUNTIME_MLOCKALL_MODE=2`
- `PIPELINE_RUNTIME_UART_LOG_ENABLE=0`
- `PIPELINE_RUNTIME_GUEST_LOG_ENABLE=1`
- `PIPELINE_RUNTIME_GUEST_DEEP_LOG_ENABLE=0`
- `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE=0`
- `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PROBE_ENABLE=0`
- FireSim 前在 `sims/firesim` 下 source manager env

## Infra Reset Status

上一轮实例：

- `i-0186f32a93ab26b76`

已由：

- `pairdummy-sbus128-terminaterunfarm-20260415-122729`

发起回收，
当前 AWS 状态已确认是：

- `shutting-down`

## Next Step

重新执行：

1. `launch`
2. `infrasetup`
3. `run`

并继续以 guest 文件日志 + live snapshot 为主证据。
