## 2026-04-13T07:57:18Z

### 本轮目标
- 基于上一轮真实前沿 `stage=0 tensor=2 target_seq=1 page=60/64 submitwait-begin`，把 export submit/wait probe 窗口前移到更接近当前冻结页的 token 段，减少盲区。

### 静态依据
- `dma_submit_wait_annotated_scoped()` 的 export 内层 probe 由：
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END`
  控制。
- 上一轮 window `2698..2720` 是围绕旧前沿 `tok~2708` 设置的。
- 当前 live run 已越过该窗口，对应新冻结页位于：
  - `target_seq=1`
  - `page=60/64`
- 结合旧记录中 `tok~2708` 对应 `tensor=2` export 前段页面，当前估算需要把 probe window 前移到 `~2810-2830` 邻域。

### 本轮修改
- 文件：
  - `scripts/pairdummy_sbus128_fixed_env.sh`
- 修改内容：
  - `PIPELINE_RUNTIME_PROFILE_ID`
    - `pairdummy-sbus128-fixed-v2 -> pairdummy-sbus128-fixed-v3`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START`
    - `2698 -> 2808`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END`
    - `2720 -> 2836`
- 修改目的：
  - 保持 probe 仍然窄窗口，避免重回高频 guest 文件写扰动
  - 同时覆盖当前估算的 `address2 page~60/64` token 区间
  - 通过 profile id 变化建立新的 freshness 闭环，避免旧 image 混入

### 本轮校验
- `bash -n scripts/pairdummy_sbus128_fixed_env.sh` 通过

### 下一步固定流程
1. `terminate`
2. `image-closure`
3. `launch`
4. `infrasetup`
5. `run`
6. 本地/远端 freshness
7. 私网 IP + `debugfs cat` 继续盯 guest 文件日志

### 约束再次确认
- 实例查询 / SSH 一律使用私网 IP
- FireMarshal 一律通过 `scripts/firemarshal-tmux-run.sh`
- FireSim 一律通过 `scripts/firesim-tmux-run.sh`
- 在 FireMarshal / FireSim 前先：
  - `cd sims/firesim`
  - `set +u; source sourceme-manager.sh --skip-ssh-setup; set -u`
- 主观测面仍为 guest 文件日志，不以 `uartlog` 为主
