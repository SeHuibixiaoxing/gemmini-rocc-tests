# 2026-04-15 10:49:44 UTC

## Round Goal

把主线从高扰动 `dma-fixed-load` trigger rerun 切回低扰动观测面，准备执行“仅过滤 `subbatch=5` 的 breadcrumb-only rerun”。

## Prior Authoritative State

上一轮最有价值的结论来自 `rr` trigger rerun：

- authoritative frontier:
  `sb=5 / tensor=0 / mgr=0 / page=63 / dma_doneflag_end`
- code:
  `src/prt_dma.c:2648`

而 `dma-fixed-load/page63` rerun 的 frontier 前移到：

- `sb=4 / tensor=2 / mgr=0 / page=62 / dma_wait_after_trace_complete`
- code:
  `src/prt_dma.c:3181`

并且 trigger 文件根本未命中 page63，说明该 probe 家族本身就足以扰动时序。

## Actions This Round

1. 终止上一轮高扰动 run farm，避免继续在错误观测面上消耗时间。
2. 检查 `render_pairdummy_guest_env.sh`，确认其此前只支持 `PIPELINE_RUNTIME_DEBUG_TRIGGER_*`。
3. 补齐 selected `PIPELINE_RUNTIME_BREADCRUMB_*` overlay 支持。
4. 本地验证 `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH=5` 会被正确写入 effective guest env。
5. 更新 workflow / observability 文档，明确 breadcrumb-only rerun 的使用边界。

## New Constraint For Next Rerun

下一轮主探针采用：

- `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=0`
- `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH=5`
- `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH_RADIUS=0`

保持：

- fixed runtime config / HWDB / AGFI 不变
- guest 文件日志优先，`uartlog` 仅作次级 boot 证据
- 只使用私有 IP 做远端检查

## Expected Benefit

breadcrumb 过滤发生在 breadcrumb 事件输出点本身，作用是减少落盘量；
它不会像 `dma-fixed-load` trigger 那样，在每个 fixed-load page 上额外走一遍 trigger 匹配与短日志路径。
因此这轮 rerun 的扰动应明显小于 `dma-fixed-load` 家族。

## Next Step

执行：

1. `debug-preflight`
2. `launch`
3. `infrasetup`
4. `current-private-ip`
5. `run`

然后继续以 guest 文件系统日志和 breadcrumb 为主监控新的 frontier。
