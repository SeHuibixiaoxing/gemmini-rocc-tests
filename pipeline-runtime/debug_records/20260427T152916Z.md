# Debug Record 20260427T152916Z

调试类别：gdbserver

## 本轮目标

用户指出刚结束的 NIC bitstream 不包含本轮 `fromHostValidDrive`
修复，要求先用这颗旧 AGFI 再试一次，确认是否还能暴露其它需要修复的
问题；随后把 `fromHostValidDrive` 修复连同当前 host debug 修复一起构建
新的 NIC bitstream。

## 输入状态

- 旧 NIC AGFI：`agfi-08ec9b835f726aef0`
- 旧 NIC build session：
  `rocket-singlecore-nic-emptytok-buildbitstream-20260427-1246`
- 旧 NIC build exitcode：`0`
- 旧 AGFI 不包含 2026-04-27 13:43 UTC 后的
  `fromHostValidDrive = fromHostTokenAvailable` 修复。
- 当前源码包含该修复：
  `generators/firechip/goldengateimplementations/src/main/scala/SimpleNICBridge.scala`

## 旧 AGFI quiet FPGA 复现

- runfarm session：
  `rocket-singlecore-nic-emptytok-agfi08ec-launchrunfarm-20260427-1512`
- run host：`i-0ec62c9bb1cadbcb7` / `192.168.1.117`
- infrasetup session：
  `rocket-singlecore-nic-emptytok-agfi08ec-infrasetup-20260427-1513`
- runworkload session：
  `rocket-singlecore-nic-emptytok-agfi08ec-runworkload-20260427-1517`
- runtime：
  `config_runtime_f2_rocket_singlecore_nic_gdbserver_smoke_notrace_quiet_preserve_30mhz.yaml`
- result dir：
  `sims/firesim/deploy/results-workload/2026-04-27--15-18-10-rocket-singlecore-nic-gdbserver-smoke-f2-rocket-singlecore-nic-gdbserver-smoke-notrace-quiet-longheartbeat-preserve/`
- 保存现场：
  `debug_records/artifacts/20260427_nic_emptytok_agfi08ec_quiet_prefix/`

### 关键现象

- 未进入 Linux boot：
  - `uartlog` 只有 FireSim / driver 初始输出。
  - `heartbeat.csv` 只有表头。
  - `niclog0` 为空。
  - `blkdev-log0` 为空。
- `uartlog` 确认实际刷入旧 AGFI：

```text
AFI ID for Slot  0: agfi-08ec9b835f726aef0
```

- host->target stream 反复卡在 FIFO 满：

```text
CPU_STREAM DEBUG push_blocked ... stream=SIMPLENICBRIDGEMODULE_0_from_cpu_stream ... count=3072 ... space_available=0 ... requested_bytes=57856 ...
```

## 判断

旧 AGFI 仍然稳定复现 pre-boot 的 host->target 输入流不被 FPGA target
继续消费的问题。它没有越过最早期 token/stream 互等点，因此无法观察到
Linux boot、guest NIC driver、gdbserver 或 remote gdb attach 的后续问题。

这与 `fromHostValidDrive` 修复前的 ready/valid 互等回归一致：

- host 侧还能继续尝试向 `from_cpu_stream` push；
- FPGA 端不再消费该 stream，导致 stream FIFO 满；
- 因为还没有进入 boot，当前旧 AGFI 没有暴露新的后续 blocker。

## fresh verbose 尝试

为了抓更细的 `SimpleNIC` debug regs，尝试启动 fresh F2 verbose runtime：

- session：
  `rocket-singlecore-nic-emptytok-agfi08ec-verbose-launchrunfarm-20260427-1524`
- runtime：
  `config_runtime_f2_rocket_singlecore_nic_gdbserver_smoke_notrace_preserve_30mhz.yaml`

结果：

- AWS F2 多轮返回 `insufficient capacity`，未分配实例。
- 为避免新 NIC bitstream 构建被不确定容量等待阻塞，手动停止该
  `launchrunfarm`。
- exitcode：`120`
- 没有 live F2 instance 残留。

## 清理

quiet run 的现场保存后，已回收对应 runfarm：

- terminate session：
  `rocket-singlecore-nic-emptytok-agfi08ec-terminate-20260427-1523`
- terminated instance：
  `i-0ec62c9bb1cadbcb7`
- 终止后实例进入 `shutting-down`。

## 新 bitstream 构建

已启动包含当前 `fromHostValidDrive` 修复的新 NIC bitstream 构建：

- session：
  `rocket-singlecore-nic-validdrive-buildbitstream-20260427-1528`
- build config：
  `config_build_f2_rocket_singlecore_nic_notrace_30mhz.yaml`
- build recipes：
  `config_build_recipes_f2_rocket_singlecore_nic_notrace_30mhz.yaml`
- build tag：
  `f2rocketsinglecorenicnotracebuild`
- build host type：
  `z1d.2xlarge`

启动时另一个非 NIC pairdummy bitstream 仍在独立 tmux 中运行：

- `pairdummy-sbus128-cfg32-nonic-buildbitstream-20260427-0630`

本轮没有终止或修改该构建。

## 当前结论

1. 旧 AGFI `agfi-08ec9b835f726aef0` 只复现了已知的早期
   host->target stream deadlock，没有暴露新的后续 NIC 问题。
2. 因为旧 AGFI 卡在 Linux boot 之前，不能用它验证 gdbserver 或 remote
   gdb attach。
3. 当前应等待新 bitstream 完成，再用 fresh F2 runfarm 验证：
   - Linux 是否能进入 boot；
   - guest NIC 是否能启动；
   - gdbserver smoke 是否能运行；
   - remote gdb attach 是否能建立双向通信。
