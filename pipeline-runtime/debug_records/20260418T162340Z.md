# 20260418T162340Z

## 本轮目标

- 按 `docs/plans/tracerv_metasim_bringup_plan_20260418.md`
  执行 `stage A`：
  用最小 baremetal metasim 验证
  `TracerV bridge -> host pull -> TRACEFILE copy-back`
  是否打通。

## 输入状态

- 新 runtime config：
  `/home/ubuntu/chipyard/sims/firesim/deploy/config_runtime_metasim_rocket_hello_baremetal_tracerv.yaml`
- 新 workload json：
  `/home/ubuntu/chipyard/sims/firesim/deploy/workloads/hello-baremetal-tracerv-metasim.json`
- build recipe：
  `/home/ubuntu/chipyard/sims/firesim/deploy/config_build_recipes_f2_rocket_30mhz.yaml`
- hwdb：
  `/home/ubuntu/chipyard/sims/firesim/deploy/config_hwdb.yaml`
- host-side `TracerV` 观测增强补丁已在工作树中：
  `generators/firechip/bridgestubs/src/main/cc/bridges/tracerv.{h,cc}`

## 执行命令

```bash
cmake -S /home/ubuntu/chipyard/tests -B /home/ubuntu/chipyard/tests/build -D CMAKE_BUILD_TYPE=Debug
cmake --build /home/ubuntu/chipyard/tests/build --target hello
cp -f /home/ubuntu/chipyard/tests/hello.riscv /home/ubuntu/chipyard/sims/firesim/deploy/workloads/hello.riscv

/home/ubuntu/chipyard/scripts/firesim-tmux-run.sh --session-name metasim-rocket-tracerv-launchrunfarm-20260418 \
  launchrunfarm -c config_runtime_metasim_rocket_hello_baremetal_tracerv.yaml \
  -a config_hwdb.yaml -r config_build_recipes_f2_rocket_30mhz.yaml

/home/ubuntu/chipyard/scripts/firesim-tmux-run.sh --session-name metasim-rocket-tracerv-infrasetup-20260418 \
  infrasetup -c config_runtime_metasim_rocket_hello_baremetal_tracerv.yaml \
  -a config_hwdb.yaml -r config_build_recipes_f2_rocket_30mhz.yaml

ssh-keygen -f /home/ubuntu/.ssh/known_hosts -R 192.168.1.109
mkdir -p /home/ubuntu/chipyard/sims/firesim/deploy/workloads/hello-baremetal-tracerv-metasim
cp -f /home/ubuntu/chipyard/sims/firesim/deploy/workloads/hello-baremetal/hello.riscv \
  /home/ubuntu/chipyard/sims/firesim/deploy/workloads/hello-baremetal-tracerv-metasim/hello.riscv

/home/ubuntu/chipyard/scripts/firesim-tmux-run.sh --session-name metasim-rocket-tracerv-infrasetup-retry1-20260418 \
  infrasetup -c config_runtime_metasim_rocket_hello_baremetal_tracerv.yaml \
  -a config_hwdb.yaml -r config_build_recipes_f2_rocket_30mhz.yaml

/home/ubuntu/chipyard/scripts/firesim-tmux-run.sh --session-name metasim-rocket-tracerv-runworkload-20260418 \
  runworkload -c config_runtime_metasim_rocket_hello_baremetal_tracerv.yaml \
  -a config_hwdb.yaml -r config_build_recipes_f2_rocket_30mhz.yaml
```

## 配置 / 环境

- run farm tag：
  `metasimrockettrv`
- metasim host simulator：
  `verilator`
- runtime tracing：
  - `enable: yes`
  - `selector: 0`
  - `output_format: 0`
  - `plusarg_passthrough: +trace-min-batch=0`
- workload copy-back：
  - `TRACEFILE*`
  - `metasim_stderr.out`

## 中途问题

### 1. 首轮 `infrasetup` 失败不是 TracerV 语义问题

失败日志：

- tmux pane：
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/metasim-rocket-tracerv-infrasetup-20260418.pane.log`
- manager log：
  `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-18--16-13-30-infrasetup-GAPQ3EBQS6SC7544.log`

直接原因有两个：

- 私网 IP `192.168.1.109` 在
  `/home/ubuntu/.ssh/known_hosts`
  中残留旧 host key，
  导致 rsync/ssh 被拒。
- 新 workload 的 `benchmark_name`
  是 `hello-baremetal-tracerv-metasim`，
  FireSim 因此去找：
  `deploy/workloads/hello-baremetal-tracerv-metasim/hello.riscv`
  但该目录当时不存在。

### 2. 修复后 `infrasetup` 通过

成功日志：

- tmux pane：
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/metasim-rocket-tracerv-infrasetup-retry1-20260418.pane.log`
- manager log：
  `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-18--16-19-45-infrasetup-PMNWMO76A5NU9F9F.log`

## 证据路径

- `launchrunfarm` pane：
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/metasim-rocket-tracerv-launchrunfarm-20260418.pane.log`
- `runworkload` pane：
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/metasim-rocket-tracerv-runworkload-20260418.pane.log`
- `runworkload` manager log：
  `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-18--16-20-56-runworkload-8QB25M9P90WN08RO.log`
- results dir：
  `/home/ubuntu/chipyard/sims/firesim/deploy/results-workload/2026-04-18--16-20-56-hello-baremetal-tracerv-metasim-metasim-rocket-tracerv/`
- `TRACEFILE-C0`：
  `/home/ubuntu/chipyard/sims/firesim/deploy/results-workload/2026-04-18--16-20-56-hello-baremetal-tracerv-metasim-metasim-rocket-tracerv/hello-baremetal-tracerv-metasim0/TRACEFILE-C0`
- `metasim_stderr.out`：
  `/home/ubuntu/chipyard/sims/firesim/deploy/results-workload/2026-04-18--16-20-56-hello-baremetal-tracerv-metasim-metasim-rocket-tracerv/hello-baremetal-tracerv-metasim0/metasim_stderr.out`
- `uartlog`：
  `/home/ubuntu/chipyard/sims/firesim/deploy/results-workload/2026-04-18--16-20-56-hello-baremetal-tracerv-metasim-metasim-rocket-tracerv/hello-baremetal-tracerv-metasim0/uartlog`

## 新观察

### 1. `stage A` 成功

`runworkload` 成功完成，且自动 terminate run farm。

关键证据：

- `metasim_stderr.out` 第 7 行：
  `TracerV[0]: first nonzero host pull bytes=192 selector=0 min_batch_beats=0 tracefile=TRACEFILE`
- `metasim_stderr.out` 第 14716 行：
  `TracerV[0]: flush summary nonzero_pulls=6083 total_bytes=941376`
- `TRACEFILE-C0` 大小：
  `661987` bytes
- `uartlog` 明确打印：
  `Hello world from core 0, a rocket`

### 2. 当前 `TracerV` host 观测补丁是有效的

这一轮已经不再需要通过
 “`TRACEFILE-C0` 是不是 0 字节”
 来猜 host 有没有真实 pull token。

现在可以直接依据：

- `first nonzero host pull`
- `flush summary`

判断 host-side `TracerV` 是否实际拉到数据。

### 3. metasim 默认 stderr 仍会被 DASM 指令打印淹没

本轮 `metasim_stderr.out` 约 `2.1MB`，
其中包含大量逐条指令的 `DASM(...)` 输出。

这不影响 `TracerV` 成功判定，
但说明第二阶段若继续用 metasim 做更长 workload，
仍应优先考虑收掉这类无关 stderr 噪声。

## 分析

- 这轮已经严格证明：
  在 FireSim manager 驱动的 baremetal metasim 下，
  `TracerV` 的
  `runtime config -> plusargs -> bridge enable -> host pull -> serialize -> copy-back`
  路径是通的。
- 因而后续如果在 `selector=3` 或更接近 `pairdummy` 的路径上看不到 trace，
  怀疑重心应转向：
  - 指令 trigger compare / arm path
  - workload 内 marker 是否确实落在目标 hart / 目标窗口
  - 更接近当前主线硬件配置的差异
- 当前不支持把这轮结论外推成：
  “pairdummy/sbus128 FPGA 主线上的 TracerV 问题已解决”。
  这轮只证明最小 metasim baremetal smoke 已经打通。

## 下一步

1. 在不改 Linux pairdummy 主线的前提下，
   进入 `stage B`：
   用轻量 baremetal repro 验证
   `selector=3`
   的 exact instruction trigger。
2. 候选入口优先：
   `rerocc_lc_export_dma_bertmini_segment3_repro`
   加入 `.word 0x00008013 / 0x00010013` marker。
3. 第二轮拉新机器前，
   先向用户汇报本轮结论和修改。
