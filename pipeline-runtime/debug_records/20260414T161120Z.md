# Debug Record 20260414T161120Z

## 目标

- 继续跟进 `2026-04-14 15:55 UTC` 启动的
  `dma-export page24` trigger rerun
- 判断这轮是否真的推进到
  `subbatch=3 tensor=2 page=24`
  附近
- 若没有推进到目标窗口，静态归因
  trigger 机制本身是否已把卡点前移

## 输入状态

- active run:
  - host private IP: `192.168.1.76`
  - session:
    `pairdummy-sbus128-runworkload-20260414-155507`
  - manager log:
    `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-14--15-55-09-runworkload-0JFQPC1EDG3G726D.log`
- 本轮仍不改硬件、不做 bitstream 重建
- 目标 trigger overlay 仍是：
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=dma-export`
  - `SEGMENT=0`
  - `GLOBAL_STAGE=0`
  - `LOCAL_STAGE=0`
  - `SUBBATCH=3`
  - `MANAGER=0`
  - `TENSOR_ID=2`
  - `PAGE=24`

## 命令

- 远端 live 检查：
  - `ssh ... ubuntu@192.168.1.76 'tail -n 5 /home/ubuntu/sim_slot_0/heartbeat.csv; tail -n 120 /home/ubuntu/sim_slot_0/uartlog'`
  - `ssh ... ubuntu@192.168.1.76 'sudo debugfs -R \"cat /root/pipeline-runtime-debug/bertmini-batch8.status\" \"$img\"'`
  - `ssh ... ubuntu@192.168.1.76 'sudo debugfs -R \"cat /root/pipeline-runtime-debug/bertmini-batch8.log\" \"$img\" | tail -n 80'`
  - `ssh ... ubuntu@192.168.1.76 'sudo debugfs -R \"cat /root/pipeline-runtime-debug/bertmini-batch8.breadcrumb.bin\" \"$img\" | base64 -w0'`
- 静态阅读：
  - `sed -n ... src/prt_trigger_log.c`
  - `sed -n ... src/prt_dma.c`
  - `sed -n ... src/prt_gemmini_artifacts.c`
- 历史基线对照：
  - `rg -n "artifacts file pread|init validate-artifacts end" tmp/firesim-aws-f2/captures ...`

## 新观察

- 这轮 run 已明确进入 guest wrapper/runtime，
  不是 Linux boot 早期问题：
  - `status=running`
  - `runner.stage` 已到
    `after-bin-spawn`
  - `runner-proc.stage` 显示
    `rerocc_pipeline_runtime-linux`
    仍在运行
- `bertmini-batch8.log`
  已推进过初始化并进入 export 区：
  - pointwise `mgr=7 flushed`
  - `export-target-dispatch stage=0 tensor=2 target_seq=1 ... phase=begin`
  - `dma-export-host ... page=0 ...`
- 但这份主日志尾部之后不再继续前进，
  而 `heartbeat.csv` 仍持续前进；
  因而这类 guest 文本尾部在本轮再次不可靠，
  不能直接当 authoritative frontier。
- 通过实时抓取并解码
  `bertmini-batch8.breadcrumb.bin`
  得到新的 authoritative 结论：
  - `2026-04-14 16:07:25 UTC`
    到
    `2026-04-14 16:09:09 UTC`
    连续多轮轮询中：
    - `update_count=7284`
    - `last_update_ns=6057165000`
    - `last_kind=dma`
    - `last_phase=dma_page_end`
    - `sb=1 tensor=2 mgr=0 page=42`
    完全不变
  - 同时 heartbeat 从
    `13485945482, 724`
    前进到
    `15563834744, 829`
- 因而当前 trigger rerun 的真实冻结前沿
  不是旧的
  `sb=3 page24 cleanup -> page25`
  窗口，
  而是更早地前移到了：
  `stage=0 subbatch=1 tensor=2 manager=0 page42 end -> page43 begin`
  之间。
- `trigger.log`
  在当前 run 中仍未形成有效内容；
  这和上面的新前沿一致：
  它根本还没跑到目标的
  `sb=3 page24`
  命中窗口。

## 静态归因

- 历史 baseline 显示：
  - 在同样
    `mapping cache disabled`
    的 profile 下，
    `validate-artifacts`
    与 `pread`
    路径此前都能顺利通过；
    本轮没有新的初始化 blocker 证据。
- 当前更可信的根因是：
  `trigger` 机制本身在“命中前”已经引入了持续热路径开销。
- `src/prt_trigger_log.c`
  当前旧逻辑中，
  `prt_trigger_log_note()`
  会在检查是否命中之前就：
  - `seq` 自增
  - `prt_trigger_log_format_line()`
  - `prt_trigger_log_ring_push()`
- `src/prt_dma.c`
  的 export host 路径会在每个 page/chunk 上调用：
  - `pset`
  - `v2p-b`
  - `v2p-e`
  - `sw-b`
  - `sw-e`
  这些 `dma_trigger_export_host(...)`
  点位
- 也就是说，
  即便目标是
  `sb=3 page=24`，
  当前实现仍会在此前大量
  `sb/page`
  上持续做格式化和 ring push；
  这与用户提出的
  “靠近卡点再开日志”
  目标相违背，
  也足以解释为什么 frontier 被前移到
  `sb1/page42`

## 结论

- 当前这轮 trigger rerun 不能再被当成
  `page24` 窄窗口取证结果。
- 它的有效结论是：
  1. 当前主日志尾部再次显著滞后于 breadcrumb
  2. 现有 trigger 实现的 pre-match 开销会真实移动卡点
  3. 下一轮必须先修 trigger 热路径，
     让未命中前只保留严格目标维度内的收集

## 下一步

1. 修改 `prt_trigger_log_note()`
   的 pre-match 行为：
   未激活且未落入目标维度时直接返回，
   不再提前格式化和 ring push
2. 本地做语法校验
3. 重新走
   `image-closure -> infrasetup -> remote-freshness -> runworkload`
4. 继续用同一组
   `sb=3 tensor=2 page=24`
   trigger overlay
   做 control rerun
5. 若修正后 frontier 再次回到
   `sb=3 page24`
   附近，
   就把这次
   `sb1/page42`
   明确归类为
   `probe-disturbance`

