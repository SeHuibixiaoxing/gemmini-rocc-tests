# 20260420T080618Z

调试类别：main

## 本轮目标

- 在已经落下 `host-watchdog` 双层超时补丁之后，
  用同一套 `g6 bertmini` workload
  做一轮真实 FPGA 复跑。
- 判定两件事：
  1. 旧的 `idle=625s` host 误杀是否消失
  2. 若误杀消失，真实 stall 能否用 live guest artifact 钉住

## 本轮输入

- workflow:
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_g6_workflow.sh`
- host monitor:
  - `/home/ubuntu/chipyard/scripts/firesim-prt-host-watchdog.sh`
- 关键日志：
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260420-073931.host-watchdog.log`
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260420-073931.pane.log`
- live snapshot:
  - `/home/ubuntu/chipyard/tmp/pipeline-runtime-live/20260420T080131Z`
  - `/home/ubuntu/chipyard/tmp/pipeline-runtime-live/20260420T080430Z`

## 使用命令

- `pairdummy_sbus128_g6_workflow.sh local-freshness`
- `pairdummy_sbus128_g6_workflow.sh launch`
- `pairdummy_sbus128_g6_workflow.sh infrasetup`
- `FIRESIM_MONITOR_HEARTBEAT_LIVENESS_ENABLE=1 FIRESIM_RUNWORKLOAD_LIVE_IDLE_TIMEOUT_SECONDS=10800 pairdummy_sbus128_g6_workflow.sh run 192.168.1.80`
- `ssh ubuntu@192.168.1.80 ... tail heartbeat.csv / uartlog`
- `sudo debugfs -R "cat <guest-file>" <remote-img>`
- `decode_prt_breadcrumb.py <breadcrumb.bin>`
- `pairdummy_sbus128_g6_workflow.sh terminate`

## 新观察

### 1. host-watchdog 误杀已经被实际消除

- 这轮 watchdog 在
  `2026-04-20T07:46:20Z`
  以
  `guest-status arm`
  进入 armed 状态
- 随后 guest-visible 进展停在：
  - `guest_sparse=319955`
  - 对应 heartbeat 大约 `hb=... , 576`
- 之后 heartbeat 持续推进：
  - `608`
  - `640`
  - `705`
  - `953`
  - `1202`
  - `1484`
- 同时 watchdog 记录：
  - `idle=625s hb_idle=0s`
  - `idle=688s hb_idle=0s`
  - `idle=907s hb_idle=0s`
- 这轮在 `idle=625s`
  没有再触发 host capture / terminate，
  说明旧的误杀路径已被切断

### 2. 真实 stall 现在被钉在同一个前沿，而不是“移动卡点”

- 两次 live snapshot：
  - `20260420T080131Z`
  - `20260420T080430Z`
  之间，
  以下文件字节数完全一致：
  - `bertmini-batch8.log = 319955`
  - `bertmini-batch8.breadcrumb.bin = 7272`
  - `bertmini-batch8.runner-proc.stage = 9265`
  - `bertmini-batch8.runner.stage = 479`
  - `bertmini-batch8.wrapper-proc.stage = 2574`
- 两次 breadcrumb 解码也完全一致：
  - `update_count=13537`
  - `last_kind=rr`
  - `last_phase=rr_acquire_after_call`
  - `seg=0 gstage=0 lstage=0 sb=3 mgr=2 tok=1`
  - `line=356`
- 同时 sparse 尾部两次都一致：
  - `worker stage=0 subbatch=3 begin`
  - `conv-sync-strided stage=0 mgr=0 ... flushed use_pointwise=1`
  - `conv-sync-strided stage=0 mgr=1 begin`
    半行后全是 `NUL`
- 因而当前更可信的解释是：
  **真实 stall 固定在 `subbatch=3` 的 pointwise / RR handoff 窗口，
  不是 sparse 热路径扰动导致的“前沿移动”。**

### 3. 这轮结束原因是我们主动停机，不是 guest 自己报错

- 在拿到足够 live 证据后，
  手动执行了
  `terminaterunfarm --forceterminate`
- 随后 `runworkload` manager
  和之前一样在 SSH 轮询处报：
  `Error reading SSH protocol banner`
- 但这次该报错发生在我们主动 terminate 之后，
  只说明 manager 在实例下线过程中收尾，
  不能再被误读成 guest 自发失败

## 本轮结论

- `host-watchdog` 这一层已经从当前主线 blocker 中移除。
- 当前 `g6 bertmini` 的真实 stall
  已被收敛到：
  `seg=0 stage=0 subbatch=3`
  的 pointwise 路径中，
  更具体地说，
  落在
  `mgr=2 rr_acquire_after_call`
  与 coarse sparse 中
  `mgr=1 begin`
  之间的窄窗口。
- 当前不需要再继续拍脑袋放宽 timeout；
  下一步应转向：
  **围绕 `rr_acquire_after_call` / pointwise acquire handoff`
  做静态代码审计和低扰动定点观测。**

## 下一步

1. 以当前 live 证据为 authoritative frontier，
   回到代码中静态审计：
   - pointwise path
   - RR acquire / release
   - manager 0/1/2 间的 handoff 语义
2. 只在必要时做新的单变量 rerun；
   下一轮 probe 应该直接瞄准
   `subbatch=3 mgr=2 rr_acquire_after_call`
   附近，
   不要再泛化到整段 sparse frontier。
