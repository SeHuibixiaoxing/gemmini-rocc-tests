# 2026-04-14T09:37:12Z 调试记录

## 本轮目标

- 重新通读主文档，
  纠正对约束、架构和 plan 的执行偏差。
- 把新的 boot 等待硬约束写回 authoritative 文档。
- 同时继续推进当前 `v13` rerun，
  但严格按“boot 早期无明确报错就继续等”的规则执行。

## 本轮执行

- 重新阅读：
  - `README.md`
  - `docs/project_guide.md`
  - `docs/CURRENT_STATUS.md`
  - `docs/constraints/hard_constraints.md`
  - `DECISIONS.md`
  - `docs/architecture/runtime_mechanisms.md`
  - `docs/architecture/alignment_constraints.md`
  - `docs/plans/runtime_alignment_plan.md`
  - `docs/plans/hybridmapper_alignment_plan.md`
  - `docs/plans/roadmap.md`
  - `docs/testing/observability.md`
  - `docs/workflows/pairdummy_sbus128.md`
- 重新确认当前主线约束：
  - fixed workflow / fixed profile / freshness 双闭环
  - static-first debug 顺序
  - `HybridMapper + MudnacSim` 语义基线
  - `DRAM_DEPEN` / `ALL_RINGBUFFER` 对齐目标
  - Linux boot 早期等待规则
- 继续推进 live run：
  - `launchrunfarm` 最终成功拿到
    `192.168.1.247`
  - `infrasetup` 完成，
    driver readiness preflight 通过
  - `remote-freshness 192.168.1.247`
    通过，
    image 内确认仍是
    `pairdummy-sbus128-fixed-v13`
  - 启动：
    `pairdummy-sbus128-runworkload-20260414-093500`
- 重新运行 artifact 审计，
  结果为：
  `PASS target=rerocc_globalnoc_pairmanager_dummy16x16_c4_g12_d12_spad1024kb_dram19_noc64_mac256_sbus128 segments=13`

## 新结论

- 当前最需要牢记的硬约束已经再次确认：
  Linux boot 早期只要没有明确 boot error / panic / crash，
  且 `heartbeat` 仍在前进，
  就继续等；
  不要仅凭 `uartlog` 静默窗口把它记成新的异常或 blocker。
- 当前语义/架构基线仍是：
  - runtime 只兑现 artifact，
    不在执行期重建 mapping
  - `tensor_id`
    是 transport 主键
  - `DRAM_DEPEN`
    不能退化成纯 ring
  - `ALL_RINGBUFFER`
    不能在内部 transport 后无条件 export alias materialize
- 当前 active run：
  - host：
    `192.168.1.247`
  - runworkload session：
    `pairdummy-sbus128-runworkload-20260414-093500`
  - 截至 `2026-04-14 09:36 UTC`，
    `heartbeat.csv`
    已推进到
    `92s`
  - `uartlog`
    仍位于正常 Linux boot 早期阶段
  - image 内尚无
    `/root/pipeline-runtime-debug`
  - 因此当前动作不是加新探针，
    而是继续等待 boot 越过用户态入口

## 下一步

1. 继续按硬约束等待 `192.168.1.247` 完成 Linux boot。
2. 一旦 `/root/pipeline-runtime-debug` 出现，
   读取 `status/log/breadcrumb`，
   看是否已推进到 `c2-export` page0。
3. 只有真正进入 runtime 后，
   才继续按既定计划细分到：
   `v2p-begin / v2p-end / first-chunk-submitwait-begin/end`
