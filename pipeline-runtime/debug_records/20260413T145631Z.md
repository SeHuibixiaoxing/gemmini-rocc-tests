# 2026-04-13T14:56:31Z 调试记录

## 本轮目标
- 继续监控 `v10` fresh run，确认它是否已经从 boot/init 阶段推进到真正的 pipeline runtime 执行阶段。
- 在到达 `segment=0 / stage=0` 前，不误判新的“卡点前移”。

## 本轮改动
- 无代码改动。
- 主要做 live 观测：
  - 私网 SSH 到 `192.168.1.216`
  - 读取 guest 镜像中的 `status / runner.stage / runner-proc.stage / sparse log / breadcrumb`
  - 对照 host watchdog 的 `guest_sparse` 变化

## 关键产物
- run session：
  - `pairdummy-sbus128-runworkload-20260413-144705`
- host watchdog：
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-144705.host-watchdog.log`
- live breadcrumb dump：
  - `/tmp/bertmini-batch8-live-20260413T1455.breadcrumb.bin`

## 当前结论
- 这轮 run 已真实进入 runtime，不是停在 boot。
- 直接证据：
  - `bertmini-batch8.status` 显示 `state=running`
  - `runner.stage` 已到：
    - `after-prepare-hugetlb`
    - `after-bin-spawn method=ours2 pid=196`
  - `runner-proc.stage` 里 runtime 进程 `rerocc_pipeline_runtime-linux` 正在运行
- 当前 live 进度还主要在 artifact / layer mapping 装载阶段：
  - `artifacts mapping load end ... elapsed_ms=538`
  - `artifacts mapping parse progress ... entries=8192`
- host watchdog 也确认了真实推进：
  - `guest_sparse=1009`
  - `guest_sparse=203610`
  - `guest_sparse=211474`

## 当前不是卡点的东西
- 不能把当前 `bertmini-batch8.log` 里看到的 `layer_mapping` 读取过程当成新 blocker。
- 也不能把 breadcrumb 仍停在 `runtime_init_done` 误判为卡死：
  - 当前 breadcrumb 过滤条件只关注 `segment=0 / global_stage=0 / local_stage=0`
  - 在正式进入 `segment=0/stage=0` 前，breadcrumb 不继续变化是合理的

## 当前状态
- guest heartbeat 已推进到至少：
  - `10088674179, 552`
- host watchdog 最新：
  - `guest_status=1386`
  - `guest_sparse=211474`
- 说明 run 仍在前进，但还没到之前排查的 `fixed-load tensor=1000001` 热点区域

## 下一步
1. 继续低频监控 `guest_sparse` 是否增长。
2. 一旦 sparse log 进入 `segment=0` 执行区域，立刻重新抓 breadcrumb。
3. 到达 `fixed-load tensor=1000001` 后，检查 `v10` 的 probe 是否真的输出 page 级信息。
