# Debug Record 20260414T103114Z

## 目标

- 沿用户要求继续遵守：
  先静态 artifact 审计与代码阅读，
  再加最小扰动 probe。
- 本轮目标是验证：
  旧 frontier 清除后，
  `worker stage=0 subbatch=1 done`
  之后的下一轮
  `C1`
  输入 fixed-load
  是否在
  `stage0 tensor0 page0`
  处卡住。

## 本轮静态结论

- `pipeline_mapping ... ours2.yaml`
  的 segment0/stage0：
  - `entryTensorIdList=[0]`
  - `entryTensorTypeList=[DRAM]`
  - `localSpmPageCountList` 里
    `tensor 0`
    占
    `64`
    页
- `prt_process_c1()`
  对 stage0 输入 `tensor 0`
  走：
  - `prt_dma_copy_dram_to_spm_pages_prefix()`
  - `dma_copy_host_to_spm_pages_linux()`
- 旧 fixed-load sparse probe
  硬编码在
  `tensor_id == 1000001U`
  上；
  所以之前的 env-only 调参看不到
  `tensor 0`
  路径。
- `dma_blocking_wait()`
  里
  `after-shared-fence`
  与
  `after-release`
  之间，
  对当前 external RR scope 路径没有新的 DMA / RR / release 动作；
  若 frontier 落到这里，
  高概率是观测扰动，
  不是硬件语义错误。

## 本轮修改

- 给 fixed-load sparse probe 增加 env 选择器：
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PROBE_STAGE_ID`
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PROBE_TENSOR_ID`
- checkpoint / submit / wait / page probe
  统一复用同一组
  `stage/tensor`
  过滤条件。
- 把固定 profile 切到
  `pairdummy-sbus128-fixed-v14`
  并指向：
  - `stage=0`
  - `tensor=0`
  - `page=0`
- 补齐 env 传递链：
  - `workload/host-init.sh`
  - `scripts/firemarshal-tmux-run.sh`
  - `pairdummy_sbus128_workflow.sh show`

## 构建与 freshness

- `image-closure`：通过
- local freshness：通过
- remote freshness：通过
- remote host：
  `192.168.1.87`
- 远端 guest image 已确认：
  `pairdummy-sbus128-fixed-v14`

## live run

- manager session：
  `pairdummy-sbus128-runworkload-20260414-101536`
- results dir：
  `sims/firesim/deploy/results-workload/2026-04-14--10-15-37-rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy-f2-gemmini-rerocc-pairmanager-dummy16x16-4c12p12-sbus128-linux-bertmini-batch8-fileonly-sync/`
- pane：
  `tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260414-101536.pane.log`
- run host：
  `192.168.1.87`

## live 结果

- `bertmini-batch8.status`：
  `state=running`
- `heartbeat.csv`
  截至记录时已推进到：
  `882s`
- sparse log 已明确证明：
  - `worker stage=0 subbatch=1 done`
  - 随后
    `stage0 tensor0 page0`
    的
    `page-setup -> v2p -> submitwait-end`
    全部通过
  - `tok=643..649`
    的 fixed-load submit/wait
    也持续推进
- 当前 live 文本前沿稳定停在：
  `dma-fixed-load-wait phase=after-shared-fence token=649 stage=0 tensor=0 valid=1`
- 尚未看到同 token 的：
  `after-release`

## 当前判断

- 本轮已经排除：
  `C1 tensor0 page0`
  是 blocker。
- 当前
  `tok=649 after-shared-fence`
  更像 probe/frontier 自身扰动：
  因为源码里该点之后没有新的硬件语义动作，
  只剩 breadcrumb note 和下一条文本日志。
- 当前 breadcrumb 仍停在更早的
  `tensor=2 page=63 subbatch=1 dma_page_end`；
  静态上这是因为
  `C1`
  预取路径发生在 worker 的
  `prt_log_gate_set_context()`
  之前，
  现有 breadcrumb 对这段路径并不敏感。

## 下一步

1. 若当前 run 继续稳定停在
   `tok=649 after-shared-fence`，
   先按 probe 扰动处理，
   不升级成硬件 blocker。
2. 下一轮优先缩：
   `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PROBE_TOKEN_START/END`
   到
   `649`
   附近，
   不再保留全窗口 token 日志。
3. 或者改成更低扰动的
   `C1`
   breadcrumb/context
   覆盖，
   替代当前高频文本 wait probe。
