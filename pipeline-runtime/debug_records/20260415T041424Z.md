# Debug Record 20260415T041424Z

## 目标

- 按既定 SOP 开启新一轮
  `pairdummy-sbus128-fixed-v20`
  调试
- 先做制品审计、
  静态代码复核、
  抓取结果分诊，
  再决定是否进入单变量重跑
- 若进入重跑，
  只允许使用
  `PIPELINE_RUNTIME_DEBUG_TRIGGER_*`
  覆盖参数

## 输入状态

- 当前时间：
  `2026-04-15T04:14:24Z`
- 本轮开始前未做新的代码修改、
  未发起新的 FireSim run
- 当前 authoritative docs / records
  以
  `2026-04-14`
  重组后的文档体系为准
- 上一轮 authoritative capture：
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/pairdummy-sbus128-runworkload-20260414-162449-192.168.1.147-host-watchdog-20260414T164512Z`

## 命令

- 建立本轮记录前后的上下文确认：
  - `git status --short`
  - `sed -n '1,220p' docs/workflows/pairdummy_sbus128.md`
  - `sed -n '1,220p' docs/CURRENT_STATUS.md`
- 锁定本轮制品：
  - `pairdummy_sbus128_workflow.sh show`
  - `ls -1 conference/HybridMapper/output/pipeline_runtime/bertmini`
  - `sed -n '1,220p' conference/HybridMapper/output/pipeline_runtime/bertmini/manifest.yaml`
- 静态 artifact audit：
  - `python3 scripts/audit_pipeline_runtime_artifact.py --pipeline-yaml ...ours2.yaml --hardware-yaml ...hardware_target....yaml --model-yaml ...model.layers.yaml --expect-target-key rerocc_globalnoc_pairmanager_dummy16x16_c4_g12_d12_spad1024kb_dram19_noc64_mac256_sbus128`
- 旧 capture triage：
  - `python3 scripts/triage_prt_capture.py --breadcrumb ...guest-breadcrumb.bin --sparse-log ...guest-sparse-log.txt --trigger-log ...guest-trigger-log.txt --change-kind observability --emit-trigger-env`
  - `python3 scripts/decode_prt_breadcrumb.py --all ...guest-breadcrumb.bin | tail -n 120`
- 静态代码复核：
  - `nl -ba src/prt_dma.c | sed -n '1580,1675p'`
  - `nl -ba src/prt_dma.c | sed -n '1676,1755p'`
  - `nl -ba src/prt_scheduler.c | sed -n '318,399p'`
  - `nl -ba src/prt_scheduler.c | sed -n '450,535p'`
  - `nl -ba src/prt_rerocc.c | sed -n '340,390p'`
  - `nl -ba src/prt_trigger_log.c | sed -n '260,380p'`
- 单变量 rerun：
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=1`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=rr`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_GLOBAL_STAGE=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_LOCAL_STAGE=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_SUBBATCH=4`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_MANAGER=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_TOKEN=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_PRE_RING=8`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_POST_BUDGET=96`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_MATCH_ONCE=1`
  - `pairdummy_sbus128_workflow.sh debug-preflight`
  - `pairdummy_sbus128_workflow.sh image-closure`
  - `pairdummy_sbus128_workflow.sh launch`
  - `pairdummy_sbus128_workflow.sh infrasetup`
  - `pairdummy_sbus128_workflow.sh remote-freshness 192.168.1.109`
  - `pairdummy_sbus128_workflow.sh run 192.168.1.109`
- live 观测：
  - `tail -n ... tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260415-042528.*`
  - `ssh -i /home/ubuntu/firesim.pem ubuntu@192.168.1.109 'tail -n 80 /home/ubuntu/sim_slot_0/uartlog'`
  - `ssh -i /home/ubuntu/firesim.pem ubuntu@192.168.1.109 'tail -n 20 /home/ubuntu/sim_slot_0/heartbeat.csv'`
  - `ssh -i /home/ubuntu/firesim.pem ubuntu@192.168.1.109 'sudo debugfs -R "cat /root/pipeline-runtime-debug/..." <img>'`

## 新观察

- `artifact-audit` 通过：
  - `segments=13`
  - `tensor_type_coverage={'ALL_RINGBUFFER': 14, 'DRAM': 41, 'ISOLATE_SPM': 8, 'SHARED_SPM': 33}`
  - `buffer_binding_coverage={'PIPE': 96, 'RING': 7, 'WEIGHT': 56}`
  - `split_kind_coverage={'oc': 32, 'resadd_spatial': 6, 'single': 2}`
  - `op_type_coverage={'conv': 32, 'resadd': 8}`
  - `rr_stage_scope_budget=7 reserved_cfg=15`
- 这说明当前 pairdummy sbus128 主线所用的
  `HybridMapper`
  产物在静态层面仍与
  `target_key`
  匹配，
  没有出现“拿错 2-pair / 旧 target”问题。
- 对
  `20260414T164512Z`
  capture
  重新 triage 后，
  结论比上一轮更收敛：
  - `trigger_budget_exhausted=yes`
  - 当前权威边界是
    `frontier=rr/rr_release_end seg=0 gstage=0 lstage=0 sb=4 mgr=0`
  - 不是
    `page63`
    自身，
    也不是
    `page28`
    截断点
- `decode_prt_breadcrumb.py --all`
  进一步确认顺序是：
  - `sb=4 tensor=2 page=63 dma_submitwait_after_cleanup`
  - `sb=4 tensor=2 page=63 dma_page_end`
  - `sb=4 rr_release_end`
- 静态代码复核给出新的细化判断：
  - `page63 dma_submitwait_after_cleanup`
    发生在
    [`prt_dma.c`](../src/prt_dma.c)
    的
    `prt_dma_token_cleanup()`
    返回前后
  - `rr_release_end`
    来自
    [`prt_rerocc.c`](../src/prt_rerocc.c)
    的
    `prt_rr_release_scope()`
  - 真正还没被当前观测面分开的，
    是
    [`prt_scheduler.c`](../src/prt_scheduler.c)
    的
    `prt_progress_export_dma()`
    中，
    `prt_dma_token_cleanup()` 返回之后的几步：
    - `buf->full[target_idx] = 0`
    - `buf->cmd_running[target_idx] = 0`
    - `buf->cmd_count[target_idx] = 0`
    - `buf->dma_token_live[target_idx] = 0`
    - `prt_trace_on_export_retire(rt)`
    - `pthread_cond_broadcast(&buf->cv)`
- 本轮因此没有加新的代码探针，
  只做一次更窄的
  `rr`
  触发重跑，
  并把
  `POST_BUDGET`
  从
  `32`
  提到
  `96`。
- `debug-preflight` 通过，
  且没有混入其它高风险 probe family。
- `image-closure`
  和
  `remote-freshness`
  都通过，
  重点确认：
  - `PIPELINE_RUNTIME_PROFILE_ID='pairdummy-sbus128-fixed-v20'`
  - `PIPELINE_RUNTIME_MLOCKALL_MODE='2'`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND='rr'`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_SUBBATCH='4'`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_MANAGER='0'`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_TOKEN='0'`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_POST_BUDGET='96'`
- 新实例信息：
  - instance:
    `i-05835f41d4f0dbac6`
  - private_ip:
    `192.168.1.109`
  - AGFI:
    `agfi-0dc8dcfa4c7735f40`
- 当前 live run 会话：
  - tmux session:
    `pairdummy-sbus128-runworkload-20260415-042528`
  - manager log:
    `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-15--04-25-29-runworkload-4PNUMES3PKA0SUMG.log`
- 截至本记录更新时，
  live probe 只看到：
  - `uartlog`
    仍处在 Linux early boot
  - `heartbeat.csv`
    持续推进到
    `2004136994 cycles / 116 seconds`
  - guest image 内
    `/root/pipeline-runtime-debug/bertmini-batch8.*`
    还没有开始生成内容
- 因而：
  本轮尚未重新抵达旧的
  `sb4/rr_release_end`
  frontier；
  当前 live 状态只是 guest 仍在启动链前段，
  不能解释成回退到旧 DMA 卡点。

## 结论

- 本轮已经完成一整套符合 SOP 的
  单变量 rerun
  闭环：
  - artifact audit
  - static code reading
  - capture triage
  - trigger-only rerun
  - freshness revalidation
  - live monitoring
- 当前最细的静态 frontier
  仍是：
  `sb=4`
  上
  `rr_release_end`
  之后、
  `prt_progress_export_dma()`
  内部状态退休与 worker 唤醒之间的窗口。
- 本轮还没有新的 runtime 语义证据，
  也没有新的硬件不匹配证据。
- live run 已正常起飞，
  但 guest 还没启动到 workload，
  所以这轮暂时还不能判断
  `POST_BUDGET=96`
  的
  `rr`
  触发窗是否足以越过旧 frontier。

## 下一步

1. 继续监控
   `pairdummy-sbus128-runworkload-20260415-042528`
   的
   host-watchdog
   和 guest 文件抓取
2. 一旦 guest 开始写
   `bertmini-batch8.status`
   /
   `bertmini-batch8.log`
   /
   `bertmini-batch8.trigger.log`
   ，
   立即重新 triage
3. 若新 trigger capture
   仍只停在
   `rr_release_end`
   ，
   再考虑是否需要在
   `prt_progress_export_dma()`
   里补更窄的、
   条件化的低扰动日志
