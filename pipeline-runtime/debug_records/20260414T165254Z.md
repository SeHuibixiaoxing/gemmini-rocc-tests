# Debug Record 20260414T165254Z

## 目标

- 收尾并复盘最新一轮
  `v20` strict-trigger control rerun
- 判断
  `guest-trigger-log`
  末尾的
  `sb=3 page=28 ph=pset`
  是否真是新 frontier
- 静态复核旧的
  `sb3/page24..28`
  export host
  窗口，
  避免把 trigger 截断误记成 blocker

## 输入状态

- host:
  `192.168.1.147`
- runworkload session:
  `pairdummy-sbus128-runworkload-20260414-162449`
- manager log:
  `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-14--16-24-50-runworkload-4EWZOJ3EUGMJ6TFN.log`
- official capture prefix:
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/pairdummy-sbus128-runworkload-20260414-162449-192.168.1.147-host-watchdog-20260414T164512Z`
- 本轮仍不改硬件、
  不做 bitstream 重建、
  不走 metasim

## 命令

- 读 trigger/status/manager 尾部：
  - `tail -n 80 ...guest-trigger-log.txt`
  - `tail -n 80 ...status.txt`
  - `tail -n 120 ...runworkload-4EWZOJ3EUGMJ6TFN.log`
- 解码 breadcrumb：
  - `python3 scripts/decode_prt_breadcrumb.py ...guest-breadcrumb.bin`
  - `python3 scripts/decode_prt_breadcrumb.py --all ...guest-breadcrumb.bin`
- 读 sparse / stage：
  - `tail -n 120 ...guest-sparse-log.txt`
  - `rg -n "subbatch=3|subbatch=4|c2-export|export-target-dispatch" ...guest-sparse-log.txt`
- 静态阅读：
  - `sed -n ... src/prt_dma.c`
  - `sed -n ... src/prt_page_table.c`
  - `sed -n ... src/prt_scheduler.c`
  - `sed -n ... src/prt_runtime.c`
- EC2 状态：
  - `aws ec2 describe-instances --instance-ids i-0cfb64404d409fed9 ...`
- triage：
  - `python3 scripts/triage_prt_capture.py --breadcrumb ... --sparse-log ... --trigger-log ... --change-kind observability --emit-trigger-env`

## 新观察

- 这轮 manager 退出原因仍是基础设施侧：
  `Error reading SSH protocol banner`
  不是 guest panic / kernel crash。
- 对应 EC2
  `i-0cfb64404d409fed9`
  最终状态已确认：
  `terminated`
- `guest-trigger-log.txt`
  只有
  `33`
  行，
  且内容是：
  - 第 1 行：
    `sb=3 page=24 ph=pset`
  - 第 33 行：
    `sb=3 page=28 ph=pset`
- 结合当前 trigger 配置：
  - `POST_BUDGET=32`
  - `MATCH_ONCE=1`
  可直接判定：
  这份 trigger log
  正好等于
  `match line + post_budget`
  窗口，
  即：
  `page28 pset`
  只是 capture cutoff，
  不是 authoritative frontier。
- sparse log 里已经明确穿过旧窗口：
  - `c2-export stage=0 tensor=2 phase=copy-end idx=0 subbatch=3 rc=0`
  - `worker stage=0 subbatch=3 done`
  - `worker stage=0 subbatch=4 begin`
  - `c2-export stage=0 tensor=2 phase=copy-begin idx=0 subbatch=4`
- breadcrumb `--all`
  也说明 capture 时 guest 已经推进到
  `subbatch=4`
  的 export 后段：
  - `sb=4 tensor=2 page=63 dma_submitwait_after_cleanup`
  - `sb=4 tensor=2 page=63 dma_page_end`
  - `sb=4 rr_release_end`
- 这说明：
  strict-trigger 修复后的 control rerun
  至少已经把旧的
  `sb3/page24`
  frontier
  明确穿过去了。

## 静态审计

- 对旧的
  `sb3/page24..28`
  export host
  窗口做静态复核后确认：
  - `pset -> v2p-b`
    之间没有新的硬件指令
  - 只剩：
    `dma_chunk_needs_bounce()`
    与紧邻其后的
    `prt_host_virt_to_phys()`
- 按本轮真实地址计算：
  - `dst_base = 0x3f847a1400`
  - page stride = `0x400`
  - page24..28 / page63
    的
    `src mod64 == dst mod64 == 0`
  - 因而
    `dma_chunk_needs_bounce() == false`
- 旧窗口不会走 bounce path，
  当前没有看到新的
  DMA 对齐 /
  pair-manager /
  `HybridMapper`
  语义错位证据。
- 同时，
  `req.src_acc == req.dst_acc == manager_id`
  和外层 RR scope 的 acquire/release
  顺序仍与当前 runtime 设计一致，
  没看到需要改硬件才能解释的静态异常。

## 结论

- 之前把
  `page28 pset -> v2p-b`
  当成新 frontier
  的说法无效；
  它只是 trigger budget 打满后的截断点。
- 这轮真正成立的结果是：
  1. strict-trigger 修复有效，
     旧的 pre-match probe-disturbance 已被消掉
  2. 旧的
     `sb3/page24`
     frontier
     已被 control rerun 穿过
  3. 新的 blocker 还没有被重新精确定义，
     但它至少已经后移到
     `subbatch=4`
     export 后段 / cleanup
     附近或之后
- 当前没有“必须改硬件”的证据。

## 下一步

1. 下一轮不要再用
   `sb3/page24`
   trigger
2. 优先改成：
   - `family=dma-export`
   - `subbatch=4`
   - `tensor=2`
   - `page=63`
   - `POST_BUDGET=64`
     或
     `96`
3. 继续坚持：
   先静态读代码，
   再单变量 rerun，
   不回退到大面积 deep log
4. 继续用
   `triage_prt_capture.py`
   检查：
   - `trigger_line_count`
   - `trigger_budget_exhausted`
   - `recommended_next_probe`
