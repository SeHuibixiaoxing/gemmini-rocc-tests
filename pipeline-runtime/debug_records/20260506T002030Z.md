# 2026-05-06 00:20:30 UTC - noTrace cfg32 NIC completes place_design

## Goal

Record the next build milestone for the active
`12p4c128sbus32cfg + optimized DMA + current NIC` noTrace fallback build. The
previous checkpoint showed the build had crossed `Phase 3 Detail Placement`;
this checkpoint confirms Vivado completed the full `place_design` command.

## Build

- FireSim tmux session:
  `pairdummy-cfg32-nic-notrace-20260505T171453Z`
- Build host:
  `i-0ecf73dad2a8ae8d0`, private IP `192.168.0.213`
- Build config:
  `sims/firesim/deploy/config_build_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_cfg32_nic_notrace.yaml`
- Build recipe:
  `sims/firesim/deploy/config_build_recipes_f2_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_nic_notrace.yaml`
- Target config:
  `FireSimGemminiReRoCCPairDummy16x16C4P12Sbus128NICNoTraceConfig`
- Platform config:
  `FRFCFS16GBQuadRank_BaseF2Config`
- Strategy/frequency:
  `TIMING`, `20MHz`
- Remote Vivado log:
  `/home/ubuntu/firesim-build/platforms/f2/aws-fpga-firesim-f2/hdk/cl/developer_designs/cl_f2-firesim-FireSim-FireSimGemminiReRoCCPairDummy16x16C4P12Sbus128NICNoTraceConfig-FRFCFS16GBQuadRank_BaseF2Config/build/scripts/2026_05_05-191909.vivado.log`

## Evidence

At `2026-05-06T00:19:35Z`, the remote Vivado log reported:

```text
Phase 4 Post Placement Optimization and Clean-Up | Checksum: 28d38496c
Ending Placer Task | Checksum: 1c39260a0
281 Infos, 4 Warnings, 0 Critical Warnings and 0 Errors encountered.
place_design completed successfully
place_design: Time (s): cpu = 05:51:53 ; elapsed = 02:43:07 .
AWS FPGA: (00:17:25): Writing post-place design checkpoint and report
```

The same tail included the expected warning for a dense debug image:

```text
WARNING: [Place 46-14] The placer has determined that this design is highly congested and may have difficulty routing.
```

and a post-placement timing summary:

```text
INFO: [Place 30-746] Post Placement Timing Summary WNS=-3.250.
```

AWS state at the same checkpoint still showed only one relevant build host:

```text
i-0ecf73dad2a8ae8d0 z1d.3xlarge running 192.168.0.213
```

No F2 runfarm was running.

## Interpretation

This confirms that the noTrace fallback has cleared the whole placement command,
not just the detail-placement phase that killed the mainline cfg32 NIC build.
The current blocker is now route/bitstream/AFI completion, with congestion and
negative post-placement timing as the main risks to watch.

This is still not a usable gdbserver AGFI. Do not update the cfg32 NIC HWDB and
do not launch a runfarm until FireSim produces an AGFI/AFI and the noTrace
driver bundle path is paired with that AGFI.

## Next actions

1. Continue the active 1200s polling loop.
2. If route, bitstream generation, and AFI creation succeed, update the cfg32
   NIC HWDB to the new noTrace AGFI plus the prepared noTrace driver bundle,
   then commit before launching `infrasetup`.
3. If route or timing fails, collect the first failing Vivado phase, the
   post-place and route reports, and only then choose whether the next build
   should be a strategy variant or a further resource-reduction variant.
