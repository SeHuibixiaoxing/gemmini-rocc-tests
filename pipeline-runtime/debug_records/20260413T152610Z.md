# 2026-04-13T15:26:10Z debug record

## Scope
- Continue monitoring the live `v11` run on `12-pair sbus128`.
- Confirm whether the run re-stabilizes at the previously known `page36/page37` export plateau or reaches a new frontier.

## Runtime context
- Instance id: `i-0ad2e1c97e4708e08`
- Private IP: `192.168.1.115`
- Session: `pairdummy-sbus128-runworkload-20260413-151358`
- Profile: `PIPELINE_RUNTIME_PROFILE_ID=pairdummy-sbus128-fixed-v11`
- Export probe window:
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START=36`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END=37`

## Observed before this record
- host watchdog arm:
  - `2026-04-13T15:20:47Z`
  - `hb='7092665095, 393'`
  - `guest_status=1386`
- guest sparse growth sequence:
  - `203604`
  - `211468`
  - `404951`
- latest watchdog state at record creation:
  - `hb='13112237668, 705'`
  - `idle=125s`
  - `guest_sparse=404951`
- Interpretation:
  - this is a new plateau candidate for the current live run
  - exact code-location still needs sparse-tail + breadcrumb confirmation

## Immediate next actions
1. Read guest sparse log tail and filter only execution markers near `segment=0`.
2. Decode current breadcrumb from the guest image.
3. Compare the frontier with the previously known `page36 end -> page37 begin` boundary.

## Confirmed result
- The previously known `v10` plateau (`subbatch=3 / target_seq=1 / page36->37`) is no longer the active blocker.
- Current live `v11` run has materially advanced beyond that region:
  - sparse log shows repeated successful completion of:
    - `target_seq=0 page36/page37`
    - `target_seq=1 page36/page37`
  - sparse log also shows:
    - `worker stage=0 subbatch=3 done`
    - `worker stage=0 subbatch=4 done`
    - `worker stage=0 subbatch=5 begin`
- Current stable plateau is now:
  - `segment=0`
  - `stage=0`
  - `subbatch=5`
  - `tensor=2`
  - `target_seq=0`
  - `page42 end -> page43 begin`

## Evidence
- host watchdog:
  - `guest_sparse=404951` stopped growing
  - heartbeat continued from `10561685985, 577` to at least `16936756225, 898`
- guest sparse bytes:
  - stable at `404951`
- live breadcrumb stayed unchanged across repeated reads:
  - `update_count=13061`
  - `last_kind=dma`
  - `last_phase=dma_page_end`
  - `seg=0 gstage=0 lstage=0 sb=5 tensor=2 page=42 rc=0`
  - `dst=0x1047f6c00`
- The destination physical address range `0x1047f5400 -> 0x1047f6c00` matches the first export target (`target_seq=0`, `address` path), not the second alias target.

## Interpretation
- This is a real forward move, not just a logging shift:
  - `subbatch=3` and `subbatch=4` are both past
  - the blocker migrated to a later subbatch and a new page boundary
- The next minimal probe should retarget the export window from `36..37` to `42..43` while keeping the same `v2p-begin/end` instrumentation.
