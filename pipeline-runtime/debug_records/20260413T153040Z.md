# 2026-04-13T15:30:40Z debug record

## Scope
- Start the next minimal-probe round after confirming the new live plateau.
- Keep code instrumentation unchanged and retarget only the fixed profile window.

## Inherited conclusion
- Confirmed live plateau from the previous round:
  - `segment=0`
  - `stage=0`
  - `subbatch=5`
  - `tensor=2`
  - `target_seq=0`
  - `page42 end -> page43 begin`
- Evidence already captured in:
  - `20260413T152610Z.md`

## Planned change
- Update the fixed profile:
  - `PIPELINE_RUNTIME_PROFILE_ID=pairdummy-sbus128-fixed-v12`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START=42`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END=43`
- Keep:
  - `v2p-begin/end` code probe in `prt_dma.c`
  - breadcrumb settings
  - file-log-only workflow

## Planned workflow
1. Edit `pairdummy_sbus128_fixed_env.sh`.
2. Rebuild image through the canonical `image-closure` path.
3. Terminate the current run farm.
4. Relaunch + `infrasetup`.
5. Run remote freshness over private IP only.
6. Start the next run and re-check whether the blocker is:
   - inside `v2p`
   - after `v2p` and before submit
   - after submit/wait

## Executed
- Edited:
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh`
- Actual profile diff:
  - `pairdummy-sbus128-fixed-v11` -> `pairdummy-sbus128-fixed-v12`
  - export page window `36..37` -> `42..43`
- Started canonical rebuild:
  - `pairdummy_sbus128_workflow.sh image-closure`
  - current clean session: `pairdummy-sbus128-clean-20260413-153110`
- Rebuild completed successfully.
- Local freshness confirmed:
  - `PIPELINE_RUNTIME_PROFILE_ID='pairdummy-sbus128-fixed-v12'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START='42'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END='43'`
- Current live run farm terminated successfully:
  - terminated instance: `i-0ad2e1c97e4708e08`
- Started `launchrunfarm` for the `v12` rerun.

## External blocker
- `launchrunfarm` is currently blocked on AWS capacity, not on local workflow logic.
- FireSim manager is repeatedly retrying `f2.6xlarge` allocation across the configured subnets and reporting:
  - `Tried all subnets, but there was insufficient capacity to launch your instances`
- This is an infrastructure-side blocker before `infrasetup` / `remote-freshness` / `run`.

## Later progress
- AWS capacity eventually cleared and `launchrunfarm` succeeded.
- New instance:
  - `i-0ae4c7c84456eeaac`
  - private IP `192.168.1.104`
- `infrasetup` completed successfully on `192.168.1.104`.
- Remote freshness passed on `192.168.1.104`:
  - remote image SHA matches local image SHA
  - guest env confirms `pairdummy-sbus128-fixed-v12`
  - guest env confirms export page window `42..43`
- Started new run:
  - session `pairdummy-sbus128-runworkload-20260413-154224`

## Current observation on the new `v12` run
- host watchdog is alive on:
  - instance `i-0ae4c7c84456eeaac`
  - private IP `192.168.1.104`
- As of heartbeat `4610832478, 260`:
  - `status-bytes = 0`
  - `sparse-bytes = 0`
  - `runner-early-bytes = 0`
- Interpretation:
  - the new run is still in early boot / pre-wrapper startup
  - this is not yet enough to classify as a regression, because the previous successful entry into guest-file logging also happened substantially later than boot start
