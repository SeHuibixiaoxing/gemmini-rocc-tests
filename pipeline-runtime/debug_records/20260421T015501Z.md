# 20260421T015501Z

调试类别：main

本轮在补上新的 low-disturbance direct-path breadcrumb 之后，重新跑了一轮完整 FireSim `g6 bertmini` 主线。结果表明：之前反复讨论的 `segment0 tensor2 export page29` 歧义走廊已经被明确越过；本轮新的稳定卡点出现在更靠后的 `segment3` compute 区域。

## 本轮执行

- `image-closure`
- `launchrunfarm`
- `infrasetup`
- `runworkload`
- 在确认新稳定卡点后，按 SOP 手工 `terminaterunfarm` 收尾

## 关键信息

- runworkload session：
  `pairdummy-sbus128-runworkload-20260421-012435`
- terminaterunfarm session：
  `pairdummy-sbus128-terminaterunfarm-20260421-015240`
- 远端实例：
  `i-0069b03d475d2dbe6`
  / `192.168.1.239`
- 结果目录：
  `/home/ubuntu/chipyard/sims/firesim/deploy/results-workload/2026-04-21--01-24-37-rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy-g6-f2-gemmini-rerocc-pairmanager-dummy16x16-4c12p12-sbus128-linux-bertmini-batch8-fileonly-sync-g6/`
- 本地现场抓取：
  `/home/ubuntu/chipyard/tmp/pipeline-runtime-live/20260421T015240Z-g6-segment3-stall-capture/`

## 本轮新结论

### 1. 老的 export frontier 已经被越过

- guest 成功进入 `pipeline-runtime` 用户程序：
  - `bertmini-batch8.status` 为 `state=running`
  - `runner.stage` 记录到：
    - `after-bin-spawn method=ours2 pid=198`
  - `runner-proc.stage` 记录到实际命令行：
    - `/root/rerocc-linux-tests/pipeline-runtime/rerocc_pipeline_runtime-linux ...`
- 新 probe 生效，且成功避开旧的 `slot14` 歧义：
  - 当前 breadcrumb 中出现：
    - `kind=dma`
    - `phase=dma_page_direct_path_decided`
    - `seg=0 gstage=0 lstage=0 sb=7 tensor=2 tok=2 mgr=0 page=62`
    - `slot=43`
  - 这说明：
    - 程序已经进入 `segment0/stage0/tensor2` export direct path
    - 且这一条证据不再复用旧的 `page29/token0 -> slot14` 等价类
- 因而本轮可以强说：
  **此前反复怀疑的 `page28 after_accounting -> page29 direct-path` 走廊，不是这轮主线 run 的阻塞点。**

### 2. 新的稳定卡点出现在 `segment3` compute 区域

- `guest_sparse` 日志在本轮后半段推进到：
  - `segment3 stage0` 固定 tensor load 完成
  - `stage-exec-views phase=end segment=3 stage=0 layer=3 rc=0 flush=1`
  - `worker stage=0 subbatch=1 begin op=1 acc=0 dma=0 tiles=2`
  - 多次
    `conv-sync-strided stage=0 mgr=0/1 ... flushed use_pointwise=1`
  - `worker stage=1 subbatch=0 compute-done`
  - `worker stage=1 subbatch=0 done`
  - `worker stage=1 subbatch=0 waiting phase=entry-full kind=c3-isolate tensor=6 ...`
  - `worker stage=2 subbatch=0 begin op=1 acc=4 dma=4 tiles=2`
  - `conv-sync-strided stage=2 mgr=4/5 ... flushed use_pointwise=1`
- 随后出现了新的稳定 plateau：
  - `guest_sparse` 文件大小固定在 `627538`
  - 稀疏日志尾部在两个长窗口内完全相同
  - `segment0` 过滤下的 breadcrumb 摘要也完全不再变化
- 但与此同时：
  - `heartbeat.csv` 继续从
    `1171`
    增长到
    `1629`
  - FireSim manager 一直认为 simulation 仍在运行
  - host watchdog 没有误杀，本轮终止是人工执行 `terminaterunfarm`

### 3. 因此“卡点移动”的解释已经更新

- 之前看到的 `segment0 export frontier` 更像是：
  - 旧观测路径存在歧义
  - 以及不同 rerun / 不同观测扰动下，冻结位置会变化
- 这轮带新 probe 的 fresh rerun 已经证明：
  - 程序确实能越过先前那个 export direct-path 争议点
  - 当前主线真正需要接力调试的位置，已经后移到
    `segment3`
    的 compute / pipe interaction 区域

## 为什么这轮可以把它认成“新的稳定卡点”

- 不是只看单次日志尾部，而是同时满足：
  - `guest_sparse` 大小长期不变
  - 稀疏日志尾部内容长期不变
  - `heartbeat` 持续增加，说明仿真没停
  - manager 一直显示 `Sim running: True`
- 在最终收尾前，host watchdog 已记录：
  - `hb='31259656576, 1613'`
  - `idle=1001s`
  - `hb_idle=0s`
  - `guest_sparse=627538`
- 这说明：
  - 不是 host 误杀
  - 也不是 guest 彻底死机
  - 而是进入了“仿真仍活、但 guest 可观测主线不再前进”的 plateau

## 本轮局限

- 当前 breadcrumb filter 仍然是：
  - `segment=0`
  - `global_stage=0`
  - `local_stage=0`
- 所以它只足够证明：
  - 旧 `segment0 export` frontier 已被越过
- 它不足以直接给出：
  - `segment3 stage0/stage2` 的精确硬件停点

## 下一步建议

- 下一轮 observability 要转移到 `segment3` compute 主线，优先考虑：
  1. 给 `segment3 stage0/stage2` 的 pointwise / conv-sync-strided 路径补低扰动 breadcrumb 或可综合 marker
  2. 明确 `mgr=0/1` 与 `mgr=4/5` 这两组 Gemmini/DMA/PairManager 交互中，哪一组最后完成、哪一组停住
  3. 继续避免高扰动深日志；优先复用当前“guest 文件 + breadcrumb + watchdog + live capture”的方法
  4. 若要抓更细的硬件停点，再针对 `segment3` 做最小化 trace / metasim 复现

## 验证与收尾

- `runworkload` 退出码：
  `1`
  - 原因是人工 terminate 后，manager 在 `monitor_jobs_wrapper` 阶段丢失远端 SSH banner
- `terminaterunfarm` 退出码：
  `0`
- 本轮停机不是 watchdog 误杀，而是为了结束已确认 plateau 的 run
