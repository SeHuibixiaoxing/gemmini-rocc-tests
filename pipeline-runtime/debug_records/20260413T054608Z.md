# 2026-04-13T05:46:08Z 调试记录

## 本轮目标
- 基于当前 live run 的新前沿，判断旧 blocker 是否已被真正越过。
- 如果新的冻结点收敛到极短 `DMA program` 窗口，则按低扰动原则收缩日志，避免再次被 checkpoint/file-log 人工扰动。
- 修改代码后重新走固定 workflow fresh 重跑。

## 本轮接手现场
- live session:
  - `pairdummy-sbus128-runworkload-20260413-052551`
- 实例:
  - `i-0ab668f09c2fdbef9`
  - 私网 IP:
    `192.168.1.133`
- 关键日志:
  - manager:
    `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-13--05-25-52-runworkload-UTJH4DVXWXW2OE5J.log`
  - watchdog:
    `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-052551.host-watchdog.log`

## 关键 live 结论
- 这轮 run 明确越过了之前稳定停住的旧 blocker：
  - `tensor=2` 第二个 alias target 导出已经完整出现：
    - `export-target-dispatch ... target_seq=1 ... phase=end`
- 后续还继续推进到：
  - `worker stage=0 subbatch=1 done`
  - `stage-fixed-load-sparse phase=begin ... tensor=1000000`
  - `stage-fixed-load-sparse phase=end ... tensor=1000000`
  - `stage-fixed-load-sparse phase=begin ... tensor=1000001`
- host watchdog 已 arm，且一度正确看到：
  - `guest_sparse` 增长
  - `guest_checkpoint` 增长
- 但随后进入新的长时间 idle：
  - heartbeat 继续增长
  - `guest_sparse` 固定在 `313953`
  - `guest_checkpoint` 固定在 `264844`

## 新冻结前沿
- 当前新的稳定冻结点不再是 alias export，也不是 `doneflag-begin -> doneflag-end`。
- `checkpoint.log` 最后稳定可见到：
  - `tensor=1000001`
  - `doneflag-end token=715 rc=0`
- 后面没有再出现：
  - `program-begin`
- `sparse log` 的最后相关前沿是：
  - `stage-fixed-load-sparse phase=begin ... tensor=1000001`
  - `rr-acquire-wrap phase=after-call ... rc=0`
- 因而本轮收敛出的最窄窗口是：
  - `tensor=1000001`
  - `doneflag-end rc=0`
  - 到
  - `program-begin`
  之间的超短路径

## 静态分析
- 对照 `prt_dma.c` 可知，`doneflag-end -> program-begin` 中间几乎没有业务逻辑，只剩：
  - `if (tok->hw_done_flag_pa_rc != PRT_OK) return ...`
  - `PRT_PROGRESS_RAW_LINE("[prt-raw] dma pre-program")`
  - `checkpoint=program-begin`
- 现场里：
  - `doneflag-end rc=0`
  已经成立，所以不是 doneflag acquire/release 失败。
- 结合既有文档：
  - `CURRENT_STATUS.md`
  - `NEXT_SESSION_PROMPT.md`
  之前已经多次收敛过同类现象：
  - 高频 checkpoint/file-log 会把真实 frontier 人为拉回到
    `program-begin -> program-post-fence`
    或更早的超短窗口
- 本轮再次命中同一类窗口，因此本轮判断是：
  - 当前更大的嫌疑不是 DMA 功能本身又坏了
  - 而是 `tensor>=1000000` fixed-load 路径上的高频 checkpoint 仍然扰动了 live 行为

## 本轮代码改动
- 文件：
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c`

### 1. 默认关闭 fixed-load 高频 checkpoint
- 新增：
  - `dma_fixed_load_checkpoint_enabled()`
- 行为：
  - `stage=0 && tensor>=1000000` 的 checkpoint 不再默认开启
  - 只有显式环境变量
    `PIPELINE_RUNTIME_DMA_FIXED_LOAD_CHECKPOINT_ENABLE=1`
    时才重新开启
- 目的：
  - 降低 `tensor=1000001` fixed-load 路径对 `checkpoint.log` 的写放大扰动

### 2. 给 `tensor=1000001` fixed-load 加低扰动 sparse 页级打点
- 新增 helper：
  - `dma_should_sparse_fixed_load_page_probe()`
- 在 `dma_copy_host_to_spm_pages_linux()` 中新增：
  - `dma-fixed-load-host ... phase=submitwait-begin`
  - `dma-fixed-load-host ... phase=submitwait-end`
- 触发策略：
  - `stage=0 && tensor=1000001`
  - 前 8 页全打
  - 后续每 8 页打一次
  - 最后一页打一次
- 目的：
  - 用 sparse 页级观测替代高频 checkpoint

### 3. 给 `tensor=1000001` fixed-load 的 DMA 提交路径加低扰动相位打点
- 新增 helper：
  - `dma_should_sparse_fixed_load_submit_probe_tok()`
- 在 `dma_blocking_submit()` 中新增 sparse：
  - `dma-fixed-load-submit ... phase=doneflag-end`
  - `dma-fixed-load-submit ... phase=program-begin`
  - `dma-fixed-load-submit ... phase=program-post-fence`
  - `dma-fixed-load-submit ... phase=program-post-dst`
  - `dma-fixed-load-submit ... phase=program-post-src`
- 目的：
  - 在不恢复大体积 checkpoint 的前提下，继续把 `tensor=1000001` 的提交窗口收窄

## 本轮执行命令
- 通过私网 SSH + `debugfs` 读取 live image 内：
  - `bertmini-batch8.log`
  - `bertmini-batch8.checkpoint.log`
  - `bertmini-batch8.status`
  - `runner-early.stage`
  - `runner.stage`
  - `runner-proc.stage`
  - `wrapper-proc.stage`
- 查看 watchdog 尾部：
  - `tail -n 80 ...pairdummy-sbus128-runworkload-20260413-052551.host-watchdog.log`
- 静态对照：
  - `sed -n ... prt_dma.c`
  - `sed -n ... CURRENT_STATUS.md`
  - `sed -n ... NEXT_SESSION_PROMPT.md`

## 下一步
- 停掉当前已经进入长时间 idle 的 live run。
- 严格按固定约束重新走：
  - `cd /home/ubuntu/chipyard/sims/firesim`
  - `set +u; source sourceme-manager.sh --skip-ssh-setup; set -u`
  - 再通过固定脚本走：
    - image freshness / image closure
    - `launchrunfarm`
    - `infrasetup`
    - `runworkload`
- 重跑后重点观察新的 sparse：
  - `dma-fixed-load-host ... submitwait-begin/end`
  - `dma-fixed-load-submit ... doneflag-end/program-begin/program-post-fence/...`
- 目标：
  - 验证当前 `tensor=1000001 doneflag-end -> program-begin` 是否只是 logging-induced 假冻结
  - 如果是，frontier 应继续明显后移
