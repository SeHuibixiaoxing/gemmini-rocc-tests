# 2026-04-16 02:26:51 UTC

## Round Summary
- Goal: continue `12-pair sbus128 / bertmini batch8 / file-only / pairdummy` debug on the existing fixed profile, after the user manually terminated the previous run farm.
- Code changes in this round: none.
- Config changes in this round: none. Reused the existing `page57` low-disturbance trigger overlay.
- This round was a pure rerun + observation round.

## Active Configuration
- Profile: `pairdummy-sbus128-fixed-v24`
- Runtime config: `sims/firesim/deploy/config_runtime_f2_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_linux_bertmini_pipeline_runtime_batch8_fileonly_sync.yaml`
- Instance: `i-01832a1e8ccabf18c`
- Private IP: `192.168.1.246`
- Runworkload session: `pairdummy-sbus128-runworkload-20260416-021039`
- Guest image freshness:
  - local/remote sha256 = `0252f5a2fa822ff859e30ea9721d28712b1c8d6d8c046ae99641f624be7e516b`
- Fixed profile constraints confirmed from guest status:
  - `uart_log_enable=0`
  - `guest_log_enable=1`
  - `breadcrumb_enable=1`
  - `mlockall_mode=2`
  - `skip_model_bin_load=1`
  - `skip_input_load=1`
  - `skip_golden_check=1`

## Trigger Overlay
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=dma-fixed-load`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_SEGMENT=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_GLOBAL_STAGE=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_LOCAL_STAGE=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_SUBBATCH=6`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_MANAGER=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_TENSOR_ID=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_PAGE=57`
- Breadcrumb narrowing kept:
  - `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH=6`
  - `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH_RADIUS=0`

## Commands Used
- Monitor:
  - `tail -n 260 tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260416-021039.monitor.log`
- Guest file inspection over private IP:
  - `ssh -i /home/ubuntu/firesim.pem ubuntu@192.168.1.246 ... debugfs -R 'cat /root/pipeline-runtime-debug/...'`
- Breadcrumb pull/decode:
  - `decode_prt_breadcrumb.py --all tmp/firesim-aws-f2/live/pairdummy-sbus128-runworkload-20260416-021039/bertmini-batch8.breadcrumb.bin`

## Observations
- The rerun successfully rebooted and reached runtime hot-path execution.
- Host watchdog showed a step change:
  - before hot path: `guest_trigger=0`
  - after hitting the trigger: `guest_trigger=2443`
- Trigger log proved that the narrow `page57` window is not the terminal stall:
  - `page57`: `pset -> v2p-b -> ... -> sw-e`
  - `page58`: `pset -> v2p-b -> ... -> sw-e`
  - `page59`: at least reached `wf-e`
- Breadcrumb decode showed later progress than the trigger tail:
  - `sb=6 tensor=0 page=63 dma_page_end`
  - latest slot: `kind=rr phase=rr_release_end`
- Therefore:
  - this run again confirms the `page57` DMA wait path is not the final blocker
  - the `page57` page-trigger was useful to rule out the earlier misleading stop position

## Coarse Log Frontier
- The guest coarse log tail no longer stopped at the fixed-load DMA page window.
- The latest visible coarse tail is:
  - `stage-fixed-load-sparse phase=end ... slot=1 tensor=1000001 rc=0`
  - then `spm-xlate-flush mgr=0..5`
  - final visible line is truncated at:
    - `spm-xlate-release mgr=5 cfg=15 phase=restore-begin prev_opc3=0x1`
- Static control-flow alignment:
  - fixed-load DMA path returns from [`prt_dma.c#L650`](/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c#L650)
  - caller logs `stage-fixed-load-sparse phase=end` at [`prt_runtime.c#L2120`](/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_runtime.c#L2120)
  - runtime then enters stage SPM translation flush via [`prt_runtime.c#L2195`](/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_runtime.c#L2195)
  - per-manager flush calls [`prt_gemmini_spm_xlate_flush()`](/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_runtime.c#L415)
  - the final visible coarse line corresponds to `rr_restore_opcode_binding(3U, prev_binding)` in [`prt_rerocc.c#L170`](/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_rerocc.c#L170)

## Interpretation
- No code regression was introduced in this round; this was a clean rerun on a fresh image.
- The narrow `dma-fixed-load/page57` trigger achieved its purpose:
  - it disproved `page57` as the true terminal wait point
  - it correlated the later frontier with the post-fixed-load SPM-translation flush path
- Current best hypothesis:
  - the new effective stall is later than the old DMA wait suspicion
  - the likely next narrow probe target is the `spm-xlate-release/restore` path, especially around manager `5`
- Important nuance:
  - breadcrumb latest event is still `rr_release_end`, but coarse log already shows execution later than that
  - this is not a contradiction: breadcrumb is a fixed-size filtered ring; coarse log is append-only and currently gives the later visible control-flow point

## Live Status At Record Time
- Monitor still showed heartbeat advancing.
- Monitor also showed no further guest-file growth after the hot-path jump:
  - `guest_sparse=358582`
  - `guest_trigger=2443`
- Idle time had already grown past `125s`, so the run was being left alive to see whether watchdog capture would produce a stable timeout artifact.

## Next Step
- Keep the current run alive and monitor for timeout/capture.
- If it stabilizes as a timeout, inspect the captured guest files before making any code change.
- If the run remains reproducibly stuck, the next observability overlay should move from `dma-fixed-load page57` to a narrow `spm-xlate release/restore` probe, without touching the fixed profile semantics.
