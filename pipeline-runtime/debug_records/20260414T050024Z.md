# 2026-04-14T05:00:24Z 调试记录

## 本轮目标

- 延续上一轮 live run，不重启，先确认 pointwise breadcrumb 新边界是否已经把旧 blocker 推过去。
- 静态阅读当前新前沿对应代码路径，先缩小可疑窗口，再决定是否需要新的窄日志。
- 同步把这一轮结果写回主文档，避免 `CURRENT_STATUS / NEXT_SESSION_PROMPT` 继续停留在旧 pointwise 叙事。

## 本轮执行

- 检查现有 run：
  - `tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260414-044606.host-watchdog.log`
  - `tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260414-044606.pane.log`
  - `sims/firesim/deploy/logs/2026-04-14--04-46-07-runworkload-UYE4ZS9TF4HI6YHQ.log`
- 直接从 run host 用 `debugfs` 抓取新 capture：
  - `tmp/firesim-aws-f2/captures/live-direct-20260414T045749Z.guest.log`
  - `tmp/firesim-aws-f2/captures/live-direct-20260414T045749Z.guest-breadcrumb.bin`
  - `tmp/firesim-aws-f2/captures/live-direct-20260414T045749Z.*stage`
  - `tmp/firesim-aws-f2/captures/live-direct-20260414T045749Z.bertmini-batch8.status`
- 对 capture 跑分诊：
  - `python3 .../triage_prt_capture.py ...guest-breadcrumb.bin --sparse-log ...guest.log`
- 静态阅读并对齐：
  - `src/prt_runtime.c`
  - `src/prt_dma.c`
  - `src/prt_page_table.c`
  - `scripts/pairdummy_sbus128_fixed_env.sh`
- 用 artifact 再核对 stage-0 语义：
  - `pipeline_mapping...sbus128.ours2.yaml`
  - `model.layers.yaml`
  - `audit_pipeline_runtime_artifact.py`

## 新证据

- 这轮 capture 已经明确越过旧的 pointwise caller-return/frontier：
  - `oc-split-pointwise stage=0 tile=7/8 mgr=7 ... end`
  - `worker stage=0 subbatch=4 done`
- `triage_prt_capture.py` 的最后 breadcrumb 是：
  - `kind=dma`
  - `phase=dma_page_end`
  - `seg=0 gstage=0 lstage=0 sb=4 tensor=2 mgr=0 page=63 rc=0`
  - 这对应导出 DMA 最后一页完成，不再是旧的 pointwise stall。
- 最后一批 sparse 行把新 authoritative frontier 缩到：
  - `stage-exec-views phase=begin segment=0 stage=0 layer=0`
  - `stage-fixed-load-sparse phase=begin ... slot=1 tensor=1000001 pages=64 bytes=65536`
  - 随后只看到一轮 `rr-acquire-wrap phase=after-call stage=0 manager=0 opcode=2 rc=0`
  - 之后没有：
    - `stage-fixed-load-sparse phase=end`
    - 也没有任何 `dma page begin/end`
- 因而当前更可信的 stable blocker 已经从 late pointwise 区间前移为：
  - `worker stage=0 subbatch=4 done`
  - 之后再次进入 `stage_prepare_exec_views()`
  - 在 `tensor=1000001` 的 fixed-load 路径里挂住
  - 并且挂点位于第一个 DMA page breadcrumb 之前

## 新静态结论

- `segment=0 / stage=0` 的 artifact 语义再次确认：
  - `entry tensor 0`: `DRAM`
  - `export tensor 2`: `DRAM`
  - fixed tensors: `1000000 / 1000001`
  - `tensorUseLazyFetch`: `[0, 0, 0, 0]`
- 因此当前停点不是：
  - `ALL_RINGBUFFER` 语义漂移
  - 也不是 fixed tensor lazy reuse 分支
- 当前最可疑的小窗口是：
  - `prt_runtime.c:stage_prepare_exec_views()`
  - `prt_dma_copy_dram_to_spm_pages()`
  - `dma_copy_host_to_spm_pages_linux()`
  - 第一页第一次 chunk 的
    `page-setup -> bounce decision -> prt_host_virt_to_phys() -> dma_breadcrumb_page_begin`
    之间
- 现有固定权重 probe 之前默认关闭，所以即便卡在这段代码里，也看不到
  `dma-fixed-load-host`
  的更细边界。

## 本轮动作结果

- 已对当前卡死 run 做 force terminate：
  - instance: `i-074a77cdc73782078`
  - private IP: `192.168.1.245`
- 已加最小新探针并重建 fresh image；
  但新的 `launchrunfarm` 目前被 AWS `f2.6xlarge` 容量不足阻塞，还没拿到新实例。

## 对后续调试的直接影响

1. 不要再把当前 blocker 叙述成 late pointwise / Gemmini fence。
2. 下一轮 live repro 的第一优先级是读 fixed-load 第一页前的
   `page-setup / bounce / v2p`
   边界。
3. 如果新 run 再次停在 `tensor=1000001` begin 之后：
   - 若最后一条是 `phase=page-setup`，继续看 bounce / v2p
   - 若最后一条是 `phase=v2p-begin`，重点怀疑 `prt_host_virt_to_phys()`
   - 若最后一条是 `phase=v2p-end`，再看 `dma_submit_wait_annotated_scoped()`
4. 继续遵守“先静态读代码，再加最窄日志”的顺序；这轮已经验证该顺序能把旧 pointwise 假前沿排掉。
