# 2026-04-15 13:19:34 UTC

## Round Goal

基于上一轮
`20260415T125427Z.md`
的 trigger rerun 结果，继续把真实 blocker 收紧到
`target_seq=1 / page=5`
附近，并且尽量不增加日志扰动。

本轮目标：

- 先确认当前 run 是否形成新的稳定前沿
- 用 breadcrumb / trigger / guest log 交叉确认最新真实位置
- 如果 blocker 已收紧到单页 submit 路径，则只做单变量 observability rerun

## Input State

- hardware:
  `firesim_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128`
- runtime config:
  `/home/ubuntu/chipyard/sims/firesim/deploy/config_runtime_f2_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_linux_bertmini_pipeline_runtime_batch8_fileonly_sync.yaml`
- current instance private ip:
  `192.168.1.71`
- current runworkload session:
  `pairdummy-sbus128-runworkload-20260415-130247`
- live image:
  `/home/ubuntu/sim_slot_0/rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy0-rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy.img`

## Commands Used

- manager / tmux status:
  - `tail -n 160 tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260415-130247.pane.log`
  - `tail -n 160 sims/firesim/deploy/logs/2026-04-15--13-02-48-runworkload-IRAAVRXNJLGGGLTM.log`
- private-IP live image inspection:
  - `ssh ... ubuntu@192.168.1.71 'debugfs -R "cat ...trigger.log" "$img"'`
  - `ssh ... ubuntu@192.168.1.71 'debugfs -R "cat ...bertmini-batch8.log" "$img"'`
  - `ssh ... ubuntu@192.168.1.71 'debugfs -R "cat ...status" "$img"'`
  - `ssh ... ubuntu@192.168.1.71 'tail -n 40 /home/ubuntu/sim_slot_0/heartbeat.csv'`
- two-poll stability check:
  - 间隔约 `35s` 重复读取 heartbeat / trigger tail / guest log tail
- breadcrumb capture:
  - `debugfs cat -> base64 -> local bin`
  - `python3 .../decode_prt_breadcrumb.py --all ...`
- static code audit:
  - `nl -ba src/prt_dma.c | sed -n '720,900p'`
  - `nl -ba src/prt_dma.c | sed -n '1480,1610p'`
  - `nl -ba src/prt_dma.c | sed -n '2367,3055p'`
  - `nl -ba src/prt_dma.c | sed -n '360,520p'`

## Evidence Paths

- tmux pane:
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260415-130247.pane.log`
- manager log:
  `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-15--13-02-48-runworkload-IRAAVRXNJLGGGLTM.log`
- local breadcrumb capture:
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/live/pairdummy-sbus128-runworkload-20260415-130247/bertmini-batch8.breadcrumb.20260415T1316Z.bin`

## New Observations

- manager 侧仍显示 sim running，heartbeat 持续增长：
  - 约从 `716` 增到 `772`
- 但 live image 中 guest 文件在两次采样之间稳定不变：
  - `trigger.log` 末尾都停在
    `seq=56 fam=dexp ph=sw-b ... pg=5`
  - `bertmini-batch8.log` 末尾都停在
    `target_seq=1 / page=0 submitwait-end`
- 这说明当前不是单纯 sync 滞后，而是形成了新的稳定前沿

### Breadcrumb Decode

- `all_slots` 中最新时间戳事件已经推进到：
  - `tensor=2 / page=5 / phase=dma_page_begin`
- 同时 page0..page4 都已经出现：
  - `dma_submitwait_after_cleanup`
  - `dma_page_end`
- 因此旧结论再次被推翻：
  - blocker 不在 `rr_acquire_after_call`
  - 也不在 `target_seq=1 / page0`
  - 而是在 `target_seq=1 / page5`

## Static Analysis

对照
`src/prt_dma.c`
当前卡点位于：

- export host loop 中已执行到：
  - `dma_trigger_export_host("sw-b", ...)`
  - `dma_breadcrumb_page_begin(...)`
- 但尚未观察到：
  - `DMA_SUBMIT_BEGIN`
  - `DMA_WAIT_BEFORE_FENCE`
  - `wf-e / sf-e / rel-e`
  - `dma_submitwait_after_cleanup`

因此本轮静态结论是：

1. `page5` 已经通过 `v2p`
2. `page5` 已经到达 submit/wait 调用边界
3. 真实 blocker 更像在 `dma_submit_wait_annotated_scoped()` 最早阶段
4. 下一轮不应再扩大日志范围，而应只针对：
   - `tensor=2`
   - `page=5`
   - token 约 `1162`
   打开 export submit/wait probe

## Modification Decision

本轮决定做一个最小 observability 修改，不改核心逻辑：

- 更新 fixed env profile 到 `v21`
- 启用单变量 export probe：
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE=1`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START=1161`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END=1163`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START=5`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END=5`

理由：

- 只观察 page5 附近 submit/wait
- 避免再次同时开启 trigger + export probe
- 利用现有代码路径，不先加新探针

## Next Step

1. 记录 change record
2. patch fixed env profile
3. 通过 `debug-preflight` 保证只启用单一高风险 probe family
4. 终止当前 runfarm
5. 重新 `launch -> infrasetup -> run`
6. 再次以 private IP + live image `debugfs` 读取：
   - `bertmini-batch8.log`
   - `bertmini-batch8.trigger.log`
   - `bertmini-batch8.breadcrumb.bin`
7. 判断 page5 submit 路径究竟停在：
   - doneflag acquire
   - program-post-fence
   - hw_dma_set_dst / hw_dma_set_src
   - hw_dma_fence
