# 20260425T032850Z

调试类别：gdbserver

## 本轮目标

- 接续 `20260425T031328Z` 的 `SimpleNICBridge` 修复与 no-Trace NIC 配置。
- 按用户要求继续推进到需要 bitstream 的阶段。
- 使用 FireSim tmux wrapper 启动长时间构建，并在早期阶段确认没有 target config / elaboration / host driver 的立即失败。

## 启动的构建

tmux session：

- `rocket-singlecore-nic-notrace-buildbitstream-20260425-032337`

启动命令：

```bash
scripts/firesim-tmux-run.sh \
  --session-name rocket-singlecore-nic-notrace-buildbitstream-20260425-032337 \
  buildbitstream \
  -b /home/ubuntu/chipyard/sims/firesim/deploy/config_build_f2_rocket_singlecore_nic_notrace_30mhz.yaml \
  -r /home/ubuntu/chipyard/sims/firesim/deploy/config_build_recipes_f2_rocket_singlecore_nic_notrace_30mhz.yaml
```

tmux metadata：

- `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/rocket-singlecore-nic-notrace-buildbitstream-20260425-032337.metadata`

pane log：

- `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/rocket-singlecore-nic-notrace-buildbitstream-20260425-032337.pane.log`

## 早期检查结果

### 1. FireSim recipe / target config 已被接受

日志显示：

- `Building Verilog for f2-firesim-FireSim-FireSimRocketNICNoTraceConfig-BaseF2Config`

这说明：

- `TARGET_CONFIG: FireSimRocketNICNoTraceConfig` 可被 FireSim manager 解析。
- 新增 target config 至少已经通过 Scala 编译和 Chisel elaboration 的入口解析。

### 2. 新 `SimpleNICBridge` debug regs 已进入生成头文件

抽查：

```bash
rg -n "local_queues_ready|target_cycle_ready|from_host_valid_drive|SIMPLENICBRIDGEMODULE_struct" \
  sims/firesim/sim/generated-src/f2/f2-firesim-FireSim-FireSimRocketNICNoTraceConfig-BaseF2Config \
  sims/firesim/sim/output/f2/f2-firesim-FireSim-FireSimRocketNICNoTraceConfig-BaseF2Config
```

结果显示：

- `SIMPLENICBRIDGEMODULE_struct` 生成大小为 `52 * sizeof(uint64_t)`
- 新增字段 offset 已生成：
  - `local_queues_ready`
  - `target_cycle_ready`
  - `to_host_ready_drive`
  - `from_host_valid_drive`
  - `target_in_bits_*`
  - `target_out_bits_*`

关键文件：

- `/home/ubuntu/chipyard/sims/firesim/sim/generated-src/f2/f2-firesim-FireSim-FireSimRocketNICNoTraceConfig-BaseF2Config/FireSim-generated.const.h`
- `/home/ubuntu/chipyard/sims/firesim/sim/output/f2/f2-firesim-FireSim-FireSimRocketNICNoTraceConfig-BaseF2Config/build/FireSim-generated.const.h`

### 3. no-Trace 目标没有生成 `TracerV` bridge

抽查：

```bash
rg -n "TracerV|tracerv" \
  sims/firesim/sim/generated-src/f2/f2-firesim-FireSim-FireSimRocketNICNoTraceConfig-BaseF2Config \
  sims/firesim/sim/output/f2/f2-firesim-FireSim-FireSimRocketNICNoTraceConfig-BaseF2Config || true
```

结果：

- 无命中。

注意：

- `trace` 小写仍会在 Rocket 内部 CSR/trace signal 中出现，这不是 FireSim `TracerVBridge`。

### 4. host driver 编译已越过 `simplenic` 新字段

pane log 显示：

- 先完成 `FireSim-f2` host simulator/driver 相关编译。
- 随后进入：
  - `Building FPGA driver for f2-firesim-FireSim-FireSimRocketNICNoTraceConfig-BaseF2Config`

这说明：

- `simplenic.cc/h` 和生成的 `SIMPLENICBRIDGEMODULE_struct` offset static asserts 已经匹配。
- 没有出现由新增 host-side fields / plusargs 引起的 C++ 编译失败。

### 5. 远端 build instance 已启动

AWS 查询：

```bash
aws ec2 describe-instances \
  --filters \
    Name=tag:fsimbuildcluster,Values=f2rocketsinglecorenicnotracebuild \
    Name=instance-state-name,Values=pending,running,stopping,stopped \
  --query 'Reservations[].Instances[].{Id:InstanceId,State:State.Name,Type:InstanceType,PrivateIp:PrivateIpAddress,Launch:LaunchTime}' \
  --output table
```

结果：

- instance id: `i-05496af0bc18a63dc`
- state: `running`
- type: `z1d.2xlarge`
- private IP: `192.168.2.30`
- launch time: `2026-04-25T03:28:26+00:00`

pane log 也显示：

- `LAUNCHING: ... fsimbuildcluster=f2rocketsinglecorenicnotracebuild`
- `Waiting for instance boots: 1`
- `i-05496af0bc18a63dc booted!`
- remote command:
  - `/home/ubuntu/firesim-build/platforms/f2//aws-fpga-firesim-f2/hdk/cl/developer_designs/cl_f2-firesim-FireSim-FireSimRocketNICNoTraceConfig-BaseF2Config/build-bitstream.sh --cl_dir ... --frequency 30 --strategy TIMING`
- Vivado started:
  - `AWS FPGA: 2026-04-25 03:29:59 - Build starts`
  - `Vivado v2024.2`

## 当前状态

- `buildbitstream` 仍在后台 tmux session 中运行。
- 远端 Vivado build 已经开始。
- 本轮尚未得到 AGFI。
- 后续需要继续监控：
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/rocket-singlecore-nic-notrace-buildbitstream-20260425-032337.pane.log`
  - `sims/firesim/deploy/results-build/`
  - `sims/firesim/deploy/built-hwdb-entries/firesim_rocket_singlecore_nic_notrace_30mhz`

## 下一步

1. 等 `buildbitstream` 完成。
2. 检查 post-route timing，不能只看 manager exit code。
3. 若生成 AGFI，把 built hwdb entry 同步到 no-Trace hwdb。
4. 重新执行最小 `rocket-singlecore-nic-gdbserver-smoke` 的 `launch -> infrasetup -> network-prepare -> run`。
