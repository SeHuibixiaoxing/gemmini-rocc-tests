# Pipeline Runtime Debug Record

日期：2026-04-13T10:23:54Z

## 目标

- 在不继续放大文本日志扰动的前提下，收敛 `pairdummy-sbus128` 当前真实卡点
- 把 `rr_acquire` 也纳入 breadcrumb 观测面

## 本轮起点

- live run:
  - session: `pairdummy-sbus128-runworkload-20260413-100907`
  - instance: `i-0b15289da51e98ddd`
  - private_ip: `192.168.1.211`
- 机制文档已单独落盘：
  - `docs/pipeline_runtime_breadcrumb_mechanism_20260413.md`

## 当前 live 观测

- 本轮已实质越过上一轮的 `runtime init / artifacts mapping parse` 阶段
- 已进入 `segment=0 stage=0`
- `dma-fixed-load` 文本 probe 已消失，说明误开修复生效
- 当前粗日志最后稳定边界：
  - `c2-export stage=0 tensor=2 phase=copy-begin idx=0 subbatch=3`
  - `rr-acquire-inner phase=before-csr-write ...`
  - `rr-acquire-inner phase=after-csr-write ...`
- 当前没有再出现：
  - `rr-acquire-inner phase=after-csr-read ...`
- 当前 breadcrumb 最后稳定边界：
  - `kind=dma`
  - `phase=dma_page_end`
  - `seg=0 gstage=0 lstage=0 sb=3 tensor=2 mgr=0 page=63`
- 当前 heartbeat 仍在增长，但 sparse log 已停住

## 结论

- 真正的 freeze 已从上一轮被污染的 `fixed-load` 路径移开
- 当前更可信的责任边界是：
  - `rr_write_csr(CSR_RRCFGx, wdata)` 之后
  - `rr_read_csr(CSR_RRCFGx)` 返回之前
  - 或这两者之间紧邻的文本日志写出
- 因此下一步不应继续靠 `rr-acquire-inner` 文本日志加密排查，而应把 `rr_acquire` 改成 breadcrumb 低扰动观测

## 本轮修改

- 新增 `rr` breadcrumb kind / phase
- 在 `prt_rr_acquire_scope_cfg()` 中新增 breadcrumb：
  - before-csr-write
  - after-csr-write
  - after-csr-read
  - before-set-opc
  - after-set-opc
  - before-return
- 在 `prt_rr_acquire_scope()` wrapper 中新增 breadcrumb：
  - after-call
- 更新 decode 脚本支持 `rr` kind / phase
- 更新机制文档说明 `rr` breadcrumb 字段含义

## 本轮中途失败与修复

- 第一次 `marshal build` 失败
- 失败原因不是流程或旧 image，而是 `prt_rerocc.c` 的新 breadcrumb 代码存在作用域错误：
  - `before-return` breadcrumb 使用了块内的 `csr_id / wdata / cfg_state / retries`
  - 宿主侧 `cc -fsyntax-only` 没抓到，但 RISC-V 交叉编译抓到了
- 修复方式：
  - 去掉多余局部块，把这几个变量保持在整个 `__riscv` acquire 路径作用域内
- 修复后：
  - 宿主语法检查通过
  - `image-closure` 重新执行成功

## fresh image 闭环

- local freshness: PASS
- runtime binary sha256:
  - `8dc9cb03a8e914174942a4ee55bf4c461c249c338b37f6ad667fac7ee6b0a595`

## 当前后续状态

- 新一轮 `launchrunfarm` 已启动
- 中途一度阻塞在 AWS `f2.6xlarge` 容量不足
- 之后自动重试成功
- 新实例：
  - `i-0bc5181b004a93a55`
  - private_ip=`192.168.1.6`
- `infrasetup` 已完成
- remote freshness 已通过
- 新 runworkload session：
  - `pairdummy-sbus128-runworkload-20260413-103725`
- 当前仍在 guest boot 阶段：
  - 该描述已过时，见下方新增进展

## 新增 live 进展

- guest 已正常完成 Linux boot 并进入 `S99run`
- wrapper 已生成 `status`
- breadcrumb 已初始化并越过：
  - `runtime_init_done`
- 本轮已再次进入 `segment=0 stage=0`
- 与上轮相比，已实质越过：
  - `subbatch=3 c2-export -> rr_acquire after-csr-write`
- 当前更深的新稳定边界：
  - `segment=0`
  - `global_stage=0`
  - `local_stage=0`
  - `subbatch=4`
  - `tensor=2`
  - `page=18`
  - `phase=dma_wait_after_release`
- 对应 breadcrumb：
  - `kind=dma`
  - `phase=dma_wait_after_release`
  - `tok=1432`
  - `flags=0x7`
  - `src=0x40202800`
  - `dst=0x1047a9c00`
  - `aux0=0x102dfc000`
- 这一边界之后，至少 63 秒内：
  - heartbeat 继续增长
  - 但 `guest_sparse` 停在 `440882`
  - breadcrumb `update_count` 不再增长

## 对当前边界的静态结论

- `dma_wait_after_release` 代码位于：
  - `src/prt_dma.c:2677`
- 它之后在 `dma_blocking_wait()` 内只剩：
  - `dma_token_complete(tok, PRT_OK)`
  - `dma_trace_complete_once(rt, tok)`
  - `return PRT_OK`
- 再往上层 `dma_submit_wait_annotated_scoped()` 返回后，只剩：
  - `prt_dma_token_cleanup(&tok)`
  - `return rc`
- 因此当前真实责任区间已经从硬件 wait/fence 本身，收窄到：
  - `dma_wait after_release` 之后的纯软件 completion / cleanup 路径

## 新增修改

- 为继续收窄上述区间，又新增了 4 个 breadcrumb phase：
  - `dma_wait_after_complete`
  - `dma_wait_after_trace_complete`
  - `dma_submitwait_after_wait`
  - `dma_submitwait_after_cleanup`
- 相应更新：
  - `include/prt_breadcrumb.h`
  - `scripts/decode_prt_breadcrumb.py`
  - `src/prt_dma.c`
  - `docs/pipeline_runtime_breadcrumb_mechanism_20260413.md`
- 宿主语法检查已通过：
  - `cc -fsyntax-only .../prt_dma.c`
  - `python3 -m py_compile .../decode_prt_breadcrumb.py`

## 下一步

- 终止当前已取证 run
- 重建 fresh image
- 用新增 cleanup breadcrumb 重新跑一轮
- 判断 freeze 是否稳定落在：
  - `after_release -> after_complete`
  - `after_complete -> after_trace_complete`
  - `after_wait -> after_cleanup`
