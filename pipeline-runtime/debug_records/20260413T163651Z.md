# 2026-04-13T16:36:51Z 调试记录

## 本轮目标

- 回答用户提出的“随着探针加入，卡点位置发生偏移”是否属实。
- 用当前 `v12` host watchdog capture 重新确认 export `page42/43` 是否还是稳定 blocker。
- 在不继续加新热路径探针的前提下，给后续文档重构和低扰动调试提供最新 authoritative 结论。

## 本轮输入状态

- 运行：
  `pairdummy-sbus128-runworkload-20260413-154224`
- 实例：
  `i-0ae4c7c84456eeaac`
- 私网 IP：
  `192.168.1.104`
- host watchdog：
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-154224.host-watchdog.log`
- capture 前缀：
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/pairdummy-sbus128-runworkload-20260413-154224-192.168.1.104-host-watchdog-20260413T160246Z`

## 本轮执行命令

- `tail -n 120 ...runworkload-20260413-154224.host-watchdog.log`
- `python3 .../decode_prt_breadcrumb.py ...guest-breadcrumb.bin`
- `grep -a -E "worker stage=0 subbatch=5|worker stage=0 subbatch=6|stage-fixed-load-sparse|export-target-dispatch|dma-export-host|v2p-|page-submitwait" ...guest-sparse-log.txt | tail -n 260`
- `tail -n 120 ...guest-sparse-log.txt`
- `python3 - <<'PY' ... read sparse tail / last nonzero bytes ... PY`
- `rg -n "rr_acquire_after_call|pointwise-matmul-fallback|conv-sync-strided|oc-split-pointwise" .../src`

## 新观察

- `v12` 这轮并没有停在 `page42/43`：
  - `target_seq=0` 的 `page42/43`
    出现完整的
    `v2p-begin -> v2p-end -> page-submitwait-begin -> page-submitwait-end`
  - `target_seq=1` 的 `page42/43`
    也出现完整同样序列
- 更晚的稀疏日志已经推进到：
  - `worker stage=0 subbatch=5 begin`
  - `oc-split-pointwise stage=0 tile=0/8 mgr=0 ... end`
  - `oc-split-pointwise stage=0 tile=1/8 mgr=1 ... end`
  - `oc-split-pointwise stage=0 tile=2/8 mgr=2 ... end`
  - `conv-sync-strided stage=0 mgr=3 dispatch=pointwise`
  - 然后日志在
    `pointwise-matmul-fallback stage=0 mgr=3`
    附近截断
- sparse 文件尾部出现：
  - 大量 `NUL`
  - `last_nz` 之后还有 `619` 字节空洞
- breadcrumb 最后槽位为：
  - `kind=rr`
  - `phase=rr_acquire_after_call`
  - `seg=0 gstage=0 lstage=0 sb=5`
  - `mgr=3`
  - `line=288`

## 结论

- 用户关于“探针加入后卡点位置偏移”的判断成立。
- `page42/43` 不是这轮的真实稳定 blocker；当前 capture 已明确越过它。
- 这次前沿已经推进到更晚的 pointwise / RR 区间。
- 同时，`NUL` 尾与半行日志再次说明：
  热路径文本探针本身仍会干扰前沿判定。

## 对后续工作的影响

- 当前主 blocker 不应再写成 `page42/43 export DMA`。
- 后续若继续追这条主线，应优先：
  - 保留 breadcrumb
  - 保留极窄条件日志
  - 避免在 pointwise inner hot path 上继续加长文本日志
- 当前文档体系需要收敛，否则旧 `CURRENT_STATUS` / `NEXT_SESSION_PROMPT` 会继续保留大量已经失效的 blocker 叙事。

## 下一步

1. 完成文档体系重构，建立新的 authoritative 入口。
2. 把本轮新结论写入新的 `CURRENT_STATUS.md` 与 `project_guide.md`。
3. 后续重跑时继续采用低扰动观测策略，重新确认 late pointwise / Gemmini 同步区间的真实 blocker。
