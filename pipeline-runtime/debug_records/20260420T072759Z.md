# 20260420T072759Z

调试类别：main

## 本轮目标

- 对刚落下的 `host-watchdog` 补丁做静态收尾：
  - 语法校验
  - 记录补全
  - 主状态文档同步
- 不启动新的 FPGA run。

## 本轮输入

- 修改脚本：
  - `/home/ubuntu/chipyard/scripts/firesim-prt-host-watchdog.sh`
  - `/home/ubuntu/chipyard/scripts/firesim-tmux-run.sh`
- 状态文档：
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/CURRENT_STATUS.md`
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/NEXT_SESSION_PROMPT.md`
- 上一轮记录：
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260420T072430Z.md`
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/change_records/20260420T072430Z.md`

## 使用命令

- `bash -n scripts/firesim-prt-host-watchdog.sh`
- `bash -n scripts/firesim-tmux-run.sh`
- `python3 generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/generate_record_category_index.py`
- `git diff -- scripts/firesim-prt-host-watchdog.sh scripts/firesim-tmux-run.sh ...`
- `rg -n "20260420T072430Z|host-watchdog|live-idle|cfg15|g6" ...`

## 新观察

### 1. 双层超时脚本至少通过了 shell 语法层

- `firesim-prt-host-watchdog.sh`
  与
  `firesim-tmux-run.sh`
  的 `bash -n`
  都通过
- 当前还没有 live rerun 证据，
  但至少排除了明显的 shell 语法错误

### 2. 先前 change/debug record 少记了一处 wrapper 语义变化

- `scripts/firesim-tmux-run.sh`
  除了新增 help 文本，
  还把
  `PIPELINE_RUNTIME_DEBUG_TRIGGER_*`
  加入了透传环境变量集合
- 这不改变默认 profile，
  但会直接影响后续 trigger overlay
  是否能进到 detached `tmux` session
- 因而这条需要补记到已有记录里，
  否则记录与实际代码不一致

### 3. 主状态文档还停在“准备修 watchdog”的阶段

- `CURRENT_STATUS.md`
  与
  `NEXT_SESSION_PROMPT.md`
  顶部 override
  之前都还在描述：
  “先修 watchdog，再申请 rerun”
- 现在更准确的状态应是：
  “watchdog 双层超时补丁已落地并过语法检查，
  但尚未获批复跑”

## 本轮修改

- 补全 `20260420T072430Z`
  两份记录里关于
  `PIPELINE_RUNTIME_DEBUG_TRIGGER_*`
  透传的说明
- 更新：
  - `docs/CURRENT_STATUS.md`
  - `NEXT_SESSION_PROMPT.md`
  使顶部 authoritative 状态与当前代码一致
- 重新生成：
  - `debug_records/CATEGORY_INDEX.md`
  - `change_records/CATEGORY_INDEX.md`

## 本轮结论

- 当前主线仍然是：
  **先验证 host-watchdog 误杀是否被消除，
  再谈 guest 内部新 blocker。**
- 在没有新 rerun 之前，
  最合理的状态表达不是
  “guest 卡死”，
  也不是
  “cfg15 已确认阻塞 g6”，
  而是：
  **已有一版更合理的 host-side timeout 语义，
  等待用户批准后做同 workload 复跑验证。**

## 下一步

1. 向用户汇报这轮脚本修补的语义、验证结果和边界。
2. 若用户批准，再复跑同一套 `g6 bertmini` workload。
3. 复跑只看一个首要判据：
   是否不再在 `idle=625s` 左右被 host watchdog 提前回收。
