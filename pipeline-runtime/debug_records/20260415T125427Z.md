# 2026-04-15 12:54:27 UTC

## Round Goal

基于上一轮
`20260415T123220Z.md`
已确认的稳定 blocker：

- `seg=0 / stage=0 / sb=3 / tensor=2`
- `export target_seq=1`
- `target=address2`
- `rr_acquire_after_call`

本轮执行一个单变量 observability rerun，
不改 repo 代码，只改 guest trigger overlay，
目标是跨过：

- `target_seq=0 page63`

到：

- `target_seq=1 page0`

从而判断当前冻结点是在：

1. `dma-export pset` 之前
2. `dma-export v2p-begin` / `prt_host_virt_to_phys`
3. `dma submit/wait`

## Planned Trigger Overlay

- `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=1`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=dma-export`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_SEGMENT=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_GLOBAL_STAGE=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_LOCAL_STAGE=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_SUBBATCH=3`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_MANAGER=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_TENSOR_ID=2`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_PAGE=63`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_PRE_RING=8`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_POST_BUDGET=64`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_MATCH_ONCE=1`

## Operational Constraints

- 仍使用 private-IP-only SSH
- 仍使用 file-only guest logging
- 仍保持：
  - `PIPELINE_RUNTIME_MLOCKALL_MODE=2`
  - `PIPELINE_RUNTIME_UART_LOG_ENABLE=0`
  - `PIPELINE_RUNTIME_GUEST_LOG_ENABLE=1`
  - `PIPELINE_RUNTIME_GUEST_DEEP_LOG_ENABLE=0`
- FireSim 命令前必须在
  `/home/ubuntu/chipyard/sims/firesim`
  下 source manager env

## Run Reset

已先终止上一轮 runfarm：

- terminate session:
  `pairdummy-sbus128-terminaterunfarm-20260415-125450`
- terminated instance:
  `i-0069afcb68a57fa9f`

## Launch Status

已发起新的 launch：

- session:
  `pairdummy-sbus128-launchrunfarm-20260415-125510`

当前状态：

- FireSim manager 正在自动重试 `f2.6xlarge`
- 还没有拿到新的 pending/running 实例
- pane log:
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-launchrunfarm-20260415-125510.pane.log`

最近明确观测：

- `0 of 1 f2.6xlarge instances have been launched`
- `Tried all subnets, but there was insufficient capacity to launch your instances`
- 约 `0:01:38` 时仍在重试

## Next Step

继续等待 launch 成功。
一旦拿到实例，立刻按同一 trigger overlay 执行：

1. `infrasetup`
2. `run`
3. private-IP remote freshness
4. live image `debugfs` 读取 trigger log / sparse log / breadcrumb
