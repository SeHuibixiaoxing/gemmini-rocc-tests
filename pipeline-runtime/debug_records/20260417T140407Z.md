# 2026-04-17 14:04:07 UTC

## 本轮目标

- 在不继续扩大热路径文本日志的前提下，静态解释“卡点移动”的原因。
- 判断为什么新的 guest-side delayed `ptrace` probe 没能在 live host 观测面中出现。
- 对 `workerpc` 调试链做最小修正，让下一轮能直接看到 freeze 当刻的 `pc/syscall`。

## 当前 live run

- session:
  `pairdummy-sbus128-tracerv-workerpc-runworkload-20260417-134737`
- instance:
  `i-001f6388b15387aa2`
- private ip:
  `192.168.1.99`

## 新证据

### 1. 本轮已经形成新的稳定 freeze，而不是“还在慢慢跑”

- host watchdog 已连续多次记录：
  `hb='9939019896, 545'`
- 同期 `idle` 从 `31s` 增长到 `312s`
- `guest_sparse` 停在：
  `283374`

因此当前 run 在 `hb=9939019896,545` 之后没有新的 heartbeat，也没有新的 guest 文件增长。

### 2. 当前 freeze 之前的最后稳定 sparse frontier 已再次推进到热路径深处

最新 `bertmini-batch8.log` 尾部显示：

- `stage-fixed-load-sparse phase=end ... slot=1 tensor=1000001 rc=0`
- `spm-xlate-release mgr=7 cfg=15 phase=restore-end`
- `worker stage=0 subbatch=1 begin`
- `conv-sync-strided stage=0 mgr=0..7 flushed use_pointwise=1`
- `export-target-dispatch stage=0 tensor=2 target_seq=0 ... phase=end`
- `export-target-dispatch stage=0 tensor=2 target_seq=1 ... phase=end`
- `worker stage=0 subbatch=1 compute-done`
- `c2-export stage=0 tensor=2 phase=copy-begin idx=0 subbatch=1`
- `dma-export-host stage=0 tensor=2 phase=first-chunk-submitwait-end page=0 copied=0 chunk=1024 rc=0 bounce=0`

这说明所谓“卡点移动”不是随机乱跳，而是同一条顺序路径上，不同观测面对最后可见边界的截断位置不同。

### 3. 当前 `runner-proc.stage` 的 delayed probe 证据没有 live 可见，不代表它没执行

live `debugfs stat` 显示：

- `runner-proc.stage size=13721`
- `mtime = Thu Jan 1 00:00:04 1970`

而其内容只到：

- `after-bin-spawn`
- `ptrace-label=after-bin-spawn`
- `bin-poll-1`

这和本轮 live freeze 时刻明显不匹配。

同样，`breadcrumb.bin` 的 live `mtime` 也只到第 `6s`。

结合项目文档与既往记录，可得出：

- guest 文件对 host `debugfs` 的 live 可见性依赖 wrapper / shell 侧周期性 `sync`
- 文件没更新到 host 可见层，不等价于 guest 内没有继续写
- 当前 delayed probe 更像“写到了 guest page cache，但 host 读取不到”

### 4. 旧镜像 run 的最终 timeout capture 也确认没有 delayed probe 现场

- watchdog 在 `idle=625s` 后产出 capture：
  `pairdummy-sbus128-tracerv-workerpc-runworkload-20260417-134737-192.168.1.99-host-watchdog-20260417T140758Z`
- 该 capture 的 `guest-runner-proc-stage.txt` 仍只到：
  - `after-bin-spawn`
  - `ptrace-label=after-bin-spawn`
  - `bin-poll-1`
- `trace-files.txt` 仍显示 `TRACEFILE-C0..C3 = 0`

因此旧镜像这轮已经完整封口：

- 当前修正尚未部署到这轮 run
- 这轮没有提供 freeze 当刻的 stuck-PC 证据
- 但它足以证明旧 image 下 delayed probe live/capture 可见性都不够

### 5. 历史 triage 已经证明“最后一条文本日志”会因为观测边界不同而漂移

历史记录中，同一主线曾先后把 frontier 记录成：

- `dma_submitwait_after_cleanup sb=3 page=20`
- `rr_release_end sb=4`
- `rr_acquire_after_set_opc sb=5`
- `spm-xlate-release ... restore-end`
- 以及本轮再次进入 `c2-export / dma-export-host`

这些边界在代码上都是同一顺序执行链的相邻窗口，不足以单独证明 blocker 本体在“随机移动”。

## 静态结论

1. `TRACEFILE-C0..C3` 继续为 `0 bytes`，
   说明当前 `TracerV` 仍没有提供可用的 stuck-PC 证据。
2. 当前真正稳定的 freeze 观测面仍是：
   `stage=0 / subbatch=1 / c2-export -> dma-export-host` 之后的更深窗口。
3. “卡点移动”更像是：
   - 同一路径上的不同截断边界
   - 外加 guest 文件可见性滞后
   而不是 blocker 本体不断随机迁移。

## 本轮修改

- 为 `workerpc` 专用 delayed probe 新增后台单次 `sync` 开关：
  `PIPELINE_RUNTIME_RUNNER_BIN_DELAY_PROBE_FORCE_SYNC`
- 默认仅在 `pairdummy_sbus128_tracerv_workerpc_workflow.sh` 中打开

## Freshness

- `workerpc` workflow 的 `local-freshness` 当前按预期失败在：
  `runner-script`
- 因此后续任何带本轮修正的 rerun，
  都必须先更新 FireMarshal image；
  不能继续复用当前旧镜像解释新行为。

## 下一步

1. 等当前 live run 自然超时或手动结束后，不把它当作带新修正的 run。
2. 重新做 `workerpc` image closure / freshness。
3. 用新镜像 rerun，优先查看：
   - `runner-proc.stage` 是否出现 `bin-delay-probe-180s`
   - 是否带出 `ptrace pid=... pc=...`
   - `/proc/<pid>/syscall`
4. 若下一轮 delayed `ptrace` 仍不可见，
   再考虑把 stuck snapshot 改为专用单文件输出，
   而不是继续扩大 runtime 热路径日志。
