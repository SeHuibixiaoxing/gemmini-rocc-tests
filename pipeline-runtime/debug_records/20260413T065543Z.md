# 2026-04-13T06:55:43Z 调试记录

## 本轮目标
- 延续 `12-pair sbus128 dummy-model` 硬件上的 `pipeline-runtime` 调试。
- 在不使用 `uartlog` 作为主观测面的前提下，继续推进新的 FireSim run。
- 回答并固化一个当前状态问题：`DMA` 等待是否仍依赖 `doneflag` 轮询。

## 本轮静态结论
- `doneflag` 仍然存在，但用途是“给 DMA 硬件提供完成写回地址”，不是软件轮询等待条件。
- 当前完成等待语义仍然是 `fence` 路径：
  - `hw_dma_fence()` 位于 `src/prt_dma.c`，其本体是 `fence rw, rw`。
  - `dma_blocking_wait()` 在等待路径里调用 `hw_dma_fence()`，随后进入 `rr` 范围内的 fence/release 流程。
- 相关代码位置：
  - `generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c:1422`
  - `generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c:1521`
  - `generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c:2310`
  - `generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_runtime.c:4397`

## 本轮运行状态
- 新一轮 `launchrunfarm` 已成功，不再卡在 AWS 容量不足。
- 新实例：
  - instance id: `i-0dcd3c1a6b9a7bc2a`
  - private ip: `192.168.1.169`
  - launch time: `2026-04-13T06:54:54Z`
- 这次后续所有远端检查必须继续使用私网 IP，不使用公网 IP。

## 本轮后续推进
- `infrasetup` 已成功完成。
- 远端 `remote freshness` 已通过，确认 guest image 里已经带上本轮需要的 env：
  - `PIPELINE_RUNTIME_PROFILE_ID='pairdummy-sbus128-fixed-v2'`
  - `PIPELINE_RUNTIME_UART_LOG_ENABLE='0'`
  - `PIPELINE_RUNTIME_GUEST_LOG_ENABLE='1'`
  - `PIPELINE_RUNTIME_MLOCKALL_MODE='2'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE='1'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START='2698'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END='2720'`
- `runworkload` 已启动：
  - tmux session: `pairdummy-sbus128-runworkload-20260413-065957`
  - host watchdog 已开启
  - 结果目录：
    - `sims/firesim/deploy/results-workload/2026-04-13--06-59-58-rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy-f2-gemmini-rerocc-pairmanager-dummy16x16-4c12p12-sbus128-linux-bertmini-batch8-fileonly-sync/`

## 当前最新观测
- 当前没有重新回到此前的 `DMA wait` 卡点；更前面的现象是 guest 启动很慢。
- 具体现象：
  - `heartbeat.csv` 持续前进，说明仿真没有静止。
  - `uartlog` 也在缓慢增长，从约 102 行增长到 116 行。
  - 最新串口内容仍停留在 Linux 早期 boot 阶段，尚未出现 `firemarshal`、wrapper、runner、`pipeline-runtime-debug` 的任何标记。
  - 通过 `debugfs` 查看 image，`/root` 里仍只有静态 overlay 内容，`/root/pipeline-runtime-debug` 还没有实际日志文件内容。
- 因此当前 front-most blocker 已临时前移为：
  - guest Linux / rootfs / init 路径尚未推进到 `firemarshal` 启动点
  - 还没有进入 runtime 本体，更谈不上复现 export tok 窗口里的 `DMA` 问题

## 后续推进结果
- 上述“guest 启动很慢”不是永久卡死，后续已继续推进：
  - `uartlog` 继续增长，最终出现：
    - `Run /init as init process`
    - `running /etc/init.d/S99run`
  - image 内已出现：
    - `bertmini-batch8.wrapper.stage`
    - `bertmini-batch8.wrapper-proc.stage`
    - `bertmini-batch8.runner-early.stage`
    - `bertmini-batch8.runner.stage`
    - `bertmini-batch8.runner-proc.stage`
    - `bertmini-batch8.log`
    - `bertmini-batch8.checkpoint.log`
- wrapper/runner 关键阶段：
  - wrapper 已到 `before-child-wait`
  - runner 已到 `after-bin-spawn`
  - runtime binary 进程 pid `197` 已在 guest 内运行

## 当前前沿
- `runtime_init` 已成功通过：
  - `mlockall_mode=2` 但当前 build 配置路径显示 `skip mlockall by build config`
  - `dma-completion-pool after-mlock rc=0`
  - `dma-backend using blocking_fence completion via hw_dma_fence + rr_fence_scope`
- `runtime_run` 已成功通过：
  - `model.layers.yaml` 读取与解析
  - `pipeline_mapping...ours2.yaml` 读取、解析、校验
  - `gemmini_layer_mapping...yaml` 12MB 大文件分块 `pread`
  - mapping parse progress 已推进到至少 `entries=8192`
- 最新运行前沿已推进到 segment 执行本体：
  - `stage=0` 固定页加载正在运行
  - 稀疏日志尾部已经出现大量：
    - `dma-fixed-load-submit ... phase=program-begin`
    - `dma-fixed-load-submit ... phase=program-post-fence`
    - `dma-fixed-load-host ... phase=submitwait-begin/end`
  - checkpoint 日志已出现：
    - `worker stage=0 checkpoint=...`
    - `rr-acquire-inner checkpoint=...`
- 当前尚未重新回到此前 `stage=0 tensor=2 export tok≈2708` 的精确前沿，但已经重新走回 segment 0 的执行路径。

## 补充代码定位
- `doneflag` 相关：
  - `src/prt_dma.c:979-1000`：`dma_debug_capture_done_flag()` 获取完成写回槽位
  - `src/prt_dma.c:2042-2105`：submit 阶段清零并分配 `doneflag`
  - `src/prt_dma.c:2310-2333`：wait 阶段执行 `hw_dma_fence()` 后刷新 `hw_done_flag`
  - `src/prt_dma.c:1422-1427`：`hw_dma_fence()` 本体
- 同步模式选择：
  - `src/prt_runtime.c:4397-4399`：`PRT_DMA_BACKEND_BLOCKING_FENCE`

## 本轮检查到的固定配置
- 固定 profile 文件：
  - `generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh`
- 当前关键 env：
  - `PIPELINE_RUNTIME_PROFILE_ID=pairdummy-sbus128-fixed-v2`
  - `PIPELINE_RUNTIME_MLOCKALL_MODE=2`
  - `PIPELINE_RUNTIME_UART_LOG_ENABLE=0`
  - `PIPELINE_RUNTIME_GUEST_LOG_ENABLE=1`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE=1`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START=2698`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END=2720`

## 本轮执行命令
```bash
sed -n '4388,4410p' generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_runtime.c
sed -n '1456,1478p' generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c
sed -n '2008,2032p' generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c
sed -n '2250,2272p' generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c
tail -n 80 tmp/firesim-aws-f2/tmux/pairdummy-sbus128-launchrunfarm-20260413-065237.pane.log
cd sims/firesim && set +u; source sourceme-manager.sh --skip-ssh-setup; set -u; \
  aws ec2 describe-instances --filters Name=tag:fsimcluster,Values=pairbertb8d12s128a Name=instance-state-name,Values=pending,running ...
```

## 下一步
- 继续监控 segment 0 后续推进，重新收敛到此前的 `export` 相关前沿。
- 一旦再次逼近 `stage=0 tensor=2 export tok=2698..2720`，检查窄窗口 probe 是否只在该 token 区间出现。
- 若后续再次停顿，优先以 guest 文件日志与 checkpoint 日志为准，避免退回以 `uartlog` 为主的观测方式。
