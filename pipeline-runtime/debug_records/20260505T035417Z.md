调试类别：gdbserver / old AGFI / recovered driver

# old AGFI recovered-driver 1BP GDB smoke PASS

日期：2026-05-05T03:54:17Z

## 结论

旧 1BP single-core NIC AGFI 已重新跑通软件断点 remote gdbserver 核心能力测试：

```text
[remote-swbreak-expect] expect_rc=0
[remote-swbreak-expect] PASS
```

这说明当前软件测试 harness、rootfs/workload、SSH tunnel、host TAP 静态邻居配置和
FireSim switch 修复后的软件路径，在 old AGFI + recovered 原始 driver 上可用。

## 环境

- AGFI：`agfi-0079cbbca617eca4e`
- AFI：`afi-0ee7774f829acd4de`
- 类型：single-core Rocket + NIC + no TraceIO + 30MHz，非 8BP
- run host：`i-0e97c45c5c4e0b062` / `192.168.1.139`
- HWDB：`config_hwdb_f2_rocket_singlecore_nic_notrace_30mhz_agfi0079_recovered_driver.yaml`
- runtime：`config_runtime_f2_rocket_singlecore_nic_gdbserver_smoke_notrace_clean1bp_recovereddriver_30mhz.yaml`
- workload：`rocket-singlecore-nic-gdbserver-smoke`
- recovered driver bundle：
  `tmp/firesim-aws-f2/driver-bundles/agfi0079-recovered-20260504/driver-bundle.tar.gz`
- recovered `FireSim-f2` sha256：
  `dc94d9f93b34dfc32dd994d8926d6cd81cf325003a8b2ba551eb9b3ff8d05a92`

关键 plusargs：

```text
+simplenic-empty-switch-poll-interval=1024
+cpu-managed-stream-debug=0
+simplenic-relaxed-required-bytes=1
```

## 本轮修复

`run_remote_gdbserver_software_expect_smoke.sh` 调整为旧能力矩阵：

- 移除默认条件断点段；
- 不再 `delete breakpoints` 后设置 `break smoke_iteration_hook if iteration >= 2`；
- 在命中 worker 普通断点后禁用 worker 断点，再继续命中主线程
  `smoke_iteration_hook`；
- `Ctrl-C` 抢回控制后用 `detach` 收尾；
- wrapper 校验 marker 时同时检查 `remote-swbreak.expect.log` 与
  `expect-driver.stdout`，避免 marker false negative。

## 通过能力

transcript 和 expect marker 覆盖以下能力：

- `target remote`
- 多个普通 software breakpoint
- `continue` 命中 `main`
- `next`
- `continue` 命中 worker `smoke_worker_heartbeat`
- `info threads`
- `thread apply all bt`
- 寄存器读取
- 当前 PC 反汇编
- 全局变量读取
- 内存写入并读回：`smoke_memory_probe[7] = 0xabcdef`
- 线程现场参数读取
- 禁用 worker 断点后命中主线程 `smoke_iteration_hook`
- `Ctrl-C` 抢回控制
- 抢回后再次读取线程、backtrace、寄存器和反汇编
- `detach`

## 现场

证据目录：

`pipeline-runtime/debug_records/artifacts/20260505T035417Z_oldagfi_recovereddriver_legacy_pass/`

包含：

- `remote/uartlog`
- `remote/heartbeat.csv`
- `remote/sim-run.sh`
- `remote/switchlog`
- `remote/gdb-tap0-recovereddriver-finalpass.pcap`
- `local/remote-swbreak-expect-20260505T035417Z-192_168_1_139/remote-swbreak.expect.log`
- `local/remote-swbreak-expect-20260505T035417Z-192_168_1_139/expect-driver.stdout`
- `local/oldagfi0079-recovereddriver-runworkload-20260505-0339.pane.log`

runfarm 已终止，实例 `i-0e97c45c5c4e0b062` 已进入 `shutting-down`。

## 下一步

以此作为软件路径基线，转向当前 1BP 硬件 bitstream：

1. 使用已通过的 harness 和相同 workload 测当前 HWDB `agfi-05336229a1326f9ff`
   或最新有效 1BP AGFI；
2. 如果当前 AGFI 仍失败，优先判断失败点是否回到 RSP payload corruption /
   shmem/TAP 数据破坏，而不是 GDB/harness 问题；
3. 如果需要重新构建，只使用 `FireSimRocketNICNoTraceConfig + BaseF2Config` 的
   1BP 配置，不再回到 8BP 路线。
