# Debug Record 20260420T101447Z

## 目标

- 按当前主线结论，直接缩窄
  `sb=3 / mgr=2`
  的 pointwise precall 窗口
- 不再扩大 coarse log，
  只补低扰动边界 probe

## 输入状态

- authoritative frontier 仍是：
  - `seg=0`
  - `stage=0`
  - `subbatch=3`
  - `mgr=2`
  - `phase=rr_acquire_after_call`
- 当前未知窗口位于：
  `prt_rr_acquire_scope()` 返回之后，
  到
  `gemmini_pointwise_call_begin`
  之前

## 本轮静态决定

- 不改硬件语义
- 不重新打开 pointwise 热路径 coarse progress
- 只在
  `conv_call_for_manager_sync_strided()`
  里补 5 个 precall breadcrumb：
  - `precall_after_scope_marker`
  - `precall_after_binding_snapshot`
  - `precall_dispatch_decided`
  - `precall_after_postflush_snapshot`
  - `precall_ready`
- 额外补 1 个 budgeted compact snapshot，
  用于对比
  `mgr0/1/2`
  的 precall 关键参数

## 预期解释力

- 若前沿仍停在
  `rr_acquire_after_call`
  与
  `precall_after_scope_marker`
  之间：
  优先怀疑 acquire 返回后的 caller-side marker/helper
- 若能推进到
  `precall_after_binding_snapshot`
  但停在
  `precall_dispatch_decided`
  之前：
  优先怀疑 dispatch 判定前后的纯软件窗口
- 若推进到
  `precall_ready`
  但还没到
  `call_begin`：
  说明真实 frontier 已逼近现有 pointwise caller breadcrumb
- compact snapshot 若显示
  `mgr2`
  参数与
  `mgr0/1`
  不同，
  可优先对比地址边界 / alias / stride / cfg 语义

## 本轮未做

- 未启动新
  `launchrunfarm / infrasetup / runworkload`
- 未修改硬件
  `LoadController / ReservationStation / LoopMatmul / LoopConv`
