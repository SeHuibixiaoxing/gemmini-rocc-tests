# 2026-04-13T05:28:13Z 调试记录

## 本轮目标
- 接手上一轮新启动的 `pairdummy-sbus128` live run，确认其是否已进入 guest 侧 `pipeline-runtime` 日志阶段。
- 按约束为本轮单独创建一份时间戳记录，不复用旧文件。
- 一旦 guest 文件日志目录出现，立刻切换到镜像内文件日志作为主观测面。

## 本轮输入状态
- live session:
  - `pairdummy-sbus128-runworkload-20260413-052551`
- 实例:
  - `i-0ab668f09c2fdbef9`
  - 私网 IP: `192.168.1.133`
- manager log:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-13--05-25-52-runworkload-UTJH4DVXWXW2OE5J.log`
- watchdog log:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-052551.host-watchdog.log`
- pane log:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-052551.pane.log`

## 本轮执行命令
- `tail -n 120 /home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-052551.host-watchdog.log`
- `tail -n 120 /home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-13--05-25-52-runworkload-UTJH4DVXWXW2OE5J.log`
- `tail -n 120 /home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-052551.pane.log`
- `ssh -i /home/ubuntu/firesim.pem ubuntu@192.168.1.133 'sudo ls -l /root/pipeline-runtime-debug || true; ...'`
- `ssh -i /home/ubuntu/firesim.pem ubuntu@192.168.1.133 'sudo cat /home/ubuntu/sim_slot_0/heartbeat.csv | tail -n 5; sudo tail -n 60 /home/ubuntu/sim_slot_0/uartlog'`

## 本轮新观察
- watchdog 目前只打印了启动信息，还没有出现：
  - `guest-status arm`
  - `guest sparse/checkpoint growth`
- FireSim manager 侧显示：
  - `Sim running: True`
- 私网实例上 `heartbeat.csv` 已持续增长，最近观测到：
  - `2286163778, 132`
  - `2427914225, 140`
  - `2570014725, 148`
  - `2712091940, 156`
  - `2854167413, 164`
- 这说明当前仿真前进正常，尚未冻结在 host 级别。
- 但镜像内尚未出现：
  - `/root/pipeline-runtime-debug`
- 说明 guest 还没有推进到我们要观察的文件日志阶段，或相关初始化尚未完成。
- 宿主机上的 guest 镜像路径已确认：
  - `/home/ubuntu/sim_slot_0/rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy0-rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy.img`
- 已修正观测方式：
  - 不能直接在宿主机 `ls /root/pipeline-runtime-debug`
  - 正确方式是对上述镜像执行 `debugfs`
- `debugfs -R "ls -l /root"` 显示镜像内当前只有：
  - `rerocc-linux-tests-coupleddma`
  - `firemarshal.sh`
  - `etc`
  - `rerocc-linux-tests`
- `debugfs` 读取以下路径目前仍为空，说明对应文件尚未创建：
  - 这是本轮较早时间的观测；后续已更新，见下文新增前沿
- `uartlog` 最新尾部显示 guest 已经推进到：
  - `Run /init as init process`
  - `Mounting /dev/iceblk as root device`
  - `EXT4-fs (iceblk): mounted filesystem ... r/w without journal`
  - `Loaded platform drivers, booting from disk`
- 继续观察后，`uartlog` 已出现：
  - `running /etc/init.d/S01syslogd`
  - `running /etc/init.d/S02klogd`
  - `running /etc/init.d/S02sysctl`
  - `running /etc/init.d/S10mdev`
  - `running /etc/init.d/S40network`
  - `running /etc/init.d/S99run`
- `uartlog` 在 `ttySIF0` 启用后夹杂一段乱码，但后续日志继续正常前进；当前先记录为串口噪声现象，不据此判为新卡点。
- 随着 guest 继续推进，镜像内已经出现：
  - `/root/pipeline-runtime-debug`
  - `/root/pipeline-runtime-debug/traces`
  - `/root/pipeline-runtime-debug/bertmini-batch8.log`
  - `/root/pipeline-runtime-debug/bertmini-batch8.deep.log`
  - `/root/pipeline-runtime-debug/bertmini-batch8.audit.log`
  - `/root/pipeline-runtime-debug/bertmini-batch8.checkpoint.log`
  - `/root/pipeline-runtime-debug/bertmini-batch8.runner-early.stage`
  - `/root/pipeline-runtime-debug/bertmini-batch8.runner.stage`
  - `/root/pipeline-runtime-debug/bertmini-batch8.runner-proc.stage`
  - `/root/pipeline-runtime-debug/bertmini-batch8.wrapper.stage`
  - `/root/pipeline-runtime-debug/bertmini-batch8.wrapper-proc.stage`
  - `/root/pipeline-runtime-debug/bertmini-batch8.status`
- `bertmini-batch8.status` 当前内容显示：
  - `state=running`
  - `profile_id=pairdummy-sbus128-fixed-v1`
  - `periodic_sync_enable=1`
  - `periodic_sync_seconds=1`
  - `uart_log_enable=0`
  - `guest_log_enable=1`
  - `checkpoint_log_enable=1`
  - `dummy_gemmini_mode=1`
  - `mlockall_mode=2`
  - `skip_model_bin_load=1`
  - `skip_input_load=1`
  - `skip_golden_check=1`
  - `auto_poweroff=1`
- `bertmini-batch8.wrapper.stage` 当前前沿：
  - `wrapper-enter`
  - `after-log-preamble`
  - `before-child-spawn runner=/root/rerocc-linux-tests/run_rerocc_pipeline_runtime_bertmini.sh`
  - `after-child-spawn pid=132`
- 随后再次轮询，已确认 guest image 里的 wrapper 脚本不是旧版本：
  - `/root/rerocc-linux-tests/run_rerocc_pipeline_runtime_bertmini_fileonly_sync_capture.sh`
    确实显式传入了：
    - `PIPELINE_RUNTIME_SKIP_GUEST_ENV_SOURCE=1`
    - `PIPELINE_RUNTIME_RUNNER_STAGE_SYNC_ENABLE=0`
- 再次读取 live image 后，runner 已明确越过 earlier shell 卡点：
  - `runner-early.stage` 已到：
    - `script-entry`
    - `skip-guest-env inherited path=/firemarshal.env`
    - `after-arg-parse batch=8`
    - `after-bin-check path=/root/rerocc-linux-tests/pipeline-runtime/rerocc_pipeline_runtime-linux`
    - `before-runner-enter`
  - `runner.stage` 已到：
    - `after-runner-enter batch=8`
    - `after-resolve-manager-layout gemmini=12 dma=12 pair=1`
    - `before-prepare-hugetlb`
    - `after-prepare-hugetlb`
    - `before-bin method=ours2`
    - `after-bin-spawn method=ours2 pid=196`
  - `runner-proc.stage` 已抓到 runtime binary：
    - `pid=196`
    - `cmdline=/root/rerocc-linux-tests/pipeline-runtime/rerocc_pipeline_runtime-linux ... --pair-manager-mode 1 --spm-pt-require-hugetlb 1 --skip-model-bin-load --skip-input-load --skip-golden-check`
- `bertmini-batch8.log` 现已进入 runtime 主体，并明确出现：
  - `[prt-early] enter main`
  - `[prt-early] process memory lock mode=disabled flags=0x0`
  - `[prt-progress] init begin ...`
  - `[prt-progress] spm-pt pool init chunk_bytes=2097152`
  - `[prt-progress] yaml model load begin`
  - `[prt-progress] yaml pipeline load end ... segments=13 total_stages=40 subbatch_size=1`
  - `[prt-progress] artifacts mapping load end ... bytes=12029923 elapsed_ms=543`
  - `[prt-progress] artifacts mapping parse progress ... entries=8192`
- `bertmini-batch8.checkpoint.log` 已出现：
  - `spm-pt checkpoint=huge-page-size-enter cached=0`
  - `spm-pt checkpoint=huge-page-size-exit size=2097152`
- host watchdog 已正常 arm：
  - `guest-status arm observed at 2026-04-13T05:32:39Z`
  - 且随后多次记录到：
    - `progress`
    - `guest_sparse` 增长
    - `guest_checkpoint` 增长
- 再往后追踪 `bertmini-batch8.log` 尾部，已确认本轮明确越过旧 blocker：
  - `export-target-dispatch stage=0 tensor=2 target_seq=1 ... phase=end`
  - 说明“第二个 alias target 导出卡死”这条旧卡点本轮已经被跑过去
- 旧 blocker 之后的继续推进证据：
  - `worker stage=0 subbatch=1 compute-done`
  - `c2-export stage=0 tensor=2 ... phase=copy-end`
  - `c2-export stage=0 tensor=2 phase=retire idx=0 rc=0 next_sbatch=2 full=0`
  - `worker stage=0 subbatch=1 done`
  - `stage-exec-views phase=begin segment=0 stage=0 layer=0 tensors=4`
  - `stage-fixed-load-sparse phase=begin ... tensor=1000000`
  - `stage-fixed-load-sparse phase=end ... tensor=1000000`
  - `stage-fixed-load-sparse phase=begin ... tensor=1000001`
- `checkpoint.log` 同步证明 DMA 完成等待路径也在正常前进：
  - 已看到多轮
    - `doneflag-begin`
    - `doneflag-acquire-*`
    - `program-begin`
    - `wait-fence-done`
  - 说明当前并没有重新卡回“doneflag 等待 / DMA 完成判断”老问题

## 当前判断
- 当前 run 还处于较早启动窗口。
- 现阶段不能根据“文件日志目录不存在”判断为失败，因为 heartbeat 仍在增长，manager 也确认 job 正在运行。
- guest 已越过内核早期启动，也已经进入 `firemarshal` wrapper。
- 当前 live 前沿已明显前移：
  - 已越过 wrapper / runner shell / hugetlb 准备
  - 已进入 runtime 二进制主体
  - 已越过 `artifacts mapping parse`
  - 已越过此前的 `tensor=2 second alias target export` 旧 blocker
- 当前最新可见前沿继续前推到：
  - `segment=0 stage=0`
  - `worker stage=0 subbatch=1 done`
  - `stage-fixed-load-sparse ... tensor=1000001`
- 因而本轮较早阶段“child 尚未写第一条 runner.stage”的判断已被后续 live 观测推翻，属于时序窗口内的暂态结论。
- 下一步应直接监控：
  - `bertmini-batch8.log` 尾部
  - `bertmini-batch8.checkpoint.log`
  以确认新的冻结点是否会继续后移。

## 相关约束再次确认
- live 查询只使用私网 IP：`192.168.1.133`
- 不以 `uartlog` 为主观测面，`uartlog` 仅作辅助手段
- 每一轮调试单独记录
- 后续如需重跑，仍必须做 freshness 闭环

## 下一步
- 继续轮询 watchdog / manager / guest 文件目录。
- 继续直接用 `debugfs` 读取：
  - `bertmini-batch8.log` 尾部
  - `bertmini-batch8.checkpoint.log`
  - 必要时回看 `runner-proc.stage`
- 如果 watchdog 后续 arm 并出现停滞：
  - 抓新 capture
  - 对照本轮新增 `target_seq` 与 `page=48..63` 补点缩小冻结页
