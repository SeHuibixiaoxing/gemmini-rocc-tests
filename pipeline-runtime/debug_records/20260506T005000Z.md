# 2026-05-06 00:50:00 UTC - noTrace cfg32 NIC reaches post-phy_opt

## Goal

Record the next Vivado implementation milestone for the active noTrace
`12p4c128sbus32cfg + optimized DMA + current NIC` F2 build.

## Build

- FireSim tmux session:
  `pairdummy-cfg32-nic-notrace-20260505T171453Z`
- Build host:
  `i-0ecf73dad2a8ae8d0`, private IP `192.168.0.213`
- Target config:
  `FireSimGemminiReRoCCPairDummy16x16C4P12Sbus128NICNoTraceConfig`
- Platform config:
  `FRFCFS16GBQuadRank_BaseF2Config`
- Strategy/frequency:
  `TIMING`, `20MHz`
- Remote Vivado log:
  `/home/ubuntu/firesim-build/platforms/f2/aws-fpga-firesim-f2/hdk/cl/developer_designs/cl_f2-firesim-FireSim-FireSimGemminiReRoCCPairDummy16x16C4P12Sbus128NICNoTraceConfig-FRFCFS16GBQuadRank_BaseF2Config/build/scripts/2026_05_05-191909.vivado.log`

## Poll evidence

The `2026-05-06T00:49:37Z` 1200s poll showed:

```text
place_design completed successfully
AWS FPGA: (00:22:56): Start physical-optimizing customer design ...
AWS FPGA: phys_opt command: phys_opt_design -directive AggressiveExplore
Phase 4 Single Cell Placement Optimization
Phase 5 Multi Cell Placement Optimization
Phase 9 Single Cell Placement Optimization
Phase 10 Multi Cell Placement Optimization
Phase 15 Single Cell Placement Optimization
Phase 16 Multi Cell Placement Optimization
Phase 29 Single Cell Placement Optimization
Phase 30 Multi Cell Placement Optimization
AWS FPGA: (00:49:13): Writing post-phy_opt design checkpoint and report
```

The same poll still showed the Vivado process alive and the same single
`z1d.3xlarge` build host running. No F2 runfarm was running.

At the exact poll moment, the reports/checkpoints list had not yet shown the
new post-phy_opt artifacts, so this checkpoint records the log transition into
post-phy_opt artifact writing rather than completed route.

## Interpretation

The build has now moved beyond placement and pre-route physical optimization.
The next decisive phase is route. There is still no AGFI/AFI, so the cfg32 NIC
HWDB remains unchanged and no workload/runfarm should be launched yet.

If this later fails in route/timing, collect post-phy_opt and route timing
reports before deciding whether the next build should be a strategy variant or
further resource reduction.
