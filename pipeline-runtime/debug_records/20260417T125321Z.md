# 2026-04-17 12:53:21 UTC

## 本轮目标

- 在不继续移动 guest 卡点的前提下，
  用当前 live `tracerv-inst` round 的直接证据判断：
  `selector=3` instruction trigger 到底有没有在真实 freeze 窗口内生效。
- 如果 `selector=3` 证据已经足够强，则准备一个不依赖 instruction trigger 的
  `selector=2` worker-PC fallback workflow。

## 输入状态

- live run:
  `pairdummy-sbus128-tracerv-inst-runworkload-20260417-123511`
- runtime config:
  `/home/ubuntu/chipyard/sims/firesim/deploy/config_runtime_f2_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_linux_bertmini_pipeline_runtime_batch8_fileonly_sync_tracerv_inst.yaml`
- run host:
  `192.168.1.138`
- FireSim PID:
  `3864`

## 使用命令

- `ssh ... ubuntu@192.168.1.138 'tail -n 20 /home/ubuntu/sim_slot_0/heartbeat.csv; sudo wc -c /home/ubuntu/sim_slot_0/TRACEFILE-C*; ...'`
- `ssh ... ubuntu@192.168.1.138 'tr "\\0" "\\n" </proc/$pid/cmdline | grep -E "trace|slotid|binary|profile|workload"'`
- `ssh ... ubuntu@192.168.1.138 'sudo find /proc/$pid/fd -maxdepth 1 -type l -lname "/home/ubuntu/sim_slot_0/TRACEFILE-C*" -printf "%f -> %l\n"'`
- `ssh ... ubuntu@192.168.1.138 'for fd in 7 8 9 10; do sudo cat /proc/$pid/fdinfo/$fd; done'`
- `sed -n '3920,4015p' src/prt_runtime.c`
- `sed -n '4335,4405p' src/prt_runtime.c`
- `/home/ubuntu/chipyard/.conda-env/riscv-tools/bin/riscv64-unknown-linux-gnu-objdump -d .../rerocc_pipeline_runtime-linux`
- `sed -n '150,240p' sims/firesim/docs/Advanced-Usage/Debugging-and-Profiling-on-FPGA/TracerV.rst`
- `sed -n '1,260p' generators/firechip/bridgestubs/src/test/scala/TracerVSuite.scala`

## 关键新证据

### 1. live run 已再次精确到达旧 freeze 热点，而且 heartbeat 已持续停住

- `2026-04-17T12:48:19Z` 远端仍显示 heartbeat 最后 3 行为：
  - `9378394454, 516`
  - `9652331641, 529`
  - `9918714556, 543`
- 同时 sparse tail 仍稳定停在：
  - `stage-fixed-load-sparse phase=begin ... slot=1 tensor=1000001`
  - 最后只到 `rr-acquire-wrap phase=after-call ... rc=0 cfg=0 valid=1`
- 说明本轮不是“还没跑到上次卡点”；
  guest 已重新进入同一 runtime 热路径，并在该处再次冻结。

### 2. 当前 run 的 TracerV plusargs 与文件句柄都真实存在

- `/proc/3864/cmdline` 直接显示：
  - `+tracefile=TRACEFILE`
  - `+trace-select=3`
  - `+trace-start=ffffffff00008013`
  - `+trace-end=ffffffff00010013`
  - `+trace-output-format=0`
  - `+trace-min-batch=0`
- `/proc/3864/fd` 里已打开：
  - `7 -> /home/ubuntu/sim_slot_0/TRACEFILE-C0`
  - `8 -> /home/ubuntu/sim_slot_0/TRACEFILE-C1`
  - `9 -> /home/ubuntu/sim_slot_0/TRACEFILE-C2`
  - `10 -> /home/ubuntu/sim_slot_0/TRACEFILE-C3`
- 但 `/proc/3864/fdinfo/{7,8,9,10}` 的 `pos` 全部仍为 `0`。
- 同期 `sudo wc -c /home/ubuntu/sim_slot_0/TRACEFILE-C*` 仍全部为 `0`。

### 3. worker start marker 的确在当前 freeze 路径之前，而且 binary 里位置唯一

- `src/prt_runtime.c` 显示：
  - `prt_firesim_tracerv_worker_start_marker();`
    在 `stage_worker_main` 主循环之前
  - `prt_firesim_tracerv_worker_end_marker();`
    在 worker 退出前
- 交叉 `objdump` 直接确认当前 runtime ELF 中：
  - start marker:
    `000000000002a2b0: 00008013`
  - end marker:
    `000000000002ab22: 00010013`
- 这两条都位于 `stage_worker_main`：
  - `stage_worker_main` 起点：
    `000000000002a042`
- 因而“trace 为 0 只是因为还没跑到 marker”这一解释已经不成立。

### 4. FireSim 文档与当前 start/end 取值一致，但 instruction-trigger 回归测试长期被禁用

- `TracerV.rst` 明确写明：
  - selector `3` 为 instruction trigger
  - `start/end` 的上 32 位为 mask，下 32 位为 compare
  - `ffffffff00008013` /
    `ffffffff00010013`
    是官方示例
- 同 repo 的 `TracerVSuite.scala` 中，
  唯一 selector `3` 的 case：
  `TracerVF1TestCount5`
  仍被注释，并注明
  `disabled until FireSim issue #1428 is resolved`

## 分析结论

- 到这一轮为止，`selector=3` 的现象已经不是“观测不足”：
  - guest 已进入真实 freeze 热点；
  - worker start marker 已在此前执行路径上；
  - host `TRACEFILE-C*` 已打开；
  - `+trace-min-batch=0` 已部署；
  - 但 trace 文件与 fd `pos` 仍全为 `0`。
- 因此当前更合理的判断是：
  **本分支上的 TracerV instruction trigger（selector=3）本身不可靠，至少不足以作为这次 freeze 定位的主路径。**
- 接下来最稳妥的路线不是继续在 guest 里移动 marker，
  而是回退到 repo 内已知可用的 `selector=2` PC trigger，
  但把边界从旧的粗 PC 改成这次直接反汇编得到的
  `stage_worker_main` marker PCs：
  - start: `0x2a2b0`
  - end: `0x2ab22`

## 下一步

1. 保留当前 live `tracerv-inst` run 直到 watchdog 自然超时并收集 capture。
2. 使用新建的 `workerpc` workflow 做下一轮 rerun：
   - selector `2`
   - start `2a2b0`
   - end `2ab22`
   - 保持 `+trace-min-batch=0`
3. 若 `workerpc` round 能产出 `TRACEFILE-C*`，
   直接用 trace 尾部回映射 freeze 指令窗口；
   若仍无 trace，再回头检查更底层的 TracerV/trace bundle 路径。
