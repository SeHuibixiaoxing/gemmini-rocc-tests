# 2026-04-15 04:49:43 UTC Debug Record

## Round Metadata
- Timestamp: `20260415T044943Z`
- Objective: Continue FPGA debug on the canonical `12-pair sbus128` path following the documented SOP.
- Scope of this round:
  - Reconfirm the active hardware/runtime configuration.
  - Re-read the latest official stalled capture.
  - Statically inspect the fixed-load direct path around `sb=4`, `tensor=0`, `page=1`, especially the `v2p-b -> v2p-e` interval.
- Constraints carried into this round:
  - SSH/live inspection must use private IP only.
  - Use filesystem logs / watchdog captures as the primary observation surface, not `uartlog`.
  - Do not reintroduce DMA doneflag polling.
  - Preserve hugetlb / physically contiguous page assumptions.
  - Preserve `PIPELINE_RUNTIME_MLOCKALL_MODE=2`.
  - Before FireSim / FireMarshal / compile flows, source `sims/firesim/sourceme-manager.sh` from inside `sims/firesim`.
  - Use the freshness / preflight workflow before any rerun.

## Initial State
- Previous round record: `debug_records/20260415T041424Z.md`
- Latest official stalled capture prefix:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/pairdummy-sbus128-runworkload-20260415-042528-192.168.1.109-host-watchdog-20260415T044550Z`
- Expected live blocker candidate at round start:
  - `seg=0`, `gstage=0`, `lstage=0`, `sb=4`, `manager=0`, fixed-load path, likely inside or immediately after `prt_host_virt_to_phys()` for source page `1`.

## Actions
- Created this round record before any further debug action.
- Reconfirmed the active FireSim runtime config:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/config_runtime_f2_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_linux_bertmini_pipeline_runtime_batch8_fileonly_sync.yaml`
- Reconfirmed the active HWDB entry:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/config_hwdb_f2_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_bertmini.yaml`
  - key: `firesim_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128`
  - AGFI: `agfi-0dc8dcfa4c7735f40`
- Re-read the official stalled capture status / sparse / trigger / breadcrumb artifacts.
- Re-read the fixed-load host direct path in `src/prt_dma.c`.
- Re-read `prt_host_virt_to_phys()` in `src/prt_page_table.c`.
- Performed a new image closure after an observability-only code change:
  - FireMarshal clean/build/install completed.
  - Local image freshness passed.
- Started a fresh FPGA rerun sequence:
  - `launchrunfarm` completed.
  - `infrasetup` started and progressed on a new private-IP host.
  - new instance: `i-026f018c64c851dd7`
  - new private IP: `192.168.1.154`
  - runworkload tmux session:
    - `pairdummy-sbus128-runworkload-20260415-050642`
  - host watchdog log:
    - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260415-050642.host-watchdog.log`

## Findings
- The previous official capture still authoritatively shows progression past:
  - Linux boot
  - hugetlb setup
  - YAML model/pipeline/layer-mapping load
  - `subbatch=3` completion and `rr_release_end`
- The official stall remains narrowed to:
  - `seg=0`, `gstage=0`, `lstage=0`, `sb=4`, `manager=0`
  - fixed-load direct path for `tensor=0`, around `page=1`
  - trigger tail:
    - `dfix ph=pset ... tn=0 pg=1`
    - `dfix ph=v2p-b ... tn=0 pg=1`
- `status.txt` confirms the guest is using file-backed logs, not UART as the main observation surface.
- `status.txt` also confirms:
  - `profile_id=pairdummy-sbus128-fixed-v20`
  - `mlockall_mode=2`
  - `guest_deep_log_enable=0`
  - `breadcrumb_enable=1`
- The runner script launched the runtime binary successfully; the guest-side child process PID was `196` in the stalled run.
- `prt_host_virt_to_phys()` itself has very little code between:
  - fixed-load `v2p-b`
  - and fixed-load `v2p-e`
  - so the next missing distinction is inside the host VA->PA helper.
- Critical static fact:
  - the synthetic model blob in the stalled run was allocated at
    `0x3fb3c7a000`
  - it is 4 KiB aligned
  - it was prefaulted and `mlock()` succeeded (`rc=0`)
  - therefore logical tensor page `0` and logical tensor page `1` for `tensor=0`
    both live within the same host 4 KiB page
  - this makes a genuine “page1-only pagemap translation content failure” much less likely.
- `triage_prt_capture.py` still classifies the old capture as `observability_only` and suggests `control-rerun-first`, but that recommendation does not account for the new code-level narrow probe added in this round.
- Fresh rerun status so far:
  - `launchrunfarm` exit code: `0`
  - `infrasetup` exit code: `0`
  - remote image freshness passed on `192.168.1.154`
  - AGFI on the host was reflashed to:
    - `agfi-0dc8dcfa4c7735f40`
  - guest boot is still progressing; heartbeat has continued advancing past 200s of simulated runtime with no early crash signal observed.

## Decisions
- Added a narrow internal trigger probe inside `prt_host_virt_to_phys()` instead of enabling a broader log family.
- Reused the existing trigger-log infrastructure instead of adding a new temporary overlay environment variable.
- Kept the fixed profile semantics unchanged:
  - no new foreground `sync`
  - no deep-log global enable
  - no doneflag polling reintroduction
  - no hugetlb / contiguous-page semantic rollback
- Identified and fixed a harness issue in `/home/ubuntu/chipyard/scripts/firesim-tmux-run.sh`:
  - the tmux wrapper had been propagating `FIRESIM_*` envs but not `PIPELINE_RUNTIME_DEBUG_TRIGGER_*`
  - this can silently drop a shell-side trigger overlay during detached FireSim runs
  - the wrapper now explicitly propagates `PIPELINE_RUNTIME_DEBUG_TRIGGER_*`

## Next Steps
- Let the current `infrasetup` finish.
- Run the trigger-gated FPGA workload rerun on the new image.
- Treat the currently running `2026-04-15 05:06:42 UTC` rerun as follows:
  - it is still useful for low-disturbance frontier observation
  - it is **not** authoritative for trigger-subphase diagnosis because the trigger overlay was dropped by the old tmux wrapper
- If the rerun stalls again, inspect the new trigger log for the added internal phases:
  - `iv2p-b`
  - `iv2p-pb`
  - `iv2p-pe`
  - `iv2p-rf`
  - `iv2p-np`
  - `iv2p-p0`
  - `iv2p-ok`
- Use those phases to decide whether the block is:
  - before `pread`
  - in `pread`
  - after `pread` with invalid entry
  - or after successful VA->PA translation
