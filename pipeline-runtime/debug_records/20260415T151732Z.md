# Debug Record 20260415T151732Z

## 目标

- 继续监控 `pairdummy-sbus128-fixed-v24` 低扰动 run
- 判断 `v24` 是否已经稳定越过此前被扰动的 `v23` 假前沿
- 在不增加新代码探针的前提下，重新确定当前 authoritative frontier
- 基于现有 observability 机制选择下一轮最窄 rerun

## 本轮对象

- session:
  `pairdummy-sbus128-runworkload-20260415-150238`
- instance:
  `i-04318a7e470c093de`
- private ip:
  `192.168.1.228`
- runtime config:
  `/home/ubuntu/chipyard/sims/firesim/deploy/config_runtime_f2_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_linux_bertmini_pipeline_runtime_batch8_fileonly_sync.yaml`
- hwdb:
  `/home/ubuntu/chipyard/sims/firesim/deploy/config_hwdb_f2_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_bertmini.yaml`
- guest profile:
  `pairdummy-sbus128-fixed-v24`

## 命令

- host monitor:
  - `tail -n 260 tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260415-150238.monitor.log`
- guest status / log / heartbeat:
  - 使用私网 IP `192.168.1.228`
  - 通过 `debugfs` 读取 guest image 内
    `bertmini-batch8.status`
    `bertmini-batch8.log`
    `heartbeat.csv`
- breadcrumb:
  - 拉回
    `bertmini-batch8.breadcrumb.next.bin`
  - 拉回
    `bertmini-batch8.breadcrumb.recheck.bin`
  - `python3 scripts/decode_prt_breadcrumb.py --all <breadcrumb>`
- 静态代码复核:
  - `src/prt_rerocc.c`
  - `src/prt_dma.c`
  - `src/prt_scheduler.c`
  - `src/prt_runtime.c`
- triage:
  - 首次对临时 capture 目录直接跑 `triage_prt_capture.py`
    因文件名不符合脚本默认模式而未识别
  - 随后显式传入
    `--breadcrumb`
    `--sparse-log`
    重新 triage

## 关键观察

- `v24` 已明确越过 `v23` 的假前沿：
  - guest log 至少推进到
    `worker stage=0 subbatch=5 done`
  - 与之对应的 breadcrumb 前沿推进到
    `sb=6`
- host watchdog 在本轮早段确认过多次推进：
  - `guest_sparse=1009`
  - `guest_sparse=204111`
  - `guest_sparse=211484`
  - `guest_sparse=349957`
- 之后进入稳定静止：
  - `guest_sparse=349957`
    持续不变
  - host watchdog `idle`
    从
    `156s`
    继续增长到
    `250s`
- 二次 breadcrumb 复取结果完全一致：
  - `update_count=23333`
  - `last_kind=rr`
  - `last_phase=rr_release_end`
  - `seg=0 gstage=0 lstage=0 sb=6 mgr=0`

## 当前 authoritative frontier

- breadcrumb 最后事件：
  - `rr/rr_release_end`
  - `segment=0`
  - `global_stage=0`
  - `local_stage=0`
  - `subbatch=6`
  - `manager=0`
- 同一份 breadcrumb 的最近 DMA 完成事件已经到：
  - `tensor=0`
  - `page=63`
  - `dma_page_end`
- 因而当前更准确的描述是：
  - `segment0/stage0/subbatch6/tensor0`
    的
    `DRAM -> SPM`
    页搬运已经完成到最后一页
  - 随后最近可见 breadcrumb 停在
    `rr_release_end`

## 静态代码判断

- 这一轮前沿不再是旧的
  `tensor=2 export`
  热路径日志问题。
- `stage0 tensor0`
  在当前产物里确认为：
  - `entryTensorIdList: [0]`
  - `entryTensorTypeList: [DRAM]`
  - 无 ring
  - 位于 segment 0 / stage 0
- 对应代码路径是：
  - `prt_scheduler.c:263`
    `prt_process_c1()`
  - 其中真实搬运调用：
    `prt_dma_copy_dram_to_spm_pages_prefix()`
- `page63 / rr_release_end`
  之后按源码还会继续执行：
  - 回到 `prt_process_c1()`
  - `pthread_mutex_lock(&buf->lock)`
  - 设置
    `buf->full[idx] = 1`
    `buf->subbatch_offset += 1`
    `pthread_cond_broadcast(&buf->cv)`
  - 再回到 worker 主循环
  - 经过
    `prt_pipebuf_wait_full()`
    `stage_wait_exports_ready()`
    `build_stage_task_desc()`
- 当前 coarse log 没有出现
  `subbatch=6`
  的
  `stage-exec-views phase=begin`
  或
  `worker ... begin`
  相关文本，
  说明卡点更可能位于：
  - `c1` 返回后的 very-short window
  - 或其后、但早于下一条 coarse worker/build log 的窗口
- `prt_pipebuf_wait_full()` /
  `stage_wait_exports_ready()`
  这类超时等待路径理论上会继续产生日志；
  当前没有看到新的 timeout wait 文本，
  所以更可疑的仍是：
  - `rr_release_end` 之后的无日志短窗口
  - 尤其是 `prt_process_c1()` 返回后立刻需要拿的 `buf->lock`

## Triage 结果

- 显式指定文件后的 triage 输出：
  - `frontier=rr/rr_release_end seg=0 gstage=0 lstage=0 sb=6 mgr=0 tensor=any page=any tok=0`
  - `recommended_next_probe=control-rerun-first`
  - `verdict=frontier at rr/rr_release_end`
- triage 给出的默认 trigger env 是
  `rr family`
  窗口：
  - `global_stage=0`
  - `local_stage=0`
  - `subbatch=6`
  - `manager=0`
- 但结合静态代码复核，
  只看 `rr family`
  仍无法分开
  `rr_release_end`
  之后、
  `c1 post-DMA -> worker next-step`
  之间的窗口。

## 结论

- 本轮有实质推进。
  与 `v23` 相比，
  `v24` 已越过被 observability 扰动出来的假前沿，
  当前新稳定前沿是
  `sb=6`
  而不是
  `sb=3`
- 本轮没有新的代码修改；
  当前结论来自：
  - 低扰动 live 复测
  - 二次 breadcrumb 一致性确认
  - pipeline artifact 静态核对
  - 相关源码路径静态复核
- 当前最合理的下一步不是立刻开更宽文本日志，
  而是先做一次 breadcrumb-only control rerun：
  - 保持 fixed profile 语义参数不变
  - 保持主日志仍写 guest 文件系统
  - 只临时收窄
    `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH=6`
    `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH_RADIUS=0`
  - 先看 frontier 是否会在更低扰动下自然继续后移

## 下一步

1. 结束当前已稳定冻结的 `v24` run
2. 通过 workflow 脚本做一次 breadcrumb-only control rerun
3. 只覆盖允许的 breadcrumb 窗口参数，不改 fixed profile 其它语义项
4. 若 rerun 仍稳定停在同一位置，再考虑开启单 family、单窗口的更窄 trigger
