# 2026-05-05 19:23 UTC - lightweight host tests while cfg32 builds run

## Context

Both cfg32 NIC F2 bitstream builds were still running, so this round only ran
lightweight local tests that do not consume the active build hosts.

Active builds at the time:

- Mainline `cfg32_nic`: `pairdummy-cfg32-nic-mainline-20260505T132956Z`,
  remote host `192.168.0.60`, in Vivado placement.
- noTrace `cfg32_nic`: `pairdummy-cfg32-nic-notrace-20260505T171453Z`,
  remote host `192.168.0.213`, in early Vivado synthesis.

## Commands and results

### SSHPort checksum/ARP repair

```bash
g++ -std=c++17 -Wall -Wextra -O2 \
  sims/firesim/target-design/switch/tests/sshport_repair_test.cc \
  -o /tmp/sshport_repair_test
/tmp/sshport_repair_test
```

Result:

```text
PASS sshport_repair_test
```

Notes: compile emitted existing `strncpy` truncation warnings in `sshport.h`.

### Shmem empty marker clearing

```bash
g++ -std=c++17 -Wall -Wextra -O2 \
  sims/firesim/target-design/switch/tests/shmemport_marker_test.cc \
  -o /tmp/shmemport_marker_test
/tmp/shmemport_marker_test
```

Result:

```text
PASS shmemport_marker_test
```

Notes: compile emitted existing string-literal-to-`char *` warnings in
`shmemport.h`.

### DDR stat split pipe xsim

```bash
cd sims/firesim/platforms/f2/aws-fpga-firesim-f2
xsim test_lib_pipe_ddr_stat_split_sim -runall
```

Result:

```text
PASS test_lib_pipe_ddr_stat_split
```

## Interpretation

These checks support the existing static conclusion that the current host-side
switch repair logic, shmem empty-marker handling, and DDR status split pipe
module are not the immediate blockers for remote `gdbserver`.

They do not cover:

- FireSim CPU-managed stream end-to-end behavior
- target IceNIC runtime behavior
- Vivado placement/routing
- F2 guest Linux networking
- pipeline-runtime DMA/Gemmini/SPM execution

The next decisive check remains a successful new AGFI followed by real
`gdb target remote` attach on the cfg32 NIC workload.
