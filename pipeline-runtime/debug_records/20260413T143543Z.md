# 2026-04-13T14:35:43Z 调试记录

## 本轮目标
- 把上一轮 `v9` live run 的真实推进结论补档，避免继续把旧怀疑点 `shared-fence/page=11` 误判成当前 blocker。
- 修补 `fixed-load probe` 的 guest env 透传缺口，切换到 `v10` profile，继续定位新的 plateau。

## 本轮改动
- 确认并记录：
  - 旧卡点 `stage=0 / sb=3 / tensor=2 / export page=11 / shared-fence` 已被真实越过。
  - 当前稳定 plateau 更像在 `subbatch=4 / fixed-load / tensor=1000001` 邻域。
- 准备修改以下脚本：
  - `rerocc-linux-tests-coupleddma/workload/host-init.sh`
  - `pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh`
  - `pipeline-runtime/scripts/pairdummy_sbus128_workflow.sh`
  - `pipeline-runtime/scripts/verify_pairdummy_firemarshal_image_freshness.sh`
  - `pipeline-runtime/scripts/verify_pairdummy_firesim_remote_image_freshness.sh`

## 使用命令
```bash
date -u +%Y%m%dT%H%M%SZ
sed -n '1,220p' generators/gemmini/software/gemmini-rocc-tests/rerocc-linux-tests-coupleddma/workload/host-init.sh
sed -n '1,240p' generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh
sed -n '1,260p' generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_workflow.sh
sed -n '1,260p' generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/verify_pairdummy_firesim_remote_image_freshness.sh
rg -n "DMA_FIXED_LOAD|FIXED_LOAD_PROBE|FIXED_LOAD_CHECKPOINT" generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime generators/gemmini/software/gemmini-rocc-tests/rerocc-linux-tests-coupleddma -S
```

## 配置 / 环境
- 当前 live run session：
  - `pairdummy-sbus128-runworkload-20260413-141848`
- 当前实例：
  - `i-00b3514f97391f9c2`
- 当前私网 IP：
  - `192.168.1.121`
- 约束继续保持：
  - 只允许私网 IP SSH
  - 以 guest 文件系统日志为主，不依赖 `uartlog`
  - 不回退到 doneflag 轮询
  - 不回退 hugetlb / prefault + mlock 路径

## 产物 / 日志
- 当前 host watchdog：
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-141848.host-watchdog.log`
- 当前 manager pane：
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-141848.pane.log`

## 新卡点
- 旧卡点已经不是稳定 blocker。
- 新 plateau 证据：
  - sparse 文本最后稳定停在：
    - `worker stage=0 subbatch=3 done`
    - `stage-fixed-load-sparse phase=begin segment=0 stage=0 slot=1 tensor=1000001 ...`
    - `rr-acquire-wrap phase=after-call ...`
  - breadcrumb 最后稳定停在：
    - `seg=0 gstage=0 lstage=0 sb=3 tensor=2 page=63 phase=dma_page_end`

## 分析
- `prt_dma.c` 里 fixed-load probe 代码是存在的。
- 但 `host-init.sh` 的 `write_pipeline_runtime_guest_env()` 白名单缺少：
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PROBE_ENABLE`
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PROBE_TOKEN_START`
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PROBE_TOKEN_END`
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_CHECKPOINT_ENABLE`
- 这意味着：
  - 即使 host 侧 fixed profile 切到 `v10`，guest 内也可能拿不到这些变量。
  - 因而下一轮必须先修透传链，再重建 image 做 freshness 校验。

## 下一步
1. 在 `host-init.sh` 补 fixed-load env 白名单。
2. 把 fixed profile 从 `v9` 切到 `v10`：
   - 关闭 export probe
   - 开启 `fixed-load probe`
   - token 窗口设为 `1230..1300`
3. 更新 workflow `show_config()` 与本地/远端 freshness 输出，确保能肉眼确认 `v10` 真进镜像、真到远端。
4. 终止当前 run farm，按固定流程重建：
   - `image-closure`
   - `launch`
   - `infrasetup`
   - `remote-freshness <private-ip>`
   - `run <private-ip>`
