# 2026-04-17 12:56:24 UTC

## 本轮目标

- 等待 `pairdummy-sbus128-tracerv-inst-runworkload-20260417-123511`
  自然走到 host watchdog 超时，
  读取新加入的 timeout capture 中的 `TRACEFILE-C*` 证据。

## 输入状态

- run session:
  `pairdummy-sbus128-tracerv-inst-runworkload-20260417-123511`
- watchdog capture prefix:
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/pairdummy-sbus128-tracerv-inst-runworkload-20260417-123511-192.168.1.138-host-watchdog-20260417T125533Z`
- instance:
  `i-066e88561cca961fb`
- private IP:
  `192.168.1.138`

## 使用命令

- `tail -n 80 tmp/firesim-aws-f2/tmux/pairdummy-sbus128-tracerv-inst-runworkload-20260417-123511.host-watchdog.log`
- `sed -n '1,120p' ...125533Z.trace-files.txt`
- `sed -n '1,80p' ...125533Z.trace-fds.txt`
- `sed -n '1,120p' ...125533Z.TRACEFILE-C0.tail.txt`
- `tail -n 40 ...125533Z.guest-sparse-log.txt`
- `aws ec2 describe-instances --instance-ids i-066e88561cca961fb ...`

## 最终 capture 结果

### 1. 这轮 run 确实是 watchdog 在 guest freeze 后主动回收

- watchdog log 显示：
  - 最后一次 guest 侧文本增长后，
    heartbeat 固定为 `9918714556, 543`
  - idle 一直累积到 `625s`
  - 随后：
    `host idle timeout reached after 625s`
  - 然后执行：
    `calling terminaterunfarm --forceterminate`
- 到 `2026-04-17T12:56:24Z`，
  AWS 状态已变为：
  `shutting-down`

### 2. timeout capture 中 `TRACEFILE-C*` 仍然严格为 0

- `...trace-files.txt` 直接记录：
  - `/home/ubuntu/sim_slot_0/TRACEFILE-C0..C3`
    全部 `0 bytes`
  - `wc -c` 总计也是 `0`
- `...trace-fds.txt` 仍显示 FireSim-f2 已打开：
  - `7 -> TRACEFILE-C0`
  - `8 -> TRACEFILE-C1`
  - `9 -> TRACEFILE-C2`
  - `10 -> TRACEFILE-C3`
- `...TRACEFILE-C0.tail.txt` 为空文件。

这说明：

- 不仅 live 观察时没有看到 trace；
- 连 watchdog 超时点的最终 authoritative capture 里，
  trace 文件也仍然没有任何内容。

### 3. guest 在 timeout 前已经明显深入到真实 runtime 热路径

`...guest-sparse-log.txt` 的尾部不只是“刚进入 slot=1”：

- 先看到上一轮迭代完整收尾：
  - `dma-export-host ... first-chunk-submitwait-end ... rc=0`
  - `c2-export ... copy-end`
  - `c2-export ... retire`
  - `worker stage=0 subbatch=1 done`
- 接着开始下一轮：
  - `stage-exec-views phase=begin segment=0 stage=0 layer=0 tensors=4`
  - `stage-fixed-load-sparse phase=begin ... slot=0 tensor=1000000`
  - `stage-fixed-load-sparse phase=end ... slot=0 tensor=1000000`
  - `stage-fixed-load-sparse phase=begin ... slot=1 tensor=1000001`
- 最后仍停在：
  - `rr-acquire-wrap phase=after-call ... rc=0 cfg=0 valid=1`

因此这轮 capture 比前面的 live 观察更强：

- guest 不仅到达了 `slot=1 tensor=1000001`；
- 而且 stage-0 worker 已完成过至少一个 subbatch，
  再次进入相同 fixed-load 热点后冻结。

## 结论

- 在以下条件同时成立的前提下：
  - worker-local start marker 已在 `stage_worker_main` 主循环前；
  - `+trace-select=3/+trace-start=ffffffff00008013/+trace-end=ffffffff00010013/+trace-min-batch=0`
    已真实传给 FireSim-f2；
  - guest 已深入到 freeze 热路径；
  - timeout capture 中 `TRACEFILE-C*` 仍严格为 `0`
- 现在可以把当前 `selector=3` 路径判定为：
  **不足以继续作为 freeze 定位主手段。**

换句话说，
当前最稳妥的下一步不是继续在 instruction trigger 上加日志/换 marker，
而是转到刚新增的 `workerpc` fallback：

- selector `2`
- start `0x2a2b0`
- end `0x2ab22`

## 下一步

1. 确认 `workerpc` workflow 的本地 freshness 已通过。
2. 用 `pairdummy_sbus128_tracerv_workerpc_workflow.sh`
   走标准 FireSim manager 流程：
   - `launch`
   - `infrasetup`
   - `run`
3. 若 `workerpc` round 产出 trace，
   直接用 `TRACEFILE-C*` 尾部定位最终退休指令窗口。
