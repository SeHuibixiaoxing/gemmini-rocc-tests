# Debug Record 20260413T092022Z

## 目标

- 停止继续在 DMA / SPM xlate 热路径追加文本 probe。
- 落地一套低扰动 breadcrumb 调试机制。
- 把 guest env、runner、capture、outputs、文档链路一次性补齐。

## 本轮改动

- 新增 breadcrumb 头文件与实现：
  - `pipeline-runtime/include/prt_breadcrumb.h`
  - `pipeline-runtime/src/prt_breadcrumb.c`
- `prt_log_gate` 新增上下文快照 getter，供 breadcrumb 读取当前 `segment/global_stage/local_stage/subbatch`
- `prt_runtime.c`
  - `runtime_init` 调用 `prt_breadcrumb_init()`
  - `runtime_destroy` 调用 `prt_breadcrumb_destroy()`
- `prt_dma.c`
  - 新增 page-level transfer context + DMA submit/wait breadcrumb phase 写入
  - 不再依赖新增热路径文本日志来收窄 DMA 卡点
- `prt_rerocc.c`
  - `spm-xlate flush/release` 路径新增 breadcrumb phase 写入
- guest env / runner / capture / output 链路补齐：
  - `rerocc-linux-tests-coupleddma/workload/host-init.sh`
  - `rerocc-linux-tests/run_rerocc_pipeline_runtime_bertmini.sh`
  - `rerocc-linux-tests-coupleddma/workload/run_rerocc_pipeline_runtime_bertmini_fileonly_sync_capture.sh`
  - workload json outputs
  - host watchdog capture
- 新增 decode 工具：
  - `pipeline-runtime/scripts/decode_prt_breadcrumb.py`
- 新增机制文档：
  - `pipeline-runtime/docs/pipeline_runtime_breadcrumb_mechanism_20260413.md`

## 配置调整

- fixed profile 从 `pairdummy-sbus128-fixed-v4` 升到 `pairdummy-sbus128-fixed-v5`
- 为降低扰动，fixed profile 默认切到：
  - `PIPELINE_RUNTIME_PROGRESS_RAW=0`
  - `PIPELINE_RUNTIME_PROGRESS_HOT=0`
  - `PIPELINE_RUNTIME_CHECKPOINT_LOG_ENABLE=0`
  - `PIPELINE_RUNTIME_DMA_SUBMIT_TRACE_ENABLE=0`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE=0`
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PROBE_ENABLE=0`
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_CHECKPOINT_ENABLE=0`
  - `PIPELINE_RUNTIME_BREADCRUMB_ENABLE=1`

## 本轮判断

- 这轮不是继续做“更细文本日志”的局部修补，而是把 hot path 可见性从文本日志切换到了内存映射 breadcrumb。
- 之后定位 DMA / SPM xlate 卡点时，应优先看：
  - `bertmini-batch8.log` 的粗粒度前沿
  - `bertmini-batch8.breadcrumb.bin` 的最后 slot/phase
- 不再继续围绕 export/fixed-load token window 追加新的文本 probe。

## 下一步

- 先做静态检查：
  - `bash -n`
  - `python3 -m py_compile`
  - `cc -fsyntax-only`
- 再重建 image，确认 `v5` 和 breadcrumb env 进入 guest。
- 后续 live 调试以 breadcrumb 为主，不再回到旧的 hot-path 文本 probe 策略。
