# 2026-04-13T14:28:29Z 调试记录

## 本轮目标
- 利用 `v9` 新增的 `DMA_WAIT_BEFORE_SHARED_FENCE` breadcrumb 基线和 `page 10..12` 窄窗口，确认之前怀疑的
  `stage=0 / sb=3 / tensor=2 / page=11 / shared fence`
  是否还是当前真实卡点。

## 本轮关键结论
- 不是。
- 当前 live run 已经明确越过了这条旧疑点路径，而且不是只过一次，而是至少过了两轮：
  - `tok=1168 / page=11`
  - `tok=1232 / page=11`
- 这两个 token 都出现了完整序列：
  - `program-post-src`
  - `dma-export-wait phase=after-fence`
  - `dma-export-wait phase=after-shared-fence`
  - `dma-export-wait phase=after-release`
  - `dma-export-host ... page-submitwait-end`

## 直接证据
- 第一轮窗口内导出：
  - `page=10` 对应 `tok=1167` 完整结束
  - `page=11` 对应 `tok=1168` 完整结束
  - `page=12` 对应 `tok=1169` 完整结束
  - 然后：
    - `export-target-dispatch stage=0 tensor=2 target_seq=1 ... phase=end`
- 后续更晚的一轮同样再次出现：
  - `page=10` 对应 `tok=1231` 完整结束
  - `page=11` 对应 `tok=1232` 完整结束
  - `page=12` 对应 `tok=1233` 完整结束

## 这意味着什么
- 本轮新增的 `shared-fence` 前 breadcrumb 不是白加的，但当前 run 并没有在这一步停住。
- 因而，先前 `v8` capture 中“最后 breadcrumb 落在 `dma_wait_after_fence tok=1168 page=11`”的现象：
  - 要么是当轮偶发 freeze
  - 要么是更后前沿没有被及时 capture 到
  - 至少已经不能再把它视为当前稳定 blocker
- 这属于实质推进，因为这次不是“仅仅把日志打得更细”，而是实际看到旧疑点路径被完整执行并返回。

## 当前状态
- run 仍在继续，没有完成。
- 旧怀疑点已通过，当前需要继续向后寻找新的真实前沿。

## 下一步
- 继续保持：
  - 私网 IP 观测
  - guest 文件系统日志为主
  - `uartlog` 仅用于 boot 活性确认
- 继续监控 host watchdog 与 sparse log：
  - 若 guest_sparse 持续增长，就继续自然推进
  - 若再次 plateau，再抓新的 breadcrumb / sparse tail，重新定位新的 blocker
