# 2026-04-15 06:17:52 UTC Debug Record

## Round Goal
- Continue live diagnosis on the first trustworthy trigger-enabled rerun.
- Determine whether the run merely repeated the old `sb=4` frontier, or whether it progressed to a new blocker.

## Trustworthiness Check
- This round uses the rerun started in:
  - `debug_records/20260415T060220Z.md`
- Remote freshness had already proven:
  - guest `/firemarshal.env` contains the intended rr trigger overlay
  - therefore `guest_trigger > 0` in this round can be treated as a real trigger hit, not a stale-image artifact

## Live Host-Watchdog Progress
- Observed progression:
  - `guest_sparse=1009`
  - `guest_sparse=203602`
  - `guest_sparse=211466`
  - `guest_sparse=341658`
- Critical change in this round:
  - `guest_trigger` transitioned from `0` to `2428`
- Interpretation:
  - the guest-side trigger infrastructure is now definitely active on the real FPGA rerun

## Manual Live Capture
- Because the sparse log plateaued while heartbeat kept advancing, a manual private-IP capture was taken instead of waiting for the full idle timeout:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/manual-192.168.1.10-20260415T061446Z.*`
- A second confirmation capture was taken later:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/manual-192.168.1.10-20260415T061752Z.*`

## Key Findings
- The rr trigger selected in this rerun was too early:
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=rr`
  - `subbatch=4`
  - `manager=0`
  - `token=0`
- Trigger log content shows it activated at the first qualifying rr acquire in `sb=4`, then exhausted the post-budget inside the early fixed-load pages:
  - only pages `0..2` of the fixed-load path were captured
- Therefore the trigger log is useful as proof-of-life for trigger propagation, but not yet sufficient to localize the new blocker.

## Authoritative Frontier Update
- `decode_prt_breadcrumb.py` on both manual captures reports the same latest event:
  - `kind=dma`
  - `phase=dma_page_end`
  - `seg=0 gstage=0 lstage=0`
  - `sb=5`
  - `tensor=0`
  - `manager=0`
  - `page=49`
- The latest associated cleanup breadcrumb in the same ring is:
  - `dma_submitwait_after_cleanup`
  - `sb=5 tensor=0 manager=0 page=49`
- No page-50 breadcrumb was present in either manual capture.

## Meaning
- This round achieved a real frontier advance beyond all earlier claims:
  - past the previous `sb=4` boundary
  - into `sb=5`
  - up to fixed-load page `49`
- The sparse log's apparent plateau at `341658` is not the real frontier.
- The current plausible blocker window is now:
  - fixed-load host->SPM path
  - `subbatch=5`
  - `manager=0`
  - `tensor=0`
  - after page `49` cleanup / page-end
  - before any observable page `50` activity

## Static Narrowing
- Relevant function:
  - `dma_copy_host_to_spm_pages_linux()` in `src/prt_dma.c`
- Page-49 breadcrumb proves the following completed successfully for page 49:
  - host VA->PA translation
  - `dma_submit_wait_annotated_scoped()`
  - breadcrumb `dma_submitwait_after_cleanup`
  - breadcrumb `dma_page_end`
- The next code window without breadcrumb coverage is the page-advance interval after:
  - `dma_breadcrumb_page_end(...)`
  - and before the next iteration's:
  - `dma_trigger_fixed_load_host(\"pset\", ...)`

## Next Action
- Stop treating `rr` as the primary trigger family for the next rerun.
- Prepare the next rerun around:
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=dma-fixed-load`
  - `subbatch=5`
  - `manager=0`
  - `tensor_id=0`
  - `page=49`
- Goal of the next rerun:
  - determine whether the path reaches:
    - page-49 `sw-e`
    - page-49 post-submit cleanup
    - page-50 `pset`
  - or stalls strictly between page-49 completion and page-50 setup
