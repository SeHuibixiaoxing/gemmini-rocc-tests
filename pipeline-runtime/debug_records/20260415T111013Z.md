# 2026-04-15 11:10:13 UTC

## Round Goal

判断 breadcrumb-only rerun 是否只是“把 page63 周围看得更细”，还是已经真正越过旧 blocker。

## Authoritative Evidence

本轮使用：

- `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=0`
- `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH=5`
- `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH_RADIUS=0`

在完全关闭 trigger 的前提下，guest 文件与 breadcrumb 均已稳定落盘。

### 1. Sparse Log 已推进到旧 page63 之后

当前 `bertmini-batch8.log` 已增长到 `361880` bytes。
其中与旧 blocker 最相关的尾部证据包括：

- `c2-export stage=0 tensor=2 phase=copy-end idx=0 subbatch=5 rc=0 bytes=65536`
- `c2-export stage=0 tensor=2 phase=retire idx=0 rc=0 next_sbatch=6 full=0`
- `worker stage=0 subbatch=5 done`
- 随后又进入新的
  `stage-fixed-load-sparse phase=begin segment=0 stage=0 slot=1 tensor=1000001 ...`

### 2. Breadcrumb 的真实最新事件已到 page63 之后

`decode_prt_breadcrumb.py --all` 输出：

- `update_count=3627`
- `last_update_ns=6076640000`
- `last_kind=rr`
- `last_phase=rr_release_end`

按 `all_slots` 的 `mono_ns` 排序，最新窗口尾部包括：

- `page=60` `dma_submitwait_after_cleanup`
- `page=61` `dma_submitwait_after_cleanup`
- `page=62` `dma_submitwait_after_cleanup`
- `page=63` `dma_submitwait_after_cleanup`
- `page=63` `dma_page_end`
- `rr_release_end`

因此，旧的
`sb=5 / page=63 / dma_doneflag_end`
已被实质性越过。

## New Narrowed Window

当前新的最后 guest-side 未过滤 sparse 前沿稳定在：

- `stage-fixed-load-sparse phase=begin segment=0 stage=0 slot=1 tensor=1000001 pages=64 bytes=65536 lazy=0 reuse=0 dma=0`

其前面只有对应的 RR acquire 成功日志；
其后没有新的 `stage-fixed-load-sparse phase=end`。

同时：

- heartbeat 继续从 `576` 推进到 `664`
- 但以下文件在两个采样点之间保持不变：
  - `bertmini-batch8.log = 361880`
  - `bertmini-batch8.runner.stage = 481`
  - `bertmini-batch8.breadcrumb.bin = 7272`

这说明仿真仍在跑，但 guest 侧 pipeline-runtime 观测面在新窗口处停住。

## Static Interpretation

新的疑似 blocker 不再是原来的 `subbatch=5 tensor=2 c2-export page63`；
它已收敛到下一段：

- `stage 0`
- `slot 1`
- `tensor 1000001`
- `stage-fixed-load` 的 host-buffer -> SPM DMA 路径

对应该路径的代码入口：

- `src/prt_runtime.c:2095` `stage-fixed-load-sparse phase=begin`
- `src/prt_runtime.c:2109` 调用 `prt_dma_copy_dram_to_spm_pages(...)`
- `src/prt_dma.c:499` `dma_copy_host_to_spm_pages_linux(...)`

## Important Negative Finding

静态代码核查显示，`tensor 1000001` 的 source slice 来自 `rt->model_blob`：

- `get_layer_tensor_source_slice()` -> `resolve_model_addr_to_slice()`

而当前 dummy 模式下 `rt->model_blob` 来自 `allocate_synthetic_model_blob()`，
其分配后会执行：

- prefault
- `mlock`

因此当前新窗口不像是“模型 source 页未锁定”导致的旧类问题。

## Next Step

下一轮不再用 `subbatch=5` breadcrumb 过滤，
改为保持：

- `segment=0`
- `global_stage=0`
- `local_stage=0`
- trigger 仍关闭

把 `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH` 放空，
以便直接捕获 `tensor=1000001 stage-fixed-load` 这段不带 `subbatch=5` 标签的 page 级 breadcrumb，
继续收缩新 blocker。
