# 2026-04-14T03:55:57Z 调试记录

## 本轮目标

- 把“先静态分析，再打点日志”的调试顺序落成可执行入口，而不是继续靠人工记忆。
- 对当前 `bertmini` artifact 再做一轮静态审计，确认是否还有会在 live run 之前就能判掉的结构性风险。
- 静态阅读 late pointwise / Gemmini 路径，收敛下一轮 live repro 前最值得优先怀疑的边界。

## 本轮执行

- 阅读并对齐：
  - `README.md`
  - `docs/project_guide.md`
  - `docs/constraints/hard_constraints.md`
  - `docs/testing/observability.md`
  - `docs/testing/test_plan.md`
  - `docs/CURRENT_STATUS.md`
  - `NEXT_SESSION_PROMPT.md`
- 静态阅读代码：
  - `src/prt_gemmini_adapter.c`
  - `src/prt_rerocc.c`
  - `src/prt_runtime.c`
  - `src/prt_gemmini_artifacts.c`
  - `src/prt_schedule_action.c`
- 用当前 artifact 做结构检查：
  - segment 数
  - 每个 segment 的 stage 数
  - 每个 segment 的 `accUtil` 总和
  - `vAccIdxList`
  - pointwise / RR / Gemmini 边界路径

## 新静态结论

- 当前 `bertmini` artifact 每个 segment 最多 `6` 个 local stage；
  现阶段还没有踩到当前 RR cfg 容量边界。
- 当前 `bertmini` artifact 各 segment 的 `accUtil` 总和都没有超过目标硬件 `num_gemmini=12`。
- 但 runtime 仍保留一个潜在静态风险：
  - `rr_cfg_id_for_stage()` 现在按 `((stage_id * 2) + lane) % 16` 映射
  - `cfg15` 又保留给 `spm_xlate`
  - 因而一旦某个 segment 升到 `8+` local stages，
    `opcode=3` 的 stage scope 就可能撞到保留 cfg
  - 这不是当前 `bertmini` live blocker，但应在 artifact 阶段先挡掉
- 对 late pointwise 路径的静态阅读结论维持不变：
  - `oc-split-pointwise` 的 tile 0/1/2 和 tile 3 走的是同一条
    `conv_call_for_manager_sync_strided() -> prt_run_pointwise_matmul_fallback_*()`
    路径
  - 因此旧 breadcrumb 落在 `rr_acquire_after_call` 并不能单独证明“卡在 RR acquire”
  - 下一轮应优先用新的 Gemmini breadcrumb 判断 frontier 是否已经进入
    `tiled_matmul_nn_stride_auto()`

## 对后续调试的直接影响

1. 下次先跑 artifact 静态审计，再看 capture。
2. 下次先用 capture 分诊脚本汇总 sparse + breadcrumb frontier，再决定读哪段代码。
3. 只有新的 pointwise breadcrumb 仍不足以区分相邻边界时，才开新的窄 deep log。
