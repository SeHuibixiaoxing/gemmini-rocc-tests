# 2026-04-19 host-side bootdiag for hart0 MSIP wakeup

## 本轮目标

- 不再继续加 guest breadcrumb。
- 直接验证 boot ROM 唤醒链最后一步：
  `tsi_t::reset() -> write_chunk(0x2000000, 4, 1)` 是否真的发生。
- 若发生，再判断问题是在 host 控制流之前，还是在 serial-TL / CLINT 投递之后。

## 输入状态

- 上一轮 authoritative 结论：
  `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260418T184721Z.md`
- 上一轮已经静态证明：
  - `firesim_tsi_t::reset()` 等待 load completion 后进入
    `testchip_tsi_t::reset()`
  - `testchip_tsi_t::reset()` 在 `write_hart0_msip = true` 时调用
    `tsi_t::reset()`
  - `tsi_t::reset()` 的关键动作只有
    `write_chunk(0x2000000, 4, &one)`
- live state 也已证明：
  - host / target 侧 TSI 与 LoadMem 队列都已排空
  - 两个 hart 仍停在 boot ROM `wfi`
  - `clint.ipi_0 = 0`

## 使用命令

- 重新跑本地 metasim `infrasetup`
- 检查 tmux pane / exitcode / 进程树
- 检查 runtime config 中 `metasimulation_only_plusargs`
- 检查 `runtime_config.py` 中 plusarg 拼接路径
- 检查 `VFireSim.csrc` 中间产物是否包含 `tsi-bootdiag`

## 配置 / 环境

- runtime:
  `/home/ubuntu/chipyard/sims/firesim/deploy/config_runtime_local_metasim_rerocc_lc_baremetal_export_dma_bertmini_segment3_repro_tracerv_sel0_bootdiag.yaml`
- manager task:
  `firesim infrasetup -c config_runtime_local_metasim_rerocc_lc_baremetal_export_dma_bertmini_segment3_repro_tracerv_sel0_bootdiag.yaml -a config_hwdb.yaml -r config_build_recipes_f2_gemmini_rerocc_globalnoc_coupleddma_10mhz.yaml`
- tmux pane:
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/local-metasim-rerocc-tracerv-sel0-bootdiag-infrasetup-20260419.pane.log`
- 说明：
  本轮按仓库约束继续走 FireSim manager 的本地 metasim 流程，没有绕开 `infrasetup/runworkload`。

## 证据路径

- host debug 源码：
  `/home/ubuntu/chipyard/generators/testchipip/src/main/resources/testchipip/csrc/testchip_tsi.h`
  `/home/ubuntu/chipyard/generators/testchipip/src/main/resources/testchipip/csrc/testchip_tsi.cc`
- runtime config：
  `/home/ubuntu/chipyard/sims/firesim/deploy/config_runtime_local_metasim_rerocc_lc_baremetal_export_dma_bertmini_segment3_repro_tracerv_sel0_bootdiag.yaml`
- plusarg 拼接代码：
  `/home/ubuntu/chipyard/sims/firesim/deploy/runtools/runtime_config.py`
- 中间编译产物目录：
  `/home/ubuntu/chipyard/sims/firesim/sim/generated-src/f2/f2-firesim-FireSim-WithDefaultFireSimBridges_WithFireSimConfigTweaks_chipyard.GemminiLearningConfigSpadReRoCCGlobalNoC2C1x2G2x1x2D2x1x2CoupledDMA-FRFCFS16GBQuadRank_BaseF2Config/VFireSim.csrc`

## 新观察 / 新卡点

- 已确认 `+tsi_bootdiag=1` 不是“只写在 yaml 里”：
  `runtime_config.py` 会把 `metasimulation_only_plusargs` 直接拼进 metasim driver 启动命令。
- 本轮 `infrasetup` 最慢阶段是 full `verilator --cc` 前端展开；
  当时 `verilator_bin` 持续单核满载，内存并未吃满或进入 swap。
- `2026-04-19 03:19:38 UTC` 左右，`VFireSim.csrc` 目录开始生成，
  说明 Verilator 前端结束，进入 C++ 编译阶段。
- 在最终链接完成之前，已经能从中间产物直接看到 bootdiag 改动已编入：
  `grep -a -m1 'tsi-bootdiag' .../VFireSim.csrc/testchip_tsi.o`
  命中了：
  - `+tsi_bootdiag`
  - `+tsi_bootdiag=1`
  - `[tsi-bootdiag] write_chunk addr=0x%llx nbytes=%zu data=0x%x`

## 分析

- 这证明当前修改至少已经进入中间 object，不存在“代码改了但没编进任何产物”的问题。
- 因此本轮剩余不确定性只剩两项：
  1. `infrasetup` 是否顺利完成并把新 driver 准备好给 `runworkload`
  2. 新 driver 运行后，三条 bootdiag 线索里究竟能看到哪几条
- 一旦看到：
  - only `entering`：
    问题在 `tsi_t::reset()` 内部或更早
  - `write_chunk` 也出现，但 hart 仍在 boot ROM sleep：
    问题在 serial-TL / CLINT 投递路径

## 下一步

1. 等待本轮 `infrasetup` 完成。
2. 用最终 `VFireSim` 二进制再次验证 `tsi-bootdiag` 字符串存在。
3. 通过 `scripts/firesim-tmux-run.sh` 启动新的 bootdiag `runworkload`。
4. 第一优先级检查：
   - `metasim_stderr.out`
   - `uartlog`
   - runworkload tmux pane
5. 若仍挂住，则转入已准备好的后备路径：
   直接从 `tsibridge_t::fesvr -> tsi_t::{host,target}` 的 `ucontext`
   做 batch gdb 栈定位。
