# Debug Record 20260420T105238Z

## 目标

- 跑完新增 pointwise precall probes 之后的
  `g6 bertmini`
  FPGA 主线复跑
- 若确认没有 watchdog 误杀，
  就把真实 stall frontier
  重新固定下来

## 本轮执行

- 使用 workflow：
  `scripts/pairdummy_sbus128_g6_workflow.sh`
- 已完成：
  - `debug-preflight`
  - `image-closure`
  - `launch`
  - `infrasetup`
  - `remote-freshness`
  - `runworkload`
- runworkload session：
  `pairdummy-sbus128-runworkload-20260420-103633`
- 远端实例：
  `i-07ae30fdb242904a8`
  / `192.168.1.84`

## 关键 live 观测

- Linux boot 能推进到
  `/etc/init.d/S99run`
  之后
- guest image 中的
  `bertmini-batch8.status`
  显示：
  - `state=running`
  - `exit_code=` 空
  - `profile_id=pairdummy-sbus128-g6-fixed-v1`
- wrapper 进程：
  - `pid=132`
  - `cmdline=/bin/sh /root/rerocc-linux-tests/run_rerocc_pipeline_runtime_bertmini.sh --batch 8`
  - `wchan=do_wait`
- runtime binary：
  - `pid=198`
  - `cmdline=/root/rerocc-linux-tests/pipeline-runtime/rerocc_pipeline_runtime-linux ... --num-gemmini-mgrs 6 --num-dma-mgrs 6 ...`
  - `State: R (running)`

## 稀疏日志前沿

- live sparse log 明确推进到：
  - `stage-fixed-load-sparse phase=end segment=3 stage=0 slot=0 tensor=1000006 rc=0`
  - `stage-exec-views phase=end segment=3 stage=0 layer=3 rc=0 flush=1`
  - `worker stage=0 subbatch=1 begin`
  - `worker stage=1 subbatch=0 done`
  - `worker stage=2 subbatch=0 begin op=1 acc=4 dma=4 tiles=2`
  - `conv-sync-strided stage=2 mgr=4 flushed use_pointwise=1`
  - `conv-sync-strided stage=2 mgr=5 flushed use_pointwise=1`
  - `worker stage=2 subbatch=0 compute-done`
- 此后 sparse tail 固定不再增长

## Watchdog / heartbeat 结论

- `host-watchdog` 没有误杀
- guest-visible progress：
  `guest_sparse=1011 -> 178681 -> 240481 -> 309312 -> 636757`
- heartbeat 同时推进：
  `427 -> 454 -> 515 -> 580 -> 613 -> 774`
- plateau 后仍观察到：
  - `idle=95s hb_idle=0s`
  - `idle=126s hb_idle=0s`
  - `idle=157s hb_idle=0s`
- 因而这轮可以确认：
  guest 内部真实 stall
  已经出现，
  不是 host 侧 monitor / watchdog 误判

## Breadcrumb 结论

- live breadcrumb 已从 guest image 拉回本地并解码
- 本地 snapshot：
  `/home/ubuntu/chipyard/tmp/pipeline-runtime-live/20260420T105143Z-g6-stage2-compute-done-stall`
- decode 摘要：
  - `bertmini-batch8.breadcrumb.bin = 7272`
  - `update_count=37154`
  - `last_kind=spm_xlate`
  - `last_phase=spm_xlate_restore_end`
  - `mgr=5`
- 但当前 breadcrumb filter
  仍是旧 focus：
  - `segment=0`
  - `global_stage=0`
  - `local_stage=0`
- 所以这轮 breadcrumb
  只能证明最后被 ring 留下的低层事件
  落在
  `mgr5 spm_xlate_restore_end`，
  与 sparse 中
  `mgr4/5`
  的 xlate cleanup 相一致；
  它不能单独覆盖当前
  `stage=2`
  post-compute 窗口

## 本轮 authoritative 结论

- 旧的
  `sb=3 mgr=2 rr_acquire_after_call`
  frontier
  已经不是当前主线 authoritative frontier
- 当前更可信的表述是：
  **真实 stall 固定在
  `worker stage=2 subbatch=0 compute-done`
  之后、
  下一条 stage2 后继事件之前的 post-compute 窗口**
- 当前 g6 仍不是
  `cfg15`
  静态别名风险的直接受害者；
  这轮 stall 的首要解释不应再回退到该风险

## 收尾

- 已按 SOP 固化 live snapshot
- 已执行：
  `scripts/pairdummy_sbus128_g6_workflow.sh terminate`
- `terminaterunfarm` exit code：
  `0`
- 实例状态进入：
  `shutting-down`
- `runworkload` tmux exit code：
  `1`
  - 原因是人工 terminate 后的 manager 收尾，
    不是 guest 自然退出码

## 下一步建议

1. 静态审计
   `worker stage=2 subbatch=0 compute-done`
   之后的软件路径，
   重点看 stage2 的 post-compute /
   export /
   release /
   RR handoff。
2. 检查 stage2 路径是否仍复用了只在 stage0 正确的假设，
   尤其是 segment-local state、
   pair-manager cleanup、
   RR release / reacquire、
   xlate restore 后的下一跳。
3. 若需要下一轮 rerun，
   先把 breadcrumb / trigger
   重定向到
   `stage=2`
   邻域；
   不要继续沿用旧的
   `seg0/stage0`
   filter。
