## 2026-04-15T13:50:08Z

### Round summary
- Objective: continue the `v21` observability run on `12-pair sbus128` and locate the new stable blocker after proving `page5` is not the blocker.
- Run under inspection:
  - runworkload session: `pairdummy-sbus128-runworkload-20260415-133151`
  - instance id: `i-0c2033142ec019495`
  - private ip: `192.168.1.139`
  - results dir: `/home/ubuntu/chipyard/sims/firesim/deploy/results-workload/2026-04-15--13-31-52-rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy-f2-gemmini-rerocc-pairmanager-dummy16x16-4c12p12-sbus128-linux-bertmini-batch8-fileonly-sync/`

### Constraints respected
- Remote inspection used the private IP only.
- Evidence order remained: guest filesystem files -> live image via `debugfs` -> manager pane.
- No runtime code change in this round before recording the observation.

### Evidence collected
- `heartbeat.csv` kept increasing during the whole sampling window:
  - earlier sample ended at `15705550697, 834`
  - recheck reached `18758735583, 987`
- Guest text artifacts remained frozen:
  - `bertmini-batch8.log` size stayed `382061`
  - `bertmini-batch8.breadcrumb.bin` size stayed `7272`
  - `bertmini-batch8.runner-proc.stage` size stayed `9273`
- Runner process snapshot stayed effectively unchanged:
  - process state still `R (running)`
  - `voluntary_ctxt_switches=32`
  - `nonvoluntary_ctxt_switches=15`
- Manager pane watchdog also reported a long idle window while heartbeat continued:
  - `idle=376s`
  - `guest_sparse=382061`

### Breadcrumb finding
- Re-fetched breadcrumb twice; both decodes were identical:
  - `update_count=18327`
  - `last_kind=dma`
  - `last_phase=dma_page_end`
  - `last_slot page=51`
  - `last_update_ns=6091562000`
- `decode_prt_breadcrumb.py --all` also showed a later populated slot:
  - `slot=50 kind=dma phase=dma_page_begin ... sb=4 tensor=2 page=52 ... mono_ns=6091630000`
- Practical interpretation for debugging:
  - the stable frontier is already past the previously suspected `page5`
  - the exporter reached `stage=0 / subbatch=4 / tensor=2 / target_seq=1`
  - it completed `page51`
  - the next missing boundary is inside the `page52` submit/wait/cleanup path

### Static source read performed
- `src/prt_dma.c`:
  - line `123`: `dma_breadcrumb_page_begin`
  - line `142`: `dma_breadcrumb_page_end`
  - line `1645`: `dma_submitwait_after_cleanup`
  - export host loop around lines `687-949`
  - export sparse page probe selection around lines `1070-1081`
  - export submit/wait sparse probe gating around lines `997-1010`
  - blocking fence wait path around lines `2924-3155`
- Main static conclusion:
  - `page52 begin` is emitted immediately before `dma_submit_wait_annotated_scoped(...)`
  - if runtime stops there, the missing boundary is one of:
    - submit inner path
    - `hw_dma_fence()`
    - shared fence / release
    - cleanup

### Important conclusion
- This is now a stable blocker, not just log drift:
  - host heartbeat advances
  - guest runner process remains alive
  - guest text log and breadcrumb header stay frozen
- `page5` should no longer be treated as the current root-cause candidate.

### Next step
- Narrow observability to a single variable rerun focused on export `page52`:
  - regenerate baseline env correctly through the workload host-init path
  - keep `breadcrumb`
  - retarget `dma export probe` to `page52`
  - avoid widening trigger + export probe simultaneously
