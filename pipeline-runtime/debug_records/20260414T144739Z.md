# Debug Record 20260414T144739Z

## 目标

- 把用户批准的 blocker-debug SOP 真正落地成可执行工具链
- 实现“程序内可控、到达 frontier 附近才开启、每行尽量短”的 trigger-gated 日志
- 静态审计当前接入点，确认没有改动硬件语义
- 让下一轮 FPGA rerun 可以按单变量方式执行

## 输入状态

- 上一轮 authoritative 动态结论仍停留在 `v18` / late pointwise 窗口
- 用户硬约束保持不变：
  - 先静态读代码，再打点
  - Linux boot 早期静默但 `heartbeat.csv` 前进时不算异常
  - 不改硬件；若后续必须改硬件，要先停下来说明原因
- 工作区里已存在一半 trigger-log 改动，但 triage / docs / 验证还没收口

## 命令

- 代码与文档阅读：
  - `sed -n ... README.md`
  - `sed -n ... docs/project_guide.md`
  - `sed -n ... docs/testing/observability.md`
  - `sed -n ... docs/CURRENT_STATUS.md`
  - `sed -n ... NEXT_SESSION_PROMPT.md`
  - `sed -n ... src/prt_rerocc.c`
  - `sed -n ... src/prt_gemmini_adapter.c`
  - `sed -n ... src/prt_dma.c`
  - `sed -n ... src/prt_runtime.c`
- 脚本 / 语法校验：
  - `python3 -m py_compile .../triage_prt_capture.py`
  - `bash -n .../pairdummy_sbus128_fixed_env.sh .../pairdummy_sbus128_workflow.sh ...`
  - `cc -std=gnu11 -fsyntax-only .../prt_trigger_log.c`
  - `cc -std=gnu11 -fsyntax-only .../prt_runtime.c .../prt_rerocc.c .../prt_gemmini_adapter.c .../prt_dma.c`
- workflow 预检：
  - `pairdummy_sbus128_workflow.sh debug-preflight`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=1 PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=rr ... pairdummy_sbus128_workflow.sh debug-preflight`
- triage 验证：
  - `triage_prt_capture.py /tmp/prt-triage-smoke.log --emit-trigger-env`
  - `triage_prt_capture.py --breadcrumb tmp/firesim-aws-f2/captures/20260414-live/192.168.1.18-bertmini-batch8-v17.breadcrumb.bin --sparse-log tmp/firesim-aws-f2/captures/20260414-live/192.168.1.18-bertmini-batch8-v17.log --previous-frontier 'conv-sync-strided stage=0 mgr=7 flushed use_pointwise=1' --change-kind observability --emit-trigger-env`

## 配置 / 环境

- fixed profile：
  `pairdummy-sbus128-fixed-v19`
- 默认观测面：
  - `PIPELINE_RUNTIME_BREADCRUMB_ENABLE=1`
  - `PIPELINE_RUNTIME_GUEST_DEEP_LOG_ENABLE=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=0`
- 验证过的 trigger overlay 示例：
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=1`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=gemmini-pointwise`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_GLOBAL_STAGE=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_LOCAL_STAGE=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_SUBBATCH=4`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_MANAGER=7`

## 证据路径

- 真实 capture：
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/20260414-live/192.168.1.18-bertmini-batch8-v17.breadcrumb.bin`
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/20260414-live/192.168.1.18-bertmini-batch8-v17.log`
- 相关源码：
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_trigger_log.c`
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c`
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_rerocc.c`
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_gemmini_adapter.c`
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_runtime.c`

## 新观察 / 新卡点

- `triage_prt_capture.py` 原先会被较早的 `dma-export` sparse 行误导，
  导致真实 late pointwise capture 也吐出 `dma-export` trigger env；
  现已修正为：
  - fixed-load frontier 仍优先
  - late `gemmini` / `rr` frontier 优先于陈旧 export 行
  - 若 breadcrumb 停在较早 manager，
    但 sparse 已推进到更晚的 pointwise manager，
    trigger env 会改用更晚的 sparse manager
- `pairdummy_sbus128_fixed_env.sh` 原先会无条件把 trigger env 覆盖回默认值，
  导致 `--emit-trigger-env` 生成的下一轮配置实际上不可用；
  现已修正为：
  仅 `PIPELINE_RUNTIME_DEBUG_TRIGGER_*` 可以做受控 overlay，
  其余 fixed-profile 参数不允许漂移
- workflow 预检已验证：
  - 默认 `v19` 配置 `debug-preflight` 通过
  - 叠加单一 trigger family 后 `debug-preflight` 仍通过
- 真实 `v17` capture 重新 triage 后的结果为：
  - `claim_class=observability_only`
  - `disturbance_risk=high`
  - `requires_control_rerun=yes`
  - `recommended_next_probe=control-rerun-first`
  - 推荐 trigger family：
    `gemmini-pointwise`
  - 推荐 window：
    `stage=0 subbatch=4 manager=7`

## 分析

- 这轮改动没有引入新的硬件语义动作。
  静态阅读 `prt_rerocc.c` / `prt_dma.c` / `prt_gemmini_adapter.c` / `prt_runtime.c` 后确认：
  - trigger note 都是附着在既有边界前后
  - 默认关闭
  - 不改 CSR 编程顺序
  - 不改 DMA / RR / Gemmini fence / release 语义
- 当前仍没有证据要求改硬件。
  新工具链的目标是先把“frontier 因日志漂移”与“真正的 blocker 穿过”分开。
- `v17` 真实 capture 现在被明确归类为
  `observability_only` 候选，
  所以下一轮必须先做 control rerun，
  不能把任何 frontier 移动直接记成修复。

## claim_class / disturbance_risk / requires_control_rerun

- `claim_class=observability_only`
- `disturbance_risk=high`
- `requires_control_rerun=yes`

## 下一步

1. 用 `v17` 重新 triage 生成的 `PIPELINE_RUNTIME_DEBUG_TRIGGER_*` block，
   在单独 subshell 中叠加 trigger overlay
2. 先跑：
   `pairdummy_sbus128_workflow.sh debug-preflight`
3. 通过后做单变量 rerun
4. rerun 结束后对照 control rerun，
   再决定 frontier 是否真的后移
5. 继续禁止硬件修改，除非新的静态和动态证据都逼到硬件边界
