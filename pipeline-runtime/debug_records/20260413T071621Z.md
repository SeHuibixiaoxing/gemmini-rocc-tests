# 2026-04-13T07:16:21Z 调试记录

## 本轮目标
- 回答“现在卡点在哪里”。
- 基于最新 guest 文件日志、checkpoint 日志、host-watchdog 三个观测面，重新锁定当前真实前沿。

## 当前运行上下文
- FireSim run 仍在运行：
  - 实例：`i-0dcd3c1a6b9a7bc2a`
  - 私网 IP：`192.168.1.169`
- guest 已进入 runtime 主体：
  - `model.layers.yaml` 已加载
  - `pipeline_mapping...ours2.yaml` 已加载和校验
  - `gemmini_layer_mapping...yaml` 12MB 文件已完整分块读取
  - 已进入 `segment=0 stage=0` 执行

## 当前卡点
- 不是 `doneflag` 轮询等待。
- 当前前沿停在 `segment=0 stage=0` 的固定页加载结束之后，接着进行 `spm-xlate-flush` 的位置。
- 稀疏日志最后一条稳定停在：
  - `spm-xlate-flush mgr=4 cfg=15 prev_opc3=0x1`
- 紧前一批已完成的动作：
  - `stage-fixed-load-sparse phase=end segment=0 stage=0 slot=1 tensor=1000001 rc=0 pages=64 bytes=65536 lazy=0 reuse=0 dma=0`
  - `spm-xlate-flush mgr=0/1/2/3 ...`
  - 对 `mgr=0/1/2/3` 都已经出现 `flush` 和 `flush-restore`
  - 但到 `mgr=4` 只看到 `flush`，还没看到对应 `flush-restore`

## checkpoint 侧的最后前沿
- checkpoint 日志最后稳定停在：
  - `worker stage=0 checkpoint=before-build-stage-task subbatch=4 op_hint=0 tile_count=8 mgr0=0`
  - 随后多次 `rr-acquire-inner ... before-return stage=0 manager=0 opcode=2 cfg=0`
- 说明执行线程已经在 `stage=0`、`subbatch=4`、build-stage-task / rr-acquire 邻域内。

## 为什么判断这是“当前卡点”
- `host-watchdog` 显示：
  - `heartbeat.csv` 仍持续前进，说明仿真整体没死。
  - 但 `guest_sparse` 和 `guest_checkpoint` 在约 `hb=10559264049` 后不再增长，后续 idle 已累计到数百秒。
- 因此当前不是 guest 早期 boot，也不是 mapping 解析，而是 runtime 执行前沿停在上述位置。

## 当前结论
- 当前最可疑前沿：
  - `spm-xlate-flush mgr=4` 附近
  - 或与其同时间域内的 `stage=0 subbatch=4 build-stage-task / rr-acquire-inner`
- 下一步应继续围绕：
  - `spm-xlate-flush` 路径
  - `rr-acquire-inner` / `build-stage-task`
  - `stage=0` 后续 export 前沿
  进行更细粒度定位。
