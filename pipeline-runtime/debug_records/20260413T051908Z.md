# 2026-04-13T05:19:08Z 调试记录

## 本轮目标
- 接续上一轮冻结现场，在 watchdog 回收后固化 capture 结论。
- 做极窄补点，不改语义，只把 `tensor=2` 第二个 alias target 的冻结页进一步缩小。
- 尝试本地快速编译确认补点无语法错误。

## 输入现场
- watchdog 已在 `idle=626s` 后触发：
  - `host idle timeout reached after 626s`
- capture 目录前缀：
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/pairdummy-sbus128-runworkload-20260413-045532-192.168.1.182-host-watchdog-20260413T051554Z.*`
- run farm 已由 watchdog 调用 `terminaterunfarm --forceterminate` 回收。

## 关键 capture 结论
- 本轮 freeze 不是 watchdog 漏判，因为：
  - `guest_sparse`
  - `guest_checkpoint`
  两者都在最后长时间不增长。
- watchdog 最终回收前仍能看到 heartbeat 增长到：
  - `23089829685, 1204`
- manager log 在实例被回收后出现：
  - `Error reading SSH protocol banner`
  这是 run farm 被终止后的尾部现象，不是新的 guest 卡点。

## 本轮代码改动

### 1. `prt_dma.c`
- 文件：
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c`
- 改动：
  - `dma_should_sparse_export_page_probe()` 对 `stage=0 && tensor=2` 新增：
    - `page_idx >= 48` 时逐页打点
- 目的：
  - 之前只在 `0 / 每8页 / 最后一页` 输出，无法区分卡在 `49..55` 哪一页。
  - 现在下一轮能精确知道 freeze 页号。

### 2. `prt_runtime.c`
- 文件：
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_runtime.c`
- 改动：
  - `copy_tensor_pages_to_model_alias_target()` 新增 `target_seq`
  - 对 `stage=0 && tensor=2` 输出：
    - `export-target-dispatch ... phase=begin`
    - `export-target-dispatch ... phase=end`
  - 在 `copy_tensor_pages_to_model_aliases()` 调用处把 `seen_count - 1U` 作为 alias target 序号传入
- 目的：
  - 明确区分当前导出的是第几个 alias target
  - 避免下一轮仍只能靠目的地址猜测“当前是 address 还是 address2”

## 本轮尝试的本地快速编译
- 严格先执行：
  - `cd /home/ubuntu/chipyard/sims/firesim`
  - `set +u; source sourceme-manager.sh --skip-ssh-setup; set -u`
- 然后尝试：
  - `make -C generators/gemmini/software/gemmini-rocc-tests/build rerocc_pipeline_runtime-linux -j4`
- 结果：
  - 顶层 build 目录没有这个直接目标
- 随后检查 `build/Makefile` 与 `rerocc-linux-tests/Makefile`，确认真实构建入口在子目录里。
- 再检查环境后发现：
  - 当前 shell 没有 `CC_LINUX`
  - PATH 中也没有：
    - `riscv64-unknown-linux-gnu-gcc`
    - `riscv64-linux-gnu-gcc`
- 结论：
  - 这次“本地快速编译失败”是环境缺少 RISC-V Linux 交叉编译器，不是本轮补点代码的已确认语法错误。
  - 后续继续沿用既有 FireMarshal 固定流程更稳妥。

## 本轮执行过的关键命令
- 查看 watchdog 超时后的尾部：
  - `tail -n 120 /home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-045532.host-watchdog.log`
- 查看 manager log 尾部：
  - `tail -n 80 /home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-13--04-55-33-runworkload-0C04PCNBLXXR3JFS.log`
- 查看 workflow / build 规则：
  - `nl -ba .../pairdummy_sbus128_workflow.sh`
  - `nl -ba .../build/Makefile`
  - `nl -ba .../rerocc-linux-tests/Makefile`
- 检查本地编译器环境：
  - `which riscv64-unknown-linux-gnu-gcc`
  - `which riscv64-linux-gnu-gcc`
  - `source /home/ubuntu/chipyard/env.sh`
  - `echo CC_LINUX=${CC_LINUX-}`

## 当前状态
- 现场 capture 已固化。
- 针对性补点已完成。
- 下一步直接进入固定 workflow：
  - `image-closure`
  - `launch`
  - `infrasetup`
  - `run`
- 期间仍需严格执行：
  - 只用私网 IP 做 remote freshness / live 查询
  - 以 guest 文件日志作为主观测面
  - 重跑前后做 freshness 闭环

## 下一步
- 使用固定脚本：
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_workflow.sh`
- 先走完整 `image-closure`
- 再重新 `launch -> infrasetup -> run`
- 新 run 中重点观察：
  - `export-target-dispatch ... target_seq=0/1`
  - `dma-export-host ... page=48..63`
- 目标是把冻结点从“第二个 alias target 的后半段”进一步压缩到单个页，或直接跑过该窗口。
