# 2026-04-14T09:48:19Z 调试记录

## 本轮目标

- 继续观察 `v13` active rerun，
  判断它是否真的进入 runtime，
  以及是否已经把旧的
  `c2-export page0`
  可疑区间穿过去。
- 一旦前沿继续推进，
  立即把 authoritative blocker 更新到更晚位置。

## 本轮执行

- 继续检查 active run：
  - host：
    `192.168.1.247`
  - manager session：
    `pairdummy-sbus128-runworkload-20260414-093500`
- 先按硬约束等待 Linux boot，
  直到：
  - `uartlog`
    到
    `S99run`
  - image 内出现
    `/root/pipeline-runtime-debug`
- 通过 `debugfs`
  读取：
  - `bertmini-batch8.status`
  - `bertmini-batch8.runner.stage`
  - `bertmini-batch8.runner-proc.stage`
  - `bertmini-batch8.log`
- 静态回读：
  - `src/prt_dma.c`
  - `src/prt_rerocc.c`
  - `src/prt_page_table.c`
  - `src/prt_gemmini_artifacts.c`
  - `src/prt_runtime.c`
- 验证当前 init 并未卡在 artifact parse：
  `artifacts mapping parse progress`
  已完整推进到
  `16848`
  entries。

## 新结论

- 当前 rerun 已经把之前锁定的三类候选全部穿过：
  1. `dma_batch_scope_acquire()` /
     `prt_rr_acquire_scope()` 返回边界
  2. `prt_host_virt_to_phys((const void *)dst_ptr, &dst_pa)`
  3. page0 首个
     `dma_submit_wait_annotated_scoped()` /
     `prt_dma_submit()` /
     `prt_dma_wait()`
- 这不是单次偶然通过；
  当前 sparse log 已在
  `subbatch=1..4`
  上重复看到：
  - `rr-acquire-wrap phase=after-call ... rc=0`
  - `dma-export-host phase=v2p-begin/end`
  - `dma-export-host phase=first-chunk-submitwait-begin/end`
  - `c2-export phase=copy-end`
  - `worker stage=0 subbatch=N done`
- 当前新的最后持久化前沿是：
  - `worker stage=0 subbatch=4 done`
  - 随后的
    `rr-acquire-wrap phase=after-call stage=0 manager=0 opcode=2 rc=0 cfg=0 valid=1`
- 因此后续排查必须以后移后的路径为起点，
  不要再回到旧的
  `rr-acquire / v2p / first submitwait`
  区间加日志。

## 现场状态

- `bertmini-batch8.status`：
  `state=running`
- 截至记录时：
  - `heartbeat.csv`
    已推进到
    `721s`
  - guest sparse log
    已有
    `4129`
    行
- 运行仍在前进，
  当前没有 crash / panic / poweroff 证据。

## 下一步

1. 继续观察当前 run，
   等待更晚的稳定前沿显现。
2. 如果再次停住，
   先静态阅读
   `prt_runtime.c`
   中
   `worker ... done`
   之后的路径，
   再决定新的窄日志边界。
3. 不改硬件，
   不走 metasim，
   继续保持 FPGA 软件调试主线。
