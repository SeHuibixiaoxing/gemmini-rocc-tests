# Debug Record 20260414T125526Z

## 目标

- 继续遵守用户要求：
  先静态读代码，
  再用低扰动观测面定位前沿。
- 本轮目标是：
  1. 重新审计
     `stage0/subbatch4`
     pointwise fallback
     路径
  2. 判断
     `v16`
     breadcrumb 是否可信
  3. 如有必要，
     用软件-only 的方式修正 observability，
     然后重新部署到 FPGA

## 输入状态

- 已知旧结论：
  - export page0 /
    RR acquire /
    first submitwait
    都已经被更晚 live run 清掉
  - `v15`
    把 export 前沿缩到
    `page63 dma_page_end`
    之后的 release 窗口
  - `v16`
    已把 sparse log 前沿推进到
    `stage0/subbatch4`
    pointwise fallback
- 关键输入 capture：
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/20260414-live/192.168.1.68-bertmini-batch8-v16.breadcrumb.bin`
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/20260414-live/192.168.1.68-bertmini-batch8-v16.log`

## 本轮静态结论

- `conv_call_for_manager_sync_strided()`
  到
  `prt_run_pointwise_matmul_fallback_*()`
  的 breadcrumb 相位本来就覆盖了：
  - `GEMMINI_POINTWISE_CALL_BEGIN/RETURN`
  - `GEMMINI_POINTWISE_MATMUL_BEGIN/RETURN`
  - `GEMMINI_POINTWISE_POSTCALL_*`
- 对
  `oc-split-pointwise`
  直连 fastpath
  的静态审计：
  - `split_1d_range()`
    与 host 参考实现一致
  - 子 conv 只偏移
    `weights/bias/output`
    的
    `oc_beg`
  - `in_stride=256`
    `weight_stride=256`
    `out_stride=256`
    保持 full stride
  - 这与当前
    `HybridMapper`
    按
    `OC`
    切分的语义对齐
- 若
  `v16`
  sparse frontier 为真，
  从
  `subbatch4 mgr3 fence-end`
  到下一条可见日志之间，
  语义动作只剩：
  - `prt_rr_release_scope(&scope)`
  - 返回到
    `oc-split-pointwise ... end`
  - 再到
    `mgr4`
    入口

## 关键观察

- 同一份
  `v16`
  capture
  内，
  sparse log 与 breadcrumb 前沿不一致：
  - breadcrumb：
    `subbatch=4 mgr=6 gemmini_pointwise_call_begin`
  - sparse log：
    只持久化到
    `subbatch=4 mgr=3 fence-end`
- `triage_prt_capture.py`
  因为依赖旧 breadcrumb，
  仍给出
  `between pointwise caller entry and inner matmul entry`
  的判断；
  这与静态代码和 sparse log 已不匹配
- 阅读
  [`src/prt_breadcrumb.c`](../src/prt_breadcrumb.c)
  后确认：
  旧实现按
  `stage`
  选单槽，
  所有
  `stage 0`
  的 Gemmini / RR / DMA 事件都在争抢一个 slot；
  `last_phase`
  因此会被误导

## 本轮修改

- 做了纯软件 observability 修正：
  - breadcrumb slot 从
    `stage-only`
    改成 hash：
    `stage/subbatch/kind/manager/tensor/page/token`
  - decode 脚本改为
    `--all`
    按
    `seq`
    排序
  - fixed profile 升级到
    `pairdummy-sbus128-fixed-v17`
    以区分旧镜像

## 构建 / freshness / 部署

- `image-closure`：
  通过
- local freshness：
  通过
- remote freshness：
  `192.168.1.18`
  通过
- 关键哈希：
  - runtime binary：
    `b28dac70ebe90adceeea7daf5bbe8eb582d120dd44908d0e27912df7fcf6eb2d`
  - firemarshal env：
    `f683cd974dab52d12be295ab2974d4d1965741d6e6a8c6b8b4b56861dc5cab61`
  - remote image：
    `884d014492db104108e80b3b5294a7ad4500fc7c3a60a426bc1ee13adfa4b2ad`
- 新 host：
  `192.168.1.18`
- 新 runworkload session：
  `pairdummy-sbus128-runworkload-20260414-125651`

## 当前 live 状态

- 截至
  `2026-04-14 13:05 UTC`
  - `heartbeat.csv`
    已推进到
    `394s`
  - `/root/pipeline-runtime-debug`
    已出现
  - `bertmini-batch8.status`
    为：
    `state=running`
    `profile_id=pairdummy-sbus128-fixed-v17`
  - 第一份
    `v17`
    sparse log 还停在
    runtime init / artifact load
    阶段，
    尚未进入
    `stage0`
    执行
  - 第一份
    `v17`
    breadcrumb
    已出现多个分散 slot：
    - `runtime_init_done`
    - 多个
      `spm_xlate_restore_end`
    - 多个
      `rr_release_end`
  - 这说明新的 breadcrumb slot 分散已经生效，
    不再是旧的
    `stage-only`
    单槽坍缩

## 证据路径

- `v16` breadcrumb：
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/20260414-live/192.168.1.68-bertmini-batch8-v16.breadcrumb.bin`
- `v16` sparse log：
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/20260414-live/192.168.1.68-bertmini-batch8-v16.log`
- launch pane：
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-launchrunfarm-20260414-125102.pane.log`
- infrasetup pane：
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-infrasetup-20260414-125227.pane.log`
- run pane：
  `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260414-125651.pane.log`
- firemarshal build log：
  `/home/ubuntu/chipyard/software/firemarshal/logs/rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy-build-2026-04-14--12-48-24-ZKH03NPRHYLGNBMI.log`
- firemarshal install log：
  `/home/ubuntu/chipyard/software/firemarshal/logs/rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy-install-2026-04-14--12-49-34-ERSCWKLUDO5R2ZWL.log`

## 下一步

1. 继续等待
   `192.168.1.18`
   上当前
   `v17`
   run 进入 runtime。
2. 一旦
   `/root/pipeline-runtime-debug`
   出现，
   抓第一份
   `status/log/breadcrumb`
   快照。
3. 等 sparse log 真正进入
   `stage0/subbatch4`
   后，
   再用新的 sharded breadcrumb
   判断真实前沿是否仍在
   `mgr3 release`
   附近。
4. 只有在这一步仍不够清楚时，
   才考虑下一层窄 probe；
   继续不改硬件。
