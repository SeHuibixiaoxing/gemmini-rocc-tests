# 2026-04-15 05:20:52 UTC Debug Round

## Scope
- Continue pairdummy sbus128 FPGA debugging.
- Reconfirm active runtime/HWDB/AGFI selection before further action.
- Monitor the live low-disturbance control rerun and determine whether it has reached a new stable stall/capture point.

## Artifact Audit
- Runtime config:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/config_runtime_f2_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_linux_bertmini_pipeline_runtime_batch8_fileonly_sync.yaml`
- Active HW config key:
  - `firesim_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128`
- HWDB:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/config_hwdb_f2_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_bertmini.yaml`
- AGFI:
  - `agfi-0dc8dcfa4c7735f40`

## Live Run Under Observation
- Runworkload tmux session:
  - `pairdummy-sbus128-runworkload-20260415-050642`
- EC2 instance:
  - `i-026f018c64c851dd7`
- Private IP:
  - `192.168.1.154`
- Host watchdog:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260415-050642.host-watchdog.log`

## Current Observation Snapshot
- This run is still the low-disturbance control rerun where trigger env did not propagate before the tmux-wrapper fix.
- As rechecked at round start, the watchdog still shows the run stalled at:
  - `guest_sparse=340536`
  - `guest_trigger=0`
- At the latest sampled line during round start, idle had already reached `251s`, so this run remains useful only as a control/final capture candidate, not as a valid trigger-subphase diagnosis run.

## Immediate Next Actions
1. Recheck whether the watchdog has crossed idle timeout or already captured remote state.
2. If this control run is exhausted, terminate it using the documented FireSim stop flow and verify EC2 state.
3. Start the next rerun with the now-fixed tmux env propagation so `PIPELINE_RUNTIME_DEBUG_TRIGGER_*` actually reaches the detached manager process.

## Manual Capture Added During This Round
- Manual private-IP capture performed instead of waiting for the 600 s host idle timeout:
  - prefix: `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/manual-192.168.1.154-20260415T052234Z`
- Captured artifacts included:
  - `heartbeat.csv`
  - `uartlog.txt`
  - guest filesystem copies of:
    - `bertmini-batch8.status`
    - `bertmini-batch8.log`
    - `bertmini-batch8.trigger.log`
    - `bertmini-batch8.breadcrumb.bin`
    - wrapper/runner stage files

## Triage Result
- `decode_prt_breadcrumb.py` reports:
  - `update_count=18545`
  - `last_kind=rr`
  - `last_phase=rr_release_end`
  - `seg=0 gstage=0 lstage=0 sb=4 mgr=0`
- `triage_prt_capture.py` reports:
  - previous frontier: `sb=3/page63`
  - new frontier: `rr/rr_release_end seg=0 gstage=0 lstage=0 sb=4 mgr=0`
  - `claim_class=observability_only`
  - `requires_control_rerun=yes`
- Interpretation for this round:
  - This control rerun already served the needed purpose because it proved the run moved beyond the old `sb=3/page63` boundary.
  - The current actionable frontier is later, around `sb=4`, while the last sparse-text line remains inside the second export path for `tensor=2`, right after `dma-export-host ... phase=v2p-end page=0/64`.
  - Trigger log stayed empty in this control rerun, consistent with the previously discovered tmux env-drop issue.

## Run Shutdown
- Terminated the exhausted control rerun via documented FireSim flow:
  - command family: `firesim terminaterunfarm --forceterminate`
- Verified EC2 state transition:
  - instance `i-026f018c64c851dd7` entered `shutting-down`
- Note:
  - the original `runworkload` pane then exited with a manager-side SSH/banner failure because the instance was intentionally reclaimed mid-monitor; this is expected fallout from forced termination, not a new functional blocker.

## Next Triggered Rerun Preparation
- Re-ran preflight with the single allowed overlay family only:
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=1`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=rr`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_GLOBAL_STAGE=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_LOCAL_STAGE=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_SUBBATCH=4`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_MANAGER=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_TOKEN=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_PRE_RING=8`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_POST_BUDGET=32`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_MATCH_ONCE=1`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_LOG_PATH=/root/pipeline-runtime-debug/bertmini-batch8.trigger.log`
- `pairdummy_sbus128_workflow.sh debug-preflight` passed.

## Trigger Propagation Validation
- New `launchrunfarm` session:
  - `pairdummy-sbus128-launchrunfarm-20260415-052603`
- Important validation from the pane log:
  - `scripts/firesim-tmux-run.sh` now prints the propagated env list and it includes the expected `PIPELINE_RUNTIME_DEBUG_TRIGGER_*` variables.
- This directly verifies that the previous tmux-wrapper env-drop bug is fixed for the upcoming rerun.

## Current Status
- `launchrunfarm` is in progress.
- Immediate external issue at this moment is AWS F2 capacity retrying across subnets, not a guest/runtime regression.

## Later Launch Outcome
- Capacity retry eventually succeeded.
- New instance:
  - `i-083221b97a2a7fbf3`
- New private IP:
  - `192.168.1.194`
- `launchrunfarm` session:
  - `pairdummy-sbus128-launchrunfarm-20260415-052603`
- `launchrunfarm` completed successfully.

## Static Narrowing From This Round
- Re-read the relevant code paths:
  - `prt_dma.c` around `dma_copy_spm_pages_to_host_linux()`
  - `prt_dma.c` around `dma_submit_wait_annotated_scoped()` and `dma_blocking_wait()`
  - `prt_runtime.c` around `copy_tensor_pages_to_model_alias_target()`
  - `prt_scheduler.c` around `c2-export`
  - `prt_rerocc.c` around `prt_rr_release_scope()`
- Key narrowing:
  - The sparse-text boundary `dma-export-host ... phase=v2p-end` sits immediately before:
    - request field fill (`req.src_addr`, `req.dst_addr`, `req.bytes`)
    - trigger note `sw-b`
    - page-submitwait logs
    - page breadcrumb begin
    - `dma_submit_wait_annotated_scoped(...)`
  - `rr_release_end` is emitted only when `prt_rr_release_scope()` executes.
  - In this export path, that release happens at function exit (`done:` path) via `dma_batch_scope_release(&scope)`, so the breadcrumb frontier proves execution progressed beyond the bare `v2p-end` text boundary.
- Practical implication:
  - the live sparse log tail is not trustworthy enough to localize the exact stop by itself;
  - the next properly propagated trigger rerun is required to determine whether the missing point is:
    - between `v2p-end` and submit-begin,
    - inside `dma_submit_wait_annotated_scoped()`,
    - after `hw_dma_fence()`,
    - or only at final shared-scope release.

## Current Follow-Up
- Started `infrasetup` for the new instance:
  - session `pairdummy-sbus128-infrasetup-20260415-052857`

## Infrasetup Completion
- `infrasetup` completed successfully.
- Driver readiness preflight passed on:
  - private IP `192.168.1.194`
  - AGFI `agfi-0dc8dcfa4c7735f40`
- FireSim fingerprint check passed for slot 0.

## Triggered Rerun Started
- `run` command used explicit private IP:
  - `192.168.1.194`
- Remote freshness passed before launch.
- New runworkload session:
  - `pairdummy-sbus128-runworkload-20260415-053308`
- New runworkload log:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-15--05-33-09-runworkload-7RMLJI2Q0Q7JCH4L.log`
- New host watchdog:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260415-053308.host-watchdog.log`

## Critical Verification
- Re-checked generated command file:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260415-053308.command.sh`
- This command file explicitly exports:
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=1`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=rr`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_GLOBAL_STAGE=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_LOCAL_STAGE=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_SUBBATCH=4`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_MANAGER=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_TOKEN=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_PRE_RING=8`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_POST_BUDGET=32`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_MATCH_ONCE=1`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_LOG_PATH=/root/pipeline-runtime-debug/bertmini-batch8.trigger.log`
- Therefore this rerun is the first trustworthy detached-manager run after the tmux-wrapper propagation fix.

## Early Live Monitoring
- Host watchdog has started and bound to:
  - instance `i-083221b97a2a7fbf3`
  - private IP `192.168.1.194`
- At the latest early-live probe in this round:
  - host watchdog had not yet armed on guest status
  - remote `heartbeat.csv` advanced to:
    - `5079558986, 285`
  - remote `uartlog` size advanced to:
    - `15236` bytes
  - guest filesystem `bertmini-batch8.status` still did not exist yet
- Interpretation:
  - the rerun is alive and still progressing through early guest boot / pre-userspace stages;
  - it has not yet reached the pipeline-runtime wrapper stage that creates the tracked filesystem logs.

## Boot-To-Userspace Transition
- Later live probes confirmed the guest advanced through:
  - kernel boot
  - rootfs mount
  - BusyBox init
  - `/etc/init.d/S99run`
- After that, guest filesystem control files appeared:
  - `bertmini-batch8.status`
  - `bertmini-batch8.runner-early.stage`
  - `bertmini-batch8.log`
- Host watchdog then armed successfully:
  - `guest-status arm observed at 2026-04-15T05:39:57Z`
  - first armed snapshot:
    - `guest_sparse=1009`
    - `guest_trigger=0`
    - `guest_status=1386`

## Current Rerun Progress
- The trustworthy trigger-enabled rerun has already advanced to:
  - `guest_sparse=203602`
- Current observed state at this point:
  - `guest_trigger=0`
  - `guest_runner=481`
  - `guest_runner_post=433`
  - `guest_runner_early=487`
  - `guest_runner_proc=9273`
- Remote file observation at the same stage:
  - `bertmini-batch8.log` grew to `211466` bytes
  - `bertmini-batch8.trigger.log` still absent
- Interpretation:
  - this rerun is now aligned with the known control trajectory and has not regressed to an earlier boot/runtime failure;
  - the rr-trigger has not yet fired, so the run has not yet reached the targeted `subbatch=4 / manager=0 / token=0` window.
