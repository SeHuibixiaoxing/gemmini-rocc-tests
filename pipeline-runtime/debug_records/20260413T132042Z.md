# 2026-04-13T13:20:42Z

## Context

- Continuation of the `pairdummy-sbus128` dummy-model FireSim debug line.
- Previous round (`v7`) introduced:
  - forced inner export probe for page-windowed pages
  - export page window env support
  - guest env whitelist fix for `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START/END`

## Fresh Run Observations

- New run session:
  - `pairdummy-sbus128-runworkload-20260413-130657`
  - host instance private IP: `192.168.1.152`
- Remote freshness confirmed:
  - `PIPELINE_RUNTIME_PROFILE_ID='pairdummy-sbus128-fixed-v7'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START='32'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END='48'`

## Substantive Progress

- The old dummy-model stall was **actually crossed**.
- Evidence from guest sparse log:
  - `page=32/64 ... phase=page-submitwait-end`
  - `page=33/64 ... phase=page-submitwait-end`
  - `page=40/64 ... phase=page-submitwait-end`
  - `page=42/64 ... phase=page-submitwait-end`
  - `page=48/64 ... phase=page-submitwait-end`
- This happened for at least:
  - `target_seq=0`
  - `target_seq=1`
- Therefore the prior blocker
  - "`page=32 end` then no further progress"
  is no longer the active stall.

## New Front

- Watchdog progression:
  - `guest_sparse=204092`
  - then `guest_sparse=211465`
  - then `guest_sparse=505162`
  - then no further growth while heartbeat kept advancing
- Current sparse tail at freeze:
  - stage `0`, tensor `2`
  - new `target_seq=0` export
  - last visible forced inner line:
    - `dma-export-host ... phase=first-chunk-submitwait-end page=0 ...`
    - corresponding token: `tok=772`
- Additional breadcrumb evidence:
  - latest decoded record:
    - `sb=2`
    - `tensor=2`
    - `page=10`
    - `phase=dma_page_end`
- Combined interpretation:
  - current freeze is likely no longer near pages `32..48`
  - the live frontier appears to have advanced into the earlier uninstrumented middle pages
  - best current guess: stall is somewhere after page `10`, likely inside roughly page `11..31`

## Decision

- Do not add new code yet.
- Reuse the already-added page-window mechanism.
- Shift the fixed export page window from:
  - `32..48`
- to:
  - `8..16`
- Rationale:
  - includes the last confirmed breadcrumb frontier (`page=10`)
  - stays narrow to minimize log disturbance
  - should expose the real token/phase for the new stall without reopening the old wide window

## Changes

- File:
  - `pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh`
- Updated:
  - `PIPELINE_RUNTIME_PROFILE_ID=pairdummy-sbus128-fixed-v8`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START=8`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END=16`

## Validation

- `bash -n pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh` : PASS

## Next Actions

1. Terminate the current stuck run/farm.
2. Re-run `image-closure`.
3. Re-launch / `infrasetup`.
4. Re-run remote freshness and verify:
   - `PROFILE_ID=v8`
   - `DMA_EXPORT_PAGE_START=8`
   - `DMA_EXPORT_PAGE_END=16`
5. Re-run workload and inspect:
   - forced inner logs around pages `8..16`
   - actual token/phase of the new stall
