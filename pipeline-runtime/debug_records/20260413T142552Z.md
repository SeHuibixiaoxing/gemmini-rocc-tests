# 2026-04-13T14:25:52Z 调试记录

## 本轮目标
- 记录 `pairdummy-sbus128-fixed-v9` 这轮 live run 从 boot 进入 guest 文件日志的时刻。
- 继续从文件系统日志面监控，准备再次追 `shared fence` 前后的 breadcrumb 前沿。

## live run
- tmux session:
  - `pairdummy-sbus128-runworkload-20260413-141848`
- instance:
  - `i-00b3514f97391f9c2`
- private ip:
  - `192.168.1.121`

## 运行态进展
- host watchdog 已 arm：
  - `2026-04-13T14:25:36Z`
  - `hb='7089803197, 393'`
  - `guest_status=1385`
- 单独的 boot watcher 在 status 文件出现时也确认：
  - `state=running`
  - `profile_id=pairdummy-sbus128-fixed-v9`
  - `mlockall_mode=2`
  - `uart_log_enable=0`
  - `guest_log_enable=1`
  - `breadcrumb_enable=1`

## 镜像内文件状态
- `wrapper.stage` 当前已到：
  - `wrapper-enter`
  - `after-log-preamble`
  - `before-child-spawn`
  - `after-child-spawn pid=131`
- `runner-early.stage` 当前只有：
  - `script-entry`
- `runner.stage` 还没有内容
- `bertmini-batch8.log` 当前只有 firemarshal/running profile 的头部信息，说明：
  - 已经从 boot 面进入 guest 文件日志面
  - 但还没进入 runtime binary 的主执行区

## 当前判断
- 这轮 run 没有出现新的更早硬错误。
- 当前只是在：
  - boot 完成
  - wrapper 起来
  - runner 脚本刚进入
  这个阶段。
- 后续一旦 `runner.stage` 和 sparse log 继续增长，就重新沿着：
  - YAML / mapping load
  - segment 0
  - `stage=0 sb=3 tensor=2`
  - export page `10..12`
  的路径推进。

## 下一步
- 继续只用私网 IP + `debugfs` 读取：
  - `bertmini-batch8.log`
  - `bertmini-batch8.runner.stage`
  - `bertmini-batch8.breadcrumb.bin`
- 等 sparse log 明确进入 runtime binary 后，再判断这轮是否真实重回：
  - `dma_wait_after_fence`
  - `dma_wait_before_shared_fence`
  附近。
