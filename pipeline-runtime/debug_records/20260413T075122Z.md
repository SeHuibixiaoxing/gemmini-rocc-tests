## 2026-04-13T07:51:22Z

### 本轮目标
- 在新 run `pairdummy-sbus128-runworkload-20260413-074050` 上确认修补 `spm-xlate flush/release` 细日志后的真实新卡点。

### 运行态状态
- 活跃 run:
  - tmux session: `pairdummy-sbus128-runworkload-20260413-074050`
  - instance id: `i-01b0755b71951cab8`
  - private ip: `192.168.1.194`
- host watchdog 最新状态显示：
  - 已越过 boot 与 guest wrapper 启动
  - `guest_sparse` 从 `203871 -> 211244 -> 601397`
  - `guest_checkpoint` 从 `133 -> 26168`
  - 之后进入 idle，最新看到：
    - `hb='11370481218, 617' idle=63s guest_sparse=601397 guest_checkpoint=26168`

### 关键结论
- 新 run 已明确越过上一轮真实卡点：
  - `stage-exec-views` 的 `spm-xlate-flush mgr=4` 不再是当前前沿。
  - 新增的 `spm-xlate-flush-call` / `spm-xlate-release` 细日志表明：
    - `mgr=4` 以及后续 `mgr=5/6/7`
    - `issue-begin/end`
    - `fence-begin/end`
    - `release-begin/end`
    - `restore-begin/end`
    全部成功完成。
- 运行已推进到：
  - `segment=0 stage=0`
  - `worker stage=0 subbatch=3`
  - `tensor=2` 的 `export-target-dispatch`
  - 先完成 `target_seq=0 target=address`
  - 后进入 `target_seq=1 target=address2`

### 当前前沿
- 远端 guest 稀疏日志最后稳定停在：
  - `dma-export-host stage=0 tensor=2 phase=page-submitwait-begin page=60/64 copied=0 chunk=1024 src=0x40403c00 dst_va=0x3fbe539400 dst_pa=0x104169400 bounce=0`
- 在该行之前，`target_seq=1` 的以下页面都已完成：
  - `page=0`
  - `page=8`
  - `page=16`
  - `page=24`
  - `page=32`
  - `page=40`
  - `page=48`
  - `page=49..59`
- 当前未看到：
  - `page=60/64 ... phase=page-submitwait-end`
  - `export-target-dispatch ... target_seq=1 ... phase=end`

### 日志筛查结论
- 本轮观测必须通过私网 IP + remote image `debugfs cat` 获取 guest 文件日志，不能直接把 guest 根文件系统路径当作 host 路径读取。
- `PIPELINE_RUNTIME_DMA_SUBMIT_TRACE_ENABLE='0'`，所以没有常规 `dma-submit ...` marker。
- `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE='1'`，但 token window 为：
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START='2698'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END='2720'`
- 当前停住的 `page=60` 对应 token 大概率已超出该 window，因此缺少更细的 `dma-export-submit` / `dma-export-wait` 日志，不足以进一步区分：
  - 卡在 `prt_dma_submit()`
  - 还是卡在 `prt_dma_wait()` 内部的 `hw_dma_fence()`

### 对源码的静态映射
- `dma-export-host ... page-submitwait-begin/end` 位于：
  - `src/prt_dma.c` 的 `dma_copy_spm_pages_to_host_linux()`
- 该路径调用：
  - `dma_submit_wait_annotated_scoped()`
  - 其内部顺序是：
    - `prt_dma_submit(rt, req, &tok)`
    - `prt_dma_wait(rt, &tok, timeout_ns)`
    - 再清理 token
- 当前 runtime 已收敛到 `blocking_fence` 路径：
  - `prt_runtime.c` 强制 `rt->cfg.dma_backend = PRT_DMA_BACKEND_BLOCKING_FENCE`
  - `prt_dma_backend_init()` 打印语义为 `hw_dma_fence + rr_fence_scope`
- 因此当前前沿不是旧的 progress-thread / doneflag 轮询路径。

### 本轮使用的关键指令
- 读取 remote sparse:
  - `ssh -i /home/ubuntu/firesim.pem -o StrictHostKeyChecking=no ubuntu@192.168.1.194 "sudo debugfs -R 'cat /root/pipeline-runtime-debug/bertmini-batch8.log' '/home/ubuntu/sim_slot_0/rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy0-rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy.img' 2>/dev/null | tail -n 220"`
- 读取 remote checkpoint:
  - `ssh -i /home/ubuntu/firesim.pem -o StrictHostKeyChecking=no ubuntu@192.168.1.194 "sudo debugfs -R 'cat /root/pipeline-runtime-debug/bertmini-batch8.checkpoint.log' '/home/ubuntu/sim_slot_0/rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy0-rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy.img' 2>/dev/null | tail -n 220"`
- 筛 export 前沿：
  - `... | grep 'dma-export-' | tail -n 120`

### 当前判断
- 现在的真实卡点已经前移到 DMA export 第二个 target，而不是 `spm-xlate`。
- 且这一轮卡点不是“刚开始就挂”，而是 `address2` export 已经成功发送并等待完成了很多页，停在更后面的单页。
- 这更像是：
  - 某个特定地址/对齐/范围组合触发的 DMA fence 不返回
  - 或某个提交在更后面才把 DMA 状态机推到异常状态
- 还不能证明是语义级修复，只能证明 `spm-xlate mgr=4` 不再是当前阻塞点。

### 下一步
- 在不改变主语义的前提下，把 export probe window 挪到当前卡点 token 附近，或临时开启更细的 submit/wait trace，只覆盖 `segment=0 stage=0 tensor=2 export`，避免日志爆炸。
- 目标是分清：
  - 停在 `prt_dma_submit()`
  - 停在 `hw_dma_fence()`
  - 还是停在 `rr_fence_scope()`
