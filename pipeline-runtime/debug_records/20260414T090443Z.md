# 2026-04-14T09:04:43Z 调试记录

## 本轮目标

- 继续沿 FPGA 软件调试主线推进，
  复核 live run 是否已经穿过旧的 fixed-load `page31` frontier。
- 如果 live run 已经进入更晚的软件路径，
  就把 authoritative frontier 更新到新的 `c2-export` 首段。
- 在不改硬件的前提下，
  把固定 probe 从旧的 fixed-load page31 切到当前真实前沿。

## 本轮执行

- 读取 live run 现场：
  - manager log：
    `sims/firesim/deploy/logs/2026-04-14--08-41-38-runworkload-68SMY9AR4ENCOCA7.log`
  - run host：
    `192.168.1.166`
  - guest image 内调试目录：
    `/root/pipeline-runtime-debug`
- 通过 `debugfs` 抓回本地 live snapshot：
  - `tmp/firesim-aws-f2/captures/20260414-live/pairdummy-sbus128-runworkload-20260414-084136-192.168.1.166-live-bertmini-batch8.log`
  - 同前缀 breadcrumb
  - 同前缀 runner-proc 快照
- 静态重读相关代码：
  - `src/prt_dma.c`
  - `src/prt_rerocc.c`
  - `src/prt_page_table.c`
  - `src/prt_scheduler.c`
  - `src/prt_runtime.c`
- 切换 fixed profile 到新的 `v13`，
  然后重新走：
  `image-closure`
  与 local freshness。

## 新结论

- 旧的 fixed-load `page31` frontier 已经不再 authoritative。
- live guest 已明确穿过：
  - 两次 `export-target-dispatch stage=0 tensor=2 ... phase=end`
  - `worker stage=0 subbatch=1 compute-done`
- 当前最后一个已经持久化到 guest 文件系统的 sparse-log frontier 是：
  - `c2-export stage=0 tensor=2 phase=dispatch`
  - `c2-export stage=0 tensor=2 phase=copy-begin`
  - 随后第三次 `rr-acquire-inner ... phase=after-set-opc`
- `runner-proc.stage` 显示 guest 子进程仍是 `State: R (running)`；
  run host `heartbeat.csv` 也持续增长，
  所以这不是 crash / poweroff。
- 结合静态代码，
  当前新的可信可疑区间收敛到：
  `dma_copy_spm_pages_to_host_linux()`
  里 batch-scope acquire 之后、
  page0 首个 host-v2p / submitwait 之前。
- 当前最可信的软件候选只剩三类：
  1. `dma_batch_scope_acquire()` /
     `prt_rr_acquire_scope()` 返回边界
  2. page0 的
     `prt_host_virt_to_phys((const void *)dst_ptr, &dst_pa)`
  3. page0 首个
     `dma_submit_wait_annotated_scoped()` /
     `prt_dma_submit()` /
     `prt_dma_wait()`
- 从静态地址看，
  当前 `c2-export` page0：
  - `src=0x40002000`
  - `dst=0x...3400`
  - 两端都是 `64B` 对齐
  因此不优先怀疑 bounce-buffer 分支。

## 运行与环境状态

- 新 fixed profile 已切到：
  `pairdummy-sbus128-fixed-v13`
- 本轮 local image freshness 已通过，
  并确认 guest image 内环境为：
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START='0'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END='0'`
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PROBE_ENABLE='0'`
- 当前新的 FireSim `launchrunfarm` session：
  `pairdummy-sbus128-launchrunfarm-20260414-090234`
- 截至本记录时间，
  launch 被 AWS `f2.6xlarge` 容量阻塞，
  正在跨多个 subnet 自动重试；
  这是基础设施阻塞，
  不是本地软件构建或 freshness 问题。

## 下一步

1. 等当前 `launchrunfarm` 成功拿到 F2 实例。
2. 一旦拿到实例，
   按固定顺序继续：
   `infrasetup -> current-private-ip -> remote-freshness -> run`
3. 新 rerun 的唯一目标是把 `c2-export` page0 的 frontier 推进到：
   - `v2p-begin`
   - `v2p-end`
   - `first-chunk-submitwait-begin/end`
4. 根据新证据继续最小扰动分叉：
   - 没有 page0 `v2p-begin`：
     只补 batch-scope acquire return 边界
   - 只到 `v2p-begin`：
     只补 page0 `host-v2p`
   - 已到 `submitwait-begin` 但无 `submitwait-end`：
     只补 submit/wait 边界
