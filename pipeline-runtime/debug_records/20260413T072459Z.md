## 2026-04-13T07:24:59Z

### 本轮目标
- 延续 `12-pair sbus128 dummy-model` 调试，围绕 `segment=0 stage=0` 的 `spm-xlate-flush` 前沿继续收敛真实卡点。

### 运行态状态
- 活跃 run `pairdummy-sbus128-runworkload-20260413-065957` 已被 host watchdog 判定为超过 600s 无 guest 文件日志进展。
- watchdog 于 `2026-04-13T07:20:23Z` 自动触发 `terminaterunfarm --forceterminate`。
- AWS 查询确认实例 `i-0dcd3c1a6b9a7bc2a` 当前状态为 `terminated`。
- 因实例已终止，后续必须基于本地 capture 与源码静态排查；不能再直接 SSH 到 `192.168.1.169`。

### 关键证据
- `prt_gemmini_spm_xlate_flush()` 当前实现包含如下顺序：
  - `prt_spm_xlate_acquire_scope()`
  - `PRT_PROGRESS_LOG("spm-xlate-flush ...")`
  - `rerocc_gemmini_spm_xlate_flush()`
  - `prt_spm_xlate_release_scope()`
  - `PRT_PROGRESS_LOG("spm-xlate-flush-restore ...")`
- 本轮重新核对 capture 后发现：
  - 不是第一次 `mgr=4` flush 就挂。
  - 在 `guest-sparse-log.txt` 中，`stage-exec-views` 已经成功完成 4 次，分别对应 subbatch 0/1/2/3 前的 flush。
  - 第 5 次 `stage-exec-views`（对应 subbatch 4 前）在 `mgr=4` 处停止。
- 成功的前四轮关键位置：
  - `stage-exec-views phase=begin` at lines `2620/3293/3966/4639`
  - 对应 `phase=end ... rc=0 flush=1` at lines `3011/3684/4357/5030`
- 失败的第 5 轮关键位置：
  - `stage-exec-views phase=begin` at line `5312`
  - `stage-fixed-load-sparse phase=end ... slot=1 tensor=1000001 ...` at line `5686`
  - `spm-xlate-flush mgr=0..3` 与 `flush-restore` 正常
  - 最新最后一条停在 line `5695`: `spm-xlate-flush mgr=4 cfg=15 prev_opc3=0x1`
- 这说明问题更像“重复执行 stage view bind/flush 后，某个 manager 的 spm-xlate flush/release 状态损坏”，而不是 manager 4 在首轮就不可用。

### 与 checkpoint 的关系
- checkpoint 最新仍停在：
  - `worker stage=0 checkpoint=before-build-stage-task subbatch=4 ...`
- 这与 sparse 日志并不矛盾：
  - `build_stage_task_desc()` 在 worker 主循环里会再次触发 `build_stage_conv_desc()`
  - `build_stage_conv_desc()` 会走 stage exec view 构建/flush 路径
  - 因而 `before-build-stage-task subbatch=4` 之后，卡在第 5 次 `stage-exec-views` 内部是合理的

### 已确认约束未被破坏
- DMA 等待仍不是 doneflag 轮询；当前前沿与 DMA 等待无关。
- 本轮确认实例查询与 SSH 目标均使用私网地址约束；实例终止后无法继续 SSH 属于预期。
- 当前主观测面仍基于 guest 文件日志 capture，而不是 `uartlog`。

### 下一步
- 继续静态排查 `stage-exec-views` 为什么每个 subbatch 都会重复 `bind + flush`，以及第 5 次与前 4 次相比状态上发生了什么变化。
- 重点审查：
  - `build_stage_conv_desc()` / `build_stage_exec_views()` 的重复绑定语义
  - `prt_spm_bind_vpages_ctx()` 是否每轮追加状态而非覆盖
  - `prt_gemmini_spm_xlate_flush()` / `prt_spm_xlate_release_scope()` 是否缺少更细粒度可见性，需要下一轮补点日志

### 本轮代码改动
- 在 `src/prt_rerocc.c` 为 `spm xlate` flush/release 路径补充稀疏日志：
  - `spm-xlate-flush-call ... phase=issue-begin/end`
  - `spm-xlate-release ... phase=fence-begin/end`
  - `spm-xlate-release ... phase=release-begin/end`
  - `spm-xlate-release ... phase=restore-begin/end`
- 改动目的：
  - 将当前故障窗口从 “`mgr=4` flush/release 邻域” 进一步细分到具体指令前后
  - 下轮重跑后可直接判断卡在 `rerocc_gemmini_spm_xlate_flush()`，还是卡在 `rr_fence` / `rr_release` / `opc3 restore`

### 本轮重建与部署
- FireMarshal
  - `scripts/firemarshal-tmux-run.sh --session-name pairdummy-sbus128-clean-20260413-073600 clean ...pairdummy.json`
  - `scripts/firemarshal-tmux-run.sh --session-name pairdummy-sbus128-build-20260413-073610 build ...pairdummy.json`
  - `scripts/firemarshal-tmux-run.sh --session-name pairdummy-sbus128-install-20260413-073420 install ...pairdummy.json`
- FireMarshal freshness 结果
  - 新 runtime binary sha256 已更新为 `97027b4607f9fe755cc871cc725ec5db1c98e568b457e3bfed0f65ce6e09be09`
  - workload image 本地 freshness 校验通过
- FireSim
  - `scripts/firesim-tmux-run.sh --session-name pairdummy-sbus128-launchrunfarm-20260413-073435 launchrunfarm ...`
  - `scripts/firesim-tmux-run.sh --session-name pairdummy-sbus128-infrasetup-20260413-073510 infrasetup ...`
  - 新实例 `i-01b0755b71951cab8`
  - 私网 IP `192.168.1.194`
- 远端 image freshness
  - 使用私网 IP 运行 `verify_pairdummy_firesim_remote_image_freshness.sh`
  - 远端 image:
    - `/home/ubuntu/sim_slot_0/rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy0-rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy.img`
  - 本地/远端 image sha256 一致：
    - `0a5562341a24900764f8265b7ada33088c68a3f357fbd2cbf5227effeb7e3c5a`
  - 远端 `/firemarshal.env` 关键项确认一致：
    - `PIPELINE_RUNTIME_PROFILE_ID='pairdummy-sbus128-fixed-v2'`
    - `PIPELINE_RUNTIME_MLOCKALL_MODE='2'`
    - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE='1'`
    - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START='2698'`
    - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END='2720'`

### 当前新 run
- tmux session: `pairdummy-sbus128-runworkload-20260413-074050`
- run instance: `i-01b0755b71951cab8`
- private ip: `192.168.1.194`
- host watchdog 已启动并绑定到该实例
- 当前正在等待 guest 启动并回传新的文件日志前沿
