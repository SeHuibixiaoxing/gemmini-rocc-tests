# 2026-04-13T15:13:12Z debug record

## Scope
- Continue dummy-model debug on `12-pair sbus128` hardware target.
- Preserve current minimal probe strategy (`v11`) and avoid broad logging expansion.

## Fixed constraints re-asserted
- SSH / live inspection must use private IP only.
- Primary observation surface is guest filesystem logs, not `uartlog`.
- Do not reintroduce doneflag polling for DMA completion.
- Do not regress hugetlb / physically contiguous page assumptions.
- Before FireSim / FireMarshal actions, always:
  - `cd /home/ubuntu/chipyard/sims/firesim`
  - `set +u; source sourceme-manager.sh --skip-ssh-setup; set -u`
- FireMarshal must go through `/home/ubuntu/chipyard/scripts/firemarshal-tmux-run.sh`.
- FireSim manager commands must go through `/home/ubuntu/chipyard/scripts/firesim-tmux-run.sh`.
- Freshness checks are mandatory before trusting a rebuilt image.

## Starting point inherited from previous round
- Current profile: `PIPELINE_RUNTIME_PROFILE_ID=pairdummy-sbus128-fixed-v11`
- Current instance id: `i-0ad2e1c97e4708e08`
- Current private IP: `192.168.1.115`
- Current state: run farm launched, `infrasetup` completed, waiting for remote freshness + run.
- Latest known plateau from `v10`: `seg=0 stage=0 sb=3 tensor=2 export target_seq=1 page36 end`
- New `v11` probe in `prt_dma.c`: add `v2p-begin` / `v2p-end` around `prt_host_virt_to_phys(dst_ptr, &dst_pa)` on export non-bounce path.

## Immediate plan
1. Verify remote image freshness against private IP `192.168.1.115`.
2. Confirm remote `/firemarshal.env` reflects `v11` page window (`36..37`).
3. Start workload run.
4. Monitor host watchdog + guest sparse log + breadcrumb.
5. Use new `v2p-begin/end` markers to classify whether blocker is inside `prt_host_virt_to_phys` or after it.

## Executed
- `remote-freshness 192.168.1.115`: PASS
  - remote image SHA matches local image SHA
  - remote `/firemarshal.env` confirms:
    - `PIPELINE_RUNTIME_PROFILE_ID=pairdummy-sbus128-fixed-v11`
    - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START=36`
    - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END=37`
- `run 192.168.1.115`: started tmux session
  - session: `pairdummy-sbus128-runworkload-20260413-151358`
  - pane log: `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-151358.pane.log`
  - host watchdog log (actual filename): `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-151358.host-watchdog.log`

## Observations so far
- FireSim run is live; manager pane repeatedly reports:
  - instance `192.168.1.115`
  - simulation running `True`
- Remote `sim_slot_0` contains the expected workload image:
  - `/home/ubuntu/sim_slot_0/rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy0-rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy.img`
- Boot-level liveness confirmed from `uartlog` and `heartbeat.csv`
  - heartbeat advanced to `4608300277` target cycles at `261s` since start during this check
  - `uartlog` shows Linux kernel boot progressing; no `firemarshal` / wrapper markers yet
- Current guest filesystem state is still pre-wrapper:
  - `/root/pipeline-runtime-debug/bertmini-batch8.status`: `0` bytes
  - `/root/pipeline-runtime-debug/bertmini-batch8.log`: `0` bytes
  - breadcrumb file not yet valid / not yet materialized with runtime payload

## Debugging notes
- I initially checked `.watchdog.log`; that was the wrong file. The real monitor output is `.host-watchdog.log`.
- No program edits were made in this round after starting `v11`; all work since then is observation only.

## Later observations
- Historical baseline cross-check:
  - older host-watchdog runs typically arm around heartbeat roughly `7.3e9 / 794s`
  - therefore the earlier `4.6e9 / 261s` pre-wrapper state was not enough to classify as a new boot regression
- Current run has now armed and entered the guest-file observation phase:
  - host-watchdog arm:
    - `2026-04-13T15:20:47Z`
    - `hb='7092665095, 393'`
    - `guest_status=1386`
  - later progress snapshots:
    - `hb='8181775253, 452' guest_sparse=203604`
    - `hb='9466452001, 520' guest_sparse=211468`
- Current guest sparse tail is still in artifact loading / mapping parse progress, not yet `segment=0` execution.
- Current live breadcrumb decode:
  - `update_count=146`
  - `last_kind=runtime`
  - `last_phase=runtime_init_done`
  - this is still consistent with the breadcrumb filter only recording the targeted `segment=0 / stage=0` region later.
