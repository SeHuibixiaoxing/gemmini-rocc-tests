调试类别：gdbserver / old AGFI control / shmem marker control

# old AGFI still has corrupted RSP payload after restoring shmem empty-marker clearing

日期：2026-05-05T02:33:00Z

## 目标

验证 FireSim switch commit `b17cc8979` 是否能恢复旧 1bp AGFI 的 remote
`gdbserver` software-breakpoint 能力矩阵。

该 commit 在 `a300ebdfe` 的 ARP / IPv4 / TCP checksum repair 之上，恢复
`ShmemPort::send()` 对 `0xDEADBEEFDEADBEEF` compressed-empty marker 的清零。

旧目标仍为：

- AGFI: `agfi-0079cbbca617eca4e`
- AFI: `afi-0ee7774f829acd4de`
- config: `FireSimRocketNICNoTraceConfig + BaseF2Config`
- 1bp / non-8BP / single-core Rocket + NIC + no TraceIO + 30 MHz

## 环境

- FireSim submodule: `b17cc897977916a248c4b26fee2e338f72fa3bd9`
- run host: `i-0ad007da01f825e01` / `192.168.1.65`
- HWDB:
  `sims/firesim/deploy/config_hwdb_f2_rocket_singlecore_nic_notrace_30mhz_agfi0079_emptydriver_01e763.yaml`
- runtime:
  `sims/firesim/deploy/config_runtime_f2_rocket_singlecore_nic_gdbserver_smoke_notrace_clean1bp_gdbpayload_30mhz.yaml`
- result dir:
  `sims/firesim/deploy/results-workload/2026-05-05--02-12-08-rocket-singlecore-nic-gdbserver-smoke-f2-rocket-singlecore-nic-gdbserver-smoke-notrace-clean1bp-gdbpayload/`
- run log:
  `sims/firesim/deploy/logs/2026-05-05--02-12-08-runworkload-BG8ZNT6I42DRQBUT.log`

`infrasetup` completed and flashed:

```text
[192.168.1.65] Flashing FPGA Slot: 0 with agfi: agfi-0079cbbca617eca4e.
[192.168.1.65] Checking for Flashed FPGA Slot: 0 with agfi: agfi-0079cbbca617eca4e.
```

Switchlog no longer showed the prior early `ShmemPort send` deadbeef marker
spam, consistent with the deployed switch clearing the marker before publish.

## Result

Failed with the same GDB symptom as the previous `a300ebdfe` run.

Guest reached:

```text
running /etc/init.d/S99run
[gdbserver] phase=prelaunch net_dev=eth0 guest_ipv4=172.16.0.2 endpoint=172.16.0.2:2345 pid=0
```

Expect command:

```text
GDBSERVER_SMOKE_EXPECT_TIMEOUT=420 \
GDBSERVER_SMOKE_STATIC_NEIGH_MAC=00:12:6d:00:00:02 \
generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/run_remote_gdbserver_software_expect_smoke.sh \
  192.168.1.65 32345
```

GDB transcript:

```text
target remote :32345
Remote debugging using :32345
Invalid hex digit 59
...
continue
The program is not being run.
timeout waiting for continue pattern: Breakpoint 1,.*main
```

`expect_rc=5`.

## Packet evidence

The pcap again shows corrected TCP checksums carrying corrupted RSP bytes:

```text
172.16.0.2.2345 > 172.16.0.1.45182: Flags [P.], cksum 0x9063 (correct), length 562
0x0040:  6287 2450 6163 6b45 7453 697a 653d 3437  b.$PackEtSize=47
```

This is the same `$PackEtSize=47ff` corruption observed before marker clearing.

## Switch evidence

Switchlog:

```text
pipeline-runtime/debug_records/artifacts/20260505T022805Z_oldagfi_shmemclear_invalidhex/runhost/switchlog
```

Relevant `SSHPort` egress:

```text
SWITCH DEBUG SSHPort tap_send port=1 event=6 tap_len=630 flits=79 short=0 zero_eth_head=0 repair=0x6
SWITCH DEBUG SSHPort tap_send_bytes port=1 event=6 off=64 bytes=62 87 24 50 61 63 6b 45 74 53 69 7a 65 3d 34 37
```

Interpretation:

- marker clearing deployed and changed early switch behavior;
- checksum repair still works (`repair=0x6`);
- payload corruption persists;
- therefore shmem empty-marker preservation was not the root cause of the
  old-AGFI RSP payload corruption.

## Next direction

The remaining low-cost software control is to stop using the 01e763 driver
bundle and test the recovered original old-AGFI driver bundle:

```text
sims/firesim/deploy/config_hwdb_f2_rocket_singlecore_nic_notrace_30mhz_agfi0079_recovered_driver.yaml
```

That HWDB points at:

```text
tmp/firesim-aws-f2/driver-bundles/agfi0079-recovered-20260504/driver-bundle.tar.gz
```

whose `FireSim-f2` sha256 is:

```text
dc94d9f93b34dfc32dd994d8926d6cd81cf325003a8b2ba551eb9b3ff8d05a92
```

This matches the driver binary in the original 2026-04-30 old AGFI build
result. If that driver passes with current switch checksum repair, the root
cause is the 01e763 driver bundle. If it still fails, the corruption is likely
in the current manager/switch/runtime path outside the `FireSim-f2` binary.

## Artifacts

```text
pipeline-runtime/debug_records/artifacts/20260505T022805Z_oldagfi_shmemclear_invalidhex/
```

Contains UART, heartbeat, sim-run.sh, switchlog, pcap, tcpdump log, expect
transcript, tmux pane, and manager run log.

`gdbserver.log` and `gdbserver.info` were absent at capture time.

## Cleanup

Terminated with:

```text
firesim terminaterunfarm --forceterminate \
  -c config_runtime_f2_rocket_singlecore_nic_gdbserver_smoke_notrace_clean1bp_gdbpayload_30mhz.yaml \
  -a config_hwdb_f2_rocket_singlecore_nic_notrace_30mhz_agfi0079_emptydriver_01e763.yaml \
  -r config_build_recipes_f2_rocket_singlecore_nic_notrace_30mhz.yaml
```

Termination session:
`oldagfi0079-shmemclear-terminate-20260505-0232`, exit 0.

AWS state after termination request: `i-0ad007da01f825e01` was
`shutting-down`.

