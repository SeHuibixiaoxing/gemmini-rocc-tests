# Debug Record 20260414T132728Z

## 目标

- 继续沿着 `v17` live FPGA run 缩窄
  `stage0/subbatch4`
  pointwise 卡点
- 先做静态审计，
  再决定是否需要新的软件探针
- 若需要改动，只做软件侧、低扰动改动；
  不改硬件

## 输入状态

- 上一轮 authoritative 状态：
  `v17`
  sharded breadcrumb
  已上线，
  active host 为
  `192.168.1.18`
- 已知约束：
  - 先静态读代码和 artifact
  - 再看 capture
  - 最后才加 probe
  - 不改 bitstream / 不改硬件
  - Linux boot 早期静默但 `heartbeat.csv` 前进时不算异常

## 使用命令

- 重新阅读：
  - `README.md`
  - `docs/CURRENT_STATUS.md`
  - `NEXT_SESSION_PROMPT.md`
  - `docs/constraints/hard_constraints.md`
- live 观察：
  - 读取 `bertmini-batch8.status`
  - 读取 `heartbeat.csv`
  - 读取 `bertmini-batch8.log`
  - dump + scp `bertmini-batch8.breadcrumb.bin`
- capture 分诊：
  - `decode_prt_breadcrumb.py --all`
  - `triage_prt_capture.py`
- 静态审计：
  - 阅读 `src/prt_gemmini_adapter.c`
  - 读取 `include/prt_progress.h`
  - 读取 `src/prt_rerocc.c`
  - 运行
    `audit_pipeline_runtime_artifact.py`
- 重新部署：
  - `pairdummy_sbus128_workflow.sh image-closure`
  - `... terminate`
  - `... launch`
  - `... infrasetup`
  - `... remote-freshness 192.168.1.253`
  - `... run 192.168.1.253`

## 配置 / 环境

- `v17` 现场配置：
  - `PIPELINE_RUNTIME_PROFILE_ID=pairdummy-sbus128-fixed-v17`
  - `PIPELINE_RUNTIME_GUEST_LOG_ENABLE=1`
  - `PIPELINE_RUNTIME_GUEST_DEEP_LOG_ENABLE=0`
  - `PIPELINE_RUNTIME_BREADCRUMB_ENABLE=1`
- 本轮修改后切到：
  - `PIPELINE_RUNTIME_PROFILE_ID=pairdummy-sbus128-fixed-v18`
  - `PIPELINE_RUNTIME_CRITICAL_UART_PROBE=0`
  - 其余 fixed workflow 语义保持不变

## 证据路径

- `v17` live capture：
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/20260414-live/192.168.1.18-bertmini-batch8-v17.log`
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/20260414-live/192.168.1.18-bertmini-batch8-v17.breadcrumb.bin`
- `v17` pane / manager：
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260414-125651.pane.log`
  - `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-14--12-56-52-runworkload-HDTE9CIAA0J5WUYA.log`
- `v18` 重新部署：
  - launch pane：
    `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-launchrunfarm-20260414-131930.pane.log`
  - infrasetup pane：
    `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-infrasetup-20260414-132228.pane.log`
  - run pane：
    `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260414-132659.pane.log`
  - manager log：
    `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-14--13-27-00-runworkload-3L2IKJWKWT65VH8I.log`
  - results dir：
    `/home/ubuntu/chipyard/sims/firesim/deploy/results-workload/2026-04-14--13-27-00-rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy-f2-gemmini-rerocc-pairmanager-dummy16x16-4c12p12-sbus128-linux-bertmini-batch8-fileonly-sync/`

## 新观察 / 新卡点

- `v17`
  sparse log 已明确推进到：
  - `oc-split-pointwise stage=0 tile=7/8 mgr=7 ... begin`
  - `conv-sync-strided stage=0 mgr=7 begin`
  - `conv-sync-strided stage=0 mgr=7 acquired`
  - `conv-sync-strided stage=0 mgr=7 flushed use_pointwise=1`
- 之后：
  - `heartbeat.csv`
    继续从
    `724s`
    前进到
    `941s`
  - `bertmini-batch8.log`
    文件大小固定在
    `368377`
    bytes
  - 文件尾部始终不再超过
    `mgr=7 flushed use_pointwise=1`
- 同一时刻的 sharded breadcrumb：
  `last_kind=gemmini`
  `last_phase=gemmini_pointwise_matmul_return`
  `mgr=6`
- `triage_prt_capture.py`
  因为只看最后 slot，
  仍给出
  `mgr=6`
  视角；
  但结合 sparse tail，
  当前真正的可见前沿已经前移到：
  `mgr7 flushed`
  之后、
  `dispatch=pointwise`
  之前

- 静态读代码后确认：
  `mgr7 flushed`
  到下一条 pointwise breadcrumb 之间，
  现在没有新的硬件语义动作：
  - 没有新的 DMA 提交
  - 没有新的 RR acquire / release / fence
  - 没有新的 Gemmini 指令
- 在当前 fixed profile 下：
  - `PIPELINE_RUNTIME_ONLY_MARKER=0`
  - `PIPELINE_RUNTIME_GUEST_DEEP_LOG_ENABLE=0`
  - deep-log gate 也为 `enabled=0`
- 因而这段窗口里，
  marker / deep / crit 路径几乎都不形成新的观测面；
  剩下最显著的外部动作就是 coarse guest log append
  `dispatch=pointwise`
  以及紧随其后的 breadcrumb note

- artifact 静态审计通过：
  - `segments=13`
  - `split_kind_coverage={'oc': 32, 'resadd_spatial': 6, 'single': 2}`
  - `op_type_coverage={'conv': 32, 'resadd': 8}`

- 重新拉起 `v18` 时，
  `launchrunfarm`
  一度因为
  AWS `f2.6xlarge`
  容量不足反复重试；
  这不是新的 runtime blocker。
  最终新 host 为：
  `192.168.1.253`
  / `i-01d49373bd01fe578`

## 分析

- 新的 static-first 结论比上一轮更窄：
  当前最强嫌疑不再是 pointwise release / drain / `rr_release`，
  而是 pointwise 热路径里剩余的 coarse guest log 自干扰。
- 这也解释了为什么：
  - `mgr0..6`
    还能继续推进
  - 到 `mgr7`
    时 sparse log 文件尾正好停在下一条 coarse log 之前
- 现有证据仍不支持改硬件：
  - 当前窗口里没有新的硬件指令
  - artifact 审计通过
  - pointwise split-OC 语义仍与
    `HybridMapper + MudnacSim`
    基线一致

- 本轮采用的软件修正思路是：
  当 breadcrumb 已经覆盖 pointwise 热路径时，
  不再在同一路径上继续写 coarse guest log；
  让 pointwise 主观测面回到 breadcrumb

## 下一步

1. 继续观察
   `192.168.1.253`
   上的
   `v18`
   rerun
2. 等它进入用户态并推进到
   `stage0/subbatch4`
3. 若 `v18`
   已越过旧的
   `mgr7 flushed`
   前沿，
   继续抓新的 breadcrumb / sparse，
   看下一真实前沿在哪里
4. 若 `v18`
   仍停住，
   继续按
   “静态读代码 -> capture -> 更窄软件 probe”
   顺序推进，
   仍不改硬件
