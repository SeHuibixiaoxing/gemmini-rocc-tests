# 2026-04-15 13:26:11 UTC

## Round Goal

修复本轮 observability rerun 中暴露出的配置链路问题：

- 已改 `pairdummy_sbus128_fixed_env.sh`
- 但 render / local freshness / image patch 仍然落到旧的
  `v20 / export_probe=0`

需要确认真正的 guest env source-of-truth，并把
`v21 / page5 export probe`
真正写入 image。

## Input State

- `debug-preflight` 已显示：
  - `profile_id=pairdummy-sbus128-fixed-v21`
  - `dma_export_probe_enable=1`
- 但 `pairdummy_sbus128_workflow.sh local-freshness`
  输出的 `/firemarshal.env` 仍然是：
  - `pairdummy-sbus128-fixed-v20`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE=0`

## Commands Used

- 检查 render 链路：
  - `sed -n '1,260p' pipeline-runtime/scripts/render_pairdummy_guest_env.sh`
  - `sed -n '1,220p' workload/host-init-fileonly-sync-pairdummy.sh`
  - `sed -n '1,220p' workload/host-init.sh`
- 检查 baseline env：
  - `sed -n '1,220p' workload/overlay/firemarshal.env`
- 走正确刷新链路：
  - `cd .../workload && SKIP_BUILD=1 ./host-init-fileonly-sync-pairdummy.sh`
- 重新 patch image：
  - `render_pairdummy_guest_env.sh`
  - `apply_guest_env_to_image.sh`
  - `verify_pairdummy_firemarshal_image_freshness.sh ...`

## New Findings

根因确认：

- `render_pairdummy_guest_env.sh`
  不是从
  `pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh`
  生成有效 env
- 它直接复制：
  `generators/gemmini/software/gemmini-rocc-tests/rerocc-linux-tests-coupleddma/workload/overlay/firemarshal.env`
- 然后只允许：
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_*`
  - `PIPELINE_RUNTIME_BREADCRUMB_*`
  做临时覆盖

所以当前存在两个固定配置源：

1. `pairdummy_sbus128_fixed_env.sh`
2. `workload/overlay/firemarshal.env`

上一轮只修改了前者，没有重新生成后者，
导致 render / freshness / image patch 都继续使用旧 baseline。

## Fix Applied

采用正确链路刷新 baseline：

- 执行：
  `SKIP_BUILD=1 ./host-init-fileonly-sync-pairdummy.sh`
- 结果：
  `workload/overlay/firemarshal.env`
  已更新为：
  - `PIPELINE_RUNTIME_PROFILE_ID='pairdummy-sbus128-fixed-v21'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE='1'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START='1161'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END='1163'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START='5'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END='5'`

随后重新：

- render effective guest env
- patch `/firemarshal.env` 进本地 image
- 执行 local freshness

## Verification

最新 local freshness 已显示：

- `OK firemarshal-env sha256=aa43ccc07c11e4f2a3592bb293904709082e0fb6fca903127136d635a53dfcf5`
- 镜像内 `/firemarshal.env` 片段明确为：
  - `PIPELINE_RUNTIME_PROFILE_ID='pairdummy-sbus128-fixed-v21'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE='1'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START='1161'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END='1163'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START='5'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END='5'`

## Conclusion

本轮真正修复的不是功能逻辑，而是：

- guest env source-of-truth 漂移
- “改了 fixed profile 但 render/image 仍是旧值”的执行流程缺陷

这个问题解释了为什么此前很容易反复误用旧 image / 旧 env。

## Next Step

继续等待当前 `launchrunfarm` 拿到 F2 容量。
一旦实例成功启动，按既定流程执行：

1. `infrasetup`
2. remote freshness
3. `run`
4. private-IP live image 读取 page5 export probe 日志
