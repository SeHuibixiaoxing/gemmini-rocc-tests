# 20260424T110841Z

调试类别：gdbserver

## 本轮目标

- 在 `singlecore + NIC + gdbserver smoke` 新 bitstream 构建期间，把与后续判断强相关的静态事实补充记录下来。
- 避免后续把“build 期间的正常 FireSim/AWS shell 告警”和“本轮 lockstep patch 的真实影响”混为一谈。

## 本轮输入

- bitstream tmux session:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/firesim-build-singlecore-nic-lockstep-20260424.pane.log`
- manager build log:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-24--10-55-25-buildbitstream-Y1TB17VGYQ32A5IQ.log`
- build config:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/config_build_f2_rocket_singlecore_nic_30mhz.yaml`
- build recipe:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/config_build_recipes_f2_rocket_singlecore_nic_30mhz.yaml`
- runtime config:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/config_runtime_f2_rocket_singlecore_nic_gdbserver_smoke_30mhz.yaml`
- hwdb:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/config_hwdb_f2_rocket_singlecore_nic_30mhz.yaml`

## 新确认的静态事实

### 1. `example_1config` 不是“无网络”的单节点路径

文件：

- `/home/ubuntu/chipyard/sims/firesim/deploy/runtools/user_topology.py`

结论：

- `example_1config()` 明确实例化了：
  - 一个 `FireSimSwitchNode`
  - 一个 `FireSimServerNode`
- 因此当前 `config_runtime_f2_rocket_singlecore_nic_gdbserver_smoke_30mhz.yaml`
  走的是带交换节点的网络拓扑，不是 `NoNIC` 风格的直连路径。

这很重要，因为它说明后续 smoke 若再次复现 `SimpleNIC` host-side token 异常，
可以继续认为问题发生在真实网络桥接/交换语义链上，而不是“因为根本没走网络”。

### 2. 上游 `main` 在 `SimpleNIC` 关键文件上没有现成修复

比对对象：

- 本地 `main`
- `upstream main` (`CaiTH0618/chipyard`, commit `3e2586e66031b46027d782d4e7c1c1245ed605cf`)

比对文件：

- `generators/firechip/goldengateimplementations/src/main/scala/SimpleNICBridge.scala`
- `generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.cc`
- `generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.h`

结论：

- `git diff main FETCH_HEAD -- <these files>` 为空。
- 也就是说，上游 `main` 在这三个文件上没有额外修复。
- 当前本地未提交 patch 仍是：
  - `SimpleNICBridge.scala` 的 lockstep 约束
  - `simplenic.cc/h` 的 host debug dump

因此，这轮 patch 不是在重复上游已有修复；如果它最终有效，属于本地新增修复点。

### 3. 当前 build 里的 `clk_wiz` 和 `Designutils 20-1280/1281` 告警不是新现象

对照日志：

- 当前 build:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-24--10-55-25-buildbitstream-Y1TB17VGYQ32A5IQ.log`
- 旧成功 `singlecore + NIC` build:
  - `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-24--04-12-04-buildbitstream-9UBROKL6UHIRL0OJ.log`

当前 build 已出现：

- `CRITICAL WARNING: [Coretcl 2-1280] The upgrade of 'clk_wiz_0_firesim' ...`
- 大量 `CRITICAL WARNING: [Designutils 20-1280] Could not find module ...`
- `CRITICAL WARNING: [Designutils 20-1281] ... cl_ddr_timing_aws.xdc will not be read ...`

对照结果：

- 这些告警在旧成功 `singlecore + NIC` build 中同样存在，且模式高度一致。
- 因此当前不能把这些告警直接解释为：
  - lockstep patch 破坏了 NIC 构建
  - 当前 build 一定会失败
  - 当前 build 一定存在新的时序风险

更保守、正确的解释是：

- 它们是这套 AWS F2 shell / FireSim 构建链上的既有噪声或常见告警；
- 真正需要关注的是后续是否进入 `place/route`，以及最终是否产出 AGFI。

### 4. 当前 build 仍处于综合阶段，尚未出现新的功能性失败证据

远端 builder：

- instance id: `i-018915b3266110482`
- type: `z1d.2xlarge`

远端 `vivado` 状态：

- `build-bitstream.sh` 持续存在
- `vivado` 主进程持续占用 CPU
- 已进入多个 `synth_design` / parallel synth worker 阶段
- 当前尚未看到：
  - `Command: opt_design`
  - `Command: place_design`
  - `Command: route_design`
  - `write_bitstream`
  - `tar2afi`

因此，到本记录时间点，当前 build 的最准确状态是：

- 正在正常综合
- 还没有进入后段实现
- 也还没有出现能直接判定“当前 patch 构建失败”的证据

### 5. 公开 issue 检索没有直接命中当前症状

通过 GitHub API 检索 `firesim/firesim` issue：

- `SimpleNIC` 相关公开命中很少
- 明确相关的只有较老的：
  - `#171 Switch mapping - support simplenic.cc communicating directly with a switch on a separate host node`
- 没有直接命中：
  - `gdbserver + NIC`
  - `ERR MISMATCH! on writing tokens in`
  - `SimpleNIC stream occupancy drift`

这意味着：

- 当前问题更像是本地分支/配置组合下的具体行为问题
- 不能指望从公开 issue 直接找到现成补丁

## 对下一步调试的约束

- 在新 AGFI 出来之前，不能把任何旧 AGFI 上的运行结果当成 `SimpleNICBridge` lockstep patch 的 FPGA 验证。
- 新 AGFI 一旦产出，下一步应优先沿最小路径执行：
  - 更新 `config_hwdb_f2_rocket_singlecore_nic_30mhz.yaml`
  - `launchrunfarm`
  - `infrasetup`
  - `runworkload`
  - workload 仍用 `rocket-singlecore-nic-gdbserver-smoke.json`
- 在新的 smoke 结果出来之前，不应该再对 `pairdummy/pipeline runtime` 主线做更多推断。

## 当前结论

- 当前证据继续支持“先在最小 `singlecore + NIC` 目标上验证 lockstep patch，再回到主线”的路径。
- 当前 build 正常推进中，但还没有到足以判断 patch 成败的阶段。
- `example_1config` 的确覆盖网络交换路径；上游 `main` 没有现成修复；当前综合告警也不是新引入异常。
