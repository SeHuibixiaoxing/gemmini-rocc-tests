# 2026-04-14T07:28:35Z 调试记录

## 本轮目标

- 复核最新 FPGA rerun，
  判断旧的 `tok=726 set_src` blocker 是否仍然成立。
- 如果旧 blocker 被穿过，
  就把 authoritative frontier 更新到新的最晚有效日志边界。
- 在不改硬件的前提下，
  继续用静态代码阅读缩小下一轮最小扰动 probe 的范围。

## 本轮执行

- 分诊最新 capture：
  - `tmp/firesim-aws-f2/captures/pairdummy-sbus128-runworkload-20260414-065809-192.168.1.112-host-watchdog-20260414T071831Z.guest-sparse-log.txt`
  - 同前缀的 `.guest-breadcrumb.bin`
  - 同前缀的 `.status.txt`
- 交叉阅读代码：
  - `src/prt_dma.c`
  - `src/prt_page_table.c`
  - `src/prt_runtime.c`
- 复核当前 fixed profile 的 probe 配置与 freshness 入口脚本。

## 新结论

- 旧的 `tok=726` / `set_src` blocker 已被新 rerun 证伪。
- 在 `tok=726..738` 上，
  `program-pre-src-rr`
  与
  `program-post-src`
  都稳定出现，
  说明当前 run 已经稳定穿过旧的 `set_src` 边界。
- 新的 authoritative frontier 位于第三次
  `stage-exec-views phase=begin segment=0 stage=0 layer=0`
  内的 fixed-load 路径。
- 最后有效 sparse 日志边界是：
  - `dma-fixed-load-wait phase=after-release token=738`
  - 缺失：
    `tok=739 phase=doneflag-end`
- 结合静态代码，
  当前最可信的软件可疑区间变为：
  `token 738 after-release`
  之后、
  `token 739 doneflag-end`
  之前。
- 该区间内只剩两类高概率候选：
  1. `dma_copy_host_to_spm_pages_linux()` 中 `page31` 的 host 侧准备，
     重点是
     `prt_host_virt_to_phys((const void *)src_ptr, &src_pa)`
  2. `dma_submit_wait_annotated_scoped()` / `prt_dma_submit()`
     里 `doneflag-end` 前的 completion-slot / token 准备
- `page31` 有一个明确静态特征：
  对应的 source VA 正好落在 host `4 KiB` 页边界，
  `src_va=...71000`。
- `program-pre-src-rr` 快照在 `tok=708..738` 上始终稳定：
  - `opc_binding=0`
  - `cfg_state=0x100`
- `/dev/mem` manager-control 观测在该 guest 中当前不可用：
  - `mgr_busy_rc=-14`
  - `rocc_busy_rc=-14`
  因此这一轮不再依赖它区分 manager/front-end 与 downstream busy。

## 下一步

1. 保持软件调试路线，
   不改 bitstream，
   不走 metasim。
2. 把 fixed-load page probe 缩到 `page31`：
   - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PAGE_START=31`
   - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PAGE_END=31`
3. 重新走 image freshness + FireSim rerun，
   看 `page31` 是否出现：
   - `phase=page-setup`
   - `phase=v2p-begin`
   - `phase=v2p-end`
   - `phase=submitwait-begin`
   - `phase=submitwait-end`
   - `tok=739 phase=doneflag-end`
4. 如果只到 `v2p-begin`，
   下一轮只补 page31 专用 `host-v2p` 细节。
5. 如果已到 `submitwait-begin` 但没有 `doneflag-end`，
   下一轮只补 `dma_submit_wait_annotated_scoped()` /
   `dma_completion_flag_acquire()` 入口边界。
