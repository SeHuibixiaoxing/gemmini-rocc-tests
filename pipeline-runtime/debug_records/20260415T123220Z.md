# 2026-04-15 12:32:20 UTC

## Round Goal

在保持低扰动配置的前提下，重新执行一轮完整的
`launchrunfarm -> infrasetup -> runworkload`，
继续确认当前真实 frontier 是否仍停在：

- `stage-fixed-load-sparse phase=begin segment=0 stage=0 slot=1 tensor=1000001`

还是会再次推进到更深的：

- `subbatch=3`
- `tensor=2`
- `export target_seq=1`
- `rr_acquire_after_call`

## Fixed Constraints

- private-IP-only SSH
- FireSim 命令前必须在
  `/home/ubuntu/chipyard/sims/firesim`
  下 `source sourceme-manager.sh --skip-ssh-setup`
- `PIPELINE_RUNTIME_MLOCKALL_MODE=2`
- `PIPELINE_RUNTIME_UART_LOG_ENABLE=0`
- `PIPELINE_RUNTIME_GUEST_LOG_ENABLE=1`
- `PIPELINE_RUNTIME_GUEST_DEEP_LOG_ENABLE=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=0`
- `PIPELINE_RUNTIME_BREADCRUMB_ENABLE=1`
- `PIPELINE_RUNTIME_BREADCRUMB_SEGMENT=0`
- `PIPELINE_RUNTIME_BREADCRUMB_GLOBAL_STAGE=0`
- `PIPELINE_RUNTIME_BREADCRUMB_LOCAL_STAGE=0`
- `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH` unset
- `PIPELINE_RUNTIME_BREADCRUMB_SUBBATCH_RADIUS=0`

## Instance Acquisition

`launchrunfarm` session:

- `pairdummy-sbus128-launchrunfarm-20260415-122843`

AWS 最终分配到的新实例：

- instance id: `i-0069afcb68a57fa9f`
- private ip: `192.168.1.225`
- type: `f2.6xlarge`
- launch time: `2026-04-15T12:32:06Z`

对应 tmux pane log：

- `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-launchrunfarm-20260415-122843.pane.log`

结论：

- 这轮 launch 在持续容量重试后成功，不再停留在 AWS capacity blocker

## Preflight / Freshness

已执行：

- `pairdummy_sbus128_workflow.sh debug-preflight`

结果：

- `debug_preflight_status=pass`

已确认 image freshness / guest env：

- `firemarshal-entrypoint` OK
- `fileonly-wrapper` OK
- `runner-script` OK
- `runtime-binary` OK
- `firemarshal-env` OK
- `breadcrumb_subbatch=any`

本轮 local image sha256：

- `5d92f20d277defd4e9b0bcf961a00f8476b9005c05b06a8d20f69f00548eb615`

## Infrasetup

已执行：

- session: `pairdummy-sbus128-infrasetup-20260415-123300`

关键结果：

- host driver bundle 重新部署
- slot 0 clear 成功
- slot 0 flash 成功
- AGFI: `agfi-0dc8dcfa4c7735f40`
- driver readiness preflight passed

对应 pane log：

- `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-infrasetup-20260415-123300.pane.log`

## Run Start

已执行 remote freshness：

- remote image path:
  `/home/ubuntu/sim_slot_0/rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy0-rerocc-lc-linux-coupleddma-bertmini-pipeline-runtime-batch8-fileonly-sync-pairdummy.img`
- remote sha256:
  `5d92f20d277defd4e9b0bcf961a00f8476b9005c05b06a8d20f69f00548eb615`
- local / remote sha256 一致

`runworkload` session：

- `pairdummy-sbus128-runworkload-20260415-123617`

对应 pane log：

- `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260415-123617.pane.log`

## Next Step

继续通过 private IP `192.168.1.225` 做 live 监控，优先检查：

1. boot 是否进入 `/etc/init.d/S99run`
2. `/root/pipeline-runtime-debug` 是否出现
3. `bertmini-batch8.log`
4. `bertmini-batch8.runner.stage`
5. `bertmini-batch8.runner-proc.stage`
6. `bertmini-batch8.breadcrumb.bin`

重点判断本轮真实 frontier 是否仍停在 `tensor=1000001` fixed-load begin。

## Live Monitoring Notes

### 2026-04-15 12:38:47 UTC

首次低频轮询：

- `uart_size=7481`
- `heartbeat_last=2426927569, 140`
- `/root/pipeline-runtime-debug` 尚不存在

此时 `uartlog` 仍停在 Linux 早期内存 / page allocator 初始化附近，
不能据此认定出现新的 runtime blocker。

### 2026-04-15 12:39:47 UTC

第二次低频轮询：

- `uart_size=7481`，与第一次相同
- `heartbeat_last=3421217572, 196`
- `/root/pipeline-runtime-debug` 仍不存在

结论：

- 仿真心跳继续前进，但 `uartlog` 暂时没有新增文本
- 更像 boot 阶段串口静默窗口，而不是回退到旧的 runtime 卡点

### 2026-04-15 12:40:48 UTC

第三次低频轮询：

- `uart_size=13771`
- `heartbeat_last=4604119221, 260`
- `uartlog` 已推进到网络栈初始化：
  - `NET: Registered PF_INET6 protocol family`
  - `Run /init as init process`
  - `Mounting /dev/iceblk as root device`
- `/root/pipeline-runtime-debug` 仍不存在

结论：

- boot 在继续推进
- 本轮没有出现“前移到 hugetlb”这类早期 blocker

### 2026-04-15 12:42:10 UTC

即时检查：

- `uart_size=15654`
- `heartbeat_last=6018821251, 336`
- `uartlog` 已进入 init 脚本阶段：
  - `running /etc/init.d/S01syslogd`
  - `running /etc/init.d/S02klogd`
  - `running /etc/init.d/S02sysctl`
  - `running /etc/init.d/S10mdev`
  - `running /etc/init.d/S40network`
- `/root/pipeline-runtime-debug` 仍不存在

当前判断：

- guest 仍在正常 boot
- 尚未进入 `/etc/init.d/S99run`
- 尚未出现任何 pipeline runtime 主程序级别证据

### 2026-04-15 12:43:06 UTC

host watchdog 首次观测到：

- `guest-status arm observed`

这说明 `/etc/init.d/S99run` 之后的 wrapper 已开始运行，
不能再只看 `uartlog`。

### 2026-04-15 12:44:49 UTC

改用 live image `debugfs` 直接读取 guest 文件系统后，
确认 file-only 观测面全部正常工作：

- `/root/pipeline-runtime-debug/`
- `bertmini-batch8.log`
- `bertmini-batch8.runner.stage`
- `bertmini-batch8.runner-proc.stage`
- `bertmini-batch8.wrapper.stage`
- `bertmini-batch8.wrapper-proc.stage`
- `bertmini-batch8.status`
- `bertmini-batch8.breadcrumb.bin`

说明此前“SSH 看不到 `/root/pipeline-runtime-debug`”只是因为走错了观测面：

- SSH 看到的是 F2 host rootfs
- guest 运行时文件必须通过 live image `debugfs` 读取

### 2026-04-15 12:45:44 UTC

live image 首次到达 runtime 深层窗口：

- `bertmini-batch8.log` size=`319488`
- `bertmini-batch8.breadcrumb.bin` size=`7272`
- `runner.stage` 仍只到 `after-bin-spawn`
- `status=running`

主日志尾部已推进到：

- `conv-sync-strided ... mgr=7 flushed`
- `export-target-dispatch stage=0 tensor=2 target_seq=0 ... phase=end`
- `export-target-dispatch stage=0 tensor=2 target_seq=1 ... target=address2 ... phase=begin`
- `rr-acquire-wrap phase=after-call stage=0 manager=0 opcode=2 rc=0 cfg=0 valid=1`

这表明本轮已经实质越过了：

- `tensor=1000001 fixed-load begin`

并重新回到更深的：

- `segment=0`
- `stage=0`
- `tensor=2`
- `target_seq=1`
- `target=address2`

### 2026-04-15 12:46:44 UTC

第二次 live image 轮询：

- heartbeat: `11357830472, 616`
- `bertmini-batch8.log` 仍为 `319488`
- `runner.stage / runner-proc.stage / breadcrumb.bin` 大小均不变
- 主日志最后一条仍是：
  - `rr-acquire-wrap phase=after-call stage=0 manager=0 opcode=2 rc=0 cfg=0 valid=1`

### 2026-04-15 12:47:44 UTC

第三次 live image 轮询：

- heartbeat: `12481560489, 673`
- 上述所有 guest 文件仍完全不变

由此可以把当前稳定 blocker 记为：

- `seg=0 / stage=0 / sb=3 / tensor=2`
- `export target_seq=1`
- `target=address2`
- 稳定停在 `rr_acquire_after_call`

### Breadcrumb Decode

本地抓回：

- `/home/ubuntu/chipyard/tmp/firesim-aws-f2/live/pairdummy-sbus128-runworkload-20260415-123617/bertmini-batch8.breadcrumb.20260415T124904Z.bin`

解码结果：

- `update_count=13764`
- `last_kind=rr`
- `last_phase=rr_acquire_after_call`
- `seg=0 gstage=0 lstage=0 sb=3 mgr=0`
- `line=332`

同时 `all_slots` 明确显示：

- `sb=3 tensor=2 page=63` 的 DMA page/end 和 submitwait cleanup 已经都做完
- 最后才停在新的 `rr_acquire_after_call`

因此本轮 authoritative 结论是：

1. 旧的 `sb=3 page63` blocker 已再次越过
2. 旧的 `tensor=1000001 fixed-load begin` 不是本轮真实前沿
3. 当前真实稳定前沿重新收敛到 `address2 export`

### Static Narrowing

静态对照：

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_runtime.c:1858`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c:706`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c:839`

当前 `target_seq=1 / address2` 的最后日志是 RR scope acquire 成功返回。
也就是说当前 gap 被压缩到：

- `prt_rr_acquire_scope(...)` 成功返回之后
- `dma-export-host phase=v2p-begin` 之前

即：

- `dma_copy_spm_pages_to_host_linux(...)`
  的第一页 / 第一 chunk 进入处

### Next Step

下一轮不直接加宽日志，而采用单变量 observability rerun：

- `PIPELINE_RUNTIME_DEBUG_TRIGGER_ENABLE=1`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=dma-export`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_SEGMENT=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_GLOBAL_STAGE=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_LOCAL_STAGE=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_SUBBATCH=3`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_MANAGER=0`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_TENSOR_ID=2`
- `PIPELINE_RUNTIME_DEBUG_TRIGGER_PAGE=63`

目标：

- 让 trigger 在 `target_seq=0 page63` 末尾附近 arm
- 用较小 post-budget 跨过 `target_seq=0 -> target_seq=1` 边界
- 判断 `target_seq=1` 是否至少到达 `dma-export pset / v2p-begin`
