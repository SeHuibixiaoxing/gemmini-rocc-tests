# 2026-05-05 21:55:57 UTC - noTrace cfg32 NIC driver bundle prepared

## Goal

Prepare the local driver bundle that `infrasetup` will need if the active
noTrace cfg32 NIC TIMING bitstream produces an AGFI. FireSim skips local driver
builds when the selected HWDB entry has a non-null `driver_tar`, so a missing
tarball would otherwise become an avoidable post-build blocker.

## Context

- Active AGFI candidate:
  `pairdummy-cfg32-nic-notrace-20260505T171453Z`
- Target config:
  `FireSimGemminiReRoCCPairDummy16x16C4P12Sbus128NICNoTraceConfig`
- Platform config:
  `FRFCFS16GBQuadRank_BaseF2Config`
- Current bitstream state:
  still in Vivado placement; no AGFI/AFI yet.

The local noTrace driver binary already existed:

```text
sims/firesim/sim/output/f2/f2-firesim-FireSim-FireSimGemminiReRoCCPairDummy16x16C4P12Sbus128NICNoTraceConfig-FRFCFS16GBQuadRank_BaseF2Config/FireSim-f2
```

## Command

Created a local `driver-bundle.tar.gz` with the same member shape as existing
F2 bundles:

```text
FireSim-f2
libgcc_s.so.1
libz.so.1
libelf.so.1
libriscv.so
libdwarf.so.1
libstdc++.so.6
libgmp.so.10
```

The libraries were copied from the current conda/RISC-V toolchain paths used by
`ldd FireSim-f2`, matching the convention of existing FireSim F2 driver bundles.

## Result

Generated file:

```text
sims/firesim/sim/output/f2/f2-firesim-FireSim-FireSimGemminiReRoCCPairDummy16x16C4P12Sbus128NICNoTraceConfig-FRFCFS16GBQuadRank_BaseF2Config/driver-bundle.tar.gz
```

Checksums:

```text
FireSim-f2 sha256:
20e849403100a5f5c44da64bb00a6c736818c9680de0109ea007424a77d4d945

driver-bundle.tar.gz sha256:
a6380ebc1b3050a78ec49a950214f318c1c73bde345c0da786f111433bd86249
```

The generated tarball is a transient/generated artifact and was not committed.

## Intended HWDB update if AGFI succeeds

If the noTrace build succeeds, update:

```text
sims/firesim/deploy/config_hwdb_f2_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_bertmini_cfg32_nic.yaml
```

so the existing `firesim_gemmini_rerocc_pairmanager_dummy16x16_4c12p12_sbus128_cfg32_nic`
entry points at the new noTrace AGFI and this driver bundle path:

```text
/home/ubuntu/chipyard/sims/firesim/sim/output/f2/f2-firesim-FireSim-FireSimGemminiReRoCCPairDummy16x16C4P12Sbus128NICNoTraceConfig-FRFCFS16GBQuadRank_BaseF2Config/driver-bundle.tar.gz
```

The runtime config can continue to use the same `default_hw_config` key; the key
name is the workload-facing cfg32 NIC slot, while the HWDB values determine the
actual AGFI and driver bundle.

## Limitation

This does not prove the noTrace bitstream will succeed. It only removes a
known local packaging dependency for the `infrasetup` step after an AGFI is
available.
