# 20260505T084735Z - local switch marker and checksum tests

## Context

The 1BP non-8BP Rocket NIC no-TraceIO 30 MHz F2 builds were still running.
While waiting for Vivado route/place results, I ran small local switch-level
tests that do not require a new AGFI.

Target recovery goal remains matching the old AGFI
`agfi-0079cbbca617eca4e` remote `gdbserver` software-breakpoint smoke matrix.

## Test 1: ShmemPort compressed-empty marker behavior

Added local test:

`sims/firesim/target-design/switch/tests/shmemport_marker_test.cc`

Command:

```sh
g++ -std=gnu++11 -Wall -Wextra \
  -I /home/ubuntu/chipyard/sims/firesim/target-design/switch \
  -o /tmp/shmemport_marker_test \
  /home/ubuntu/chipyard/sims/firesim/target-design/switch/tests/shmemport_marker_test.cc
/tmp/shmemport_marker_test
```

Result:

```text
PASS shmemport_marker_test
```

Coverage:

- `ShmemPort::send()` clears `0xDEADBEEFDEADBEEF` compressed-empty marker
  before publishing a shmem round.
- `ShmemPort::send()` preserves an ordinary non-marker header.
- `ShmemPort::send()` sets the poll byte after publishing.
- `ShmemPort::tick()` clears the consumed input poll byte.

Warnings:

- The compile emits existing `shmemport.h` warnings about assigning string
  constants to `char *`. This test did not change that behavior.

## Test 2: SSHPort repair regression

Re-ran the existing local switch repair test:

```sh
g++ -std=gnu++11 -Wall -Wextra \
  -I /home/ubuntu/chipyard/sims/firesim/target-design/switch \
  -o /tmp/sshport_repair_test \
  /home/ubuntu/chipyard/sims/firesim/target-design/switch/tests/sshport_repair_test.cc
/tmp/sshport_repair_test
```

Result:

```text
PASS sshport_repair_test
```

Coverage:

- ARP sender hardware address repair.
- GDB TCP payload IPv4/TCP checksum repair.
- Non-GDB TCP payloads are left untouched.
- Zero-payload TCP control checksum repair.

## Interpretation

The local switch-side repairs still hold while the bitstream builds are
running. These tests do not validate F2 timing or the SimpleNIC RTL token
adapter, but they reduce the chance that the current failure is caused by a
regression in host TAP egress repair or shmem compressed-empty marker handling.
