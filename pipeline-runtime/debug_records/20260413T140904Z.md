# 2026-04-13T14:09:04Z 调试记录

## 本轮目标
- 接着 `pairdummy-sbus128-fixed-v8` 这轮 capture，确认 `v8` 的真实新前沿，而不是继续被 sparse 文本尾部误导。
- 在不扩大日志扰动的前提下，把下一轮定位点收紧到 `shared fence` 前后。

## 本轮使用的现成证据
- host watchdog:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-133037.host-watchdog.log`
- pane log:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-133037.pane.log`
- capture 目录前缀:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/pairdummy-sbus128-runworkload-20260413-133037-192.168.1.21-host-watchdog-20260413T135058Z.*`

## 运行态结论
- `v8` 这轮 live run 已经结束，不再是运行态：
  - host watchdog 在 `2026-04-13T13:50:58Z` 触发 idle timeout
  - 随后自动抓取 guest 文件并调用 `terminaterunfarm --forceterminate`
- 这轮 run 的 host watchdog 进展是：
  - `guest_sparse=204091 -> 211464 -> 516096`
  - 之后 heartbeat 继续前进，但 guest 文件大小不再增长

## 对 capture 的重新解读
- 单看 `guest-sparse-log.txt` 尾部，会误以为最后停在：
  - `dma-export-submit ... tok=1165 ... phase=program-post-fence`
- 但 `guest-breadcrumb.bin` 的最后有效状态更晚，而且更可信：
  - `last_kind=dma`
  - `last_phase=dma_wait_after_fence`
  - `seg=0 gstage=0 lstage=0 sb=3`
  - `tensor=2 tok=1168 mgr=0 page=11`
  - `src=0x40302400 dst=0x103ebd000 aux0(done_pa)=0x102df3000`
- 因此这次真正的新前沿不是 `tok=1165/page=8` 的 submit 内部，而是：
  - `stage=0`
  - `subbatch=3`
  - `tensor=2`
  - export `target_seq=1`
  - `page=11`
  - `dma_wait_after_fence`
  之后、`after-shared-fence` 之前。

## 对当前卡点的静态判断
- 代码路径：
  - `src/prt_dma.c:2649` 已经执行 `hw_dma_fence()`
  - 紧接着记录 `DMA_WAIT_AFTER_FENCE`
  - 然后进入 `tok->rr_scope_external` 分支
  - 调用 `dma_token_fence_scope(tok)`，其内部调用 `prt_rr_fence_scope(&scope)`
- 当前最可疑的区间已经收敛到：
  - `dma_wait_after_fence`
  - 到
  - `rr_fence_scope()` 返回
  之间
- 这与更早一轮记录 `20260413T075122Z.md` 里“还是停在 `rr_fence_scope()`”的怀疑一致，但这次证据更直接，因为 breadcrumb 不受 stdio 缓冲影响。

## 本轮实质性修改
- 新增一个低扰动 breadcrumb phase：
  - `PRT_BREADCRUMB_PHASE_DMA_WAIT_BEFORE_SHARED_FENCE = 117`
- 在 `dma_blocking_wait()` 的 `tok->rr_scope_external` 分支里，调用 `dma_token_fence_scope(tok)` 之前先记 breadcrumb。
- 这样下一轮如果再次卡住，就能在 breadcrumb 上区分：
  - 停在 `after-fence` 之后但还没进 shared fence
  - 还是已经进入 shared fence 但没返回
- 同时把固定 profile 从 `v8` 调到 `v9`，并把 export page probe window 从：
  - `8..16`
  收紧到：
  - `10..12`
  以减少无效 sparse 文本输出。

## 本轮没有改动的部分
- 没有回退 `MLOCKALL_MODE=2`
- 没有改动 hugetlb / 物理连续页路径
- 没有退回 doneflag 轮询等待
- 没有把主观测面改回 `uartlog`

## 下一步
- 按固定 workflow 重新做：
  - `image-closure`
  - `launch`
  - `infrasetup`
  - `run`
- 继续要求：
  - 编译/FireSim 前先进入 `sims/firesim` 并 `source sourceme-manager.sh --skip-ssh-setup`
  - freshness 必须重新校验
  - SSH/live 检查只用私网 IP
- 新一轮重点看：
  - breadcrumb 是否最终停在 `dma_wait_before_shared_fence`
  - 若是，则基本坐实卡在 `dma_token_fence_scope()` / `prt_rr_fence_scope()`
