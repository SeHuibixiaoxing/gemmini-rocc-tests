# 2026-04-16 02:36:15 UTC

## Round Summary
- Source of evidence: watchdog capture from the rerun started at `pairdummy-sbus128-runworkload-20260416-021039`
- Capture prefix:
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/captures/pairdummy-sbus128-runworkload-20260416-021039-192.168.1.246-host-watchdog-20260416T023103Z`
- This round added no functional code change. It finalized the previous rerun by waiting for timeout capture and extracting the stable frontier.

## Watchdog Outcome
- Host idle timeout fired at `626s`
- Watchdog captured:
  - `guest-breadcrumb.bin`
  - `guest-sparse-log.txt`
  - `guest-trigger-log.txt`
  - `guest-runner-proc-stage.txt`
  - `status.txt`
  - `heartbeat.csv`
  - `uartlog.txt`
- The watchdog then force-terminated the run farm automatically.

## Stable Frontier From Capture
- Trigger log still only covers the narrow fixed-load window and stops at:
  - `sb=6 page=59 wf-e`
- Breadcrumb remains consistent with the previous authoritative DMA frontier:
  - `sb=6 tensor=0 page=63 dma_page_end`
  - latest visible breadcrumb slot: `rr_release_end`
- The decisive new evidence is the sparse log tail:
  - current iteration reaches
    `stage-fixed-load-sparse phase=end ... slot=1 tensor=1000001 rc=0`
  - then enters
    `spm-xlate-flush mgr=0..5`
  - managers `0..4` fully complete:
    - `issue-begin/end`
    - `fence-begin/end`
    - `release-begin/end`
    - `restore-begin/end`
    - `spm-xlate-flush-restore`
  - manager `5` stops exactly at:
    - `spm-xlate-release mgr=5 cfg=15 phase=restore-begin prev_opc3=0x1`
  - no later `restore-end` appears for manager `5`
  - managers `6` and `7` of this same iteration never begin

## Conclusion
- The current best blocker is no longer the fixed-load DMA wait path.
- The stable blocker has moved later to the stage-0 SPM-translation flush path.
- Most precise current statement:
  - runtime reaches `subbatch=6`
  - fixed-load for `slot=1 tensor=1000001` returns successfully
  - stage-0 `spm-xlate` flush completes on managers `0..4`
  - then stalls at manager `5` during `restore-begin -> restore-end`
  - practical frontier:
    `spm-xlate-release mgr=5 cfg=15 phase=restore-begin`

## Supporting Details
- Sparse log line numbers near the tail:
  - `3964`: `spm-xlate-flush mgr=5 cfg=15 prev_opc3=0x1`
  - `3965`: `issue-begin`
  - `3966`: `issue-end`
  - `3967`: `fence-begin`
  - `3968`: `fence-end`
  - `3969`: `release-begin`
  - `3970`: `release-end`
  - `3971`: `restore-begin`
  - missing: `restore-end`
- This is consistent with the coarse guest log truncation seen live before timeout.

## Triage Notes
- `triage_prt_capture.py` was attempted with the capture prefix path directly and failed because the script expects a different input form. Manual inspection was used instead for this round.
- The runner/proc stage file was not useful for this stall; it only preserved the early poll snapshot and did not expose a later blocking syscall boundary.

## Next Step
- Add a minimal observability-only trigger family for `spm-xlate` so the next rerun can focus directly on:
  - `segment=0`
  - `stage=0`
  - `subbatch=6`
  - `manager=5`
  - `cfg/token=15`
- Rebuild the image through the fixed `pairdummy_sbus128_workflow.sh image-closure` path, verify freshness, then rerun with the new `spm-xlate` trigger overlay.
