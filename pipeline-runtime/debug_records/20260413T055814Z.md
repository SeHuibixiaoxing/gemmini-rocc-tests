# 2026-04-13T05:58:14Z

## Scope
- Continue live debugging for `12-pair sbus128 dummy-model` on FireSim.
- Session: `pairdummy-sbus128-runworkload-20260413-055429`
- Instance: `i-08f68317810021025`
- Private IP: `192.168.1.135`

## Hard Constraints Reconfirmed
- SSH and live inspection must use the private IP only.
- FireMarshal must run through `scripts/firemarshal-tmux-run.sh`.
- FireSim manager must run through `scripts/firesim-tmux-run.sh`.
- Before any compile / FireMarshal / FireSim action: `cd sims/firesim && set +u; source sourceme-manager.sh --skip-ssh-setup; set -u`.
- Main observation surface is guest file logging, not `uartlog`.
- Do not poll DMA `doneflag` as completion criterion; keep fence-based semantics.
- Do not replace physically contiguous hugetlb-backed page-table memory with normal anonymous pages.
- Freshness closure must be checked before reruns.
- Each debugging round gets a new timestamped record file.

## Current Live Observation
- Host watchdog active; manager shows the simulation is still running.
- `heartbeat.csv` advanced to `3987073822, 228`.
- `/home/ubuntu/sim_slot_0/uartlog` still has 116 lines and remains in early Linux boot.
- Guest-side pipeline-runtime files are not present yet; rootfs execution has not reached the wrapper stage.

## Immediate Next Step
- Keep monitoring until guest enters rootfs/init stage.
- Once `/root/pipeline-runtime-debug` appears, switch primary observation to guest file logs and verify the new sparse fixed-load probes around `tensor=1000001`.

## Progress Update
- Guest did enter `/root/pipeline-runtime-debug`; primary observation moved to ext4 guest file logs through `debugfs`.
- The earlier concern that execution had regressed to early YAML/model load was disproved. The run advanced through:
  - wrapper / runner startup
  - pipeline YAML load and validation
  - large layer-mapping YAML read and parse
  - runtime binary entry
  - stage 0 compute and repeated export attempts
- The fixed-load `tensor=1000001` window was crossed; the live frontier moved past that earlier suspect window.

## Corrected Live Frontier
- A transient reading made it look like `stage=0 tensor=2 page=51/64 submitwait-begin` was the first blocker.
- Deeper log inspection corrected this:
  - subbatch 2 and 3 both fully crossed page 51 and completed both export targets
  - the stable freeze is later, at subbatch 4, `stage=0 tensor=2`, first export target (`target_seq=0`)
  - latest stable tail remains at:
    - `export-target-dispatch ... target_seq=0 ... phase=begin`
    - `dma-export-host ... page=51/64 ... phase=page-submitwait-begin`
  - no corresponding `page-submitwait-end` has appeared for the current subbatch
- Host watchdog confirms this is a real file-log freeze, not a stale read:
  - heartbeat keeps advancing
  - `guest_sparse` and `guest_checkpoint` sizes have stayed fixed for multiple 30 s windows

## Static Diagnosis
- The outer `dma-export-host ... page-submitwait-begin/end` logs only bracket `dma_submit_wait_annotated_scoped()`.
- For ordinary export traffic (`tensor=2`), inner submit/wait visibility was too weak:
  - fixed-load checkpoints only cover `tensor>=1000000`
  - ordinary submit inner logs require `PIPELINE_RUNTIME_DMA_SUBMIT_TRACE_ENABLE=1`
  - the fixed workflow currently keeps that trace disabled
- That meant the live frontier was repeatedly truncated at the outer page probe boundary, without visibility into:
  - RR scope state install
  - done-flag allocation
  - DMA programming
  - blocking fence wait

## Code Change In Progress
- File changed:
  - `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_dma.c`
- Added a low-perturbation inner trace path for `stage=0`, `tensor=2`, `bytes<=1024`:
  - helper: `dma_should_sparse_export_submit_probe_tok()`
  - new log family:
    - `dma-export-submit ... phase=enter`
    - `... after-rr-postcheck`
    - `... after-rr-state-install`
    - `... after-doneflag-clear`
    - `... after-doneflag-acquire`
    - `... doneflag-end`
    - `... program-begin`
    - `... program-post-fence`
    - `... program-post-dst`
    - `... program-post-src`
    - `dma-export-wait ... after-fence`
    - `... after-shared-fence`
    - `... after-release`
- This is intended to locate whether the freeze is in:
  - done-flag slot acquisition
  - DMA programming
  - hardware fence completion
  - RR shared-scope fence/release

## Rebuild / Freshness
- Completed fixed workflow:
  - `terminate`
  - `image-closure`
- Confirmed new local image freshness after rebuild:
  - runtime-binary sha256:
    `f47d62e291f439dc74ba3c75ac2fd150f141c6dff134b1c4febf9d51f40ee2c5`
  - firemarshal-entrypoint sha256:
    `0d3aa9398bfbf0fa6bec0a269be9669261ba1e9b5c48e3be6528a31c94e6366e`
  - runner-script sha256:
    `691192544e250c7e3d509a4015136ac49dec2c8cc41e2d9c9ec9cd04de52efc0`
  - firemarshal-env sha256:
    `09703404918c9e4725d113ca4de3e6d09e23c0a8c4584476c21b04e7f38c79a1`
- Old instance `i-08f68317810021025` was terminated successfully before rebuild / relaunch.

## Relaunch Status
- `launchrunfarm` restarted through the fixed workflow.
- Current blocker is temporary AWS capacity shortage for `f2.6xlarge`; FireSim manager is retrying automatically across the configured subnets.

## Capacity Recovery And New Run
- FireSim manager eventually obtained new capacity and launched:
  - instance: `i-01ef621546a455e1a`
  - private IP: `192.168.1.225`
- `infrasetup` completed successfully on the new host.
- Remote freshness over the private IP passed:
  - remote image sha256 matched local image sha256:
    `906266bb7d15aa7d1b4f6ccf908f46bac2f8e04b393951685511402a63013d70`
  - runtime binary inside the remote image matched the rebuilt local binary:
    `f47d62e291f439dc74ba3c75ac2fd150f141c6dff134b1c4febf9d51f40ee2c5`
- New workload run launched:
  - session: `pairdummy-sbus128-runworkload-20260413-062048`
  - manager log:
    `/home/ubuntu/chipyard/sims/firesim/deploy/logs/2026-04-13--06-20-49-runworkload-8D74FIQ2SBP99HED.log`
  - watchdog log:
    `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260413-062048.host-watchdog.log`

## New Live Status
- `sim-run.sh` has started on `192.168.1.225`.
- Current guest observation is still in early Linux boot:
  - `uartlog` line count observed: `95`
- Primary next step remains:
  - wait until `/root/pipeline-runtime-debug` appears in the guest image
  - then switch back to guest file logs and inspect the newly added
    `dma-export-submit` / `dma-export-wait` inner probes
