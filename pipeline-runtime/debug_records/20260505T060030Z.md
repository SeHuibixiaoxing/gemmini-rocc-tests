调试类别：gdbserver / NIC / F2 timing

# CONGESTION 1bp NIC build completed, but produced a timing-violated AGFI

日期：2026-05-05T06:00:30Z

## 对象

本轮是并行对照构建，不是最终候选：

- target config：`FireSimRocketNICNoTraceConfig`
- platform config：`BaseF2Config`
- frequency：30 MHz
- build strategy：`CONGESTION`
- 类型：single-core Rocket + NIC + no TraceIO + 1bp；不是 8BP
- tmux session：`rocket-singlecore-nic-congestion1bp-build-20260505-0405`
- build host：`i-084f7d7655892c3ca` / `192.168.1.175`
- build command：

```sh
cd /home/ubuntu/chipyard/sims/firesim
../../scripts/firesim-tmux-run.sh \
  --session-name rocket-singlecore-nic-congestion1bp-build-20260505-0405 \
  buildbitstream \
  -b config_build_f2_rocket_singlecore_nic_notrace_congestion1bp_30mhz.yaml \
  -r config_build_recipes_f2_rocket_singlecore_nic_notrace_congestion1bp_30mhz.yaml
```

## 结果

FireSim manager exitcode: **0**.

Produced:

- AGFI：`agfi-0e111a637cf0834a3`
- AFI：`afi-0fa6273c9f8c65423`
- AFI state：`available` at `2026-05-05T05:46:13Z`
- build result：
  `sims/firesim/deploy/results-build/2026-05-05--04-03-44-firesim_rocket_singlecore_nic_notrace_congestion1bp_30mhz/`
- built HWDB：
  `sims/firesim/deploy/built-hwdb-entries/firesim_rocket_singlecore_nic_notrace_congestion1bp_30mhz`

However, the generated checkpoint is explicitly timing-violated:

```text
...post_route.VIOLATED.dcp
Detected a post-route DCP with timing failure for AFI creation. Design functionalities are NOT guranteed.
```

The final check found both setup and hold violations:

```text
negative setup: WRAPPER/RL_SHIM/PIPE_RST_OUT_N/... -> WRAPPER/CL/ddr_ready_pre_sync_meta_reg/CLR
negative hold:  WRAPPER/CL/ocl_fsim_ar_fire_count_reg[6]/C -> WRAPPER/RL_SHIM/PIPE_STATUS1/pipe_reg[0][22]/D
```

Route also emitted the same hold-bailout warning seen in earlier failed paths:

```text
WARNING: [Route 35-514] Design has a large number of hold violators...
Router is turning off hold fixing.
```

Final post-route/physopt timing summary remained poor:

```text
WNS=-3.470 | TNS=-4654.226 | WHS=-3.991 | THS=-4041.924
```

## Interpretation

This AGFI is **not** a clean hardware candidate for the gdbserver capability
matrix. It may be useful only as a classification artifact for the old failure
mode: `.VIOLATED.dcp` plus PIPE_STATUS/OCL-related timing paths.

I did not run remote `gdbserver` on this AGFI before this record because a
failure would not distinguish software from timing-violated hardware. The main
candidate remains the conservative `TIMING_HOLDFIX` build, which removes the
`cl_sh_status*` debug fanout and applies `route.enableHoldExpnBailout 0`.

## Evidence

Committed small artifacts:

- `debug_records/artifacts/20260505T060030Z_congestion1bp_violated_build/AGFI_INFO`
- `debug_records/artifacts/20260505T060030Z_congestion1bp_violated_build/built-hwdb-entry.yaml`
- `debug_records/artifacts/20260505T060030Z_congestion1bp_violated_build/build-tmux.metadata`
- `debug_records/artifacts/20260505T060030Z_congestion1bp_violated_build/build-tmux.exitcode`
- `debug_records/artifacts/20260505T060030Z_congestion1bp_violated_build/build-key-lines.txt`
- `debug_records/artifacts/20260505T060030Z_congestion1bp_violated_build/post_route_timing_head.rpt`
- `debug_records/artifacts/20260505T060030Z_congestion1bp_violated_build/describe-fpga-image.json`
- `debug_records/artifacts/20260505T060030Z_congestion1bp_violated_build/SHA256SUMS.txt`

Large generated collateral intentionally not committed:

- full build result tree
- 90 MB `.post_route.VIOLATED.dcp`
- full 1 MB+ timing reports
- full 1.8 MB tmux pane log

## Concurrent builds still running

At this checkpoint:

- ordinary `TIMING` comparison build is in `route_design` and already shows
  large negative WNS/WHS during route initialization.
- conservative `TIMING_HOLDFIX` build is in pre-route `phys_opt_design`.

Next key decision remains the `TIMING_HOLDFIX` result.
