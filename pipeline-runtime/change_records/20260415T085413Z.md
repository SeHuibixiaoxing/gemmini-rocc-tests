# 2026-04-15T08:54:13Z Observability Rerun Change

## Change Type
- Observability-only rerun configuration update
- No source code change
- No semantic/runtime algorithm change

## Motivation
- Previous run `pairdummy-sbus128-runworkload-20260415-080412` enabled a temporary debug trigger at:
  - `kind=dma-export`
  - `segment=0`
  - `global_stage=0`
  - `local_stage=0`
  - `subbatch=3`
  - `manager=0`
  - `tensor=2`
  - `token=1173`
  - `post_budget=48`
- That trigger budget exhausted around `tok=1179`, while breadcrumb frontier for the same run reached `page=60 / tok=1217 / dma_doneflag_end`.
- Therefore the trigger window was materially too early and did not cover the suspected hang boundary.

## Planned Rerun Adjustment
- Keep the same family and same fixed profile.
- Move the temporary trigger from token-based early capture to page-local capture near the current frontier:
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=dma-export`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_SEGMENT=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_GLOBAL_STAGE=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_LOCAL_STAGE=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_SUBBATCH=3`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_MANAGER=0`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_TENSOR_ID=2`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_PAGE=60`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_TOKEN=''`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_PRE_RING=8`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_POST_BUDGET=24`
  - `PIPELINE_RUNTIME_DEBUG_TRIGGER_MATCH_ONCE=1`

## Constraints Preserved
- SSH / remote freshness still uses private IP only.
- Guest logging remains file-only; no UART logging.
- `PIPELINE_RUNTIME_MLOCKALL_MODE=2` unchanged.
- `spm_xlate_enable=1 -> blocking_fence` behavior unchanged.
- No `doneflag` polling reintroduced.
- No runtime foreground `sync` added.
- FireSim flow remains `launchrunfarm -> infrasetup -> runworkload`.

## Required Workflow
1. Export the trigger env overrides.
2. Run `pairdummy_sbus128_workflow.sh debug-preflight`.
3. Run `pairdummy_sbus128_workflow.sh launch`.
4. Run `pairdummy_sbus128_workflow.sh infrasetup`.
5. Run `pairdummy_sbus128_workflow.sh run`.

## Expected Outcome
- Trigger log should arm near the current suspected frontier instead of exhausting early.
- If page60 truly is the live boundary, the next run should show whether execution reaches:
  - later page60 export phases,
  - page61 setup,
  - wait-after-fence / shared-fence / release,
  - or no further progress after the page60 submit path.
