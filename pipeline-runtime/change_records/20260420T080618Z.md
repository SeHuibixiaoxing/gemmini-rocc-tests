# Change Record 20260420T080618Z

调试类别：main

## 目标

- 把 `g6 bertmini` 最新 FPGA 复跑结论写回主状态文档和会话提示。

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260420T080618Z.md`

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/CURRENT_STATUS.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/NEXT_SESSION_PROMPT.md`

## 修改内容

- 更新顶部 authoritative override：
  - `host-watchdog` 误杀已在真实 FPGA 复跑中排除
  - 当前真实 stall 已收敛到
    `subbatch=3 / mgr=2 / rr_acquire_after_call`
    附近
- 同步补充：
  - 两次 live snapshot 完全一致
  - `idle=625s` 以及更高的 `688s / 907s`
    都没有再触发 host terminate
  - 这轮 `runworkload exit 1`
    是主动 `terminaterunfarm` 后的 manager 收尾副作用

## 原因

- 当前主线的 blocker 已经发生变化：
  不再是 host timeout 语义，
  而是 guest 内部真实 stall。
- 若不及时更新顶部 override，
  下一次接手时仍会把精力浪费在
  “watchdog 是否还会误杀”
  上。

## 验证

- 对照：
  - `/home/ubuntu/chipyard/tmp/firesim-aws-f2/tmux/pairdummy-sbus128-runworkload-20260420-073931.host-watchdog.log`
  - `/home/ubuntu/chipyard/tmp/pipeline-runtime-live/20260420T080131Z`
  - `/home/ubuntu/chipyard/tmp/pipeline-runtime-live/20260420T080430Z`

## 风险 / 后续

- 当前 frontier 仍然是软件级 / breadcrumb 级，
  还不是“哪一条硬件指令”的最终结论。
- 下一轮若要继续收紧，
  应直接围绕
  `rr_acquire_after_call`
  邻域做更窄的 probe。
