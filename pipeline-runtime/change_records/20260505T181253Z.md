# 2026-05-05T18:12:53Z - Record stage resource contract microtest

类别：`pipeline-runtime` / static-contract / cfg32-nic-build-wait

## 变更

- 新增 debug record：
  - `debug_records/20260505T181253Z_stage_resource_contract_microtest.md`
- 在等待 `cfg32_nic` 和 `cfg32_nic_notrace` bitstream 期间，记录一轮软件/静态合同验证：
  - `pipeline-runtime` clean rebuild 通过；
  - `ours2/gemini2/tangram2` artifact audit 通过；
  - `--hw-validate-only` 在 4 core / 12 Gemmini / 12 DMA / pair-manager /
    1024-byte SPM page / SPM xlate enabled 配置下通过；
  - stage-resource YAML 微测试确认当前三组 bertmini mapping 的 segment
    `accUtil` 总和最大为 12，没有超过目标 12 manager，且没有 explicit pAcc
    重复或 RR cfg stage budget 溢出。

## 未改变

- 没有修改 runtime C 源码、HybridMapper artifact、FireSim build config 或硬件 RTL。
- 没有停止或重启当前正在运行的 bitstream build。

## 当前限制

- 该测试不证明 F2 live DMA/Gemmini/SPM xlate/NIC 行为。
- 它只排除当前 artifact 层面明显的 segment-level manager over-subscription。
  若后续 F2 上卡住，仍需用 gdbserver 现场区分 DMA fence、completion flag、
  SPM xlate、Gemmini fence、pipe/ring wait 或硬件/NIC 问题。
