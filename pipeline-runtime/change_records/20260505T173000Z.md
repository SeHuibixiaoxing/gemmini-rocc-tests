# 20260505T173000Z change record

## Summary

Recorded a host-side `--hw-validate-only` contract check for the cfg32 pair-manager runtime configuration.

## Test result

- `4 cores / 12 Gemmini / 12 DMA / pair-manager / pages_per_acc=1024 / page_bytes=1024 / spm_xlate=1`: pass.
- Same layout with `spm_xlate=0`: pass.
- Negative test with `num_dma_mgrs=11` in pair-manager mode: fails fast with the expected
  `pair_manager_mode requires num_dma_mgrs ... to match num_gemmini_mgrs` error.

## Limitations

This test is host-only and only exercises runtime initialization. It does not validate FPGA DMA,
Gemmini custom instructions, SPM xlate hardware state, NIC, or remote `gdbserver`.
