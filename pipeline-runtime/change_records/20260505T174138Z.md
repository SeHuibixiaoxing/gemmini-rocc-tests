# 20260505T174138Z - Document DMA completion static split

## Changes

- Added `docs/testing/dma_completion_static_note_20260505.md`.
- Linked the static note from
  `docs/testing/pipeline_runtime_optimization_actions_20260505_draft.md`.

## Tests

- Static code review only:
  - `pipeline-runtime/src/prt_dma.c`
  - `generators/gemmini/src/main/scala/gemmini/GemminiCoupledDMA.scala`

## Result

The documentation now records the concrete split between:

- `hw_dma_fence()` as a DMA-manager idle wait in the current hardware; and
- completion flag as a final host-PA TileLink Put from the copy FSM.

## Limitations

No FPGA, metasim, or baremetal execution was run for this checkpoint.
