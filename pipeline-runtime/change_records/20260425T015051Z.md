# Change Record 20260425T015051Z

调试类别：gdbserver

## 目标

- 把 `singlecore + NIC + debugregs` 新 bitstream 真正接到 FireSim FPGA 验证链路上。
- 让后续 `launchrunfarm -> infrasetup -> runworkload` 默认使用这轮刚生成的 AGFI。

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260425T015051Z.md`

## 修改文件

- `/home/ubuntu/chipyard/sims/firesim/deploy/config_hwdb_f2_rocket_singlecore_nic_30mhz.yaml`

## 修改内容

### 1. 更新单核 NIC FireSim HWDB

把：

- `agfi-06fbafa631d829a13`

更新为：

- `agfi-0dd6d379b15ab226a`

对应配置项：

- `firesim_rocket_singlecore_nic_30mhz.agfi`

## 修改原因

- 新 debugregs bitstream 已完成构建；
- 如果不更新 HWDB，后续 `infrasetup` / `runworkload` 仍会继续刷旧 AGFI，导致本轮新增 observability 无法在 FPGA 上生效。

## 验证

- `buildbitstream` pane 明确给出：
  - `FireSim FPGA Build Completed`
  - `agfi-0dd6d379b15ab226a`
- 后续 `infrasetup` 明确刷入：
  - `Flashing FPGA Slot: 0 with agfi: agfi-0dd6d379b15ab226a`
- `runworkload` 已使用该 AGFI 完成一轮真实 FPGA 验证，结果见关联 debug record。

## 风险 / 后续

- 本轮没有新增软件逻辑改动；仓库层面的唯一修改就是 HWDB 切换到新 AGFI。
- 如果后续需要恢复到别的 bitstream，需要再次显式更新：
  - `/home/ubuntu/chipyard/sims/firesim/deploy/config_hwdb_f2_rocket_singlecore_nic_30mhz.yaml`
