# Change Record 20260419T075152Z

## 目标

- 把本轮 `TraceV local metasim` 的根因结论和执行约束写回仓库文档。
- 避免后续 session 继续把当前 run 误判成 boot hang，或者在未汇报的情况下直接新开 rerun。

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260419T075152Z.md`

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/NEXT_SESSION_PROMPT.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/plans/tracerv_metasim_bringup_plan_20260418.md`

## 修改内容

- 在 `NEXT_SESSION_PROMPT.md` 顶部加入当前 `TraceV local metasim` 分支的会话覆盖说明：
  - 当前只走本机 FireSim metasim
  - 不再额外起 CPU/FPGA 机
  - 新的 `infrasetup/runworkload` 前先向用户汇报结论和计划
  - 当前 active run 不在 boot ROM，而是在 `fill_pattern(stage0_src)` / `barrier_wait()`
- 更新 `tracerv_metasim_bringup_plan_20260418.md`：
  - 补入 2026-04-19 的根因结论
  - 明确当前 full-size repro 在 marker 前有超长 prefill
  - 把下一步收敛到“轻量变体优先，再 full-size 复核”

## 修改原因

- 当前仓库里的下一会话提示仍保留旧主线的“不要走 metasim”叙事，已经不适用于这条 `TraceV` 调试线程。
- 本轮已拿到足够强的静态 + 指令级证据，说明当前问题不是 reset/boot hang，而是 marker 放在长 prefill 之后。
- 若不把这个结论写回文档，后续极容易继续重复旧误判。

## 验证状态

- 仅完成文档与计划更新。
- 没有停止当前 active run。
- 没有启动新的 `infrasetup` 或 `runworkload`。

## 下一步

得到用户同意后再执行下一轮：

1. 准备一个 `segment3 TraceV` 轻量 metasim 变体。
2. 先验证 `TRACEFILE` 能快速非零。
3. 再决定是否回到 full-size repro 复核同一条触发路径。
