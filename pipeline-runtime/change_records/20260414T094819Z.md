# 2026-04-14T09:48:19Z 修改记录

## 本轮目标

- 把 active rerun 已经越过旧 blocker 的事实同步进 authoritative 文档。
- 更新下一轮接手时的 frontier、约束和执行顺序。

## 修改文件

- `docs/CURRENT_STATUS.md`
- `NEXT_SESSION_PROMPT.md`
- `debug_records/20260414T094819Z.md`
- `change_records/20260414T094819Z.md`

## 修改原因

- 当前 active run
  `192.168.1.247`
  已经反复穿过
  `c2-export page0`
  的
  `rr-acquire / v2p / first submitwait`
  区间。
- 如果文档不及时更新，
  后续很容易继续围绕已经排除的旧前沿浪费时间。

## 具体修改

- 更新 `CURRENT_STATUS.md`：
  - 把旧的三类软件候选从 authoritative blocker 中移除
  - 记录新的最新持久化前沿：
    `worker stage=0 subbatch=4 done`
    之后的下一次
    `rr-acquire-wrap phase=after-call`
  - 记录当前 active run 仍在运行，
    `heartbeat=721s`
- 更新 `NEXT_SESSION_PROMPT.md`：
  - 改写主线摘要，
    明确旧的 page0 可疑区间已被 live 现场清除
  - 把后续动作改成：
    先观察更晚前沿，
    若再次停住先静态读
    `worker ... done`
    之后的代码路径
- 新增本轮 debug / change 记录。

## 语义影响

- 本轮没有修改 runtime 源码。
- 没有修改硬件源码。
- 没有修改 guest profile。
- 只更新了文档与记录。

## 验证

- 通过 live `debugfs` 读取确认：
  - `state=running`
  - `worker stage=0 subbatch=1..4 done`
  - `dma-export-host phase=v2p-begin/end`
  - `dma-export-host phase=first-chunk-submitwait-begin/end`
  - `c2-export phase=copy-end`
