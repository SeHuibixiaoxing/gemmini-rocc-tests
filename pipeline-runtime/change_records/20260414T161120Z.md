# Change Record 20260414T161120Z

## 背景

- `2026-04-14 15:55 UTC`
  启动的
  `dma-export page24`
  trigger rerun
  没有推进到目标窗口
- authoritative breadcrumb 连续多轮稳定停在：
  `stage=0 subbatch=1 tensor=2 manager=0 page=42 dma_page_end`
- 静态阅读确认：
  `prt_trigger_log_note()`
  旧实现会在“命中前”对每个事件都先做
  `format_line + ring_push`
  这与“靠近卡点再开日志”的新 SOP 不一致，
  并且高度可疑地把 frontier 前移了

## 修改

- 更新：
  `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_trigger_log.c`
- 更新：
  `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh`
- 具体内容：
  - 在
    `prt_trigger_log_note()`
    中新增
    `eligible_for_capture`
    判定
  - 若当前：
    - trigger 尚未激活
    - 且事件也未落入目标维度
    则直接返回
  - 只有在：
    - trigger 已激活
    - 或当前事件本身已经命中目标维度
    时，
    才继续：
    - `seq` 递增
    - `format_line`
    - `ring_push`
    - `flush/write`
  - fixed profile 号从
    `pairdummy-sbus128-fixed-v19`
    升到
    `pairdummy-sbus128-fixed-v20`
    用来区分：
    - 旧的 pre-match trigger 行为
    - 新的 strict capture 行为

## 预期影响

- 未命中前不再对远离目标窗口的
  export page/event
  持续做字符串格式化与 ring push
- 新行为更贴近新的 blocker-debug SOP：
  - 靠近卡点才开日志
  - 不再让 page0..page23 这类前置路径为 page24 触发制造额外扰动
- 不改硬件
- 不需要 bitstream 重建

## 验证

- `cc -std=gnu11 -fsyntax-only -I/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests -I/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime -I/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/include -I/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src /home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_trigger_log.c`
  通过
- `bash -n /home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh`
  通过
- `bash -n /home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_workflow.sh`
  通过
