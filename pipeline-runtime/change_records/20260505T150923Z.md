# Static artifact audit contract checks

Timestamp: `2026-05-05T15:09:23Z`

## Changed files

- `scripts/audit_pipeline_runtime_artifact.py`
- `scripts/run_bertmini_host_closure.sh`
- `docs/testing/pipeline_runtime_optimization_actions_20260505_draft.md`
- `debug_records/20260505T150923Z_static_artifact_audit_enhancement.md`

## Summary

Strengthened the pipeline-runtime artifact auditor so mapper/runtime contract
mistakes fail before an FPGA run:

- Added `--page-size-bytes`.
- Added target capacity check:
  `pages_per_acc * page_size_bytes == shared_spad_local_size_bytes`.
- Added stage alias-window check against `segmentSpmPageSpan`.
- Added explicit `pAccIdxList` cross-stage duplicate manager detection.
- Fixed the host closure script to pass `model.layers.yaml` into the auditor.

## Validation

- `ours2`, `gemini2`, and `tangram2` artifacts for
  `rerocc_globalnoc_pairmanager_dummy16x16_c4_g12_d12_spad1024kb_dram19_noc64_mac256_sbus128`
  pass with `--page-size-bytes 1024`.
- The same audit fails as expected with `--page-size-bytes 4096`.
- `python -m py_compile` passes for the auditor.
- `bash -n` passes for `run_bertmini_host_closure.sh` and the cfg32 NIC
  gdbserver workflow wrapper.

## Limitations

This is a static/software-only checkpoint. It does not validate the active
cfg32 NIC bitstream or remote gdbserver on the new AGFI.
