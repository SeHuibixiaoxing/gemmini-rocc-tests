# Change Record 20260421T015501Z

调试类别：main

本轮没有新增运行时代码；主要变更是执行了一轮新的 FireSim 主线 rerun，并把新的 stall frontier、现场抓取和结论沉淀为记录。

## 目标

- 验证上一轮加入的 `dma_page_direct_path_decided` probe 是否真的生效
- 判断 `segment0 tensor2 export` 是否仍是主线 blocker
- 若 run 进入新的 plateau，则固定现场并收尾

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260421T015501Z.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260421T011522Z.md`

## 新增现场产物

- `/home/ubuntu/chipyard/tmp/pipeline-runtime-live/20260421T015240Z-g6-segment3-stall-capture/`

## 本轮结果

- 新 probe 已在 guest breadcrumb 中捕获到：
  - `dma_page_direct_path_decided`
  - `seg=0 gstage=0 lstage=0 sb=7 tensor=2 tok=2 page=62`
- 因而当前主线已确认越过旧的 `segment0 export direct-path` 歧义区
- 新 plateau 出现在：
  - `segment3`
  - 以 `stage0/stage2` 的 `conv-sync-strided` compute 路径为中心

## 收尾动作

- 对远端运行现场做了本地抓取，包含：
  - `heartbeat.csv`
  - `uartlog`
  - guest `status`
  - guest sparse log
  - guest breadcrumb bin / decode
  - runner/wrapper stage 快照
  - runworkload pane log
- 随后执行：
  - `pairdummy_sbus128_g6_workflow.sh terminate`
- 结果：
  - `terminaterunfarm` 成功
  - `runworkload` 因实例被 terminate 而以 `exit code 1` 结束

## 影响

- 当前主线调试优先级已经从：
  - `segment0 tensor2 export page29 direct-path`
  切换到：
  - `segment3 compute plateau`
- 下一轮若继续加观测点，不应再优先围绕旧 `slot14` 问题展开，而应直接服务于 `segment3` compute 根因定位
