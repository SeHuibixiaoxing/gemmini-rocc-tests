# Change Record 20260420T121216Z

## 目标

- 修正 `pipeline-runtime` 顶部状态文档中已经过期的
  `stage2 compute-done`
  叙事
- 新增一份围绕
  `segment0 stage0 tensor2 export page28 -> page29`
  的静态审计文档
- 预埋下一轮最小扰动 rerun 所需的 export `v2p` 窄 probe

## 修改文件

- `include/prt_breadcrumb.h`
- `scripts/decode_prt_breadcrumb.py`
- `src/prt_dma.c`
- `src/prt_page_table.c`
- `docs/CURRENT_STATUS.md`
- `NEXT_SESSION_PROMPT.md`
- `docs/architecture/g6_segment0_tensor2_export_page28_page29_static_audit_20260420.md`
- `debug_records/20260420T121216Z.md`
- `change_records/20260420T121216Z.md`

## 修改内容

### 1. 状态/记录文档

- 新增一份新的静态审计文档，
  专门解释：
  - 为什么当前 authoritative frontier 是
    `segment0 stage0 tensor2 export page28`
  - page28 之后还剩哪些软件/硬件边界
- 更新 `CURRENT_STATUS` 与 `NEXT_SESSION_PROMPT`
  顶部 override，
  不再继续沿用旧的
  `stage2 compute-done`
  结论
- 新建本轮 `debug_record`
  和 `change_record`

### 2. export `v2p` breadcrumb

- 新增两个 DMA breadcrumb phase：
  - `dma_page_before_v2p`
  - `dma_page_after_v2p`
- 在 export host-copy path 的
  `prt_host_virt_to_phys()`
  调用前后打点

### 3. export `prt_host_virt_to_phys` debug scope

- 给 export path 的 direct/bounce `v2p`
  增加
  `prt_host_virt_to_phys_debug_scope_push/pop()`
- 使 `prt_page_table.c`
  内部已有的 `iv2p-*`
  trigger 点可用于 export 路径

### 4. host-v2p trigger family 修正

- `prt_page_table.c`
  不再把所有 host-v2p trigger 都硬编码成
  `DMA_FIXED_LOAD`
- 当 scope tag 指向 export 路径时，
  使用 `DMA_EXPORT`
  family

## 语义影响

- 不改 DMA / Gemmini / ReRoCC 的执行顺序
- 不改 RR cfg 分配
- 不改 export page/chunk 语义
- 不改 watchdog 或 workflow
- 仅新增极窄 observability，
  用于回答：
  page29 是否卡在 export `v2p`

## 预期收益

- 下一轮若再次冻结在
  `page28 after_accounting`
  邻域，
  可以直接区分：
  - 是否已进入 page29 `v2p`
  - `v2p` 是否成功返回
  - 是否是 `v2p` error 后的 release cleanup
