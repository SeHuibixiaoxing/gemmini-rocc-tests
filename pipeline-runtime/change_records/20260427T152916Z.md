# 20260427T152916Z NIC old-AGFI 复现与新 bitstream 构建记录

调试类别：gdbserver

## 关联记录

- `../debug_records/20260427T152916Z.md`

## 修改/操作内容

### 旧 AGFI 复现

- 将 `sims/firesim/deploy/config_hwdb_f2_rocket_singlecore_nic_notrace_30mhz.yaml`
  指向旧 AGFI：
  `agfi-08ec9b835f726aef0`
- 用 quiet preserve runtime 跑一轮 fresh F2。
- 保存远端 live 现场到：
  `../debug_records/artifacts/20260427_nic_emptytok_agfi08ec_quiet_prefix/`

### 现场清理

- 保存现场后终止旧 AGFI quiet runfarm。
- 对应实例：
  `i-0ec62c9bb1cadbcb7`

### 新 bitstream 构建

- 启动新 NIC bitstream 构建：
  `rocket-singlecore-nic-validdrive-buildbitstream-20260427-1528`
- 构建使用当前源码，包含上一条记录中的关键修复：
  - `fromHostValidDrive = fromHostTokenAvailable`
  - host driver quiet 模式下抑制 cooperative wait 大块 dump
- 本轮没有新增 RTL 逻辑修复；旧 AGFI 测试只再次确认早期卡点仍是
  `from_cpu_stream` FIFO 满、target 不消费。

## 验证/检查

- `git diff --check` 通过，范围：
  - `generators/firechip/goldengateimplementations/src/main/scala/SimpleNICBridge.scala`
  - `generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.cc`
  - `generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.h`
- 旧 AGFI quiet run 关键证据：
  - `AFI ID for Slot  0: agfi-08ec9b835f726aef0`
  - `CPU_STREAM DEBUG push_blocked ... from_cpu_stream ... count=3072 ... space_available=0 ... requested_bytes=57856`
- fresh verbose F2 尝试因 AWS `insufficient capacity` 未取得实例，已停止，
  没有 live instance 残留。

## 后续

- 等待 `rocket-singlecore-nic-validdrive-buildbitstream-20260427-1528`
  完成。
- 取得新 AGFI 后更新 hwdb，并用 fresh F2 runfarm 跑
  `rocket-singlecore-nic-gdbserver-smoke`。
- 若新 AGFI 仍卡在 boot 前，应优先读取本轮已有的可综合
  `SimpleNIC` debug regs，而不是重复只看 host stream `push_blocked`。
