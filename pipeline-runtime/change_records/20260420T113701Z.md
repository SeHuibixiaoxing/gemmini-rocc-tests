# Change Record 20260420T113701Z

## 目标

- 增加一次性 worker 绑核观测点
- 为下一轮
  `g6 bertmini`
  主线 rerun
  直接回答
  “segment=3 的不同 stage 是否真的落在不同 hart 上”

## 修改文件

- `src/prt_runtime.c`
- `debug_records/20260420T113701Z.md`
- `change_records/20260420T113701Z.md`

## 修改内容

- `src/prt_runtime.c`
  - 在 `stage_bind_current_thread()` 成功执行
    `sched_setaffinity()`
    之后，
    新增一条一次性
    `worker-affinity`
    日志
  - 日志字段包括：
    - `stage`
    - `acc`
    - `dma`
    - `target_cpu`
    - `actual_cpu`
    - `actual_errno`

## 语义影响

- 不改 stage 调度逻辑
- 不改 RR cfg 分配逻辑
- 不改 Gemmini / DMA / ReRoCC 指令序列
- 仅增加每个 worker 一次性的低扰动观测
