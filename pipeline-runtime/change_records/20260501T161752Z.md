调试类别：gdbserver

# 更新 8BP single-core NIC hwdb 并记录 freshness 错配

日期：2026-05-01T16:17:52Z

## 修改

更新运行用 hwdb：

- 文件：`sims/firesim/deploy/config_hwdb_f2_rocket_singlecore_nic_notrace_8bp_30mhz.yaml`
- 旧 AGFI：`agfi-082afecb92c1f939d`
- 新 AGFI：`agfi-054e7cc532edd4698`

## 验证结果

使用新 AGFI 启动 fresh F2 后，`infrasetup` 在 `+check-fingerprint`
preflight 阶段三次 30s timeout。随后确认这是 driver/AGFI 不一致：

- 新 AGFI 不含 `2026-05-01 14:14-14:18 UTC` 后新增的
  `nicbig_ntht_first_*` / `nicbig_pcie_first_*` 观测点；
- 当前本地 driver collateral 已包含这些字段；
- 远端 F2 上部署的 `FireSim-f2` binary 也包含 `nicbig_ntht` / `nicbig_pcie`
  字符串和新增 MMIO 名称。

## 后续约束

下一轮 bitstream 启动前必须做本地 freshness gate，确认：

```bash
rg -n "nicbig_ntht_first_packet_seq|nicbig_pcie_first_packet_seq|nicbig_pcie_first_data15" \
  sims/firesim/sim/generated-src/f2/f2-firesim-FireSim-FireSimRocketNICNoTrace8BPConfig-BaseF2Config/FireSim-generated.const.h \
  sims/firesim/platforms/f2/aws-fpga-firesim-f2/hdk/cl/developer_designs/cl_f2-firesim-FireSim-FireSimRocketNICNoTrace8BPConfig-BaseF2Config/design/FireSim-generated.sv
```

远端 Vivado 开始后，还必须 SSH 到 build host 核对远端
`FireSim-generated.sv` 含同样字段，避免再次出现 AGFI 与本地 driver 不一致。
