# 20260505T183600Z - Document first live-GDB hang triage

## Changed

- Added `docs/testing/pipeline_runtime_gdb_first_hang_static_triage_20260505.md`.
- Added `debug_records/20260505T183600Z_gdb_first_hang_static_triage.md`.

## Why

The cfg32 NIC bitstreams are still building, so this records static progress
made while waiting. The document turns the current blocking-debug runtime path
into a concrete first-GDB classification checklist.

## Verification

No code was changed. Verification for this checkpoint is static source review
only:

- `src/prt_runtime.c`
- `src/prt_gemmini_adapter.c`
- `src/prt_dma.c`
- `src/main.c`

## Active Hardware Work

- Mainline cfg32 NIC bitstream remains active, no AGFI/AFI yet.
- no-TraceIO cfg32 NIC fallback remains active, no `FireSim-generated.sv` yet.

