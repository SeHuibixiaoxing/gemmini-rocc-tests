# Change Record 20260419T081124Z

## 目标

- 为 `selector=3` 增加一条最小、已知可跑通链路的 marker 验证入口。
- 避免一上来继续用 `segment3` 这种 marker 前静默窗口很长的程序做精确触发验证。

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260419T081124Z.md`

## 修改文件

- `/home/ubuntu/chipyard/tests/hello-marker.c`
- `/home/ubuntu/chipyard/tests/CMakeLists.txt`
- `/home/ubuntu/chipyard/sims/firesim/deploy/workloads/hello-baremetal-tracerv-inst-metasim.json`
- `/home/ubuntu/chipyard/sims/firesim/deploy/workloads/hello-baremetal-tracerv-inst-metasim/README`
- `/home/ubuntu/chipyard/sims/firesim/deploy/config_runtime_local_metasim_rocket_hello_baremetal_tracerv_inst.yaml`

## 修改内容

### 1. 新增最小 marker 程序

`tests/hello-marker.c`
在最小 `hello` 程序上插入：

- start marker：`.word 0x00008013`
- end marker：`.word 0x00010013`

中间只保留一条 `printf`。

### 2. 把新程序接入 `tests` 构建

在 `tests/CMakeLists.txt` 中新增：

- `add_executable(hello-marker hello-marker.c)`
- `add_dump_target(hello-marker)`

### 3. 新增 `hello` 的 instruction-trigger workload/config

新增 workload：

- `hello-baremetal-tracerv-inst-metasim.json`

新增本地 metasim runtime config：

- `config_runtime_local_metasim_rocket_hello_baremetal_tracerv_inst.yaml`

关键设置：

- `selector: 3`
- `start: "ffffffff00008013"`
- `end: "ffffffff00010013"`
- 其余尽量复用已跑通的 `hello` TraceV smoke 链路

### 4. 新增 workload 目录 README

为下一轮运行前的 bootbinary 放置路径提供明确说明：

- `deploy/workloads/hello-baremetal-tracerv-inst-metasim/hello-marker.riscv`

## 修改原因

- 当前 `segment3` 线程的问题不是“TraceV 一定坏了”，
  而是 marker 前有长 prefill。
- 先在最小 `hello` 程序上验证 marker，
  能把“精确触发链路是否正常”与“程序自身太重”这两个问题拆开。

## 验证状态

- 已完成源码与配置准备。
- 已完成本地编译与反汇编静态校验。
- `tests/hello-marker.riscv` 已成功生成。
- 反汇编已确认：
  - start marker：`0x00008013`
  - end marker：`0x00010013`
- 尚未启动新一轮 `infrasetup/runworkload`。

## 下一步

1. 向用户汇报
2. 得到同意后再复制 bootbinary
3. 启动新的 local metasim run
