## 2026-04-15T13:50:08Z

### Change type
- Observability-only config retarget for the next control rerun.

### Files changed
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh`

### What changed
- Bumped fixed profile id:
  - `pairdummy-sbus128-fixed-v21` -> `pairdummy-sbus128-fixed-v22`
- Retargeted export probe away from the disproven `page5` window:
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START=52`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END=52`
- Collapsed token window to a dummy non-matching range:
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START=0`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END=0`

### Why
- The current stable frontier is no longer `page5`.
- Live breadcrumb shows the export path has already completed `page51` and the next missing boundary is inside `page52`.
- Keeping token window at `0..0` avoids re-enabling broad token-window logging on unrelated export pages while still allowing the forced page probe on `page52` and the always-on first-chunk page-0 probe.

### Required follow-up
- Regenerate workload baseline env through:
  - `generators/gemmini/software/gemmini-rocc-tests/rerocc-linux-tests-coupleddma/workload/host-init-fileonly-sync-pairdummy.sh`
- Re-render effective guest env, patch local image, and run local/remote freshness before rerun.
