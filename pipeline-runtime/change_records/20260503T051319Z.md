调试类别：gdbserver / NIC / static-regression

# 恢复 04-30 通过版 SimpleNIC payload snapshot CR 顺序

日期：2026-05-03T05:13:19Z

## 关联记录

- `../debug_records/20260503T051319Z.md`
- `../debug_records/20260503T035648Z.md`
- `../debug_records/20260503T032845Z.md`
- `../debug_records/20260502T223837Z.md`

## 修改文件

```text
generators/firechip/goldengateimplementations/src/main/scala/SimpleNICBridge.scala
generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.h
```

## 修改内容

本轮不改变 NIC datapath 语义，只修正 payload snapshot 的可综合 CR 地址顺序，
使其与 04-30 已通过 AGFI 的 generated RTL 对齐。

具体修改：

- 在 `SimpleNICBridge.scala` 中，把
  `bigtoken_current_slot_data` / `bigtoken_last_latched_word0..2`
  的 `attachUInt32Chunks` 移到 `bigtoken_current_slot_meta` 之后、
  `nicbig_special_counter` 之前；
- 保持 `nicbig_last_pcie_out_word0..7`、`to_host_packet_seq`、
  `to_host_packet_flit_idx`、`to_host_hist_data/meta` 在
  `nicbig_pcie_out_fire_count` 之后；
- 同步调整 `SIMPLENICBRIDGEMODULE_struct` 字段顺序；
- 将 `toHostHistData/Meta` 变量名恢复成旧生成物里的
  `toHostHistoryData/Meta`，降低后续 generated RTL 对照噪声。

## 验证

本地 `replace-rtl` 成功，driver 编译通过。

生成物 SHA：

```text
FireSim-generated.sv      95b69f2083a3e2c74decb903fdf9c496a4129e91a0b4d4722e7ba6d183b51644
FireSim-generated.const.h c72fc6c59e31f405479e46cc8b2bcc363f532a37b99379abd6979815d7efb491
FireSim-f2                f8c49aade9a59050e8ec659e31feecf0f2e5810ac3bbcfd38d6c42aaf92f3b8b
```

对照结果：

- `BigTokenToNICTokenAdapter` 与 04-30 通过版逐字节一致；
- `NICTokenToBigTokenAdapter` 与 04-30 通过版逐字节一致；
- `SimpleNICBridgeModule` 的 CR 45..57 顺序与 04-30 通过版一致；
- first16 / ready 聚合 / partial-fire / PCIS write-order debug marker 均无残留。

## 后续

基于当前生成物启动新的 single-core NIC bitstream 构建。远端进入 Vivado 后再次
核验 SHA / marker，确保本轮修复真实进入远端 build collateral。
