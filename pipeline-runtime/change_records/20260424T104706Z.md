# Change Record 20260424T104706Z

调试类别：gdbserver

## 目标

- 不改主代码逻辑，先把当前 `SimpleNIC / NIC / gdbserver` 调试线的关键静态结论补成可复用文档。
- 把“这轮 run 能说明什么、不能说明什么”写清楚，避免后续继续把 host-only 观测与 target-side RTL 验证混淆。

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260424T104706Z.md`

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/testing/firesim_simplenic_static_audit_20260424.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/testing/gdbserver_integration_sop.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260424T104706Z.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/change_records/20260424T104706Z.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/CATEGORY_INDEX.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/change_records/CATEGORY_INDEX.md`

## 修改内容

### 1. 新增一份面向当前主线的 `SimpleNIC` 静态审计文档

新增：

- `docs/testing/firesim_simplenic_static_audit_20260424.md`

内容覆盖：

- `SimpleNIC` host driver 的 bulk 语义
- `CPUManagedStream` 的 `count` 真实含义
- `HostPort` 的单步 input/output token 语义
- `SimpleNICBridge` upstream 写法为什么存在“两个方向 token 漂移”的静态风险
- 为什么当前这轮 run 只能支撑 host-side 现象结论，不能直接视为 target-side RTL fix 已验证

### 2. 在 `gdbserver` SOP 中补一条关键执行约束

更新：

- `docs/testing/gdbserver_integration_sop.md`

新增约束：

- `firesim infrasetup` 会按本地源码重新生成并编译 host driver
- 因此，只要本地 target-side bridge RTL 已经改过，但实际运行的仍是旧 AGFI，
  这轮 run 就不能被当成“RTL 修复已在 FPGA 上验证”

同时把新的静态审计文档挂到 SOP 中，方便后续直接跳转。

### 3. 归档本轮 debug 结论并更新分类索引

新增：

- `debug_records/20260424T104706Z.md`
- `change_records/20260424T104706Z.md`

并重新生成：

- `debug_records/CATEGORY_INDEX.md`
- `change_records/CATEGORY_INDEX.md`

## 原因

- 用户已经明确指出：
  “改了硬件代码，没改 bitstream” 这一点必须严格对待。
- 当前这条 NIC/gdbserver 调试线里，host-only 结论和 target-side 结论非常容易被混在一起。
- 如果不把执行边界提前写清楚，后续即使跑出了更强日志，也可能继续误判“是 host bug 还是 bridge RTL bug”。

## 验证

- 本轮新增/更新的都是 Markdown 文档和记录
- 后续已重新运行分类索引脚本，确保新记录进入 `gdbserver` 类

## 后续

- 若要继续证明 `SimpleNICBridge` lockstep patch 的效果，下一轮必须使用匹配该 patch 的新 bitstream
- 若暂时仍基于旧 AGFI 做观测，则只应继续做 host-only、低扰动的现象确认
