# 2026-05-04T10:12:41Z - NIC/gdbserver build-before-freeze SOP and SimpleNIC observation expansion

类别：`gdbserver`

## 修改内容

### 1. 构建前冻结约束

更新：

- `generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/constraints/hard_constraints.md`
- `generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/testing/gdbserver_integration_sop.md`

新增约束：

- 启动任何 `buildbitstream` 前，先复核历史记录、静态排查、冻结可综合观测面并完成静态校验。
- `buildbitstream` 启动后不再继续追加本轮硬件观测点。
- 启动后的 RTL freshness gate 只用于确认已冻结改动进入 Verilog，不是继续补硬件内容的阶段。

### 2. SimpleNIC 可综合观测面

更新：

- `generators/firechip/goldengateimplementations/src/main/scala/SimpleNICBridge.scala`
- `generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.h`
- `generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.cc`

新增 `minobs_build_marker = 0x05040003`。

新增观测项覆盖：

- BigToken adapter slot emit mask。
- NIC-to-BigToken flush / pcie_out enqueue backpressure。
- `nicIn/macAddr/rlimit/pauser` leaf valid/ready/fire/blocked mask。
- target-cycle first-ready / first-fire / ready-not-ready streak。
- from-host would-blocked、leaf-blocked、per-leaf blocked/fire 计数。

## 校验

```text
Scala attach order vs C++ struct: ORDER_OK
g++ -std=c++17 -Wall -Wextra -Werror -fsyntax-only simplenic.cc: PASS
git diff --check: PASS
```

## 注意

当前正在运行的 `rocket-singlecore-nic-readgatefix-buildbitstream-20260504-0616`
启动早于本次观测面扩展，因此该 build 不包含 `0x05040003`。不能把它的 AGFI
当成本次新增观测面的验证结果。
