# Change Record 20260414T125526Z

## 目标

- 修正当前 breadcrumb observability，
  让
  `stage0/subbatch4`
  pointwise / RR release
  前沿能继续用低扰动方式定位。

## 关联记录

- debug：
  `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260414T125526Z.md`

## 修改文件

- `src/prt_breadcrumb.c`
- `scripts/decode_prt_breadcrumb.py`
- `scripts/pairdummy_sbus128_fixed_env.sh`
- `docs/CURRENT_STATUS.md`
- `NEXT_SESSION_PROMPT.md`
- `docs/workflows/pairdummy_sbus128.md`

## 修改内容

- `src/prt_breadcrumb.c`
  - breadcrumb slot 不再只按
    `stage`
    选槽
  - 改为 hash：
    `stage/subbatch/kind/manager/tensor/page/token`
  - 保留低扰动 mmap breadcrumb 机制，
    但把 stage-0 单槽争用拆开
- `scripts/decode_prt_breadcrumb.py`
  - `--all`
    输出改为按
    `seq`
    排序
- `scripts/pairdummy_sbus128_fixed_env.sh`
  - profile 升级到
    `pairdummy-sbus128-fixed-v17`
- 文档
  - 把 authoritative 状态从旧的
    `v14`
    / fixed-load 叙事
    更新到
    `v15/v16/v17`
    路线

## 修改原因

- `v16`
  capture 已经暴露出：
  sparse log 与 breadcrumb
  的前沿不一致。
- 静态根因是旧 breadcrumb slot 设计把整条
  `stage 0`
  压到单槽上，
  使
  `last_phase`
  在当前 live 并发下失真。
- 这次修改不碰硬件语义，
  只修 observability，
  符合用户要求的
  “先静态分析，再打点；
  优先软件调试，不改硬件”。

## 验证

- `python3 -m py_compile scripts/decode_prt_breadcrumb.py`
  通过
- `pairdummy_sbus128_workflow.sh show`
  已显示：
  `profile_id=pairdummy-sbus128-fixed-v17`
- `image-closure`
  通过
- local freshness
  通过
- remote freshness
  `192.168.1.18`
  通过
- 当前新 run 已启动：
  `pairdummy-sbus128-runworkload-20260414-125651`

## 风险 / 后续

- 新 breadcrumb slot 仍是 hash 分散，
  不是全历史 trace；
  只是把当前最致命的单槽争用去掉。
- 下一轮仍需用
  `v17`
  live capture
  验证：
  `mgr3 release -> mgr4`
  的前沿是否已经可见。
- 未修改硬件，
  未走 metasim，
  未改 bitstream。
