# 20260505T172846Z change record

## Summary

Recorded the generated-source freshness checkpoint for the cfg32 NIC no-TraceIO bitstream variant.

## Evidence

New debug record:

- `pipeline-runtime/debug_records/20260505T172846Z_cfg32_nic_notrace_generated_freshness.md`

## Result

The generated collateral for `FireSimGemminiReRoCCPairDummy16x16C4P12Sbus128NICNoTraceConfig` confirms:

- 4 Rocket cores with one hardware execute breakpoint each.
- 12 ReRoCC manager MMIO nodes.
- `ice-nic@10016000` in DTS and memmap.
- NIC bridge / `SimpleNICBridgeModule` annotations.
- Optimized DMA markers `bytes_written_per_beat` and `write_shift`.
- No `TracerVBridge`, `TraceIO`, or `TracePort` in the checked generated target collateral.

## Limitations

This checkpoint does not validate `FireSim-generated.sv`, Vivado, AGFI/AFI creation, HWDB update, F2 runfarm,
guest NIC bring-up, or remote `gdbserver`. It only proves the no-TraceIO target elaborated into the expected
generated source shape.
