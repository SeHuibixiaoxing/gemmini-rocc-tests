# Change Record 20260414T165254Z

## 背景

- 最新一轮
  `v20`
  control rerun
  的
  `guest-trigger-log`
  恰好停在
  `sb=3 page=28 ph=pset`
- 静态分析后确认：
  这并不是新的 authoritative frontier，
  而是
  `match line + post_budget(32)`
  恰好打满后的 trigger window 截断
- 现有
  `triage_prt_capture.py`
  还不会显式报告这类“budget exhaustion”，
  容易再次把最后一条 trigger 误记成 blocker

## 修改

- 更新：
  `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/triage_prt_capture.py`
- 更新：
  `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/CURRENT_STATUS.md`
- 更新：
  `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/NEXT_SESSION_PROMPT.md`
- 更新：
  `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/plans/blocker_debug_sop_plan.md`

## 具体内容

- `triage_prt_capture.py`
  新增：
  - `--trigger-log`
  - `--trigger-post-budget`
  - `--trigger-match-once`
- capture 目录输入现在也会自动发现：
  `*guest-trigger-log.txt`
- triage 新增 trigger 分诊输出：
  - `trigger_line_count`
  - `trigger_seq_contiguous`
  - `trigger_budget_exhausted`
  - `trigger_first_event`
  - `trigger_last_event`
- 若
  `trigger_line_count == post_budget + 1`
  且
  `match_once=1`，
  triage 会明确输出：
  `trigger_warning=...capture cutoff, not authoritative frontier`
- `recommended_next_probe`
  现在在这类场景下会提示：
  `retarget-trigger-or-raise-budget-before-frontier-claim`
- 状态/接手文档与 SOP 统一修正为：
  - `page28` 不是新 frontier
  - `sb3/page24`
    已被穿过
  - 下一轮 trigger
    应该后移到
    `sb4`
    更晚窗口

## 预期影响

- 以后再遇到 trigger log
  正好打满预算时，
  工具会先把它归类为
  `trigger window exhausted`
  而不是让人手工误判为“新卡点”
- 状态文档不再延续错误的
  `page28`
  叙事，
  后续 rerun 会直接从更晚窗口继续

## 验证

- `python3 -m py_compile /home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/triage_prt_capture.py`
  通过
- `python3 /home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/triage_prt_capture.py --breadcrumb ... --sparse-log ... --trigger-log ... --change-kind observability --emit-trigger-env`
  输出包含：
  - `trigger_line_count=33`
  - `trigger_budget_exhausted=yes`
  - `trigger_warning=trigger log exactly filled match-line + post-budget window; treat the last trigger line as a capture cutoff, not as an authoritative frontier`
