# Change Record 20260421T122600Z

调试类别：main

本轮没有新增运行时代码；主要修改是把这轮 `cfg32 + no-TraceV` 主线 rerun 的现场与 authoritative 结论写回仓库文档。

## 目标

- 固化这轮 `cfg32` 主线 rerun 的真实进度与 plateau 位置
- 记录新的 Linux `RCU stall / pagemap_*` 证据
- 更新 `CURRENT_STATUS.md` 顶部 authoritative 摘要，避免下一轮重复从临时 capture 推理

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260421T122600Z.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260421T015501Z.md`

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/CURRENT_STATUS.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260421T122600Z.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/change_records/20260421T122600Z.md`

## 修改内容

- 在 `CURRENT_STATUS.md` 顶部新增一节：
  - 说明当前 latest authoritative run 是 `cfg32 + no-TraceV` 的 `g6 bertmini` rerun
  - 固化这轮仍然停在 `segment3` compute plateau
  - 记录 `uartlog` 中新的 `RCU stall` / `pagemap_*` 证据
- 新增一份完整 debug record，记录：
  - workflow
  - 现场路径
  - guest sparse log 尾部
  - breadcrumb decode
  - `uartlog` 关键栈
  - 结论与下一步

## 原因

- 这轮结果对主线判断很关键：
  - 证明 `cfg32` 没把程序推回旧 `segment0 export` frontier
  - 证明去掉主线 `TraceV` 依赖后 stall 依旧存在
  - 把根因收敛到 `segment3` compute 主线，而不是旧 `cfg15` / `TraceV` 争议
- 若不立刻写回文档，后续会再次依赖临时 capture 目录和远端镜像手工拼结论

## 验证

- 现场已抓回本地：
  - `/home/ubuntu/chipyard/tmp/pipeline-runtime-live/20260421T122136Z-g6-cfg32-notrace-stall-capture/`
- 关键证据已在文档中引用并交叉对应：
  - `bertmini-batch8.log`
  - `bertmini-batch8.status`
  - `bertmini-batch8.breadcrumb.decoded.txt`
  - `uartlog`
- `terminaterunfarm` 成功，实例状态已进入 `shutting-down`

## 风险 / 后续

- 当前文档里的 `segment3` 结论仍是“停在该区域”，而不是“已知确切最后一条 guest/hardware 指令”
- 下一轮若新增 `segment3` 定向 breadcrumb / gdbserver 证据，应继续更新 `CURRENT_STATUS.md` 顶部摘要
