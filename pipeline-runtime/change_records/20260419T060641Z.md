# Change Record 20260419T060641Z

## 目标

- 为下一轮本机 metasim 准备一个更适合长窗口 TraceV 的 runtime config。
- 避免继续使用 `selector=0 + trace-min-batch=0` 全程 trace，导致把正常慢推进误判为新 hang。

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260419T060641Z.md`

## 修改文件

- `/home/ubuntu/chipyard/sims/firesim/deploy/config_runtime_local_metasim_rerocc_lc_baremetal_export_dma_bertmini_segment3_repro_tracerv_inst_batched.yaml`

## 修改内容

- 新增本机 metasim runtime config：
  - `externally_provisioned.yaml`
  - `localhost`
  - `num_metasims: 1`
- 继续使用已有 baremetal workload：
  - `rerocc-lc-baremetal-coupleddma-export-dma-bertmini-segment3-repro-tracerv-inst.json`
- 使用 instruction trigger：
  - `selector: 3`
  - `start: "ffffffff00008013"`
  - `end: "ffffffff00010013"`
- 去掉 `+trace-min-batch=0`：
  - 保留 `+heartbeat-polling-interval=10000000`
  - 让 `tracerv_t` 使用默认 stream-depth 批量拉取

## 修改原因

- 当前 live run 已证明 boot 已越过 `0x10034 wfi` 并进入用户程序 prefill。
- 当前全程 trace 配置会在 512 KiB prefill 和两核 trace 下极慢推进。
- 源码和 binary 都已经具备精确 instruction marker，不需要继续用 `selector=0` 抓全程。
- `tracerv_t` 默认 `tick_min_batch_beats = stream_depth`，比 `0` 更适合长 metasim 窗口。

## 验证状态

- 仅完成静态准备。
- 按用户要求，尚未停止当前 run，也尚未启动新的 `infrasetup/runworkload`。

## 下一步

得到用户同意后再执行：

1. 停止当前 `rootcause-clint-putfull-runworkload` metasim。
2. 用新增 config 执行 `infrasetup`。
3. 用新增 config 执行 `runworkload`。
4. 检查 `uartlog`、`metasim_stderr.out`、`TRACEFILE-C*`。
