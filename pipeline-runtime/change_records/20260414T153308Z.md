# Change Record 20260414T153308Z

## 目标

- 修正 `triage_prt_capture.py` 对 export capture 的 frontier 误判
- 让下一轮 trigger env 能跟真实 DMA breadcrumb frontier 对齐

## 关联记录

- debug:
  `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260414T153308Z.md`

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/triage_prt_capture.py`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/CURRENT_STATUS.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/NEXT_SESSION_PROMPT.md`

## 修改内容

- `frontier_summary()`：
  - 不再只对 `RR/Gemmini` breadcrumb 优先
  - 对 `DMA` breadcrumb 也优先输出 frontier
- `emit_trigger_env()`：
  - 新增 `slot.kind == dma` 分支
  - 当 breadcrumb 已经明确锁在 DMA page frontier 时，
    直接输出 `dma-export` / `dma-fixed-load` 的 page 级 trigger env
- `recommend_next_probe()`：
  - 对 DMA breadcrumb frontier 给出
    `static-read-dma-page-boundary-then-page-trigger`
- 文档同步：
  - `CURRENT_STATUS.md`
    更新到 `v19` 官方 capture / page24 frontier / manager 中断状态
  - `NEXT_SESSION_PROMPT.md`
    更新到当前 authoritative frontier 与下一轮 trigger rerun 入口

## 原因

- 官方 watchdog capture 与 live breadcrumb 明确显示真实 frontier 在：
  `stage=0 subbatch=3 tensor=2 page=24`
- 旧 triage 仍被陈旧 sparse page0 文本误导，
  输出错误 frontier 与错误 trigger env
- 这已经会直接拖慢下一轮 blocker 排查，因此需要先修工具

## 验证

- `python3 -m py_compile generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/triage_prt_capture.py`
- 使用官方 capture 重新 triage：
  - 现在输出：
    `frontier=dma/dma_page_end ... sb=3 ... tensor=2 ... page=24`
  - 现在输出：
    `PIPELINE_RUNTIME_DEBUG_TRIGGER_KIND=dma-export`
    `...SUBBATCH=3`
    `...MANAGER=0`
    `...TENSOR_ID=2`
    `...PAGE=24`

## 风险 / 后续

- 当前修正只解决“frontier / trigger 推荐”侧的优先级问题，
  不改变 runtime 语义
- 下一轮仍需用单变量 rerun 验证 page24->page25 窄窗口，
  不能把工具修正误记成 blocker 穿过
