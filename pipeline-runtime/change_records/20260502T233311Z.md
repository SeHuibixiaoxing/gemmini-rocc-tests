调试类别：gdbserver / NIC / static-regression

# 恢复 NIC target->host payload snapshot 并回退 PCIS W-before-AW

日期：2026-05-02T23:33:11Z

## 修改文件

```text
generators/firechip/goldengateimplementations/src/main/scala/SimpleNICBridge.scala
generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.h
generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.cc
sims/firesim/platforms/f2/aws-fpga-firesim-f2/hdk/cl/developer_designs/cl_firesim/design/cl_firesim.sv
```

## 修改内容

1. F2 PCIS 512->64 bridge 写通道恢复旧语义：

```systemverilog
s_axi_wready = (wr_state == WR_COLLECT) && !wr_wbuf_valid &&
               (wr_aw_active || wr_aw_fire);
```

2. 恢复 04-30 18:47 可工作单核 NIC AGFI 中存在的 payload-heavy snapshot：

- `BigTokenToNICTokenAdapter`：
  - 当前 selected slot data；
  - 最近 latch 的 bigtoken word0/word1/word2。
- `NICTokenToBigTokenAdapter`：
  - 最近一次 `pcie_out.fire` 的完整 512-bit bigtoken word0..word7。
- `SimpleNICBridgeModule`：
  - `hPort.nicOut` 最近 8 个 payload flit 的 data/meta history。

3. 补回 05-01 记录中计划但后来被归位裁掉的 first16 snapshot：

- `to_host_first_*`：`hPort.nicOut` 最新包前 16 个 payload flit；
- `nicbig_ntht_first_*`：`NICTokenToBigTokenAdapter.ntht` 输入侧最新包前 16 个 payload flit；
- `nicbig_pcie_first_*`：`NICTokenToBigTokenAdapter.pcie_out` 输出侧解包后的最新包前 16 个 payload flit。

4. host `simplenic_t::dump_tx_path_snapshot()` 重新启用 payload-heavy dump：

- `SIMPLENIC TX SNAPSHOT ... last_bigtoken=...`
- `SIMPLENIC TX SNAPSHOT ... hist[...]`
- `SIMPLENIC TX FIRST16 ... stage=hport`
- `SIMPLENIC TX FIRST16 ... stage=nicbig_ntht`
- `SIMPLENIC TX FIRST16 ... stage=nicbig_pcie`

## 静态验证

已通过：

```bash
git diff --check -- \
  generators/firechip/goldengateimplementations/src/main/scala/SimpleNICBridge.scala \
  generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.cc \
  generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.h

git -C sims/firesim/platforms/f2/aws-fpga-firesim-f2 diff --check -- \
  hdk/cl/developer_designs/cl_firesim/design/cl_firesim.sv

g++ -std=c++17 -fsyntax-only \
  -I generators/firechip/bridgestubs/src/main/cc \
  -I sims/firesim/sim/midas/src/main/cc \
  generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.cc
```

`FireSimRocketNICNoTraceConfig` 的 `replace-rtl` 通过，生成物与 driver 编译成功。

## Freshness gate

本地生成物已经确认包含：

```text
bigtoken_current_slot_data
bigtoken_last_latched_word0
nicbig_last_pcie_out_word7
to_host_hist_data7
to_host_first_data15
nicbig_ntht_first_data15
nicbig_pcie_first_data15
nicbig_pcie_first_packet_seq
```

下一轮 buildbitstream 进入远端 Vivado 后必须核对远端生成物 SHA：

```text
FireSim-generated.sv      077686b61217fd295fca5bdd0a2a5e860fc04b83a006669c629a0f5c21faf248
FireSim-generated.const.h 29af7efcf9f53946e8d620e717c87b434d8782d77089681986780e1dfe6f693c
FireSim-f2                3feefea2bd226ff006a577e3f9f891d8a5dd5573ec59f7c41d837611c2f98613
```
