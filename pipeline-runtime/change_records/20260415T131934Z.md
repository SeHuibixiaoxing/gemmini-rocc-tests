# Change Record 20260415T131934Z

## 目标

为 `12-pair sbus128 / bertmini batch8` 当前已收紧到的
`tensor=2 / target_seq=1 / page=5`
blocker 增加最小 submit-path observability，
避免继续做大范围 trigger / deep log 扰动。

## 关联记录

- debug record:
  `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260415T131934Z.md`

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh`

## 修改内容

- profile id:
  - `pairdummy-sbus128-fixed-v20 -> pairdummy-sbus128-fixed-v21`
- 启用 export probe：
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE=1`
- 缩窄 token window：
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START=1161`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END=1163`
- 缩窄 page range：
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START=5`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END=5`

## 原因

- 现有 authoritative 证据显示 page0..page4 已完成
- 新稳定前沿在 page5，且位于 submit/wait 边界
- 现有代码已经支持 export probe，无需先改核心实现
- 通过 page + token 双重收窄，可以把额外日志限制在极小窗口

## 验证

待执行：

1. `pairdummy_sbus128_workflow.sh debug-preflight`
2. local freshness / remote freshness
3. 新一轮 FireSim rerun
4. 核对 guest log 是否出现 page5 附近的 `dma-export-submit` / `dma-export-wait` 细日志

## 风险 / 后续

- token 号在 rerun 中可能轻微漂移
  - 已通过 page range `5..5` 提供冗余兜底
- 该改动是 observability profile，不是功能修复
  - 若拿到 page5 内部更细 blocker，再决定是否改实现
