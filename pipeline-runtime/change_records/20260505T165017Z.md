# Change record: host buildability repair and software dry-run checkpoint

Time: `2026-05-05 16:50 UTC`

## Changed

- Repaired default host buildability for `pipeline-runtime`.
- Converted strict-C inline assembly spelling from `asm volatile` to
  `__asm__ volatile` in pipeline-runtime source.
- Marked intentional platform/debug-only helper functions as unused where the
  default host build compiles them out of the active path.
- Added explicit `(void)` uses for locals that are only referenced through
  disabled log macros.

## Why

The current debugging plan depends on rebuilding the Linux workload with a
current `rerocc_pipeline_runtime-linux` binary once the cfg32 NIC bitstream is
available. A default host `make -B all` was failing before FPGA validation could
start, which made the software state non-reproducible.

## Verification

- `make -C generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime -B all`
  passed.
- `audit_pipeline_runtime_artifact.py` passed for `ours2`, `gemini2`, and
  `tangram2` with `--page-size-bytes 1024`.
- `pipeline_runtime --backend cpu` dry-run passed for `ours2`, `gemini2`, and
  `tangram2` with `--batch 1`, `--num-cores 4`, `--num-gemmini-mgrs 12`,
  `--num-dma-mgrs 12`, `--pair-manager-mode 1`, `--pages-per-acc 1024`, and
  `--spm-page-bytes 1024`.

## Build/run relation

- Active bitstream build session remains
  `pairdummy-cfg32-nic-mainline-20260505T132956Z`.
- No new AGFI/AFI exists at this change checkpoint.
- This record is a software-only checkpoint used to keep the later
  `cfg32_nic` workload rebuild reproducible.
