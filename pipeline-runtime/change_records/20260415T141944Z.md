# 2026-04-15T14:19:44Z Change Record

## Scope

- Restore the fixed `pairdummy sbus128` guest profile from the temporary
  `page52 export` proof configuration back to a low-perturbation baseline.

## Files Changed

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh`

## Exact Changes

- Bumped:
  - `PIPELINE_RUNTIME_PROFILE_ID`
    - from `pairdummy-sbus128-fixed-v22`
    - to `pairdummy-sbus128-fixed-v23`
- Disabled fixed-profile export proof probes:
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE=0`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START=""`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END=""`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START=""`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END=""`
- Left unchanged:
  - file-only logging
  - `PIPELINE_RUNTIME_MLOCKALL_MODE=2`
  - breadcrumb-first policy
  - all fixed-load probe defaults disabled
  - trigger overlay support via `PIPELINE_RUNTIME_DEBUG_TRIGGER_*`

## Rationale

- The `v22` run already proved that `stage=0 / subbatch=4 / tensor=2 / export
  page52` is not the stable blocker.
- Keeping that proof probe enabled in the fixed profile only adds perturbation
  and pulled the visible plateau back to an earlier `sb=4 / tensor=0 / page27`
  window.
- The fixed profile must therefore return to a low-noise baseline, and any
  further observability should use one-shot trigger overlays rather than
  permanent export probes.

## Expected Follow-Up

- Re-render the guest env through the workload overlay path.
- Run image freshness checks locally and remotely.
- Launch the next rerun from the restored `v23` baseline, using trigger-only
  overlay if targeted observability is still required.
