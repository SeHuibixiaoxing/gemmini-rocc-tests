# Change Record 20260420T140451Z

调试类别：main

## 目标

- 修正文档里对
  `2026-04-20 11:46:08 UTC`
  那轮 `g6 bertmini` live capture
  的一个关键误读
- 明确写清：
  export `v2p` probes
  是在 run 之后才落下的，
  所以下一轮 rerun
  才是它们的第一轮有效动态验证

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260420T140451Z.md`

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/CURRENT_STATUS.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/NEXT_SESSION_PROMPT.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/architecture/g6_segment0_tensor2_export_page28_page29_static_audit_20260420.md`

## 修改内容

- 在 `CURRENT_STATUS` 顶部 override 中补充绝对时间：
  - live run：
    `2026-04-20 11:46:08 UTC`
  - export `v2p` probe patch：
    `2026-04-20 12:12:16 UTC`
- 明确声明：
  旧 capture 没有 `before_v2p/after_v2p`
  不能被解读成
  “新 probe 已生效但没命中”
- 在 `NEXT_SESSION_PROMPT` 中把下一轮 rerun
  明确改写为：
  **已落地 `v2p` probes 的第一轮有效动态验证**
- 在 `g6_segment0_tensor2_export_page28_page29_static_audit_20260420.md`
  中同步修正：
  - 旧 run 与 probe patch 的时间先后
  - 旧 binary / 新源码 不可混读
  - 下一轮无需继续新增热路径 probe，
    先验证已落地的 `v2p` probes

## 原因

- 如果不把 run 时间和 probe patch 时间写清楚，
  后续静态推理会持续把
  “旧 capture 没看到新 probe”
  误当成实证，
  这会直接把下一轮动态验证方向带偏。
- 当前主线真正需要的是：
  在不继续扩大扰动的前提下，
  用已经落地的窄 probe 回答
  page29 是否进入 `v2p`
  以及 `v2p` 是否返回。

## 验证

- 静态核对：
  - `change_records/20260420T121216Z.md`
  - `pairdummy-sbus128-runworkload-20260420-114608`
  - 当前 `src/prt_dma.c` / `src/prt_page_table.c`
- 静态核对 live sparse：
  `target_seq=1/address2 bytes=65536 pages=64`
  证明 `page28` 不是末页
- 本轮未做新的代码语义修改
- 本轮未启动新的 `runworkload`
