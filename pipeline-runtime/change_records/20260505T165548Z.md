# Change record: cfg32 NIC remote freshness gate

Time: `2026-05-05 16:55 UTC`

## Changed

- Added a debug record for the active cfg32 NIC F2 build freshness gate.
- Recorded local and remote generated Verilog timestamps/sizes, required NIC
  and optimized DMA markers, build host identity, and current Vivado phase.

## Verification

- Local config-specific `FireSim-generated.sv` exists with mtime
  `2026-05-05 16:46:07.228185468 +0000` and size `166512822`.
- Remote build host `192.168.0.60` has the same generated Verilog mtime and
  size.
- `SimpleNICBridgeModule`, `bytes_written_per_beat`, and `write_shift` are
  present in the generated RTL.
- `pairdummy_sbus128_gdbserver_cfg32_nic_workflow.sh network-audit` passes and
  finds `ice-nic@10016000`.

## Limitations

- No AGFI/AFI has been produced yet.
- The build has not yet reached place/route, so this checkpoint does not answer
  whether the previous CLB/pblock overutilization failure is fixed.
