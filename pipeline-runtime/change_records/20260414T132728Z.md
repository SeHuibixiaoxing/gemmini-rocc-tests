# Change Record 20260414T132728Z

## 目标

- 降低 pointwise 热路径的 coarse guest-log 扰动
- 保持 breadcrumb 作为 pointwise 主观测面
- 不改变硬件语义，
  不引入新的硬件依赖

## 关联记录

- debug：
  `../debug_records/20260414T132728Z.md`

## 修改文件

- `src/prt_gemmini_adapter.c`
- `scripts/pairdummy_sbus128_fixed_env.sh`

## 修改内容

- `src/prt_gemmini_adapter.c`
  - 新增
    `prt_should_emit_pointwise_sparse_progress()`
  - 当 breadcrumb 已启用时，
    关闭 pointwise 热路径内已被 breadcrumb 覆盖的 coarse progress log：
    - `oc-split-pointwise ... begin/end`
    - `pointwise-matmul-fallback ... begin/end/chunked`
    - `conv-sync-strided ... dispatch=pointwise`
    - `... dispatch=pointwise-return`
    - `... issue-done`
    - `... fence-begin`
    - `... fence-end`
- `scripts/pairdummy_sbus128_fixed_env.sh`
  - profile 从
    `pairdummy-sbus128-fixed-v17`
    升到
    `pairdummy-sbus128-fixed-v18`
  - 把
    `PIPELINE_RUNTIME_CRITICAL_UART_PROBE`
    从
    `1`
    改到
    `0`

## 原因

- `v17`
  live run 证明：
  sparse 前沿停在
  `mgr7 flushed use_pointwise=1`
  之后
- 静态阅读该窗口代码确认：
  后续没有新的硬件动作，
  首嫌疑变成剩余 coarse guest log 自干扰
- 既然 breadcrumb 已经能覆盖 pointwise 调用、matmul、postcall fence/drain/release，
  就没有必要在同一路径继续堆 coarse guest log

## 验证

- 静态 artifact 审计：
  `audit_pipeline_runtime_artifact.py`
  PASS
- `image-closure`
  完成并通过 local freshness
- `remote-freshness`
  在
  `192.168.1.253`
  上通过
- 新 profile：
  `pairdummy-sbus128-fixed-v18`
  已进入新一轮
  `runworkload`

补充说明：

- 宿主直接 `make`
  不是这条主线的 authoritative 校验面；
  它会落到宿主编译器的
  RISC-V `asm`
  与目标相关死代码告警。
- 本轮 authoritative 校验面仍是
  FireMarshal image closure +
  local/remote freshness +
  FPGA rerun。

## 风险 / 后续

- coarse guest log 里的 pointwise 细节会减少，
  之后定位 pointwise 热路径应优先读 breadcrumb
- 若 `v18`
  仍未越过旧前沿，
  下一轮 probe 需要继续保持软件侧、窄窗口、breadcrumb-first
