# Change Record 20260420T071816Z

调试类别：main

## 目标

- 修正上一轮把 `globalStageId` 误当成 `rr_cfg_id_for_stage()` 输入的错误文档结论。

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260420T071816Z.md`

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260420T071816Z.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/change_records/20260420T071816Z.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/CURRENT_STATUS.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/NEXT_SESSION_PROMPT.md`

## 修改内容

- 新增一份勘误 debug record，
  说明：
  - `rr_cfg_id_for_stage()` 吃的是 local `stage_id`
  - `globalStageId` 只用于 log gate / observability
  - 当前 g6 每个 segment 最多 `4` 个 local stages
  - 因而当前 g6 不会因 `globalStageId=39` 静态撞上 `cfg15`
- 修正 `CURRENT_STATUS.md` 和 `NEXT_SESSION_PROMPT.md`
  顶部 override 中关于 `cfg15` 的错误表述。

## 原因

- 上一轮把全局 stage 编号和 segment-local stage 编号混用了，
  会误导后续主线调试优先级。

## 验证

- 修正后，authoritative 文档会明确写成：
  - 当前 g6 不会因 `globalStageId=39` 命中 `cfg15`
  - 真正的静态阈值是某个 segment 达到 `8+` local stages

## 风险 / 后续

- 本轮只修正文档，不改运行时代码。
- 若未来 HybridMapper 产物出现 `8+` local stages 的 segment，
  仍需把 `cfg15` budget 问题重新列为静态检查项。
