# Change Record 20260425T164540Z

调试类别：gdbserver

## 目标

- 把新 direction-fire SimpleNIC AGFI 接到 no-Trace NIC gdbserver smoke 验证链路。
- 记录本轮 FireSim FPGA 验证结果与新的调试前沿。

## 关联记录

- `../debug_records/20260425T164540Z.md`

## 修改文件

- `/home/ubuntu/chipyard/sims/firesim/deploy/config_hwdb_f2_rocket_singlecore_nic_notrace_30mhz.yaml`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260425T164540Z.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/change_records/20260425T164540Z.md`

## 修改内容

更新 no-Trace NIC HWDB：

```yaml
firesim_rocket_singlecore_nic_notrace_30mhz:
    agfi: agfi-04dcd4c2fe1a19b8c
    deploy_quintuplet_override: null
    custom_runtime_config: null
```

随后执行：

- `launchrunfarm`
- `infrasetup`
- `runworkload`
- `terminaterunfarm`

## 验证结果

基础流程：

- AFI `afi-09d16cf6ccdafe66e` / AGFI `agfi-04dcd4c2fe1a19b8c` 创建成功。
- `infrasetup` 成功，slot 0 加载新 AGFI，driver readiness preflight 通过。
- `runworkload` manager exit code 为 0。
- run farm 已终止，AWS 查询无 live/stopping/stopped 实例。

功能结果：

- simulator command 仍以 `COMMAND_EXIT_CODE="1"` 退出。
- 直接失败仍是 SimpleNIC `from_cpu_stream` push 0 bytes。
- 新 debug counters 证明 direction-fire 修复已部署并生效：
  - `to_host_fire_count=6415251`
  - `from_host_fire_count=6415250`
  - `from_host_valid_drive=1`

## 后续风险 / 下一步

当前前沿已经从 SimpleNIC 本地 ready/valid 上移到 FPGATop/global target-cycle 聚合层。下一轮若继续改硬件，不应只继续增加 SimpleNIC 局部寄存器，而应优先补：

- `targetCycleFinishing` 每个输入项的 live/first/last/fired 状态。
- HostPort aggregation 中每个 channel 的 `valid`, `ready`, `fired` 和 helper-blocker。
- UART/BlockDev/TSI/FASEDMemory 的 HostPort lockstep blocker。
