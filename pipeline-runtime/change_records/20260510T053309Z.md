# Add no-DMA performance profile and model execution trace timing

## Summary

为 `ours2` vs `gemini2` no-DMA 性能比较补齐独立运行入口和计时口径：

- runtime trace 新增模型执行窗口字段：
  - `model_exec_ns`
  - `model_compute_ns`
  - `preprocess_ns`
  - `postprocess_ns`
- 新增 dummy8x8/sbus64/cfg32/NIC/noTrace no-DMA perf profile：
  - `pipeline-runtime/scripts/pairdummy_sbus64_dummy8x8_no_dma_perf_fixed_env.sh`
  - `pipeline-runtime/scripts/pairdummy_sbus64_dummy8x8_no_dma_perf_cfg32_nic_notrace_workflow.sh`
- 新增对应 FireSim runtime config：
  - `sims/firesim/deploy/config_runtime_f2_gemmini_rerocc_pairmanager_dummy8x8_4c12p12_sbus64_linux_bertmini_pipeline_runtime_batch8_fileonly_sync_no_dma_perf_cfg32_nic_notrace.yaml`

## Validation

- `make` in `pipeline-runtime`: pass.
- artifact audit:
  - `ours2`: PASS, `segments=15`
  - `gemini2`: PASS, `segments=19`
- YAML hash check:
  - experiment artifacts match current `conference/HybridMapper/output/pipeline_runtime/bertmini` overlay artifacts.
- workflow checks:
  - `pairdummy_sbus64_dummy8x8_no_dma_perf_cfg32_nic_notrace_workflow.sh show`
  - `pairdummy_sbus64_dummy8x8_no_dma_perf_cfg32_nic_notrace_workflow.sh debug-preflight`
  - result: `debug_preflight_status=pass`
- CPU/no-DMA dry-run:
  - `ours2`: exit 0, `dma_submit_count=0`, `gemm_issue_count=320`, `trace_event_drop_count=0`
  - `gemini2`: exit 0, `dma_submit_count=0`, `gemm_issue_count=320`, `trace_event_drop_count=0`
- RISC-V Linux binary rebuild:
  - command used `PATH=/home/ubuntu/chipyard/.conda-env/riscv-tools/bin:$PATH make -C build rerocc-linux-tests`
  - `strings build/rerocc-linux-tests/rerocc_pipeline_runtime-linux` contains the new trace field names.

## Limitations

- 本地 CPU/no-DMA 时间只验证计时字段，不是 F2 性能结论。
- 下一步仍需要 `image-closure -> launch -> infrasetup -> runworkload` 后解析 F2 copy-back traces。

## 2026-05-10 F2 attempt correction

第一轮 F2 perf attempt 进入 guest wrapper 后卡在 `runtime_init` 预处理阶段：

- runworkload session:
  `pairdummy-sbus64-dummy8x8-no-dma-perf-cfg32-nic-notrace-runworkload-20260510-054251`
- evidence:
  - `bertmini-batch8.log` reached `[prt-early] calling runtime_init`
  - no `[prt-early] runtime_init done`
  - `ours2.trace` existed but remained size `0`
  - heartbeat advanced to `37530897115, 1917`, so the host simulation was alive
- termination:
  `pairdummy-sbus64-dummy8x8-no-dma-perf-cfg32-nic-notrace-terminaterunfarm-20260510-061959`

Correction: the perf profile must not inherit the debug profile's
`PIPELINE_RUNTIME_DISABLE_MAPPING_CACHE=1`; that spends F2 time in YAML/mapping preprocessing,
which is outside the requested timing window. The perf fixed env now sets
`PIPELINE_RUNTIME_DISABLE_MAPPING_CACHE=0` so the generated `.cache.bin` can be used.

## 2026-05-10 summary-only trace for no-DMA perf

The second F2 perf attempt passed the earlier `runtime_init` boundary but later stalled with a
stopped heartbeat and UART RCU stall while `TRACE_ENABLE=1` event-level instrumentation was active.
Archived evidence:
`pipeline-runtime/debug_records/artifacts/20260510T0656_no_dma_perf_trace_event_stall/`.

Change:

- added runtime env `PIPELINE_RUNTIME_TRACE_SUMMARY_ONLY=1`;
- summary-only mode keeps trace summary fields but skips event ring allocation and cycle
  calibration;
- event dump is guarded on `trace_events != NULL`;
- no-DMA perf fixed env now exports `PIPELINE_RUNTIME_TRACE_SUMMARY_ONLY=1`;
- guest env render, workflow `show` / `debug-preflight`, and image freshness summaries now surface
  the new knob.

Validation:

- `make -C pipeline-runtime -j$(nproc)`: pass.
- `pairdummy_sbus64_dummy8x8_no_dma_perf_cfg32_nic_notrace_workflow.sh show`: confirms
  `trace_enable=1`, `trace_summary_only=1`, `disable_mapping_cache=0`.
- `pairdummy_sbus64_dummy8x8_no_dma_perf_cfg32_nic_notrace_workflow.sh debug-preflight`: pass.
- rendered `/firemarshal.env` includes `PIPELINE_RUNTIME_TRACE_SUMMARY_ONLY='1'`.
- CPU/no-DMA summary-only dry-run:
  - `ours2`: exit 0, `model_compute_ns=22426157291`, `dma_submit_count=0`,
    `gemm_issue_count=320`, `trace_event_count=0`, `trace_summary_only=1`.
  - `gemini2`: exit 0, `model_compute_ns=22483266989`, `dma_submit_count=0`,
    `gemm_issue_count=320`, `trace_event_count=0`, `trace_summary_only=1`.

Limitation: local CPU timings are only sanity checks for trace semantics, not final performance
evidence. Next performance conclusion still requires one F2 rerun after image closure and
freshness verification.

## 2026-05-10 image closure checkpoint

Ran:

```bash
./pipeline-runtime/scripts/pairdummy_sbus64_dummy8x8_no_dma_perf_cfg32_nic_notrace_workflow.sh image-closure
```

Result:

- FireMarshal clean/build/install completed.
- Local image freshness passed.
- Guest env SHA:
  `9b580a62b90d8e528d235462b4985e657ad6e9563f2eba980f2e70b1a26e707c`
- Runtime binary SHA in image:
  `0d126d06d4186316961aff539e9f72670fe8eabbd13a457a79172842f87e3a56`
- `/firemarshal.env` contains `PIPELINE_RUNTIME_TRACE_SUMMARY_ONLY='1'`,
  `PIPELINE_RUNTIME_NO_DMA_COMPUTE_ENABLE='1'`,
  `PIPELINE_RUNTIME_DISABLE_MAPPING_CACHE='0'`, and `PIPELINE_RUNTIME_GDBSERVER_ENABLE='0'`.

No F2 instance was launched for this checkpoint.

## 2026-05-10 summary-only F2 stall and cache-path rollback

The third F2 attempt used the summary-only trace image:

- runworkload session:
  `pairdummy-sbus64-dummy8x8-no-dma-perf-cfg32-nic-notrace-runworkload-20260510-071554`
- AGFI: `agfi-077451484fe3b63c3`
- remote/local image SHA:
  `366d1865900027b792fc384e09e416173d7d01e74fadf0a2aad25f5b72e6bde2`
- guest env confirmed `NO_DMA_COMPUTE=1`, `TRACE_SUMMARY_ONLY=1`,
  `DISABLE_MAPPING_CACHE=0`, and `GDBSERVER=0`.
- result: stalled with heartbeat at `18382742666, 963`; UART reported
  `rcu_sched detected stalls` with running task `rerocc_pipeline`.
- no `ours2.trace` or `gemini2.trace` was emitted.
- evidence:
  `pipeline-runtime/debug_records/artifacts/20260510T0742_no_dma_perf_summary_trace_stall/`
- run farm was terminated; no running F2 instances were found afterwards.

Conclusion: summary-only trace removes the event-ring disturbance, but does not make the
`DISABLE_MAPPING_CACHE=0` perf profile complete. The earlier cache-enable correction is superseded
for the next attempt. The no-DMA perf fixed env now defaults to:

```bash
PIPELINE_RUNTIME_PROFILE_ID=pairdummy-sbus64-dummy8x8-no-dma-perf-v2-cache-disabled
PIPELINE_RUNTIME_DISABLE_MAPPING_CACHE=1
```

This intentionally returns to the stable YAML path from the known-good cfg32 no-DMA run. The
requested performance comparison remains valid because `preprocess_ns` is reported separately and
the comparison should use `model_compute_ns` / `model_exec_ns`, not total wall-clock startup time.

Local validation after the rollback:

- workflow `show`: `profile_id=pairdummy-sbus64-dummy8x8-no-dma-perf-v2-cache-disabled`,
  `trace_summary_only=1`, `disable_mapping_cache=1`.
- `debug-preflight`: pass.
- host CPU/no-DMA summary-only dry-run with `DISABLE_MAPPING_CACHE=1`:
  - `ours2`: `model_compute_ns=22460364344`, `dma_submit_count=0`,
    `gemm_issue_count=320`, `trace_summary_only=1`, `trace_event_count=0`.
  - `gemini2`: `model_compute_ns=22436967650`, `dma_submit_count=0`,
    `gemm_issue_count=320`, `trace_summary_only=1`, `trace_event_count=0`.

These timings only verify the local measurement path and are not F2 performance evidence.

Image closure after the cache-path rollback completed without launching F2:

- clean/build/install tmux sessions:
  - `pairdummy-sbus64-dummy8x8-no-dma-perf-cfg32-nic-notrace-clean-20260510-075101`
  - `pairdummy-sbus64-dummy8x8-no-dma-perf-cfg32-nic-notrace-build-20260510-075106`
  - `pairdummy-sbus64-dummy8x8-no-dma-perf-cfg32-nic-notrace-install-20260510-075211`
- guest env SHA:
  `c97f389e837ddfcb456b87c2f8116b5fdf62715ddae1c500e5e85a7c20e61866`
- runtime binary SHA in image:
  `0d126d06d4186316961aff539e9f72670fe8eabbd13a457a79172842f87e3a56`
- local freshness: PASS.
- rendered `/firemarshal.env` confirms `DISABLE_MAPPING_CACHE=1`,
  `TRACE_SUMMARY_ONLY=1`, `NO_DMA_COMPUTE=1`, and `GDBSERVER=0`.

The cache-disabled F2 attempt still did not complete:

- runworkload session:
  `pairdummy-sbus64-dummy8x8-no-dma-perf-cfg32-nic-notrace-runworkload-20260510-075758`
- instance: `i-0e51e94770ad722c1`, private IP `192.168.1.205`
- remote/local image SHA:
  `d82ab86239590c2d56893671ad464a089e1d60a6150815bc79f40650751db6fd`
- guest env SHA:
  `c97f389e837ddfcb456b87c2f8116b5fdf62715ddae1c500e5e85a7c20e61866`
- result: reached `S99run` and wrapper `after-child-spawn pid=143`, but emitted no runner stage
  and no `ours2.trace` / `gemini2.trace`.
- heartbeat stopped after one sample: `18326725699, 965`.
- evidence:
  `pipeline-runtime/debug_records/artifacts/20260510T0820_no_dma_perf_cache_disabled_early_stall/`
- run farm terminated:
  `pairdummy-sbus64-dummy8x8-no-dma-perf-cfg32-nic-notrace-terminaterunfarm-20260510-082057`;
  running-F2 query returned empty afterwards.

Static follow-up: the perf profile still differed from the known-good no-DMA run by routing
stdio to a guest rootfs log file. The fixed env now sets:

```bash
PIPELINE_RUNTIME_STDIO_CAPTURE_MODE=uart
```

This aligns with the known-good cfg32/noTrace no-DMA profile and avoids file-backed stdout during
runner launch. It should not affect the comparison window because `model_compute_ns` /
`model_exec_ns` are emitted by the runtime trace, and preprocessing/stdout overhead remains outside
the reported compute ratio.
