# 2026-04-14T06:08:23Z 修改记录

## 本轮目标

- 把新的 `tok=726` 稳定前沿与已证伪假设同步进主文档。
- 固化“先静态读代码，再加窄日志”的当前调试流程，避免后续会话回退到旧叙事。

## 修改文件

- `docs/CURRENT_STATUS.md`
- `NEXT_SESSION_PROMPT.md`
- `debug_records/20260414T060823Z.md`
- `change_records/20260414T060823Z.md`

## 修改原因

- 主文档仍停留在上一轮 fixed-load `v2p` 叙事，
  已经落后于当前 rerun 的真实稳定前沿。
- 这轮新 capture 已经把 blocker 推到 `set_src` 边界，
  同时推翻了：
  - DMA backlog 主假设
  - doneflag wraparound 主假设
- 如果不立即同步，
  后续静态审计与 probe 设计会继续围绕已被排除的窗口浪费时间。

## 文档更新内容

- `docs/CURRENT_STATUS.md`
  更新 authoritative blocker 为：
  - token `726`
  - `program-pre-src-monitor`
  - 缺失 `program-post-src`
  - `outstanding_bytes=0`
- 同时加入：
  - doneflag wraparound 已证伪
  - `FUNCT_SRC_INFO/FUNCT_DEST_INFO` 语义对齐结论
  - ReRoCC credit / `sInstAck` / arbiter 成为新的首要静态可疑点
- `NEXT_SESSION_PROMPT.md`
  用同一套新结论替换旧的 fixed-load 主线摘要，
  并补充当前 live run/capture 标识。
- 新增本轮 `debug_records/20260414T060823Z.md`
  记录当前 capture、静态结论与下一步方向。

## 语义影响

- 本轮不修改 runtime / RTL / workflow 代码。
- 仅更新文档与调试记录，
  不改变硬件指令语义、软件运行时行为或 FireSim 流程。

## 风险 / 后续

- 目前仍然只是静态与 live 观测层面的结论，
  还没有完成对 ReRoCC credit/ack 路径的严格证明。
- 下一轮若继续加 probe，
  必须先基于新的静态审计结果缩小范围，
  避免再次通过高扰动 monitor 指令改变 stall token。
