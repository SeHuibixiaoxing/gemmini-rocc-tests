# 20260505T175018Z - Refresh gdbserver SOP breakpoint names

## Changes

- Updated `docs/testing/gdbserver_integration_sop.md` so the suggested
  breakpoints match current pipeline-runtime symbols.
- Added a debug record documenting the stale breakpoint names and replacements.

## Tests

- Static symbol check with `rg` against `pipeline-runtime/src` and
  `pipeline-runtime/include`.

## Result

The first `cfg32_nic` live `gdbserver --once` attach should no longer waste time
on obsolete breakpoint names.

## Limitations

No live GDB session was run for this checkpoint.
