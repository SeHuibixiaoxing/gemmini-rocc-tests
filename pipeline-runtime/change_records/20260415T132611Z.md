# Change Record 20260415T132611Z

## 目标

修复 pairdummy guest env 的 source-of-truth 漂移，
确保本轮 `v21 / page5 export probe`
真的进入 render 链路和 FireMarshal image，
不再停留在仅修改 `fixed_env.sh` 的半完成状态。

## 关联记录

- debug record:
  `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260415T132611Z.md`

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/rerocc-linux-tests-coupleddma/workload/overlay/firemarshal.env`

间接使用并确认的配置源：

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_fixed_env.sh`

## 修改内容

通过正确生成链路刷新 baseline env：

- 执行：
  `SKIP_BUILD=1 ./host-init-fileonly-sync-pairdummy.sh`
- 使 `overlay/firemarshal.env` 与 `fixed_env.sh` 对齐到：
  - `profile_id=v21`
  - `dma_export_probe_enable=1`
  - `token_start=1161`
  - `token_end=1163`
  - `page_start=5`
  - `page_end=5`

随后重新 render 并 patch 本地 image 的 `/firemarshal.env`。

## 原因

- `render_pairdummy_guest_env.sh` 复制的 baseline 是
  `workload/overlay/firemarshal.env`
- 它不会自动读取 `pairdummy_sbus128_fixed_env.sh`
- 之前只改 fixed profile，导致 render / freshness / image 仍继续使用旧 baseline

## 验证

已通过：

- `verify_pairdummy_firemarshal_image_freshness.sh`
- 镜像内 `/firemarshal.env` 直接读取确认：
  - `pairdummy-sbus128-fixed-v21`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_ENABLE='1'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_START='1161'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PROBE_TOKEN_END='1163'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_START='5'`
  - `PIPELINE_RUNTIME_DMA_EXPORT_PAGE_END='5'`

## 风险 / 后续

- 当前仍存在“双配置源”结构：
  - fixed env shell profile
  - checked-in overlay firemarshal.env
- 这次通过重新跑 host-init 保证了两者一致，但后续最好继续收敛流程，
  避免再次只改一处。
