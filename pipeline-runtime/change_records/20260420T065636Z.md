# Change Record 20260420T065636Z

调试类别：main

## 目标

- 把 `2026-04-20 g6 bertmini` 这轮 FPGA 复盘的 authoritative 结论写回主文档，
  避免后续继续按过期摘要把 `watchdog` 误杀记成新的 guest blocker。

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260420T065636Z.md`

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260420T065636Z.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/CURRENT_STATUS.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/NEXT_SESSION_PROMPT.md`

## 修改内容

- 新增一份 `main` 类 debug record，
  固化本轮 `g6` run 的：
  - host-watchdog timeout
  - heartbeat 持续推进
  - sparse / breadcrumb 前沿差异
  - `cfg15` 静态 alias 风险
- 在 `CURRENT_STATUS.md` 顶部新增 `2026-04-20` 覆盖摘要，
  明确当前最优先问题是 watchdog 语义与 fixed profile 冲突。
- 在 `NEXT_SESSION_PROMPT.md` 顶部新增主线 override，
  把“下一轮 rerun 前先汇报、先修 watchdog 误杀风险”的约束提升到最前。

## 原因

- 现有 `CURRENT_STATUS.md` / `NEXT_SESSION_PROMPT.md`
  仍以更早轮次的摘要为主，
  不足以反映本轮 `g6` 的新 authoritative 结论。
- 如果不先把这一轮结论写回文档，
  后续很容易再次把：
  `heartbeat 继续推进 + sparse plateau + watchdog timeout`
  错记成新的 guest 卡死。

## 验证

- 文档修改后，
  下一位接手者在读 `CURRENT_STATUS.md` / `NEXT_SESSION_PROMPT.md` 顶部时，
  能直接看到：
  - 这轮 `runworkload=1` 的真实原因
  - 为什么当前不能只看 sparse plateau
  - 为什么 `g6` 仍保留 `cfg15` 静态 alias 风险

## 风险 / 后续

- 本轮没有修改 runtime / watchdog 代码本身，
  只做了文档归档与 authoritative 摘要更新。
- 真正的 rerun 前仍需要先决定：
  是改 watchdog progress 语义，
  还是只提高 timeout，
  然后再向用户汇报并等待同意。
