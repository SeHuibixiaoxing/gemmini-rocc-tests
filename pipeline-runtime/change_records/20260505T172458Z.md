# 20260505T172458Z change record

## Summary

Recorded a software-only validation checkpoint while the cfg32 NIC bitstreams continue building.

## Scope

No source behavior was changed. The new evidence is in:

- `pipeline-runtime/debug_records/20260505T172458Z_host_static_dryrun_after_cfg32_docs.md`

## Tests

- `make -C generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime -B all`: pass.
- `audit_pipeline_runtime_artifact.py` on `ours2`, `gemini2`, and `tangram2` with
  `--page-size-bytes 1024`: pass.
- `pipeline_runtime --backend cpu` batch8 dry-run for `ours2` with
  `num_cores=4`, `num_gemmini_mgrs=12`, `num_dma_mgrs=12`, pair-manager mode, and
  `pages_per_acc=1024`: pass.
- `pipeline_runtime --hw-validate-only` for the intended cfg32 pair-manager layout with
  `spm_xlate_enable=1`: pass.
- `pipeline_runtime --hw-validate-only` for the intended cfg32 pair-manager layout with
  `spm_xlate_enable=0`: pass.
- Negative `num_dma_mgrs=11` pair-manager check: failed as expected.

## Limitations

This checkpoint does not validate a bitstream, AGFI/AFI, FireSim NIC, `gdbserver`, DMA custom instructions,
Gemmini custom instructions, SPM xlate hardware, or F2 runfarm behavior. Those still require a completed cfg32
NIC bitstream, HWDB update, `firesim infrasetup`, and remote `gdbserver` attach on F2.
