# Change Record 20260414T103114Z

## 本轮变更

- `src/prt_dma.c`
  - 新增 fixed-load probe 的 env 目标选择器：
    - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PROBE_STAGE_ID`
    - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PROBE_TENSOR_ID`
  - checkpoint / submit / wait / page probe
    统一复用该目标匹配逻辑
- `rerocc-linux-tests-coupleddma/workload/host-init.sh`
  - 补齐 fixed-load selector / page / monitor / pre-src-nops
    env 白名单
- `scripts/firemarshal-tmux-run.sh`
  - 补齐 export page/token 与 fixed-load selector/page/monitor
    env 转发
- `scripts/pairdummy_sbus128_fixed_env.sh`
  - profile 升级到
    `pairdummy-sbus128-fixed-v14`
  - fixed-load probe
    改为：
    - `enable=1`
    - `stage=0`
    - `tensor=0`
    - `page=0`
- `scripts/pairdummy_sbus128_workflow.sh`
  - `show`
    输出新增 fixed-load selector 信息
- 文档：
  - `docs/testing/observability.md`
  - `docs/workflows/pairdummy_sbus128.md`
  - `docs/CURRENT_STATUS.md`
  - `NEXT_SESSION_PROMPT.md`

## 变更目的

- 不改硬件语义，
  只改软件观测面，
  用最小改动验证
  `stage0 tensor0`
  的下一轮
  `C1`
  路径。
- 让 fixed profile 的 env
  能稳定穿过：
  shell -> firemarshal tmux -> host-init -> guest image
  这条传递链。

## 验证

- artifact audit：
  `PASS`
- `pairdummy_sbus128_workflow.sh show`
  已显示：
  - `profile_id=pairdummy-sbus128-fixed-v14`
  - `dma_fixed_load_probe_enable=1`
  - `dma_fixed_load_probe_stage_id=0`
  - `dma_fixed_load_probe_tensor_id=0`
  - `dma_fixed_load_page_start=0`
  - `dma_fixed_load_page_end=0`
- `image-closure`：
  通过
- local freshness：
  通过
- remote freshness：
  通过
- live sparse log：
  已成功命中
  `stage0 tensor0`
  fixed-load probe

## 未做的事

- 没有修改硬件源码
- 没有走 metasim
- 没有改 bitstream
