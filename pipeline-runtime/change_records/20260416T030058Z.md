# 2026-04-16 03:00:58 UTC

## Change Summary
- Type: observability-only refinement
- Purpose: reduce `spm-xlate` trigger perturbation after confirming the first `spm-xlate` version was too chatty and likely moved the apparent stall frontier earlier
- Functional runtime semantics: unchanged

## Files Changed
- `pipeline-runtime/src/prt_rerocc.c`

## What Changed
- Kept the new `spm-xlate` trigger family infrastructure added in the previous change round.
- Narrowed the actual `spm-xlate` trigger emission points in `prt_spm_xlate_release_scope(...)` to only:
  - `rst-b`
  - `rst-e`
- Removed the extra trigger emissions that had been inserted around:
  - flush begin/end
  - fence begin/end
  - release begin/end

## Rationale
- The previous rerun with full `spm-xlate` trigger coverage did not even reach the earlier stable frontier near `subbatch=6`.
- That strongly suggests trigger volume itself was perturbing timing and moving the visible stall earlier.
- The narrowed version preserves exactly one probe immediately before opcode-3 restore and one probe immediately after restore, which is the minimum needed to distinguish:
  - stuck before `rr_restore_opcode_binding(3U, prev_binding)`
  - stuck inside/after that restore boundary

## Validation Status
- Code in the workspace is confirmed narrowed to `rst-b/rst-e` only.
- This narrowed version has **not** yet been rebuilt into the guest image.
- No new FireSim rerun has been launched with this narrowed patch in this round.

## Follow-up Constraint
- If the next rerun still shows measurable perturbation, do not add broader `spm-xlate` trigger points back first.
- Prefer even lighter mechanisms or fallback to breadcrumb-only / sparse-log-only evidence.
