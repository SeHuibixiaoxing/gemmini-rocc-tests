# 20260505T173746Z - Contract microtest checkpoint while cfg32 NIC builds run

## Changes

- Added a debug record for the software-only contract microtests run while the
  mainline and no-TraceIO cfg32 NIC F2 bitstreams are still building.
- No runtime source code was changed in this checkpoint.

## Tests

- Rebuilt `pipeline-runtime` host binary with `make -C .../pipeline-runtime all`.
- Audited `bertmini` `ours2`, `gemini2`, and `tangram2` artifacts for the
  `rerocc_globalnoc_pairmanager_dummy16x16_c4_g12_d12_spad1024kb_dram19_noc64_mac256_sbus128`
  target using `--page-size-bytes 1024`; all passed.
- Ran `pipeline_runtime --hw-validate-only` for a `4c/12g/12d` pair-manager
  layout with `pages_per_acc=1024`, `spm_page_bytes=1024`, and
  `spm_xlate_enable=1`; all passed.
- Confirmed two negative checks fail:
  wrong SPM page size (`4096`) and mismatched DMA manager count (`11`).

## Limitations

- This is not a hardware execution test and does not exercise F2 DMA, Gemmini,
  SPM xlate custom instructions, or remote gdbserver.
- The result is a contract/fail-fast checkpoint for the software tree before
  the next AGFI is available.
