# 20260505T190000Z

## Change

Added a host-side first-triage helper for cfg32 NIC remote gdbserver runs and
documented it in the gdbserver SOP.

## Files

- `pipeline-runtime/scripts/run_pairdummy_cfg32_gdbserver_first_triage.sh`
- `pipeline-runtime/docs/testing/gdbserver_integration_sop.md`
- `pipeline-runtime/debug_records/20260505T190000Z_cfg32_gdb_triage_helper.md`

## Rationale

The first live attach against `gdbserver --once` should be low-risk. The helper
collects the tunnel setup, debug-info sanity check, and breakpoint list in one
place so the first attach is less likely to be wasted on a typo or stale
workflow step.

## Validation

- `bash -n` passed on the script.
- `--help` printed usage successfully.
