# 20260505T180401Z ReRoCC cfg32 collision microtest

## Changed files

- `pipeline-runtime/debug_records/20260505T180401Z_rr_cfg32_collision_microtest.md`
- `pipeline-runtime/change_records/20260505T180401Z.md`

## Summary

- Recorded a software/YAML microtest checking current c4_g12 `bertmini` artifacts against the runtime ReRoCC cfg32 mapping.
- Verified that per-segment DMA/Gemmini stage cfg ids do not collide with each other or with reserved SPM xlate cfg 31.

## Validation

- Inline Python/YAML check output:

```text
rr cfg32 stage/opcode collision check passed
```

## Limitations

- This validates only the current `ours2`, `gemini2`, and `tangram2` c4_g12 `bertmini` artifacts.
- Future mapper outputs with 16 or more stages in one segment could still collide unless the cfg policy changes.
