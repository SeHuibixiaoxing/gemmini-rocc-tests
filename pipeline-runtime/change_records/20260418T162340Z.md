# 2026-04-18 16:23:40 UTC

## 目标

- 为 `TracerV` 新增一条独立于 `pairdummy/sbus128` 主线的
  baremetal metasim smoke 路径。
- 先验证最小 `TracerV` host path，
  再决定是否进入下一阶段的 `selector=3` baremetal 验证。

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/plans/tracerv_metasim_bringup_plan_20260418.md`
- `/home/ubuntu/chipyard/sims/firesim/deploy/workloads/hello-baremetal-tracerv-metasim.json`
- `/home/ubuntu/chipyard/sims/firesim/deploy/config_runtime_metasim_rocket_hello_baremetal_tracerv.yaml`

## 修改内容

### 1. 新增 `TracerV` metasim bring-up 计划

计划文件把本轮验证拆成：

- `stage A`：
  Rocket baremetal metasim 最小链路验证
- `stage B`：
  接近当前问题形态的 baremetal 指令触发验证
- `stage C`：
  何时再升级到 FPGA

### 2. 新增最小 baremetal metasim workload json

`hello-baremetal-tracerv-metasim.json`
相比现有 `hello-baremetal.json` 的关键变化是补回：

- `TRACEFILE*`
- `metasim_stderr.out`

用于把 `TracerV` trace 与 host stderr 一起 copy-back。

### 3. 新增最小 baremetal metasim runtime config

`config_runtime_metasim_rocket_hello_baremetal_tracerv.yaml`
的关键设置：

- `metasimulation_enabled: true`
- `metasimulation_host_simulator: verilator`
- `default_hw_config: firesim_rocket_singlecore_no_nic_l2_lbp_30mhz`
  指向 build recipe
- `tracing.enable: yes`
- `tracing.selector: 0`
- `plusarg_passthrough: +trace-min-batch=0`
- run farm 使用：
  `z1d.3xlarge`

## 非源码环境修复

这轮测试中还额外处理了两项非源码问题：

- 清理
  `/home/ubuntu/.ssh/known_hosts`
  中旧的私网 host key：
  `ssh-keygen -R 192.168.1.109`
- 为新 workload 补齐 FireSim 约定的 bootbinary 目录：
  `deploy/workloads/hello-baremetal-tracerv-metasim/hello.riscv`

## 验证

本轮 `stage A` 已完成并成功。

关键结果：

- `metasim_stderr.out` 出现：
  `TracerV[0]: first nonzero host pull ...`
- `TRACEFILE-C0` 非零
- `uartlog` 完成

结果目录：

- `/home/ubuntu/chipyard/sims/firesim/deploy/results-workload/2026-04-18--16-20-56-hello-baremetal-tracerv-metasim-metasim-rocket-tracerv/`

## 下一步

- 在第二阶段准备轻量 baremetal repro 的
  `selector=3`
  marker 路线。
- 第二轮拉新机器前，先向用户汇报本轮结论与修改。
