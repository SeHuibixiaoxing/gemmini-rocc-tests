# 20260505T184500Z - Record gemini2/tangram2 host threaded checks

## Changed

- Added `debug_records/20260505T184500Z_host_fpga_threaded_gemini_tangram_batch1.md`.

## Why

After `ours2` passed the software-only host threaded `--backend fpga` check,
the same check was run on `gemini2` and `tangram2` to cover all current bertmini
pipeline mapping methods.

## Verification

Both commands exited 0:

- `gemini2`: `gemm_issue_count=40`, `gemm_fence_count=40`,
  `dma_submit_count=0`, `trace_event_drop_count=0`
- `tangram2`: `gemm_issue_count=40`, `gemm_fence_count=40`,
  `dma_submit_count=0`, `trace_event_drop_count=0`

The run used the intended cfg32 target contract:

- `num_cores=4`
- `num_gemmini_mgrs=12`
- `num_dma_mgrs=12`
- `pair_manager_mode=1`
- `pages_per_acc=1024`
- `spm_page_bytes=1024`
- `spm_xlate_enable=1`
- `batch=1`

## Limitations

Host/x86 stub only. No real F2 DMA, SPM xlate, Gemmini hardware, NIC, or
gdbserver was exercised.

