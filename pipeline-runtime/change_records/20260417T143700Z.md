# Change Record 20260417T143700Z

## 目标

- 让 `workerpc` 路径在再次 freeze 到 `dma-export-host first-chunk-submitwait-end` 之前，
  更早拿到 guest-side stuck snapshot。
- 不增加新的 runtime 热路径日志，也不改变 runtime / TracerV 主语义。

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260417T143500Z.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260417T140407Z.md`

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_tracerv_workerpc_workflow.sh`

## 修改内容

- 将 `workerpc` workflow 默认导出的
  `PIPELINE_RUNTIME_RUNNER_BIN_DELAY_PROBE_DELAYS`
  从：
  `180,480,900`
  调整为：
  `10,30,60,180,480,900`

## 原因

- 2026-04-17 14:18 UTC 这轮 rebuilt-image rerun 已证明：
  - wrapper / runner / binary spawn 都正常
  - live sparse log 再次推进到
    `worker stage=0 subbatch=1 -> dma-export-host first-chunk-submitwait-end`
  - heartbeat 固定停在 `10128674493, 555`
  - `runner-proc.stage` 仍只有 `after-bin-spawn` / `bin-poll-1`
  - 没有 `bin-delay-probe-180s`
- 这说明当前 `180s` 起跳的 delayed probe 仍然太晚，
  freeze 发生前没能把 stuck-PC 带出来。

## 验证

- `bash -n generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_tracerv_workerpc_workflow.sh`
- `bash generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/pairdummy_sbus128_tracerv_workerpc_workflow.sh local-freshness`
  - 预期在 `firemarshal-env` 失败，说明 guest image 还没重新烘入新的 probe delay
- 重新执行 `image-closure` 后，再次 `local-freshness` 应恢复 `PASS`

## 风险 / 后续

- 这会更早运行 guest-side `/proc + ptrace` one-shot probe，
  但仍然是低频后台快照，不进入 runtime 热路径。
- 若下一轮 `10/30/60s` 仍抓不到 freeze 当刻现场，
  下一步应升级为专用 stuck-snapshot 文件，
  而不是继续增加 sparse / deep 日志量。
