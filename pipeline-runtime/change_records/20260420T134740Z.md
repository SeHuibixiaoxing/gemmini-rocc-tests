# Change Record 20260420T134740Z

调试类别：main

## 目标

- 把用户刚澄清的“仅限 `pipeline-runtime` 软件栈”的
  `rr_release` 全局修复范围落实到代码注释与文档
- 明确区分：
  raw `rr_release()` 的底层语义
  与 `pipeline-runtime` 当前 release wrapper 的实际语义

## 关联记录

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/debug_records/20260420T134740Z.md`

## 修改文件

- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/include/prt_rerocc.h`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/src/prt_rerocc.c`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/architecture/gemmini_dma_rerocc_instruction_semantics_guide.md`
- `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/docs/testing/observability.md`

## 修改内容

- 在 `prt_rerocc.h` 和 `prt_rerocc.c` 补充注释，
  显式声明：
  `pipeline-runtime` 调用方应使用
  `prt_rr_release_scope()`，
  而不是把 raw `rr_release()` 当成可直接复用 cfg 的同步 release。
- 在架构语义文档里补充一条实现级说明：
  - raw `rr_release()` 仍然只是发起 release
  - 但 `pipeline-runtime` 统一通过
    `prt_rr_release_scope()`
    做
    `rr_release(cfg) + rr_read_csr(RRCFGx)`
    的收口
- 在 observability 文档里补充说明：
  当前主线 release wrapper
  已经默认采用
  “release 后立即读回同一 `RRCFGx`”
  的低扰动哨兵

## 原因

- 前一轮真正的功能修补
  已经在
  `/home/ubuntu/chipyard/generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/change_records/20260419T092816Z.md`
  落下；
  本轮静态复核确认：
  该修补已经覆盖 `pipeline-runtime` 内部全部 release 调用路径。
- 如果不把这一点写清楚，
  后续很容易把 repo 级 raw helper 语义
  误投射到 `pipeline-runtime` 包装层，
  从而重复把“release-return 短窗口”当成当前主线未修风险。

## 验证

- 静态核对 `pipeline-runtime` 下的 `rr_release(` 搜索结果：
  raw helper 只剩 `src/prt_rerocc.c` 一处
- 静态核对 `prt_rr_release_scope()` 调用图：
  DMA / Gemmini / SPM-xlate 路径均经由该包装层释放
- 本轮未做新的功能性行为修改
- 本轮未启动新的 `infrasetup / runworkload`

## 风险 / 后续

- 这轮没有改变 raw `rr_release()` 的底层语义，
  也没有修 repo 其它 workload 的 raw 调用点。
- 若未来 `pipeline-runtime` 新增直接 raw `rr_release()` 调用，
  仍可能重新引入相同窗口；
  这也是本轮补注释和文档的原因。
