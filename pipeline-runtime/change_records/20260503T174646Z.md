调试类别：gdbserver / NIC / cycle0-deadlock

# 新 single-core NIC min-observability bitstream 构建完成

日期：2026-05-03T17:46:46Z

## 修改文件

```text
generators/firechip/goldengateimplementations/src/main/scala/SimpleNICBridge.scala
generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.h
generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.cc
generators/gemmini/software/gemmini-rocc-tests/pipeline-runtime/scripts/rocket_singlecore_nic_gdbserver_smoke_workflow.sh
sims/firesim/deploy/config_hwdb_f2_rocket_singlecore_nic_notrace_30mhz.yaml
```

## 修改内容

本轮构建的是 `FireSimRocketNICNoTraceConfig` 的 single-core NIC 最小可观测版，
只收敛了 payload-history OCL taps，没有改 NIC 数据通路语义。

bitstream 构建结果：

- build host：`192.168.1.155`
- 新 AFI：`afi-06362fac6b708cdbf`
- 新 AGFI：`agfi-0bd5789a1ea72630c`

构建日志显示：

- `replace-rtl`、driver 编译、place、route、post-route phys_opt 都已成功；
- `route_design` 完成后进入 post-route phys_opt；
- route 阶段仍有较高 hold pressure，WNS 在 `clk_main_a0` 组上从约 `-1.056`
  改善到约 `-0.48`，但最终仍存在 timing 负余量；
- 这轮没有再出现“源码改了但 bitstream 没带进去”的 freshness 问题。

## 新硬件绑定

已把 `sims/firesim/deploy/config_hwdb_f2_rocket_singlecore_nic_notrace_30mhz.yaml`
同步到新 AGFI，并把 smoke workflow 默认入口切到 notrace 版本：

- `rocket_singlecore_nic_gdbserver_smoke_workflow.sh`
  默认 `build_name` 改为 `firesim_rocket_singlecore_nic_notrace_30mhz`
  ，默认 runtime/build/hwdb 也切到 notrace 文件；
- 这样后续 `launch / infrasetup / runworkload` 不会再默默回到旧的
  `firesim_rocket_singlecore_nic_30mhz` 线路。

## 验证

已确认：

- 远端 build 产物里包含 `minobs_build_marker`；
- 远端 driver SHA 与本地 `FireSim-f2` 一致；
- 生成的 Verilog 中不再包含 `to_host_hist*` / `nicbig_last_pcie_out_word*`；
- 这轮 bitstream 构建本身已经结束，不需要再重跑硬件编译。

下一步：直接基于这个新 AGFI 跑 preserve 版 gdbserver smoke，尽量用软件和现有硬件现场继续推进，不再额外开新的 bitstream 构建。
