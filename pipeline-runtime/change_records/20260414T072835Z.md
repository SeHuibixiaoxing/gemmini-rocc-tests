# 2026-04-14T07:28:35Z 修改记录

## 本轮目标

- 把 fixed-load probe 进一步缩到 `page31`，
  让下一轮 rerun 只回答一个问题：
  卡点是在 `page31` 的 host-v2p，
  还是在随后 `submit/wait` 的 `doneflag-end` 之前。

## 修改文件

- `src/prt_dma.c`
- `scripts/pairdummy_sbus128_fixed_env.sh`
- `scripts/pairdummy_sbus128_workflow.sh`
- `scripts/verify_pairdummy_firemarshal_image_freshness.sh`
- `scripts/verify_pairdummy_firesim_remote_image_freshness.sh`
- `docs/CURRENT_STATUS.md`
- `NEXT_SESSION_PROMPT.md`
- `debug_records/20260414T072835Z.md`
- `change_records/20260414T072835Z.md`

## 修改原因

- 新 capture 已经表明旧的 `set_src` blocker 被穿过；
  继续围绕 `tok=726` 加探针会浪费 rerun。
- 当前新的可信边界更靠后，
  需要把观测集中到
  `page30 -> page31`
  与
  `token 738 -> 739`
  之间的软件路径。
- 这一路径里最值得优先验证的静态特征，
  就是 `page31` 对齐到 host `4 KiB` 页边界。

## 代码修改

- `src/prt_dma.c`
  新增 fixed-load page probe 范围控制：
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PAGE_START`
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PAGE_END`
  并通过
  `dma_fixed_load_page_probe_range_match()`
  接入
  `dma_should_sparse_fixed_load_page_probe()`
 。
- `scripts/pairdummy_sbus128_fixed_env.sh`
  默认固定：
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PAGE_START=31`
  - `PIPELINE_RUNTIME_DMA_FIXED_LOAD_PAGE_END=31`
- `scripts/pairdummy_sbus128_workflow.sh`
  与 freshness 校验脚本同步暴露 / 校验这两个新环境变量。
- `docs/CURRENT_STATUS.md`
  与 `NEXT_SESSION_PROMPT.md`
  更新为新的 authoritative frontier，
  并明确冻结硬件方向。

## 语义影响

- 本轮不改变 DMA/Gemmini/ReRoCC 的功能语义。
- 修改只影响 fixed-load 调试日志的触发范围，
  让下一轮日志更窄、更低扰动。

## 验证

- 代码路径已静态检查：
  `page probe range -> fixed-load sparse page probe -> page31 v2p/submitwait log`
  触发链路闭合。
- 运行时验证留到下一轮
  `image-closure + freshness + FPGA rerun`
  执行。

## 风险 / 后续

- 如果 `page31` 仍然没有到 `v2p-end`，
  下一层 probe 应该只打开 page31 专用 `host-v2p` 深日志。
- 如果 `submitwait-begin` 已出现但 `doneflag-end` 仍缺失，
  下一层 probe 只该进入 completion-slot / doneflag 获取路径。
- 当前不保留任何硬件源码修改；
  若后续证据逼到必须改硬件，
  需要先停下说明理由，再决定是否重建 bitstream。
