# 20260427T135411Z NIC / metasim 修改记录

调试类别：gdbserver

## 关联记录

- `../debug_records/20260427T135411Z.md`

## 修改内容

### SimpleNIC bridge

- 文件：
  `generators/firechip/goldengateimplementations/src/main/scala/SimpleNICBridge.scala`
- 修复 `fromHostValidDrive`：
  - 从会重新引入 ready/valid 互等的 `targetCycleReady`
  - 改为 `fromHostTokenAvailable`
- 保留：
  - `htnt_queue.io.deq.ready := targetCycleReady`
  - host->target token 只有在整组 target cycle 条件满足时才 dequeue。
- 补充注释，明确：
  - `OutputChannel.valid` 不能等待 target-side ready；
  - target-cycle handshake 只门控 `htnt_queue` dequeue。

### Host driver debug

- 文件：
  `generators/firechip/bridgestubs/src/main/cc/bridges/simplenic.cc`
- 修复 quiet metasim 下的日志噪声：
  - `round_debug_limit == 0` 时，不再打印 cooperative wait 的
    `SIMPLENIC DEBUG REGS` 大块快照。
  - 保留真正错误路径的 `dump_host_debug()`。
- 已用 `g++ -std=c++17 -fsyntax-only` 检查 `simplenic.cc`。

### Metasim runtime

- 新增：
  `sims/firesim/deploy/config_runtime_local_metasim_rocket_singlecore_nic_gdbserver_smoke_quiet_notrace_30mhz.yaml`
- 用途：
  - Linux + NIC + gdbserver smoke 的 quiet local metasim。
  - 去掉 `+simplenic-round-debug` 与 targetcycle 大块输出。
  - 保留较大的 `+fesvr-step-size=16384` 和 `+max-cycles=3000000000`。

## 验证

- `rocket-singlecore-nic-validdrive-metasim-infrasetup-20260427-1343`
  exitcode `0`。
- baremetal NIC metasim：
  - session:
    `rocket-singlecore-nic-validdrive-hello-metasim-runworkload-20260427-1348`
  - exitcode `0`
  - UART 出现 `Hello world from core 0, a rocket`
  - 未出现 `ERR MISMATCH` / `FAILED`
- Linux/gdbserver quiet metasim：
  - session:
    `rocket-singlecore-nic-validdrive-gdbserver-quiet-metasim-runworkload-20260427-1352`
  - 短窗口无 Linux banner / heartbeat。
  - 现场保存到：
    `../debug_records/artifacts/20260427_nic_gdbserver_local_metasim_validdrive_quiet_prefix/`

## 后续

- local metasim 可继续用于最小 NIC/bridge smoke。
- Linux + gdbserver 完整路径仍应以 FPGA runworkload 为主。
- 等待当前 NIC F2 bitstream 完成后，用 fresh F2 runfarm 验证：
  - Linux boot；
  - gdbserver 是否启动；
  - host remote gdb attach；
  - SimpleNIC debug regs 是否显示 host->target token 已进入 guest NIC。
