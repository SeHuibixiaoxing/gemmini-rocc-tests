1. 一个action确定了需要多少个gemmini核心和dma，因此应该是长期持有manager，不需要每次指令都重新获取manager并释放？
2. 核对所有指令阻塞/非阻塞，指令间的时序关系，结合代码判断。然后，检查所有代码，是否有违反时序的可能（例如，没有及时fence等）
1. 目前加速器分配和spm分配逻辑是怎么样的？是否跟MudnacSim对齐？
2. 目前stage并行是如何处理的？单个stage的多个gemmini核心并行是如何处理的？我看到有“issue 后立即走 fence“和“issue与fence分离”两大类，分别有什么区别？
3. gemm_issue_conv_task切分是否进行校验？是直接根据mapping数值填，还是需要额外计算？
4. Cpu fence的作用是什么？与dma指令,gemmini指令，spm xlate指令，rerocc指令之间有什么关系？
总之，需要关注：多stage并行冲突、单stage多核冲突及其并发性、多stage多计算指令和dma指令冲突的问题


1. 单action情况下，虚拟地址区间应该在开始前就分配好。运行过程中分配区间给tensor。分配的方法是什么，fifo吗？action执行结束后，应该希望把已经分配的清空，把虚拟地址区间腾出来，下一次仍然从头开始分配，现在是不是这样的？你分析下现在的分配方法是否会在单个模型执行（不同action串行）情况下有碎片风险？
2. 目前的spm page分配，每轮stage都要重新变。但这是不对的，无论是double buffer，还是ring buffer，或者是lazy fetch的fixed tensor，所有的存储空间都是提前预留号的。因此spm page与虚拟地址绑定后，不需要再次绑定。应该修改的是发送的dma和gemmini的指令上的虚拟地址，移动虚拟地址，而不是重新绑定物理页。
3. “rerocc_coupleddma_set_dst”能不能抛弃掉“completion flag”地址这个做法？现在硬件里是怎么实现的？考虑一致性了吗？能不能改成让pipeline runtime等dma模块空闲，而不是等completion flag被置位？
4. prefault+mlock的时机梳理。现在的锁页是怎么实现的？

TODO：核查AI给的这个建议：若 pages_per_acc * page_bytes != 1MiB，软件 ppn 到硬件 manager 窗口的映射会错位，见 docs/shared_spm_addressing_guide.md:222

TODO：现在还没有查看各种不同tensor的page分配逻辑是否正确

TODO：请你在计算前，加一个可以卡关的校验，确认计算时用到的tensor地址及其对应大小，不会超过被分配的spm大小（即不应该访问越界）

TODO：尝试一轮去掉dma的计算。我怀疑dma的completion flag在linux下，由于虚拟地址和物理地址的隔断，会有问题


TODO：每次dma的manager id是怎么决定的？

待核查点：
- [ ] 现在的spm xlate指令与gemmini slot是重叠的。后续应该改成，spm xlate配置发生在action执行前。action就已经决定了spm xlate，后续不应该有变动。也就无需考虑重叠的问题
问题：spm xlate指令为什么需要单独的slot？不是应该对所有的manager都去做页表相关指令吗？




无风险点：


性能优化点
- [ ] 当前DMA的搬运逻辑是怎样的？例如，数据需要从Tile A 搬运到 Tile B，DMA在Tile C，数据会先从Tile A搬运到Tile C然后再从Tile C搬运到Tile B，还是直接从Tile A搬运到TIle B？
会走到C的内部DMA缓存，不会进shared scratchspad。也就是说，会存在A->C和C->B两个noc事务。


未解决风险点：
- [ ] dma和gemmini并行时，如果需要同一个manager，fence时会不会导致二者错乱？
同一个manager在任一时刻只能属于一个stage。
因此，当同一个stage内，dma和gemmini并行的花，这是一个风险点，需要看看如何解决

- [ ] 计算页面数量时，使用num_cores，而且仅在num_cores<num_gemmini_mgrs时，才会用num_gemmini_mgrs时，采用后者。这是不对的，应该始终是后者

- [ ] 现有的关于bounce和direct path的风险点其实是有问题的
1. 首先结合AI，弄清楚对于不对齐的情况，软硬件direct path会怎么处理
2. 已经搞清楚了：需要该硬件。可以参考“docs/dma_bounce_path_guide.md 的 4.2.15 Gemmini 内部 DMA 是怎么处理 misaligned 的”。
3. 进行baremetal+metasim单元测试
4. 构建比特流
5. 强制direct path模式。


已解决风险点：

- [x] 目前cfg_id的计算方式，是不是意味着不支持多action的情况？
结论：是的，所以最新硬件扩大了cfg大小

- [x] Rerocc release后，自动接一个fence。没有接fence是不是导致卡点的原因？
结论：不需要接fece，而是接一个csr read，来把rerocc release后的段窗口修掉。现在pipeline runtime已经修掉了。
